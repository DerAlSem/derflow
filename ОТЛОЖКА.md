# Отложка выведена из обращения 11.09.2026

Работа, ждущая события снаружи, живёт в реестре не-сейчас-работы. Спрашивать
надо его, а не этот файл:

    python3 ~/.claude/scripts/waiting.py list

Двадцать четыре живых пункта и пять ожиданий без сессии переехали поставкой 2-3.
Карта старых номеров — `waiting/MIGRATION.md`, она не удаляется никогда: ссылка
из чужого хендоффа на «строку 18» без неё неоднозначна.

Тело файла целиком осталось в истории: `git log -p -- ОТЛОЖКА.md`.

🔴 **Сюда НЕ дописывать.** Новая строка заводится из каталога своего
репозитория:

    python3 ~/.claude/scripts/waiting.py new "заголовок по-русски"

Дописку в этот файл `waiting.py doctor` печатает красным и возвращает 2. Молча
дописать всё равно можно — предотвратить нельзя, увидеть можно.
