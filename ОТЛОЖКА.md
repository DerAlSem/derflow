# Отложка — работа, которая ждёт события, а не рук

Реестр заведён 06.09.2026 опросом всех живых сессий. Здесь только то, что
**ждёт снаружи**: оплаты, даты, решения человека, первого боевого случая.
Работа, которую просто не доделали, сюда не попадает — она живёт в своём
хендоффе и заявке.

## Как отвечать на вопрос «что в отложке»

1. Прогнать **ПРОВЕРКУ** каждой строки — это готовые команды, не рассуждения.
2. Разделить на «созрело» и «ещё ждёт». Созрело = проверка вернула то, что
   написано в «сработало, когда».
3. Назвать по каждому созревшему: **чем входить** (хендофф) и **что делать**.

⚠️ **Восстанавливать надо не сессию, а ветку с хендоффом.** Сессии умирают,
их id к утру бесполезны, а транскрипт ворктри в этом проекте уже пропадал при
живой сессии. Точка входа — файл хендоффа, а не «вернись в тот таб». Одна
сессия прямо попросила: контекст 450k, входить новой сессией с хендоффа.

⚠️ Строки НЕ протухают сами. Отработала — вычеркнуть руками, иначе реестр
начнёт врать в сторону «всё ещё ждём».

⚠️ **У ПРОВЕРКИ обязан быть назван ХОСТ.** Команда без хоста читается как
местная, местный прогон честно не находит путей — и рождается вывод «проверка
неисполнима», который ничем не отличается от «ещё ждёт». Строка 6 простояла так
сутки, будучи созревшей. Правило симметрично уроку строки 7: проверка, которая
ИСПОЛНЯЕТСЯ, ещё не проверка — и проверка, объявленная НЕИСПОЛНИМОЙ, тоже
требует доказательства, а «у меня локально нет такого каталога» им не является.

---

## Ждёт: 8 строк

⚠️ **Нумерация сквозная и не пересобирается.** Снятая строка остаётся
надгробием на своём номере: на номера ссылаются хендоффы, память и правки
сестринских сессий, а пересборка тихо переадресует их на чужую работу.

### 1. ОФД в rk_bot — канал включён, ждёт первого боевого платежа

*Шаг «руками на ВДС» ЗАКРЫТ 06.09.2026 в 17:48 МСК.*

- **Где:** `~/dev/rk_bot`, ветка `main`, впереди голой репы ВДС на
  `e03605a..main` (счёт снимать `git rev-list --count`, замороженный устаревает
  от следующей же правки доков). ⚠️ Не запушены намеренно: единственный
  remote — `deploy`, и пуш в него это редеплой с рестартом `bot-rk`.
  ⚠️ **Обновлено 07.09.2026: «кода нет» больше НЕ ВЕРНО.** Один коммит из
  одиннадцати несёт код — `1274b18`, лишняя «e» в подписи к PDF описания. То
  есть следующий пуш — не безобидный довоз доков, а настоящая выкатка; владелец
  велел приложить опечатку к ней и отдельного рестарта ради неё не делать.
  🔑 Считать непушеное машинно (`git rev-list --count e03605a..main`): прежние
  «шесть» были числом коммитов ТОЙ сессии, а не отставанием голой репы.
- ✅ **Сделано и проверено:** `location` вставлен, nginx перечитан,
  `OFD_CALLBACK_KEY` задан, `bot-rk` перезапущен; в журнале
  `Payment webhook listening on localhost:3005`, проба снаружи отдаёт 200
  `application/json` при заданном ключе. Канал включён — адрес приёмника
  уезжает кассе в каждой команде продажи.
- **Ждёт:** первого боевого платежа в rk_bot. Поток ~8 в неделю, то есть до
  нескольких суток. От человека больше ничего не требуется.
- **Замер 06.09 19:15** (`ssh mprz 'journalctl -u bot-rk --since "2026-09-06
  17:48" | grep -i "ОФД\|Payment webhook"'`): строк «ОФД» нет, платежа не
  было — ЖДЁТ ДАЛЬШЕ. Приёмник жив, `Payment webhook listening` в 17:48,
  18:54 и 18:55. ✅ Проверка НЕ вакуумна: тем же грепом виден настоящий
  платёж 1442 от 15:03, то есть она умеет находить.
- **Состояние линии** (от неё самой, 06.09 19:40): задачи **54/57**, открыты
  9.1, 9.5 и 11.5 — все ждут одного и того же платежа; прод на `e03605a`
  (выкачен 18:54), локально шесть непушенных, все документные.
- 🔑 **Содержание ожидания СУЗИЛОСЬ: наша половина 9.1 закрыта без платежа.**
  Подписанный POST по платежу 1442 прошёл через nginx, подпись сошлась, битая
  отвергнута с записью в журнал. Неизвестно ровно одно — **позвонит ли касса**.
- 🔑 **ПРОВЕРКА УЖЕСТОЧЕНА: искать не слово «ОФД», а АДРЕС ПРИЁМНИКА.**
  Прежний греп ловил слишком широко; различающий признак — что стоит в строке
  подачи после «приёмник».

  ```
  ssh mprz 'journalctl -u bot-rk --since "2026-09-06 17:48" | grep "приёмник"'
  ```
  Прочерк — канал не сработал, адрес — сработал. ✅ **Отрицательный образец
  проверен 06.09 19:40:** у платежа 1442 (15:03:41) стоит прочерк, и это
  правильно — он прошёл до включения ключа. То есть греп находит строки и
  умеет показать оба исхода.
- **ПРОВЕРКА:** `journalctl -u bot-rk | grep ОФД` — строка «ОФД: ссылка …
  записана через опрос» либо «… по уведомлению». Машинно: в БД rk строка
  `payments` со `status=SUCCESS`, непустыми `receipt_uuid`/`receipt_url` и
  `paid_at` позже 06.09.2026 17:48.
- ⚠️ **Строку «часы кассы отстают, разрыв растёт» в реестр НЕ заводить — вывод
  ретрагирован автором** (`5dd7a59` в rk_bot, `7c9a034` в ofd-providers; в
  обоих заголовок сам несёт ретракцию, чтобы читающий историю не остановился
  на `6a9c018`). Наблюдаемый остаток = смещение + СЕКУНДА ПОДАЧИ, касса режет
  до минуты; пять «растущих» точек просто отсортированы по секунде подачи.
  Смещение зажато в (3м39с, 3м45с] и за шесть дней не двигается ВОВСЕ.
  🔑 **Признак ошибки записан проверяемым:** ряд из пяти точек с размахом
  МЕНЬШЕ шага квантования не может свидетельствовать о тренде — размах 54 с
  при шаге 60 с. Это переносится на любой ряд, снятый с округляющего
  источника.
  ⚠️ Версия «виноваты часы кассы» снята ЦЕЛИКОМ, а не смягчена: сервер
  синхронен по NTP, ответ кассы стабилен побайтно через трое суток, владелец
  сообщает синхронность кассы — три показания не сходятся, и слабым звеном
  оказалось НАШЕ толкование `receipt_datetime` (взято из замера, а не из
  вендорского референса). Разрешит либо Эвотор, либо печатный чек следующего
  платежа. Хозяин есть, срочности нет.
- 🔴 **Про любой рестарт `bot-rk`, если придётся трогать канал:** `curl` по
  пробе сразу после рестарта отдаёт **502**, сразу после reload nginx —
  **404**, и оба раза это ГОНКА, а не отказ. Юнит `Type=simple`, поэтому
  `systemctl is-active` показывает `active` за шесть секунд до открытия сокета
  3005, а nginx ещё дообслуживает старыми воркерами. Судить по строке
  `Payment webhook listening` в журнале, дав 10 секунд. На этом уже потеряли
  час: 404 прочитали как ошибку конфига. Записана и в хендоффе; здесь
  оставлена намеренно — это свойство `Type=simple`, а не факт состояния,
  поэтому не протухнет и сторожит саму утреннюю проверку.
- **Замер 07.09 07:07** (греп по «приёмник» и «ОФД» с 06.09 17:48): строк с
  адресом приёмника нет — боевого платежа за ночь НЕ БЫЛО, ЖДЁТ ДАЛЬШЕ.
  Единственная находка — `19:24:19 ОФД: подпись не сошлась, платёж 1442`, это
  негативная проба самой линии, а не боевой случай. Приёмник жив.
- **Тогда:** закрыть группу 9 — сверить сумму и дату с ПЕЧАТНЫМ чеком ОФД,
  убедиться, что канарейка «Пространство имён кассы занято» не поднялась,
  обновить строку `romantsova_store` в реестре, затем архивация (42/47).
- **Входить:** `~/dev/rk_bot/openspec/changes/adopt-ofd-providers/HANDOFF.md`
  — там пошаговые команды ВДС и ожидаемый вывод каждой.
- **Кто ведёт сейчас:** сессия «O·внедрение rk_bot — ждём первого боевого
  платежа» (прежняя расщеплена 06.09 и закрыта). Это пометка, а НЕ способ
  входа: входить всё равно хендоффом — имя таба к утру бесполезно.

### 2. Планировщик — 🟢 СУТКИ ВЫДЕРЖАНЫ 07.09.2026 в 07:23, линия ЗАСЧИТАНА

- **Где:** ветка `scheduler-decouples-from-the-bot`, ворктри
  `.claude/worktrees/scheduler-decouple` (запушена, дерево чистое)
- **Ждёт:** не события и не человека — просто должно накопиться. Точнее, ждёт
  ОТСУТСТВИЯ события: гарантию «исполнитель один» держит не код, а то, что
  машину не перезагрузят, и проверка (а) — единственное, что это ловит. Отсчёт с
  06.09.2026 07:23. Переезд планировщика в свой юнит обнулит счёт, с которым
  его же приёмка будет сравнивать.
- **ПРОВЕРКА:** два запроса через `./scripts/prod_sql.sh -f файл.sql` —
  (а) исполнитель ОДИН: `SELECT date_trunc('minute', started_at), count(*) FROM
  job_run WHERE job_name='close_started_events' AND started_at > now() -
  interval '10 minutes' GROUP BY 1 ORDER BY 1 DESC;` — везде обязана быть 1;
  (б) сама линия: `SELECT job_name, outcome, count(*), max(items) FROM job_run
  WHERE started_at > '2026-09-06 07:23' GROUP BY 1,2 ORDER BY 1,2;`
- **Замер 06.09 19:12:** (а) десять минут подряд — везде ровно **1**, линия
  цела. (б) идёт: `pending_payment_expiry` 706 холостых, `receipt_polling`
  689 холостых при 19 рабочих, `organizer_notice_flush` 702/4. До готовности
  ~12 часов.
- 🟢 **СРАБОТАЛО 07.09.2026 в 07:23:30** — базовая линия засчитана, все четыре
  условия проверены замером:
  - (а) за последние 10 минут (07:14–07:23) — везде ровно **1**;
  - **за ВЕСЬ период 06.09 07:23 → 07.09 07:24 — ноль минут с дублем**, 1434
    прохода на 1434 разные минуты. Это сильнее предиката строки: он смотрел
    выборку в 10 минут, а гарантия нужна на сутки;
  - аптайм `mprz.ru` — **19 дней 25 минут**, перезагрузки не было;
  - `gmp-worker` — `inactive`, `enabled`; `bot-gmp` — `active running`.
- 🔴 **Семь минут суток БЕЗ прохода — это ДЕСЯТЬ ПРОД-ВЫКАТОВ, а не дрейф
  фазы.** 08:56, 12:40, 13:48, 19:18, 22:06, 22:41, 22:54.
  ⚠️ **Первое объяснение реестра («цикл спит 60 секунд ПОСЛЕ прохода») было
  НЕВЕРНЫМ и снято 07.09 в 07:45.** Опровергла сессия `scheduler-decouple-6b`,
  я перепроверил сам и подтвердил. Ошибка сделана на слабой улике: интервалы
  105 и 127 секунд вокруг дыр «сходились» с версией дрейфа, хотя ХОЛОСТОЙ
  проход длится миллисекунды — версия не объясняла саму ВЕЛИЧИНУ разрыва, и
  это было видно сразу.
  ✅ **Верное объяснение, проверено тремя независимыми источниками:**
  1. `participation_awards` за сутки — 34 строки: 24 ровно в `:50:00` и 10 вне
     сетки. Вне сетки он попадает единственным способом — `next_run_time =
     now + 90s`, догон при старте процесса (`scheduler.py:858`). Все семь дыр
     плюс 90 секунд дают внесеточные строки с точностью до секунд.
  2. `journalctl -u bot-gmp -t systemd` за те же сутки — ровно **10**
     рестартов: 08:56:12, 12:40:05, 13:47:48, 18:00:04, 19:17:49, 19:53:03,
     21:51:22, 22:05:58, 22:40:50, 22:53:57. Все чистые — `Deactivated
     successfully`, ни одного `Scheduled restart`, ни одного ненулевого
     выхода. То есть намеренные, не падения.
  3. `git reflog` дерева прода — перед КАЖДЫМ рестартом за 19–22 секунды стоит
     `pull --ff-only origin main: Fast-forward`, и SHA каждый раз двигался.
     Десять из десяти. **Каждый рестарт — прод-выкат**, включая четыре,
     сгрудившиеся в час (21:51, 22:06, 22:41, 22:54).
  Три рестарта (18:00, 19:53, 21:51) дыры в минутном проходе не оставили —
  повезло с фазой.
- 🔑 **Следствие для приёмки 9.2–9.5 — ОБРАТНОЕ прежнему.** Переезд НЕ меняет
  триггеры: ветка `scheduler-decouples-from-the-bot` только убирает запуск из
  `gmp/runtime/lifecycle.py`, а `get_scheduler` и все триггеры те же (сверено
  07.09: греп по `IntervalTrigger|CronTrigger|add_job` в диффе `gmp/` ПУСТ).
  Фазовое поведение не изменится, значит **сверять надо именно количество
  проходов**, дубли — дополнительно.
  🔑 **И это довод ЗА переезд, которого в заявке не было:** сегодня расписание
  умирает вместе с ботом ДЕСЯТЬ раз в сутки, по разу на каждый выкат. После
  переезда перестанет.
- ✅ **Ветка `did_work` исполнялась, опасение хендоффа снято** (замер соседней
  линии, 07.09): `sms_delivery_tracking` 68 раз, `receipt_polling` 19,
  `organizer_notice_flush` 6, `participation_awards` 3 (до 12 единиц за тик).
  ⚠️ «Все `idle`» в замере выше относится ТОЛЬКО к `close_started_events`.
- **Тогда — созрело.** Работу ведёт ОТДЕЛЬНАЯ сессия: владелец 07.09 в 07:12
  сказал, что взял планировщик сам (`scheduler-decouple-6b`), а дело реестра —
  присматривать. Замеры ей переданы 07.09 в 07:16, чтобы не мерила заново.
  **Строку снять руками, когда линия отработает** — сама она не протухнет.
- **Сработало, когда:** наступило 07.09 07:23 **и** (а) везде 1. Ожидание —
  28 проходов, около 15 240 тиков; поимённые числа в задаче 2.10 заявки
  `scheduler-runs-in-its-own-unit`.
- 🔴 **Линия хрупкая:** `gmp-worker` на проде намеренно остановлен, но
  `enabled` — перезагрузка машины поднимет его сама и испортит сутки. Тогда
  считать заново. Если (а) показала не 1 — линия недействительна.
- **Тогда:** снять линию → задача 10.1 → приземлить коммит `0fad22c44` через
  `land.sh` → промоушен и прод-выкат → попросить человека
  `sudo systemctl start gmp-worker` → приёмка 9.2–9.5.
- **Входить:** `.claude/handoff/scheduler-decouples-from-the-bot.md`

### 3. Фейс-контроль на 8233 — 🟢 ДОПУСК СОСТОЯЛСЯ, ждёт ОПЛАТЫ, дедлайн 08.09

*Обновлено 06.09 сессией `F·feature/face-control-toggle`: за час до этого
строка успела устареть дважды.*

- **Где:** ветка `feature/face-control-toggle`, ворктри
  `.claude/worktrees/face-control-toggle`. Всё приземлено, незакоммиченного нет.
- **Ждёт:** допуска ПАРЫ `27338`/`27339` на экране **«Команды»**. Дедлайн
  прежний — **08.09** (`registration_deadline` события 8233).
- 🔑 **Ждать Катю или Ксению НЕ обязательно — нажать может сам владелец.**
  `_require_manage` пускает супер-админа, а `approve_pair` про `may_decide` не
  спрашивает. Прежняя формулировка «сам ты этого не сделаешь, нужна Катя на
  „Моих игроках“» больше не верна: ось изменилась вместе с починкой ниже.
- **ПРОВЕРКА:**

  ```
  cd ~/dev/gmb_v2
  echo "SELECT id,status,org_approved_at FROM event_participant WHERE id IN (27338,27339);" | scripts/prod_sql.sh
  ```
- **Сработало, когда:** у `27338` непуст `org_approved_at`.
- 🟢 **СРАБОТАЛО 06.09.2026 в 18:40:58** — у `27338` (user 1) стоит допуск.
  Похоже, первое нажатие «Допустить» после починки кнопки.
- 🔴 **«Допущен один из пары» — БЫЛО НЕВЕРНО, и задача «допустить второго»
  снята 06.09 в 19:55.** Разбор ниже; строка успела продержать ложную работу
  час и один раз уехать владельцу.
- **Перепроверено 06.09 в 19:11 — без изменений.** У `27338` допуск от
  18:40:58, у `27339` пусто, обе строки `reserve`. За полчаса не сдвинулось.
- 🟢 **Разбор 06.09 19:40–19:55: дефекта нет, пара УЖЕ открыта к оплате.**
  Четыре звена, каждое проверено:
  1. `27338` — **якорная строка** (`is_primary = t` на боевой), `27339` —
     партнёр. Композиция `pair_admission_on_event`
     (`gmp/core/services/school_approval.py:289`) читает разовый допуск
     **только у якоря**: `org_approved=anchor_row.org_approved_at is not None`.
     У партнёра замка нет по построению, и второй допуск ничего не изменит.
  2. `legal_entity_player` на боевой — **0 строк**, значит все вердикты
     `NOT_REVIEWED`, значит ветвь 4 `pair_admission`, значит **`OPEN`**.
  3. Обе строки в `reserve` потому, что событие **ПЛАТНОЕ** — 2750 ₽,
     предбронь 500 ₽. Строки подтверждает оплата. Отдельная ветка
     автоподтверждения обеих строк живёт только на БЕСПЛАТНОМ событии
     (`pair_payment.py:150`), и на 8233 она не при чём.
  4. Журнал прода в 18:40:58:
     `POST /api/tournaments/8233/pair-applications/by-participant/27338/approve
     → 200 OK`. Это **новая** ручка заявки `pair-admit-addressed-by-participant`
     и её первое живое применение. Предупреждения
     «approve_pair: уведомление primary не доставлено» в журнале нет.
- **Замер 07.09 07:06:** без изменений — `27338` допуск от 06.09 18:40:58,
  `27339` пусто, обе строки `reserve`. ОПЛАТЫ за ночь не было. До дедлайна
  `registration_deadline` 08.09 — примерно сутки; если оплаты не будет к концу
  07.09, напомнить паре (а не чинить допуск).
- **Значит делать: ничего нажимать не надо — ждём ОПЛАТУ.** Дедлайн 08.09
  (`registration_deadline`). Если к 07.09 оплаты нет — напомнить паре, а не
  чинить допуск.
- 🔴 **Откуда взялась ложная задача — ловушка на будущее.** Предикат был
  «у обеих строк непуст `org_approved_at`», и он ловил не ту ось: пустая
  отметка у партнёра — норма, а не недопуск. Верный предикат — **вердикт
  композиции**, а не колонка у обеих строк, и различающий признак статуса —
  **оплата**, а не допуск. Память `face-control-never-switched-on` называла это
  прямым текстом («уведомление игроку — на ПАРЕ»), но строка реестра
  формулировала задачу по колонке.
- **Тогда:** убедиться, что догон перевёл пару из `reserve` и оплата открылась;
  закрыть задачу 8.7.
- ✅ **Красная находка про кнопку «Допустить» ЗАКРЫТА.** Она ключевалась по
  `PairInvite` при нуле строк в `pair_invite` на всей боевой — то есть не
  срабатывала никогда. Владелец прочитал, велел чинить; починено и **выкачено в
  прод**: `main` = `c183ffd0a`, якорь отката `prod-2026-09-06-pair-admit`,
  заявка `pair-admit-addressed-by-participant`. Гейт на том же SHA: 15639
  бэкендных RC=0, `vue-tsc` RC=0, 4712 фронтовых RC=0, миграций нет. После
  выката проверено дерево, оба сервиса `active`, ноль ошибок после рестарта.
- ✅ **Галка на 8233 поднята** (`requires_org_approval = t`), и механизм
  ВПЕРВЫЕ доставил: два повода `tournament_approval_pending` (7344, 7345),
  `delivered_at` 12:46:18 — первое исполнение доставки за всё его существование.
  Галка, поднятая после оплаты, фейс-контроль на турнире не включила бы вовсе;
  успели.
- ⚠️ **Подъём самой галки — не под «Войти как»:** этот гейт берёт
  `real_user_id` и отвечает 403. Владелец на этом уже споткнулся. К допуску
  пары ось другая — сверять по коду, а не по аналогии.
- **Входить:** `.claude/handoff/feature-face-control-toggle.md` (приземлён в
  `066f174bb`) — состояние, семь оплаченных ловушек и объявленный разрыв:
  демо-проверка пропущена осознанно решением владельца. Рядом
  `openspec/changes/face-control-toggle/CHECK-8233.sql` и `CHECK-8233.md`.

### 4. ~~Тексты записи~~ — СНЯТА 07.09.2026 в 07:14, обе развилки закрыты

🟢 **Владелец ответил на обе развилки 07.09 в 07:12 МСК:**
- **(а) отклонённые модерацией (`REJECTED`) — писать ОТДЕЛЬНЫЙ ТЕКСТ НА МЕСТЕ**
  (`events/registration/routes.py:301`). Перехват до сервиса записи оставить
  как есть; вариант «перестать перехватывать, чтобы сработал утверждённый
  `NAME_REJECTED_MESSAGE` и личка с кнопкой» НЕ выбран.
- **(б) ёлочки вместо ASCII-апострофов вокруг «👤 Моя запись» — ДА.** Выбрано
  при озвученной цене: правка заденет ещё 4 всплывашки.

⚠️ **Дословных строк владелец не диктовал.** Если текст на месте (а) нужно
согласовать буквально — это НОВЫЙ вопрос человеку, а не остаток этого.

Решения переданы живой ведущей сессии `G·тексты-записи` сообщением 07.09 в
07:14 (она была `waiting` ровно на этом). Дальше это обычная недоделанная
работа: её место — хендофф `.claude/handoff/booking-notice-texts.md` и заявка,
а не отложка. Всплывашки #2–#10 идут по одной (прежний выбор владельца).
⚠️ Пункт 5 хендоффа устарел: всплывашек **10**, а не 7.

### 5. ~~Чекаут-API~~ — СНЯТА 06.09.2026 в 20:40, линия закрыта целиком

🟢 19.3 исполнен владельцем. Сертификат `api.propevay.ru` выпущен и развёрнут.
Заявка `portal-checkout-api` заархивирована как `2026-09-06-portal-checkout-api`,
**212/212**, дельта влита (+32 требования, ~10 изменённых, заглушек `TBD` нет).
Активных заявок в репозитории **ноль**, голова `8d4a3e1`.

✅ **Сверено мной снаружи, а не принято на слово** (06.09 20:38):
`https://api.propevay.ru/` отдаёт **HTTP/2** и код **502**; сертификат
`notAfter = Dec 5 16:10:31 2026 GMT`. 502 здесь — успех: TLS поднялся и вхост
проксирует, а на `:8790` никого нет, потому что заявка на прод не выкатывается.
🔑 **И HTTP/2 в ответе доказывает, что шаг 10 не пропущен** — ровно та ловушка,
из-за которой на certbot останавливаться было нельзя.

🔑 **Две находки про машину `mprz`, которые шире этой линии:**
1. `propevay` был **единственным** репозиторием на машине, ходившим в GitHub по
   HTTPS, а пароли для git там отключены — `git pull` падал на аутентификации.
   `origin` переведён на SSH, как у `gmb_v2` и `mprz.ru`. **Любая ПРОВЕРКА,
   делавшая `git pull` в `~/dev/propevay`, раньше падала не по своей причине.**
2. `/opt/propevay` не существует и не существовал, но стоял в юнитах портала и
   в репозиторном `deploy/propevay.service` — то есть `cp` этого файла поверх
   установленного **уронил бы боевой движок**. Пути исправлены по факту, на
   согласованность поставлен сторож.

### 6. ~~Турба — первая оплата после переезда~~ — СНЯТА 06.09.2026 в 19:20

🟢 Сработала, и была созревшей уже сутки. Три платежа позже переезда
(05.09 16:12): `674` 05.09 21:12, `675` 06.09 09:55, `676` 06.09 15:04 — все
`COMPLETED`, `receipt_url_via=push`, `receipt_push_count=1`, `receipt_debt_at`
пуст. Задача 4.8 закрыта коммитом `6c5cd5d2` в `~/dev/mprz.ru`; заявка
`repo-split` теперь **70/70** и готова к архивации — это решение владельца, не
моё. Долг 7.5 (ротация кредов) закрыт РЕШЕНИЕМ владельца с названной ценой, а
не исполнением; при архивации он уедет в архив вместе с заявкой.

🔴 **Урок дороже самой строки: сутки строка врала «проверить нечем», потому что
у команды не был назван ХОСТ.** Прежняя редакция писала `cd
~/dev/turbo01/src/bot` — без хоста это читается как местный путь, местная
проверка честно не нашла ни `src/`, ни `.env`, ни `asyncpg`, и отсюда родился
вывод «проверка неисполнима». На проде есть всё:

```
ssh mprz 'cd ~/dev/turbo01/src/bot && ../../.venv/bin/python /tmp/chk_receipts.py'
```

**Правило для всего реестра:** у ПРОВЕРКИ обязан быть назван хост. «Проверка,
которая исполняется, ещё не проверка» (строка 7) имеет вот эту вторую сторону —
проверка, объявленная неисполнимой, тоже требует доказательства, а «у меня
локально нет такого пути» им не является.

⚠️ Скрипт на проде по-прежнему в `/tmp`, который метёт
`systemd-tmpfiles-clean.timer`. Копия рядом с заявкой — `chk_receipts.py`
(коммит `0ec66804`), ею и восстанавливать. ⚠️ В `paymentstatus` нет значения
`succeeded`, оно `COMPLETED`.

### 7. Перенос строки инбокса — 🔴 ПРОВЕРКА ПЕРЕПИСАНА, старая давала ложное зелёное

*Прежний предикат «3–5 переносов после выката» сегодня СРАБОТАЛ БЫ ВПУСТУЮ —
разбор ниже. Ждать теперь надо другого события, и оно много реже.*

- **Где:** ветка `check/move-notice-baseline`, ворктри
  `.claude/worktrees/move-notice-baseline`. Ведущей сессии больше нет.
- **Ждёт:** переноса брони, у которой ЕСТЬ строка инбокса. Заявка
  `booking-move-carries-its-notice` в проде с 06.09 10:30.
- 🔴 **Почему старая проверка врала (замер 06.09.2026, 18:10).** Она считала
  переносы, а фикс проверяется только переносом С НАГРУЗКОЙ. После выката
  переносов ровно 4 — порог взят, — но легли они в ВОСЕМЬ СЕКУНД одной пачкой
  (13:41:18–13:41:26, брони 5031–5034), и **ни у одной исходной брони не было
  ни единой строки** `booking_%`. Переносить было нечего: «восьмёрка не
  выросла» верно ВАКУУМНО и о фиксе не говорит ничего.
- 🔴 **Событие много реже, чем считала строка.** Строки `booking_%` несут
  **33 брони из 4970**, сами строки появились лишь 24.08.2026 (144 штуки), и
  из **135 переносов за всю жизнь** исходная бронь несла строки у **ДВУХ** —
  `4993←4987` (04.09) и `4586←4340` (30.08), оба до выката, оба и составляют
  восьмёрку. Попадание ≈1,5 % на перенос при медиане 3 переноса в сутки — это
  один значимый случай примерно раз в три недели, а не «07–08.09».
- **ПРОВЕРКА — теперь ОДИН запрос** (`cd ~/dev/gmb_v2`, stdin в
  `scripts/prod_sql.sh`):

  ```sql
  SELECT count(*) AS строк_на_целях_переноса
  FROM user_notification un JOIN booking b ON b.id = un.subject_id
  WHERE un.kind::text LIKE 'booking_%' AND b.moved_from_booking_id IS NOT NULL;
  ```
- **Замер 06.09 19:12:** цели переноса — **0**, восьмёрка — **8** (из них
  прочитано 5). Оба числа те же, что в базовой линии: события ещё не было.
  При попадании ≈1,5 % на перенос ждать в среднем около трёх недель.
- **Замер 07.09 07:06:** цели переноса — **0**, восьмёрка — **8** (прочитано
  5). Третий одинаковый замер подряд; при попадании ≈1,5 % на перенос это
  ожидаемо, ждать в среднем до конца сентября.
- **Сработало, когда:** результат стал больше нуля. На 06.09 18:10 он **0** —
  это базовая линия. Ноль значит «случая ещё не было», а НЕ «фикс не работает».
- ⚠️ **Считать переносы больше не надо** и порога «3–5» больше нет: он ловил
  не то событие. Прежний парный запрос про «восьмёрку» оставлен ниже только
  как контроль — сам по себе он ничего не доказывает.

  ```sql
  SELECT count(*) AS строк_на_мёртвых_бронях,
         count(*) FILTER (WHERE un.read_at IS NOT NULL) AS из_них_прочитано
  FROM user_notification un JOIN booking b ON b.id = un.subject_id
  WHERE un.kind::text LIKE 'booking_%' AND b.status = 'moved';
  ```
- **Тогда:** строки на целях появились И восьмёрка не выросла → фикс работает,
  записать в память `named-open-gaps` и закрыть. Восьмёрка выросла при нулевых
  целях → фикс НЕ работает. Оба нуля → ждать дальше.
  ⚠️ Восьмёрка не уменьшится никогда — это исторические строки до фикса,
  бэкфилл не делаем осознанно. Увидев 8, не считать фикс несработавшим.
  ⚠️ У `user_notification` нет столбца со следом переноса — связь только через
  `subject_id`, поэтому задним числом «была ли нагрузка» не восстановить.
  Отсюда и предикат по ЦЕЛИ, а не по исходной броне.
- **Входить:** `.claude/handoff/proverka-perenosa-na-prode.md`

### 8. ~~Знак Igm~~ — СНЯТА 06.09.2026 в 22:20, выкачена в прод и заархивирована

🟢 Разговор с владельцем закрыт, работа приземлена, выкачена и принята.
Тег `prod-2026-09-06-igm-mark`, дерево на VDS `4c97fde66`, заявка
`igm-mark-two-meanings` заархивирована как `2026-09-06-igm-mark-two-meanings`,
38 задач из 38.

**Открытые пункты этой строки закрыты решениями владельца, а не забыты:**
подпись контекстной кнопки — «Счёт»; на узкой ширине контекстная кнопка и
кебаб — иконки без подписей; «?» рисуется только на панели. Случай «будущее
событие с тремя кнопками» перестал существовать: «Редактировать» уехала в
меню, и в ряду осталась ровно одна контекстная кнопка.

✅ **Проверено на проде замером, а не принято на слово** (06.09 21:55):
`597` прошедших событий несли знак, `20` сохранили, `577` потеряли — это ЦЕЛЬ
правила. Публичный API в обе стороны: 8234/8235/8403 (с партиями) отдают
`index_mark_visible=true`, 8205/8404 (без партий) — `false` при
`goes_to_index=true`. `alembic current: jobrun01` (миграций в наборе нет),
`gmp-crm` и `bot-gmp` active, в логах после рестарта ни одного warning.

🔑 **Числа хендоффа (586/17/569) разошлись с прод-замером (597/20/577) не по
дефекту, а по времени** — между съёмками закрылось ещё несколько событий, три
из них с внесённым счётом. Любой замер этого вида протухает за часы.

🔑 **Свип «код против спеки» окупился** — он нашёл клаузу сценария «а знак
рисуется по `goes_to_index`», противоречившую требованию в той же дельте.
Без свипа она уехала бы в ЖИВУЮ спеку при архивации. Плюс четыре комментария
на фронте, называвших не то поле. Круг критиков снят владельцем третий раз
именно на этом основании.

⚠️ **Строка 2 (планировщик) при этом не задета — проверено, а не предположено.**
Прод-выкат перезапускает `bot-gmp`, а замер строки 2 ровно про то, не тикает ли
планировщик дважды. После рестарта проверка (а) даёт девять минут подряд по
одному исполнителю. `gmp-worker` остался остановленным — его выкат не трогает.

**Ворктри `.claude/worktrees/igm-mark` и ветка `feature/igm-mark-two-meanings`
подлежат жатве** — вся работа в стволе, дерево чистое. История разбора живёт в
`.claude/handoff/знак-igm-и-хвост-редизайна.md`.

### 9. Форма площадки — рекомпозиция ВЫКАЧЕНА, остался непройденный сценарий 403

*Сессия `gmb-v2-ef` ответила, но текстом в свой терминал, а не сообщением —
ответ снят с её транскрипта. Два факта в нём протухли, поправлены проверкой.*

- **Где:** `gmb_v2`, ветки и ворктри НЕТ. Фикс `0e80edb58` («гейт получателя
  платежей бьёт по изменению, а не по наличию ключа») приземлён, ветка
  `fix/venue-save-payee-untouched` удалена, дерево сжато. Основной чекаут в
  detached HEAD.
- 🟢 **Вопрос про ОБЪЁМ закрыт 07.09 в 07:12: рекомпозиция УЖЕ СДЕЛАНА.**
  Владелец: «экран выглядит по-другому, но мне всё ещё не нравится, я его
  вылизывать буду позднее». То есть вылизывание — его собственная будущая
  работа, а не ожидание реестра.
  ✅ **Проверено замером, а не принято на слово** (07.09 07:16): в дереве
  прода `f29483a77` лежат `ad9903a75` (рекомпозиция ритма самоброни, 06.09
  18:26), `259d662d0` (ширина поля), `8751bc456` (`aria-invalid` без вида) и
  `7e6a6768e` (решётка длительностей по видам спорта).
- 🔴 **Строка простояла ~13 часов, описывая мир, которого уже не было.** Она
  писалась 06.09 в 14:40 со словами «разбор готов, код НЕ ТРОНУТ», а код
  тронули в тот же день в 18:26–18:45. Поймал это не реестр, а фраза
  владельца.
  🔑 **Родовой признак:** у строки «ждёт СЛОВА человека» ПРОВЕРКА объявлена
  «машинной нет» — и потому строка слепа к тому, что работу успели сделать
  мимо неё. Такой строке нужна вторая, дешёвая проверка: **сделано ли уже**
  (`git log` по затронутым файлам против дерева прода). Иначе вопрос уедет
  владельцу вторично, а он уже ответил делом.
- **ПРОВЕРКА (сделано ли уже):**

  ```
  cd ~/dev/gmb_v2
  git log --oneline -6 origin/main -- crm/web/src/views/locations/LocationEditor.vue
  ```
- **ПРОВЕРКА (остаток строки):** машинной нет — это сквозной сценарий 403
  ниже, и он ТРОГАЕТ БОЕВЫЕ настройки чужой площадки, поэтому требует слова
  владельца перед прогоном.
- 🟢 **Внешнего факта ждать больше не надо** — сверено 06.09.2026 в 14:40:
  у Екатерины Осмачкиной (user 5521) заведено ЮЛ 14 «ИП Осмачкина Екатерина
  Владимировна», создано 06.09 в 10:06, и у площадки 11 «ТТ Ракеттка»
  `default_legal_entity_id = 14`. Прогонялось так (`scripts/prod_sql.sh`):

  ```sql
  SELECT id, name, owner_user_id FROM legal_entity WHERE owner_user_id = 5521;
  SELECT id, name, default_legal_entity_id FROM location WHERE id = 11;
  ```
- 🟢 **Промоушена ждать не надо:** `0e80edb58` уже в `main` (седьмым от головы
  `60cc797cc`) и накрыт тегами `prod-2026-09-06-*`. Сессия сверила и живой
  прод по ssh: дерево на `60cc797cc`, ветка `main`, коммит внутри — фикс не
  просто смержен, а крутится.
- **Тогда — созрело, делать:** пройти сквозной сценарий, который не
  проверялся ни разу: человек с рангом ≥ 2 на площадке БЕЗ получателя правит
  её настройки и НЕ получает 403. Ради этого фикс и писался.
  ⚠️ **На Кате сценарий больше ничего не различает:** у площадки 11 получатель
  теперь ЮЛ 14, которым она владеет, — её пропустит и старый код, и новый.
  Живые кандидаты (сверено 06.09.2026 в 15:00): **user 1657 на «Вешки Спорт»
  (5)** и **user 14982 на «СК Искра» (12)**, у обоих ранг 3. Площадок без
  получателя восемь: 3, 5, 6, 7, 9, 12, 13, 14.
  ✅ **Проверять МОЖНО под «Войти как»**, и здесь это верный способ: гейт
  `patch_location` читает `_viewing_actor` — то есть `user_id` ЦЕЛИ, — а роль
  `super_admin` в имперсонированном токене не сохраняется, так что актор для
  гейта не-СА. `real_user_id` в этой ручке берут только авторство журнала
  настроек (строка 787) и смена подписи (800).
  ⚠️ Это **не** мина строки 3: там ось другая. Одинаково выглядящие ручки
  расходятся именно здесь — сверять по коду, а не по аналогии.
- **Входить:** хендоффа НЕТ. Состояние — коммит `0e80edb58` и память
  `rights-on-the-wrong-axis.md` (строки 297 и 324).
  ⚠️ Сессия указывает на память `first-legal-entity-deadlock.md` — **такого
  файла нет**, его поглотило слияние памяти 06.09 (100 записей → 35).
  ⚠️ В `rights-on-the-wrong-axis.md` строка 324 «у ТТ Ракеттки нет
  `default_legal_entity_id` вовсе» тоже устарела — с 06.09 он равен 14.

---

### 10. sms-gate — алерт с наблюдениями выкачен, ждёт первого падения модема

- **Где:** `~/dev/sms-gate`, ветка `master`, ворктри нет. Выкачено 06.09.2026
  в 19:39 МСК, ревизия `5f6688f`; на 19:57 `NRestarts=0`, боевой трафик идёт.
- **Ждёт:** **первой настоящей эскалации сторожа модема.** Алерт теперь несёт
  `SIM=… reg=… signal=… operator=…`, и первое же падение ответит на вопрос,
  что говорит `AT+CPIN?` при живой неисправности. Это единственный
  недостающий факт для второй заявки — четвёртой причины в лестнице, чтобы
  сторож перестал тратить soft recovery на карту, которую не перечитывает.
  Добыть его иначе можно только выдернув симку на боевом модеме; владелец
  отказался 06.09, и отказ записан в `proposal.md`, а не потерян.
- **Заявка НЕ архивируется до этого события — решение владельца 06.09.**
  Реализована и выкачена полностью, `openspec list` показывает `✓ Complete`,
  но живая спека `outbound-send` дельту ещё не получила.
- **ПРОВЕРКА (сработал ли сигнал):**

  ```
  ssh -p 30022 home.deralsem.ru "journalctl -u sms-gate -S '2026-09-06 19:39' -g 'SIM=' --no-pager"
  ```

  Непусто — сигнал сработал: возьми из строки снимок, открывай заявку на
  четвёртую причину против раздела «Findings carried forward» в
  `openspec/changes/carry-the-observations-into-the-alert/design.md`, там
  лежат три уже оплаченные 06.09 находки.
- 🔴 **Прежняя ПРОВЕРКА давала ЛОЖНОЕ ЗЕЛЁНОЕ — переписана 07.09 в 07:10.**
  Было `journalctl -u sms-gate -S -7d -g 'hard reset'`, и на 07.09 она
  возвращает непустое: эскалация 06.09 **18:28:54** («Modem unrecoverable;
  hard reset + service restart») с чередой `hard reset on cooldown` с 18:14.
  Но выкат наблюдений — **19:39 того же дня**, то есть эти строки снимка НЕ
  несут и нужного факта не дают. Ждём мы не «эскалации», а **эскалации,
  которая напечатает снимок**, поэтому предикат теперь ищет `SIM=` и режет
  окно моментом выката.
  ✅ **Отрицательный образец есть:** с 19:39 по 07.09 07:08 ни `hard reset`, ни
  `SIM=` не встречаются ни разу, а журнал жив (последняя строка 07:04:39).
  Греп находить умеет — до окна он ловит пять строк.
  🔑 **Родовой признак, переносимый на любую строку:** проверка «сработало ли
  НОВОЕ» с окном ШИРЕ выката меряет старое поведение. У каждой строки, которая
  ждёт первого случая после выката, окно обязано начинаться моментом выката, а
  предикат — спрашивать признак, которого до выката быть не могло.
- **ПРОВЕРКА (не заархивировали ли уже):**

  ```
  cd ~/dev/sms-gate
  openspec list
  ```

- ⚠️ **Пуш в `deploy-remote` рестартит боевой сервис.** Коммиты `2f1f674` и
  `5f845ae` (бухгалтерия заявки, кода не меняют) сознательно НЕ отправлены —
  доедут со следующим выкатом. В github не пушили: владелец счёл ненужным.

### 11. 🔴 Предбронь 8233 пробьёт чек, которого решили не бить — окно 14 часов

*Заведена 06.09.2026 в 20:40. Нашла сессия `G·галка-фейс-контроль`
(диагностически, кода не правила); каждое звено сверено мной по коду и боевой.*

🔑 **Хозяин разбора назван 06.09 в 21:00:** линия фейс-контроля закрылась,
отдав диагностику владельцу, и объявила область своей — если после первой
предброни понадобится разбор КОДА, входить через
`.claude/worktrees/face-control-toggle/.claude/handoff/feature-face-control-toggle.md`
(ворктри чистый, кода линия не трогала). Имя сессии не способ входа: входить
хендоффом.

- **Ждёт:** ПЕРВОЙ предброни на 8233 — 500 ₽ с пары. Окно узкое: предбронь
  **включена 06.09 в 20:16:53** (`updated_at` события), а правило
  `prebooking_window_is_open` (`start − now >= window_days`, окно 5 дней)
  закрывает её **07.09 в 11:00 MSK** — событие 12.09 11:00, площадка 14
  `Europe/Moscow`. Срок доплаты — 3 дня от закрытия, **10.09 11:00**.
- 🔴 **Что случится:** дверь предброни `POST /me/events/{id}/prebook`
  (`gmp/crm/api/me/router.py:1627`) зовёт `create_payment_qr` **с телефоном
  плательщика**, а в `event_payment_service.py:781` блок `Receipt` собирается
  по условию **`if user_phone:`** — не спрашивая ни вида проводки
  (`kind=PREBOOKING`), ни `fiscal_routes`, ни решения «не фискализировать».
  Отказ от чека (`mark_not_fiscalized`, `PREBOOKING_SKIP_REASON`) гасит только
  НАШ облачный путь; Т-Банк печатает inline по блоку `Receipt` в `Init` и про
  наш отказ не знает.
- ✅ **Режим отказа определён: «чек напечатается», а НЕ «оплата упадёт».**
  ЮЛ 8 — ИП Дячук Ксения Степановна, `tax_system = patent`,
  `payment_provider = tbank`, и документ непротиворечив ⇒ касса его ПРИМЕТ.
  Различать обязательно: противоречивая пара «спецрежим + НДС 22 %» дала бы
  отказ `Init` и сломала бы оплату вовсе (боевое 25.08.2026).
  🔑 **Держится ЗАПОЛНЕННЫМ реквизитом, а не умолчанием** (поправка
  `G·галка-фейс-контроль`, сверена на боевой): у ЮЛ 8 `vat_rate = none` стоит
  ЯВНО, поэтому `fiscal_pair_for` выходит ПЕРВОЙ ветвью
  (`requisites.py:207`, `if req.vat_rate:`) и до умолчания по `СПЕЦРЕЖИМЫ`
  управление не доходит. Очисти кто-нибудь `vat_rate` — защита переедет на
  умолчание, исход тот же, причина другая.
- ⚠️ **Подпись в чеке вдобавок неверна по смыслу:** строка собирается как
  `f"Оплата участия в событии #{event.id}"`, тогда как это предбронь — аванс.
- ⚠️ **Признак расчёта будет `full_payment` (тег 1214), и это РЕШЕНИЕ, а не
  недосмотр.** У ЮЛ 8 `payment_method_default` пуст ⇒ подставится
  `DEFAULT_PAYMENT_METHOD = "full_payment"` (`requisites.py:40`).
  🔴 **Поправка к поправке:** соседняя сессия назвала это «неверно фискально,
  потому что `payment_method_for` на этом пути не спрашивается». Первая
  половина верна — путь предброни берёт `req.payment_method_default` напрямую.
  **Вторая половина ничего не меняет:** `ADVANCE_OVERRIDE_ENABLED = False`
  (`receipt/customer.py:94`), и при выключенном перекрытии `payment_method_for`
  возвращает `configured_default or PAYMENT_METHOD_FULL` — **тот же
  `full_payment`**. Позови его этот путь — результат был бы идентичен.
  🔑 **Почему выключено, названо в коде** (`customer.py:82-94`): признак
  «аванс» создаёт обязанность закрывающего документа с зачётом, а механизма
  зачёта нет нигде, кроме абонементов. Владелец выбрал «full-payment не
  страшно» осознанно. Значит это **принятая цена, а не дефект пути предброни**.
  ⚠️ Следствие всё равно наблюдаемо и стоит взгляда: чек объявит ПОЛНЫЙ расчёт
  на 500 ₽ по услуге, стоящей паре 5500 ₽ (2750 × 2), а доплата 5000 ₽ уйдёт
  через `pair_pay`, который inline-блок не шлёт, — второго чека от банка по ней
  не будет вовсе.
- ⚠️ **Асимметрия двух дверей одного события:** полная парная оплата
  (`pair_pay`) зовёт выпуск QR **без** телефона и блок `Receipt` не шлёт.
  Пробьёт чек именно предбронь, а не полная оплата.
- ⚠️ **Со своей стороны проверить нечем:** забираем ли мы чек, напечатанный
  эквайером, — предмет ЖИВОЙ заявки `acquirer-receipt-is-recorded`.
- **ПРОВЕРКА** (`cd ~/dev/gmb_v2`, stdin в `scripts/prod_sql.sh`):

  ```sql
  SELECT id, kind::text, amount, status::text, created_at
  FROM user_transaction
  WHERE event_id = 8233 AND kind::text = 'PREBOOKING'
  ORDER BY id DESC LIMIT 5;
  ```
- **Замер 07.09 07:06:** проводок `PREBOOKING` по 8233 — **0 строк**,
  предброни не было. 🔑 **Окно закрывается СЕГОДНЯ в 11:00 MSK** (правило
  `prebooking_window_is_open`, событие 12.09 11:00, окно 5 дней) — то есть
  строке осталось около четырёх часов. Не сработает до 11:00 — строку снимать
  как непроверенную: наблюдение не состоялось, а условие `if user_phone:`
  остаётся заряженным для следующего события с предбронью.
- **Сработало, когда:** строка появилась. Пока строк нет — предброни не было.
- **Тогда:** смотреть ЖИВЬЁМ, напечатался ли чек и с какой подписью.
- 🟢 **РЕШЕНИЕ ВЛАДЕЛЬЦА 06.09.2026 в 20:45: чек ПРИНЯТЬ и посмотреть живьём.**
  Предбронь не выключать, код в этой линии не чинить. Владелец добавил, что
  починку багов ведёт **другая сессия**, и она идёт первой.
- 🔑 **Значит эта строка теперь про НАБЛЮДЕНИЕ, а не про предотвращение.**
  Первая предбронь на 8233 — это ещё и первый боевой случай для живой заявки
  `acquirer-receipt-is-recorded`: единственный способ узнать, забираем ли мы
  чек, напечатанный эквайером. Смотреть **ЧЕТЫРЕ** факта:
  1. напечатался ли чек вообще;
  2. на какую СУММУ — ожидается 500 ₽;
  3. с какой ПОДПИСЬЮ — ожидается неверная по смыслу «Оплата участия в
     событии #8233» вместо аванса;
  4. с каким ТЕГОМ 1214 — ожидается `full_payment`, и это ожидание
     **подтверждающее решение**, а не находка: увидев `full_payment`, ничего
     не чинить.
- ⚠️ **Условие `if user_phone:` остаётся заряженным и после 8233.** Окно
  закроется само 07.09 в 11:00, но следующее событие с предбронью получит ту
  же сборку. Приняв чек здесь, владелец принял ОДИН случай, а не правило.
- **Входить:** своего хендоффа у строки нет. Состояние — эта строка плюс
  память `who-fiscalizes-what-in-prod` и `tournament-intake-already-built`;
  разбор кода — хендофф фейс-контроля, назван выше.
- ✅ **Четвёртый факт наблюдения проверен ОБЕИМИ линиями независимо и признан
  подтверждающим.** Нашедшая сессия сверила `ADVANCE_OVERRIDE_ENABLED` сама и
  сняла свой вывод: признак расчёта `full_payment` — решение 24.08.2026 на ВСЕ
  чеки проекта, а не дефект пути предброни. Увидев «полный расчёт», не чинить.

### 12. Турбо — снятая маскировка push ждёт данных, пороги ждут её

*Заведена 07.09.2026. Репозиторий `~/dev/turbo01`, ветка `main`. Хендоффа нет:
состояние живёт в заявках OpenSpec, входить ими.*

- **Чего ждём:** накопления платежей с заполненным `receipt_url_via`. Замер 1.1
  заявки `turbo-fiscal-observability` уже делали — данных не было ФИЗИЧЕСКИ:
  поле завели миграцией за часы до замера, через него прошло два платежа при
  652 всего и ~2,4 в сутки.
- **Что стоит:** `turbo-unmask-push` 12/16 — открыты только 4.1–4.3, все три
  наблюдательные, кода в них нет. За ними `turbo-fiscal-observability` 5/23:
  её 1.2 «пороги назвать НЕЛЬЗЯ» стоит ровно на этих же данных.
- **ПРОВЕРКА — хост `mprz`, БД `turboroute`:**

```
cd ~/dev/turbo01
.venv/bin/python ~/checks/receipt_via.py
```

- **Сработало, когда:** в разбивке `receipt_url_via` наберутся десятки записей
  (счёт идёт от 22 на 07.09.2026, +2…3 в сутки) — тогда 4.1 закрывается и 1.2
  получает почву. Появление `retry` — отдельное событие: до датчика это
  ЕДИНСТВЕННЫЙ признак, что push-канал лёг.
- ✅ **4.2 фактически уже выполнена:** на 07.09.2026 все 22 записи — `push`,
  ни одной `retry` с 28.08. Канал жив, перевес `retry` не наступал.
- ⚠️ **`turbo-token-refresh` (11/12) сюда НЕ входит** и в отложке ему не место:
  его 3.3 ждёт нашей выкатки, а не события снаружи. Это недоделанная работа —
  по правилу шапки она живёт в заявке.

## Ждёт без сессии — сигнал есть, хозяина нет

Это уже записано в памяти gmb_v2, здесь только чтобы не терялось при вопросе
«что созрело». Тела и точные предикаты — в названных записях.

- **Эвотор-push** ждёт первого боевого платежа ЮЛ 4; замеры групп 0 и 6
  заявки `evotor-receipt-push` до тех пор не закрыть, заявку не архивировать.
  Память: `inbound-webhook-map`.
- **Облачная касса КОМТЕТ** не проверена ничем и проснётся в день, когда
  кому-то настроят `receipt_provider`; первую же дистанционную продажу после
  этого смотреть руками. Память: `who-fiscalizes-what-in-prod`.
- **Возврат по чеку** живьём не исполнялся; сигнал — первая продажа с пробитым
  чеком на площадке с живой ККТ (сегодня «Сокол»). На демо непроверяем
  структурно. Память: `charge-has-no-refund-state`.
- **Предбронь турнира** в проде с 05.09, но нигде не включена — ждёт живого
  турнира; проверять, что первая проводка несёт `not_fiscalized` +
  `patent_exempt` и чек по ней НЕ пробит. Память: `tournament-intake-already-built`.

---

## Что всплыло самим опросом

- 🔴 **Две сессии жили в ОДНОМ ворктри одновременно** —
  `F·feature/face-control-toggle` и `G·галка-фейс-контроль`, обе на
  `feature/face-control-toggle` в `.claude/worktrees/face-control-toggle`. Не
  родитель с преемником, а две параллельные линии. Доказано не догадкой: коммит
  `280c9b5b8` одной лёг поверх двух другой, и `land.sh` подобрал его и
  приземлил как свой. Не затёрли ничего только потому, что правили разные
  файлы. По фейс-контролю будить **одну** линию, а не обе.
- ⚠️ **Поле `started` в `ListAgents` — метка старта, а не простоя.** «Пульт.ехе
  · waiting · started 4d ago» читалось как «висит четвёртые сутки», а сессия
  работала, и владелец был в диалоге минутами раньше. Судить об активности по
  этому полю нельзя — надо спрашивать.
- 🔴 **«Не ответила» значит два разных состояния, и на глаз они одинаковы.**
  `gmb-v2-ef` отработала опрос за 18 секунд и написала полный ответ — себе в
  терминал, обычным текстом. Спрашивающему не видно ничего: чужой текст до
  него не доходит, доходит только сообщение. Различить «не дошла очередь» и
  «ответила мимо» можно лишь грепом транскрипта по `msg_id` отправки —
  `grep -rl <msg_id> ~/.claude/projects/*/*.jsonl`, дальше читать хвост файла.
  Не найдя ответа, сперва грепнуть, а уж потом писать «не ответила».
- 🔴 **Скриншоты через CDP не снимаются, и виноват НЕ load.**
  `Page.captureScreenshot` таймаутит за 30 с при живом JS на той же вкладке —
  четыре раза за две сессии, при нагрузке 3.67. Прошлая сессия винила
  load 25–35 и записала это в хендофф; замер опроверг. Рабочий обход: свой
  http-сервер (обязательно `charset=utf-8`, иначе кириллица мохибейком и
  ширины врут) + `javascript_tool` + `getBoundingClientRect`.
- ⚠️ **Опрос сам себе не гарантия: строку 8 я написал по УСТАРЕВШЕМУ хендоффу,
  решив, что сессия молчит.** Она отвечала, мы разминулись, и вышло «ждёт
  человека» вместо «ведёт живой разговор, закрывать нельзя». Молчание в ответ
  на опрос — не факт, а отсутствие факта; строка, снятая с диска, обязана
  нести пометку об этом и перепроверяться у сессии, если та жива.
- ⚠️ **Имя сессии протухает.** «M·остался шаг 5» — шаг 5 исполнен в тот же
  день, имя осталось. По имени о состоянии не судить.

## Опрошены и НЕ в отложке (06.09.2026)

Чтобы не опрашивать заново.

- **dev-0d** — интерактивное администрирование домашнего сервера, не
  разработка, ветки нет. Незакрытая рекомендация, от которой владелец
  отказался: на `derserver` глобально разрешён парольный вход по ssh при
  пробросе снаружи.
- **«Стекло» в muzloto** — заведена строкой 11 06.09 вечером и в тот же
  вечер отработала: владелец прогнал площадочную Windows, подтвердил
  картинку глазами, заявка заархивирована (`2026-09-06-glass`) и влита в
  `master` перемоткой. Строка снята. ⚠️ Держалась в реестре часы, а не дни:
  событие, которого она ждала, оказалось у владельца под рукой. Прежде чем
  заводить строку — спросить, не делается ли это сейчас.
- **Пульт.ехе** — `~/dev/muzloto`, ветка `master`, работает прямо сейчас,
  владелец в диалоге. ⚠️ В `ListAgents` она выглядела «waiting, 4d» — это
  **метка старта, а не простоя**. По этому полю судить об активности нельзя.

## Не ответили на опрос (06.09.2026)

Спрошены, ответа нет. Простаивающая сессия разбирает очередь только на своём
следующем ходу, а без человека за клавиатурой его может и не быть.

- **G·знак Igm** и **G·перенос строки на проде** — состояние взято с их
  хендоффов, строки 7 и 8 выше. Там всё записано, добирать нечего.
- ~~gmb-v2-ef~~ — ответила 06.09 в 14:11, это строка 9 (форма площадки на
  «ТТ Ракеттке»). ⚠️ Ответ ушёл **текстом в её собственный терминал, а не
  сообщением**, и до спрашивающего не дошёл вовсе.
- ~~gmb-v2-99~~ — ответила позже опроса. Её строка «вход по номеру» **снята
  06.09 вечером**: владелец сам решил проверять на проде, а заявку
  `sms-login-names-the-number` заархивировали — оба SQL отвечали на вопрос
  «пора ли закрывать заявку», и вопроса больше нет. Справка о выкаченном
  состоянии осталась в `.claude/handoff/feature-login-shows-the-number.md`.

⚠️ Опрос покрыл только **интерактивные** сессии. Двадцать пять строк
`Remote Control · offline` недостижимы — их состояние живёт только на диске.
