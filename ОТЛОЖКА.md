# Отложка — работа, которая ждёт события, а не рук

Реестр заведён 06.09.2026 опросом всех живых сессий. Здесь только то, что
**ждёт снаружи**: оплаты, даты, решения человека, первого боевого случая.
Работа, которую просто не доделали, сюда не попадает — она живёт в своём
хендоффе и заявке.

**Реестр ведёт линия «G·отложка ведение».** Её состояние и следующее действие —
`~/dev/gmb_v2/.claude/handoff/отложка-ведение.md` (основной чекаут, не ворктри,
жатве не подлежит). Входить туда, а не искать сессию: сессии умирают, их имена
к утру бесполезны.

## Как отвечать на вопрос «что в отложке»

1. Прогнать **ПРОВЕРКУ** каждой строки — это готовые команды, не рассуждения.
2. Разделить на «созрело» и «ещё ждёт». Созрело = проверка вернула то, что
   написано в «сработало, когда».
3. Назвать по каждому созревшему: **чем входить** (хендофф) и **что делать**.

⚠️ **Восстанавливать надо не сессию, а ветку с хендоффом.** Сессии умирают,
их id к утру бесполезны, а транскрипт ворктри в этом проекте уже пропадал при
живой сессии. Точка входа — файл хендоффа, а не «вернись в тот таб». Одна
сессия прямо попросила: контекст 450k, входить новой сессией с хендоффа.

⚠️ Строки НЕ протухают сами. Отработала — вычеркнуть руками, иначе реестр
начнёт врать в сторону «всё ещё ждём».

⚠️ **У ПРОВЕРКИ обязан быть назван ХОСТ.** Команда без хоста читается как
местная, местный прогон честно не находит путей — и рождается вывод «проверка
неисполнима», который ничем не отличается от «ещё ждёт». Строка 6 простояла так
сутки, будучи созревшей. Правило симметрично уроку строки 7: проверка, которая
ИСПОЛНЯЕТСЯ, ещё не проверка — и проверка, объявленная НЕИСПОЛНИМОЙ, тоже
требует доказательства, а «у меня локально нет такого каталога» им не является.

⚠️ **ОТРИЦАТЕЛЬНЫЙ ОТВЕТ ПРОВЕРКИ НИЧЕГО НЕ ДОКАЗЫВАЕТ, пока та же проверка
хоть раз не показала положительный.** Правило выведено 07.09.2026 из ДВУХ
случаев одного утра — причины разные, исход один:
- строка 11 спрашивала `kind::text = 'PREBOOKING'` заглавными против строчных
  в базе и вернула `(0 rows)` при живой проводке часом раньше;
- проверка уведомлений пошла в `journalctl -u bot-gmp` и вернула чистый ноль —
  а расписание с 07.09 07:57 крутится в `gmp-worker`, и бот тут ни при чём.

🔑 **Обе врали В СТОРОНУ ТИШИНЫ** — не «тревога зря», а «ждём дальше» при уже
наступившем событии. Такая ложь неотличима от честного ожидания и не поднимает
тревогу НИКОГДА: строка молча стоит, пока событие не протухнет. Спасала оба
раза не аккуратность запроса, а сверка с НЕЗАВИСИМЫМ источником — чужой номер
проводки, чужой журнал.

**Отсюда обязанность:** у каждой ПРОВЕРКИ должен быть предъявлен
**положительный образец** — момент, когда она что-то нашла (см. строку 1: «тем
же грепом виден настоящий платёж 1442, то есть она умеет находить»). Проверка
без такого образца — не проверка, а обещание.

⚠️ **ПОЛОЖИТЕЛЬНЫЙ ОБРАЗЕЦ НЕ СПАСАЕТ ОТ ПРОТИВОПОЛОЖНОЙ ЛЖИ — в сторону
ТРЕВОГИ.** Выведено 07.09.2026 вечером из строки 13: у её проверки образец был
предъявлен утром, и к вечеру она сработала обоими плечами сразу — завелась
чужая заявка и появился новый раздел контракта, — а ответа, которого строка
ждала, в них не было вовсе. По букве предиката строку следовало снимать.
🔑 **Причина в форме предиката, а не в его аккуратности:** «появилось ли у
соседей что-нибудь новое» меряет ЧУЖУЮ АКТИВНОСТЬ, а не наш ответ, и потому
срабатывает на любом их шаге. Сторона, которая ведёт свою работу, шевелится
постоянно.
**Отсюда вторая обязанность:** предикат обязан спрашивать ТО САМОЕ решение,
а не его окрестность. Если машинно различить нельзя — проверка делается
двухшаговой: дешёвый машинный признак (появился раздел с номером больше
прежнего) плюс явное «тело прочитать обязательно, сам по себе признак — не
срабатывание». Строка, которую можно снять не читая, снимется по ошибке.

⚠️ **У КОНЪЮНКТИВНОГО предиката образец добывается ПО ПОЛОВИНАМ, и его ноль
двусмыслен.** Выведено 08.09.2026 из строки 15. Предикат «выбран И противоречив»
показывал ноль двое суток подряд, и образца у него не было ни одного — то есть
он не был отличим от сломанного. Прогон половин порознь по тому же окну снял
вопрос за одну команду: `contradicted=1` даёт 228, `outcome='chosen'` даёт 206.
🔑 **Но заодно выяснилось, что ноль конъюнкции читается ДВОЯКО:** «событий нет»
и «ветка кода ни разу не исполнялась» — не одно и то же. Здесь все 228
противоречий сидели на кандидатах, отсеянных РАНЬШЕ по другой причине, так что
переключатель не решал ничего и не решил бы. Строка честно писала «включать
нечего», а истина была сильнее: механизм не запускался. Обязанность: у
конъюнкции спрашивать не только пересечение, но и **разбивку по исходам** —
она отличает молчание от неработы.

⚠️ **Качественный предикат — не проверка, а намерение.** «Наберутся десятки»,
«станет заметно», «накопится достаточно» (строка 12) не различают 24 от 40:
момент срабатывания зависит от читающего. Такой предикат не умеет дать
отрицательный ответ, а значит не умеет дать и положительный. Числа называет
владеющая линия; пока числа нет — в строке держать ЗАМЕР И ТЕМП, чтобы
размытость была видна, а не выглядела ожиданием.

---

## Ждёт: 10 строк (сквозная нумерация — до 19)

⚠️ **Нумерация сквозная и не пересобирается.** Снятая строка остаётся
надгробием на своём номере: на номера ссылаются хендоффы, память и правки
сестринских сессий, а пересборка тихо переадресует их на чужую работу.

### 1. ОФД в rk_bot — ✅ СРАБОТАЛ В БОЮ 07.09.2026; ждёт только ответа Эвотора

*Шаг «руками на ВДС» ЗАКРЫТ 06.09.2026 в 17:48 МСК.*

- **Где:** `~/dev/rk_bot`, ветка `main`. ✅ **Выкачено 07.09.2026 07:54 МСК —
  прод на `b53a6aa`**, долг по `1274b18` (опечатка `🧾e` в подписи к PDF) закрыт
  вместе с документными коммитами, отдельного рестарта ради него не делали.
  Набор 95/95 `OK` без `skipped`; живая цепочка проверена и перепроверена через
  6 минут — строка готовности, `NRestarts=0`, проба снаружи `200`, ноль ошибок.
  🔑 Что впереди прода — смотреть `git log --oneline b53a6aa..main -- '*.py'`
  (ждёшь пустоты: доки едут бесплатно, код значит выкатку). Счёт коммитов сюда
  не писать: он ложен уже к моменту записи, потому что хендофф лежит в том же
  репозитории, о котором отчитывается.
- ✅ **Сделано и проверено:** `location` вставлен, nginx перечитан,
  `OFD_CALLBACK_KEY` задан, `bot-rk` перезапущен; в журнале
  `Payment webhook listening on localhost:3005`, проба снаружи отдаёт 200
  `application/json` при заданном ключе. Канал включён — адрес приёмника
  уезжает кассе в каждой команде продажи.
- ✅ **СОБЫТИЕ НАСТУПИЛО: платёж 1443, 07.09.2026 08:37 МСК, 1290 ₽.**
  Подача → `код 200, приёмник mprz.ru` (адрес, а не прочерк). Касса позвонила
  в 08:37:52 — **ссылку записал ЗВОНОК**; опрос пришёл в 08:37:54 и напечатал
  «уже записана, опрос ничего не пишет и не шлёт». Гонка двух путей случилась
  настоящая, с запасом 2,8 с, и разрешил её условный `UPDATE`. Задачи 9.1 и
  11.5 закрыты, заявка **56/57**.
- **Ждёт теперь только 9.5 — ответа поддержки Эвотора** (письмо у владельца).
  Заявку до него не архивировать. Кода это не касается: канал доказан.
- **Замер 06.09 19:15** (`ssh mprz 'journalctl -u bot-rk --since "2026-09-06
  17:48" | grep -i "ОФД\|Payment webhook"'`): строк «ОФД» нет, платежа не
  было — ЖДЁТ ДАЛЬШЕ. Приёмник жив, `Payment webhook listening` в 17:48,
  18:54 и 18:55. ✅ Проверка НЕ вакуумна: тем же грепом виден настоящий
  платёж 1442 от 15:03, то есть она умеет находить.
- **Состояние линии** (от неё самой, 06.09 19:40): задачи **54/57**, открыты
  9.1, 9.5 и 11.5 — все ждут одного и того же платежа; прод на `e03605a`
  (выкачен 18:54), локально шесть непушенных, все документные.
- 🔑 **Содержание ожидания СУЗИЛОСЬ: наша половина 9.1 закрыта без платежа.**
  Подписанный POST по платежу 1442 прошёл через nginx, подпись сошлась, битая
  отвергнута с записью в журнал. Неизвестно ровно одно — **позвонит ли касса**.
- 🔑 **ПРОВЕРКА УЖЕСТОЧЕНА: искать не слово «ОФД», а АДРЕС ПРИЁМНИКА.**
  Прежний греп ловил слишком широко; различающий признак — что стоит в строке
  подачи после «приёмник».

  ```
  ssh mprz 'journalctl -u bot-rk --since "2026-09-06 17:48" | grep "приёмник"'
  ```
  Прочерк — канал не сработал, адрес — сработал. ✅ **Отрицательный образец
  проверен 06.09 19:40:** у платежа 1442 (15:03:41) стоит прочерк, и это
  правильно — он прошёл до включения ключа. То есть греп находит строки и
  умеет показать оба исхода.
- **ПРОВЕРКА:** `journalctl -u bot-rk | grep ОФД` — строка «ОФД: ссылка …
  записана через опрос» либо «… по уведомлению». Машинно: в БД rk строка
  `payments` со `status=SUCCESS`, непустыми `receipt_uuid`/`receipt_url` и
  `paid_at` позже 06.09.2026 17:48.
- ⚠️ **Строку «часы кассы отстают, разрыв растёт» в реестр НЕ заводить — вывод
  ретрагирован автором** (`5dd7a59` в rk_bot, `7c9a034` в ofd-providers; в
  обоих заголовок сам несёт ретракцию, чтобы читающий историю не остановился
  на `6a9c018`). Наблюдаемый остаток = смещение + СЕКУНДА ПОДАЧИ, касса режет
  до минуты; пять «растущих» точек просто отсортированы по секунде подачи.
  Смещение зажато в (3м39с, 3м45с] и за шесть дней не двигается ВОВСЕ.
  🔑 **Признак ошибки записан проверяемым:** ряд из пяти точек с размахом
  МЕНЬШЕ шага квантования не может свидетельствовать о тренде — размах 54 с
  при шаге 60 с. Это переносится на любой ряд, снятый с округляющего
  источника.
  ⚠️ Версия «виноваты часы кассы» снята ЦЕЛИКОМ, а не смягчена: сервер
  синхронен по NTP, ответ кассы стабилен побайтно через трое суток, владелец
  сообщает синхронность кассы — три показания не сходятся, и слабым звеном
  оказалось НАШЕ толкование `receipt_datetime` (взято из замера, а не из
  вендорского референса). Разрешит либо Эвотор, либо печатный чек следующего
  платежа. Хозяин есть, срочности нет.
- 🔴 **Про любой рестарт `bot-rk`, если придётся трогать канал:** `curl` по
  пробе сразу после рестарта отдаёт **502**, сразу после reload nginx —
  **404**, и оба раза это ГОНКА, а не отказ. Юнит `Type=simple`, поэтому
  `systemctl is-active` показывает `active` за шесть секунд до открытия сокета
  3005, а nginx ещё дообслуживает старыми воркерами. Судить по строке
  `Payment webhook listening` в журнале, дав 10 секунд. На этом уже потеряли
  час: 404 прочитали как ошибку конфига. Записана и в хендоффе; здесь
  оставлена намеренно — это свойство `Type=simple`, а не факт состояния,
  поэтому не протухнет и сторожит саму утреннюю проверку.
- **Замер 07.09 07:07** (греп по «приёмник» и «ОФД» с 06.09 17:48): строк с
  адресом приёмника нет — боевого платежа за ночь НЕ БЫЛО, ЖДЁТ ДАЛЬШЕ.
  Единственная находка — `19:24:19 ОФД: подпись не сошлась, платёж 1442`, это
  негативная проба самой линии, а не боевой случай. Приёмник жив.
- **Тогда:** закрыть группу 9 — сверить сумму и дату с ПЕЧАТНЫМ чеком ОФД,
  убедиться, что канарейка «Пространство имён кассы занято» не поднялась,
  обновить строку `romantsova_store` в реестре, затем архивация (42/47).
- **Входить:** `~/dev/rk_bot/openspec/changes/adopt-ofd-providers/HANDOFF.md`
  — там пошаговые команды ВДС и ожидаемый вывод каждой.
- **Кто ведёт сейчас:** сессия «O·внедрение rk_bot — ждём первого боевого
  платежа» (прежняя расщеплена 06.09 и закрыта). Это пометка, а НЕ способ
  входа: входить всё равно хендоффом — имя таба к утру бесполезно.

### 2. ~~Планировщик~~ — СНЯТА 08.09.2026 в 06:15, приёмка 9.2–9.4 ЗАКРЫТА

🟢 **Замер 08.09 в 06:10: 28 имён из 28, 13955 проходов, плохих исходов 0.**
Недостающим был суточный `sms_pii_purge` (окно 03:40) — пришёл ночью сам, как и
предсказано вечерним замером. 9.2, 9.3 и 9.4 закрыты.

✅ **Хрупкая часть проверена отдельно и вышла СИЛЬНЕЕ предиката строки.**
Предикат спрашивал «в выборке за 10 минут везде 1»; прогнан весь период с
07.09 07:58 — **ноль минут с дублем**. Двойника исполнителя не было ни разу.
✅ Аптайм `mprz.ru` — **19 дней 23 часа**, перезагрузки не было, то есть
объявленная хрупкость («`gmp-worker` `enabled`, перезагрузка поднимет его сама
и испортит сутки») не срабатывала. Три юнита `active`.

⚠️ **Снята в 06:15, а не в объявленные 08:00, и вот почему это не срез угла.**
Ориентир «сутки» стоял по названной причине: «проход не дошёл до окна»
неотличимо от «проход потерялся». Причина исчерпана в тот момент, когда
появилось 28-е имя, — ждать оставалось не событие, а часы. 13955 проходов за
22 часа отвечают и на 9.3 («счёт пропорционален периодам»). Держать строку
ради круглого числа значило бы путать срок с предикатом.

🔴 **9.5 остаётся ОТКРЫТОЙ и уходит из отложки вместе со строкой.** Сторож
молчания настоящего молчуна не ловил ни разу; закрывается ТРЕТЬИМ выкатом
(снять регистрацию одного долгого прохода, дождаться окна, убедиться, что имя
названо). Это не ожидание события, а недоделанная работа — по правилу шапки её
место в заявке `scheduler-runs-in-its-own-unit`, а не здесь.
**Входить:** `.claude/handoff/scheduler-decouples-from-the-bot.md` (приземлён в
`origin/main`, переживает жатву ворктри).

---

*Ниже — прежнее тело строки, оставлено надгробием.*

- **Где:** ветка `scheduler-decouples-from-the-bot`, ворктри
  `.claude/worktrees/scheduler-decouple` (запушена, дерево чистое)
- **Ждёт:** не события и не человека — просто должно накопиться. Точнее, ждёт
  ОТСУТСТВИЯ события: гарантию «исполнитель один» держит не код, а то, что
  машину не перезагрузят, и проверка (а) — единственное, что это ловит. Отсчёт с
  06.09.2026 07:23. Переезд планировщика в свой юнит обнулит счёт, с которым
  его же приёмка будет сравнивать.
- **ПРОВЕРКА:** два запроса через `./scripts/prod_sql.sh -f файл.sql` —
  (а) исполнитель ОДИН: `SELECT date_trunc('minute', started_at), count(*) FROM
  job_run WHERE job_name='close_started_events' AND started_at > now() -
  interval '10 minutes' GROUP BY 1 ORDER BY 1 DESC;` — везде обязана быть 1;
  (б) сама линия: `SELECT job_name, outcome, count(*), max(items) FROM job_run
  WHERE started_at > '2026-09-06 07:23' GROUP BY 1,2 ORDER BY 1,2;`
- **Замер 06.09 19:12:** (а) десять минут подряд — везде ровно **1**, линия
  цела. (б) идёт: `pending_payment_expiry` 706 холостых, `receipt_polling`
  689 холостых при 19 рабочих, `organizer_notice_flush` 702/4. До готовности
  ~12 часов.
- 🟢 **СРАБОТАЛО 07.09.2026 в 07:23:30** — базовая линия засчитана, все четыре
  условия проверены замером:
  - (а) за последние 10 минут (07:14–07:23) — везде ровно **1**;
  - **за ВЕСЬ период 06.09 07:23 → 07.09 07:24 — ноль минут с дублем**, 1434
    прохода на 1434 разные минуты. Это сильнее предиката строки: он смотрел
    выборку в 10 минут, а гарантия нужна на сутки;
  - аптайм `mprz.ru` — **19 дней 25 минут**, перезагрузки не было;
  - `gmp-worker` — `inactive`, `enabled`; `bot-gmp` — `active running`.
- 🔴 **Семь минут суток БЕЗ прохода — это ДЕСЯТЬ ПРОД-ВЫКАТОВ, а не дрейф
  фазы.** 08:56, 12:40, 13:48, 19:18, 22:06, 22:41, 22:54.
  ⚠️ **Первое объяснение реестра («цикл спит 60 секунд ПОСЛЕ прохода») было
  НЕВЕРНЫМ и снято 07.09 в 07:45.** Опровергла сессия `scheduler-decouple-6b`,
  я перепроверил сам и подтвердил. Ошибка сделана на слабой улике: интервалы
  105 и 127 секунд вокруг дыр «сходились» с версией дрейфа, хотя ХОЛОСТОЙ
  проход длится миллисекунды — версия не объясняла саму ВЕЛИЧИНУ разрыва, и
  это было видно сразу.
  ✅ **Верное объяснение, проверено тремя независимыми источниками:**
  1. `participation_awards` за сутки — 34 строки: 24 ровно в `:50:00` и 10 вне
     сетки. Вне сетки он попадает единственным способом — `next_run_time =
     now + 90s`, догон при старте процесса (`scheduler.py:858`). Все семь дыр
     плюс 90 секунд дают внесеточные строки с точностью до секунд.
  2. `journalctl -u bot-gmp -t systemd` за те же сутки — ровно **10**
     рестартов: 08:56:12, 12:40:05, 13:47:48, 18:00:04, 19:17:49, 19:53:03,
     21:51:22, 22:05:58, 22:40:50, 22:53:57. Все чистые — `Deactivated
     successfully`, ни одного `Scheduled restart`, ни одного ненулевого
     выхода. То есть намеренные, не падения.
  3. `git reflog` дерева прода — перед КАЖДЫМ рестартом за 19–22 секунды стоит
     `pull --ff-only origin main: Fast-forward`, и SHA каждый раз двигался.
     Десять из десяти. **Каждый рестарт — прод-выкат**, включая четыре,
     сгрудившиеся в час (21:51, 22:06, 22:41, 22:54).
  Три рестарта (18:00, 19:53, 21:51) дыры в минутном проходе не оставили —
  повезло с фазой.
- 🔑 **Следствие для приёмки 9.2–9.5 — ОБРАТНОЕ прежнему.** Переезд НЕ меняет
  триггеры: ветка `scheduler-decouples-from-the-bot` только убирает запуск из
  `gmp/runtime/lifecycle.py`, а `get_scheduler` и все триггеры те же (сверено
  07.09: греп по `IntervalTrigger|CronTrigger|add_job` в диффе `gmp/` ПУСТ).
  Фазовое поведение не изменится, значит **сверять надо именно количество
  проходов**, дубли — дополнительно.
  🔑 **И это довод ЗА переезд, которого в заявке не было:** сегодня расписание
  умирает вместе с ботом ДЕСЯТЬ раз в сутки, по разу на каждый выкат. После
  переезда перестанет.
- ✅ **Ветка `did_work` исполнялась, опасение хендоффа снято** (замер соседней
  линии, 07.09): `sms_delivery_tracking` 68 раз, `receipt_polling` 19,
  `organizer_notice_flush` 6, `participation_awards` 3 (до 12 единиц за тик).
  ⚠️ «Все `idle`» в замере выше относится ТОЛЬКО к `close_started_events`.
- 🟢 **ПЕРЕЕЗД В ПРОДЕ 07.09.2026 в 07:57.** `main` = `054f8bb3c`, тег
  `prod-2026-09-07-scheduler-in-worker`. Выкатила сессия
  `scheduler-decouple-6b`; гейт на том же SHA — 15690 passed RC=0,
  `lint-imports` 3 kept 0 broken, `vue-tsc -b` RC=0, миграции не было.
  ✅ **Сверено мной независимо 07.09 в 08:17, а не принято на слово:**
  дерево прода на `054f8bb3c` с этим тегом; `gmp-worker` `active` с 07:57:18,
  `bot-gmp` с 07:57:21, `gmp-crm` `active`; ошибок уровня `err` в воркере
  после старта нет.
  🔑 **Двойника исполнителя НЕТ, и это проверено различающим признаком.**
  В журнале бота после 07:57 сперва нашлось 14 строк `apscheduler` — но
  последняя из них в 07:57:10, то есть ДО его рестарта в 07:57:21; после 07:58
  их **ноль**. По `job_run` с 07:58: 19 проходов `close_started_events` на 19
  минут, **ноль минут с дублем**, имён уже 22 из 28.
  ⚠️ **Ноль строк `apscheduler` в журнале ВОРКЕРА — это норма, а не пропажа:**
  коммит `054f8bb3c` (задача 10.1) снял подпорку INFO, след прогонов держит
  таблица `job_run`. Не читать этот ноль как «планировщик не поднялся» —
  различающий признак теперь в базе, а не в журнале.
- ✅ **Шаг «попросить человека `sudo systemctl start gmp-worker`» ВЫПАЛ из
  порядка.** Переезд добавил `gmp-worker` в `SERVICES` в `deploy.sh`, а
  `systemctl restart` поднимает и остановленный юнит. Сверено в дереве прода:
  строка 99 — `SERVICES="gmp-crm bot-gmp gmp-worker"`. Ручной команды не
  потребовалось.
- 🔴 **Строку НЕ снимать: приёмка НЕПОЛНАЯ.** По докладу ведущей линии
  (07.09 08:10): 9.4 ✅ (`error` и `timeout` ноль после рестарта); одиночность
  исполнителя ✅; **9.2 🟡** — имён 19 из 28 на шести минутах, девять просто не
  дошли до окна (десятиминутные, получасовой, часовые `:25`/`:40`,
  `cleanup_old_organizer_summaries` в 1/7/13/19, `conversation_reminder` в
  08:05, суточные `sms_pii_purge` 03:40 и `widget_door_silence` 09:20);
  **9.3 🟡** — счёт пропорционален периодам, полный ответ даёт день;
  **9.5 🔴** — открыта, сторож молчания настоящего молчуна не ловил ни разу.
  ⚠️ **Раньше суток закрывать нельзя:** «проход не дошёл до окна» неотличимо
  от «проход потерялся».
- 🟢 **ВЛАДЕЛЕЦ ЗАКРЫЛ ЛИНИЮ 07.09 в 08:53 словами «скедулер отработал»** и
  закрывает ведущую сессию `scheduler-decouple-6b`. Переезд состоялся, оба
  выката в проде, расписание крутится в своём юните.
  ✅ **Замер 07.09 08:53, мой:** 582 прохода с 07:58, **ноль минут с дублем**,
  плохих исходов 0, имён 25 из 28.
- 🔴 **Строка ОСТАЁТСЯ ЖИВОЙ, и вот почему — иначе остаток приёмки потеряет
  хозяина.** Закрытие сессии не закрывает ожидание: 9.2/9.3 требуют СУТОК
  (три имени из 28 ещё не дошли до своих окон — суточные и редкие), а 9.5
  открыта. Ждать здесь нечего, кроме времени, и это ровно случай отложки.
  ⚠️ «Отработал» относится к ПЕРЕЕЗДУ, а не к приёмке. Строка теперь стережёт
  только остаток.
- ✅ **Замер 07.09 в 18:57: приёмка почти дошла.** 27 имён из 28, **6888
  проходов**, плохих исходов **0**. Недостающее имя ровно одно и оно названо:
  **`sms_pii_purge`** — суточный, окно 03:40, отработает ночью. То есть 9.2
  закроется само к утру, руками ничего не нужно.
- **Ориентир — 08.09.2026 около 08:00 МСК**, сутки от рестарта переезда.
  **Строку снять руками тогда**, если ПРОВЕРКА ниже даст 28 имён и `ploho` = 0.
- 🔑 **Входить БЕЗ сессии** (её закрывают, имя к утру бесполезно): хендофф
  `.claude/handoff/scheduler-decouples-from-the-bot.md` — он **приземлён в
  `origin/main`**, поэтому переживёт и жатву ворктри
  `.claude/worktrees/scheduler-decouple`, где лежит его рабочая копия.
- 🔴 **9.5 — единственная содержательная дыра приёмки, и она НЕ про время.**
  Сторож молчания настоящего молчуна не ловил ни разу. Закрывается третьим
  выкатом: снять регистрацию одного долгого прохода, дождаться его окна,
  убедиться, что названо имя. Работа отдельной сессии; сюда не поместится.
- **ПРОВЕРКА созревания приёмки** (`cd ~/dev/gmb_v2`, stdin в `prod_sql.sh`):

  ```sql
  SELECT count(DISTINCT job_name) AS imen, count(*) AS prohodov,
         count(*) FILTER (WHERE outcome NOT IN ('idle','did_work')) AS ploho
  FROM job_run WHERE started_at >= '2026-09-07 07:58';
  ```
  Имён 28 и `ploho` = 0 — приёмка 9.2/9.3/9.4 закрыта. 9.5 ждёт отдельно.
- 🟢 **ВТОРОЙ ВЫКАТ 07.09 в 08:42 — починка сторожа молчания В ПРОДЕ, прежнее
  предупреждение о ночных тревогах СНЯТО.** `main` = `da2a6cba0`, тег
  `prod-2026-09-07-silence-check-fix`, владелец авторизовал отдельно. Гейт:
  15707 passed RC=0, `lint-imports` 3 kept 0 broken, миграции нет.
  ✅ **Сверено мной 07.09 в 08:44:** дерево и тег на месте, все три юнита
  `active`, ошибок `err` в воркере нет, ноль минут с дублем, 485 проходов,
  25 имён, плохих исходов 0.
  ⚠️ **Поправка к докладу линии по факту замера:** воркер поднялся в
  **08:42:38**, а не «около 08:35». На отсечку приёмки не влияет — она
  осталась от переезда.
  🔑 **Ночью 07→08.09 тревог быть НЕ должно.** Придут — это НАХОДКА, а не
  известное поведение. Различающий признак: починка меняет выводимый период
  `conversation_reminder` с часа на одиннадцать часов, окно `3 × период` стало
  33 часа вместо трёх; тревога при живом проходе означала бы, что
  `_наибольший_разрыв` не доехал.
- ⚠️ **Рестартов после переезда ДВА, не один:** 07:57:25 (переезд) и 08:42:38
  (починка). Отсечка приёмки 9.2/9.3 остаётся **`2026-09-07 07:57:25`** — сутки
  считаются от переезда, и ПРОВЕРКА выше от 07:58 менять не надо. Второй
  рестарт стоит примерно одного тика у минутных проходов и вывод «не хуже
  линии» не трогает: в сутках самой линии рестартов было ДЕСЯТЬ.
- 🔑 **`apscheduler` в журнале воркера ОБЯЗАН быть нулём, и это стережётся.**
  Исполнитель придушен явной строкой, а не наследованием от родительского
  логгера; сторож `test_scheduler_run_trace.py` перевёрнут — раньше требовал
  `INFO`, теперь `>= WARNING`. Увидеть там `apscheduler` значит найти РЕГРЕСС,
  а не восстановленную наблюдаемость.
- **Сработало, когда:** наступило 07.09 07:23 **и** (а) везде 1. Ожидание —
  28 проходов, около 15 240 тиков; поимённые числа в задаче 2.10 заявки
  `scheduler-runs-in-its-own-unit`.
- 🔴 **Линия хрупкая:** `gmp-worker` на проде намеренно остановлен, но
  `enabled` — перезагрузка машины поднимет его сама и испортит сутки. Тогда
  считать заново. Если (а) показала не 1 — линия недействительна.
- **Тогда:** снять линию → задача 10.1 → приземлить коммит `0fad22c44` через
  `land.sh` → промоушен и прод-выкат → попросить человека
  `sudo systemctl start gmp-worker` → приёмка 9.2–9.5.
- **Входить:** `.claude/handoff/scheduler-decouples-from-the-bot.md`

### 3. ~~Фейс-контроль на 8233~~ — СНЯТА 08.09.2026 в 11:50, ОПЛАТА ПРИШЛА

*Обновлено 06.09 сессией `F·feature/face-control-toggle`: за час до этого
строка успела устареть дважды.*

- **Где:** ветка `feature/face-control-toggle`, ворктри
  `.claude/worktrees/face-control-toggle`. Всё приземлено, незакоммиченного нет.
- **Ждёт:** допуска ПАРЫ `27338`/`27339` на экране **«Команды»**. Дедлайн
  прежний — **08.09** (`registration_deadline` события 8233).
- 🔑 **Ждать Катю или Ксению НЕ обязательно — нажать может сам владелец.**
  `_require_manage` пускает супер-админа, а `approve_pair` про `may_decide` не
  спрашивает. Прежняя формулировка «сам ты этого не сделаешь, нужна Катя на
  „Моих игроках“» больше не верна: ось изменилась вместе с починкой ниже.
- **ПРОВЕРКА:**

  ```
  cd ~/dev/gmb_v2
  echo "SELECT id,status,org_approved_at FROM event_participant WHERE id IN (27338,27339);" | scripts/prod_sql.sh
  ```
- **Сработало, когда:** у `27338` непуст `org_approved_at`.
- 🟢 **СРАБОТАЛО 06.09.2026 в 18:40:58** — у `27338` (user 1) стоит допуск.
  Похоже, первое нажатие «Допустить» после починки кнопки.
- 🔴 **«Допущен один из пары» — БЫЛО НЕВЕРНО, и задача «допустить второго»
  снята 06.09 в 19:55.** Разбор ниже; строка успела продержать ложную работу
  час и один раз уехать владельцу.
- **Перепроверено 06.09 в 19:11 — без изменений.** У `27338` допуск от
  18:40:58, у `27339` пусто, обе строки `reserve`. За полчаса не сдвинулось.
- 🟢 **Разбор 06.09 19:40–19:55: дефекта нет, пара УЖЕ открыта к оплате.**
  Четыре звена, каждое проверено:
  1. `27338` — **якорная строка** (`is_primary = t` на боевой), `27339` —
     партнёр. Композиция `pair_admission_on_event`
     (`gmp/core/services/school_approval.py:289`) читает разовый допуск
     **только у якоря**: `org_approved=anchor_row.org_approved_at is not None`.
     У партнёра замка нет по построению, и второй допуск ничего не изменит.
  2. `legal_entity_player` на боевой — **0 строк**, значит все вердикты
     `NOT_REVIEWED`, значит ветвь 4 `pair_admission`, значит **`OPEN`**.
  3. Обе строки в `reserve` потому, что событие **ПЛАТНОЕ** — 2750 ₽,
     предбронь 500 ₽. Строки подтверждает оплата. Отдельная ветка
     автоподтверждения обеих строк живёт только на БЕСПЛАТНОМ событии
     (`pair_payment.py:150`), и на 8233 она не при чём.
  4. Журнал прода в 18:40:58:
     `POST /api/tournaments/8233/pair-applications/by-participant/27338/approve
     → 200 OK`. Это **новая** ручка заявки `pair-admit-addressed-by-participant`
     и её первое живое применение. Предупреждения
     «approve_pair: уведомление primary не доставлено» в журнале нет.
- **Замер 07.09 07:06:** без изменений — `27338` допуск от 06.09 18:40:58,
  `27339` пусто, обе строки `reserve`. ОПЛАТЫ за ночь не было. До дедлайна
  `registration_deadline` 08.09 — примерно сутки; если оплаты не будет к концу
  07.09, напомнить паре (а не чинить допуск).
- ⚠️ **Замер 07.09 в 18:57: пара так и стоит в `pending_payment`** — доплаты
  5000 ₽ за день не было. Срок доплаты **10.09 11:00**, `registration_deadline`
  08.09. Само событие тем временем набралось: 16 `confirmed`, 16 `reserve`,
  2 `cancelled`, 2 `pending_payment` (это и есть пара).
- 🟢 **СДВИНУЛОСЬ 07.09 в 09:19: обе строки пары теперь `pending_payment`.**
  Замер 09:50: `27338` (допуск есть) и `27339` (допуска нет по построению) —
  обе `pending_payment`. Двинула их ПРЕДБРОНЬ 500 ₽ (проводка `11583`), а не
  полная оплата: до `confirmed` нужна доплата 5000 ₽ к сроку **10.09 11:00**
  (3 дня от закрытия окна). Строку 3 не снимать до неё.
- **Значит делать: ничего нажимать не надо — ждём ОПЛАТУ.** Дедлайн 08.09
  (`registration_deadline`). Если к 07.09 оплаты нет — напомнить паре, а не
  чинить допуск.
- 🔴 **Откуда взялась ложная задача — ловушка на будущее.** Предикат был
  «у обеих строк непуст `org_approved_at`», и он ловил не ту ось: пустая
  отметка у партнёра — норма, а не недопуск. Верный предикат — **вердикт
  композиции**, а не колонка у обеих строк, и различающий признак статуса —
  **оплата**, а не допуск. Память `face-control-never-switched-on` называла это
  прямым текстом («уведомление игроку — на ПАРЕ»), но строка реестра
  формулировала задачу по колонке.
- **Тогда:** убедиться, что догон перевёл пару из `reserve` и оплата открылась;
  закрыть задачу 8.7.
- ✅ **Красная находка про кнопку «Допустить» ЗАКРЫТА.** Она ключевалась по
  `PairInvite` при нуле строк в `pair_invite` на всей боевой — то есть не
  срабатывала никогда. Владелец прочитал, велел чинить; починено и **выкачено в
  прод**: `main` = `c183ffd0a`, якорь отката `prod-2026-09-06-pair-admit`,
  заявка `pair-admit-addressed-by-participant`. Гейт на том же SHA: 15639
  бэкендных RC=0, `vue-tsc` RC=0, 4712 фронтовых RC=0, миграций нет. После
  выката проверено дерево, оба сервиса `active`, ноль ошибок после рестарта.
- ✅ **Галка на 8233 поднята** (`requires_org_approval = t`), и механизм
  ВПЕРВЫЕ доставил: два повода `tournament_approval_pending` (7344, 7345),
  `delivered_at` 12:46:18 — первое исполнение доставки за всё его существование.
  Галка, поднятая после оплаты, фейс-контроль на турнире не включила бы вовсе;
  успели.
- ⚠️ **Подъём самой галки — не под «Войти как»:** этот гейт берёт
  `real_user_id` и отвечает 403. Владелец на этом уже споткнулся. К допуску
  пары ось другая — сверять по коду, а не по аналогии.
- **Входить:** `.claude/handoff/feature-face-control-toggle.md` (приземлён в
  `066f174bb`) — состояние, семь оплаченных ловушек и объявленный разрыв:
  демо-проверка пропущена осознанно решением владельца. Рядом
  `openspec/changes/face-control-toggle/CHECK-8233.sql` и `CHECK-8233.md`.

- 🟢 **СНЯТА 08.09.2026 в 11:50 — ДОПЛАТА ПРИШЛА, обе строки `confirmed`.**
  Владелец доложил словами, сверено на боевой: `27338` `confirmed` (допуск от
  06.09 18:40:58), `27339` `confirmed` (допуска нет — так и должно быть, замок
  читается только у якоря). Независимый источник — проводка **`11669`,
  5000.00 ₽, `completed`, 08.09 в 10:15:43**, у неё пусты и `cash_source`, и
  `created_by`, то есть пришла эквайрингом через `pair_pay`, а не из штаба.
  🔑 **Заодно это первое боевое подтверждение зачёта предброни:** заплачено
  ровно **5000**, а не 5500 — фикс `fa1d42d19`
  (`prod-2026-09-07-prebooking-credit-on-card`) отработал на живых деньгах.
  Строка закрыта целиком: допуск состоялся 06.09, оплата 08.09, до срока
  10.09 11:00 оставалось двое суток.

### 4. ~~Тексты записи~~ — СНЯТА 07.09.2026 в 07:14, обе развилки закрыты

🟢 **Владелец ответил на обе развилки 07.09 в 07:12 МСК:**
- **(а) отклонённые модерацией (`REJECTED`) — писать ОТДЕЛЬНЫЙ ТЕКСТ НА МЕСТЕ**
  (`events/registration/routes.py:301`). Перехват до сервиса записи оставить
  как есть; вариант «перестать перехватывать, чтобы сработал утверждённый
  `NAME_REJECTED_MESSAGE` и личка с кнопкой» НЕ выбран.
- **(б) ёлочки вместо ASCII-апострофов вокруг «👤 Моя запись» — ДА.** Выбрано
  при озвученной цене: правка заденет ещё 4 всплывашки.

⚠️ **Дословных строк владелец не диктовал.** Если текст на месте (а) нужно
согласовать буквально — это НОВЫЙ вопрос человеку, а не остаток этого.

Решения переданы живой ведущей сессии `G·тексты-записи` сообщением 07.09 в
07:14 (она была `waiting` ровно на этом). Дальше это обычная недоделанная
работа: её место — хендофф `.claude/handoff/booking-notice-texts.md` и заявка,
а не отложка. Всплывашки #2–#10 идут по одной (прежний выбор владельца).
⚠️ Пункт 5 хендоффа устарел: всплывашек **10**, а не 7.

### 5. ~~Чекаут-API~~ — СНЯТА 06.09.2026 в 20:40, линия закрыта целиком

🟢 19.3 исполнен владельцем. Сертификат `api.propevay.ru` выпущен и развёрнут.
Заявка `portal-checkout-api` заархивирована как `2026-09-06-portal-checkout-api`,
**212/212**, дельта влита (+32 требования, ~10 изменённых, заглушек `TBD` нет).
Активных заявок в репозитории **ноль**, голова `8d4a3e1`.

✅ **Сверено мной снаружи, а не принято на слово** (06.09 20:38):
`https://api.propevay.ru/` отдаёт **HTTP/2** и код **502**; сертификат
`notAfter = Dec 5 16:10:31 2026 GMT`. 502 здесь — успех: TLS поднялся и вхост
проксирует, а на `:8790` никого нет, потому что заявка на прод не выкатывается.
🔑 **И HTTP/2 в ответе доказывает, что шаг 10 не пропущен** — ровно та ловушка,
из-за которой на certbot останавливаться было нельзя.

🔑 **Две находки про машину `mprz`, которые шире этой линии:**
1. `propevay` был **единственным** репозиторием на машине, ходившим в GitHub по
   HTTPS, а пароли для git там отключены — `git pull` падал на аутентификации.
   `origin` переведён на SSH, как у `gmb_v2` и `mprz.ru`. **Любая ПРОВЕРКА,
   делавшая `git pull` в `~/dev/propevay`, раньше падала не по своей причине.**
2. `/opt/propevay` не существует и не существовал, но стоял в юнитах портала и
   в репозиторном `deploy/propevay.service` — то есть `cp` этого файла поверх
   установленного **уронил бы боевой движок**. Пути исправлены по факту, на
   согласованность поставлен сторож.

### 6. ~~Турба — первая оплата после переезда~~ — СНЯТА 06.09.2026 в 19:20

🟢 Сработала, и была созревшей уже сутки. Три платежа позже переезда
(05.09 16:12): `674` 05.09 21:12, `675` 06.09 09:55, `676` 06.09 15:04 — все
`COMPLETED`, `receipt_url_via=push`, `receipt_push_count=1`, `receipt_debt_at`
пуст. Задача 4.8 закрыта коммитом `6c5cd5d2` в `~/dev/mprz.ru`; заявка
`repo-split` теперь **70/70** и готова к архивации — это решение владельца, не
моё. Долг 7.5 (ротация кредов) закрыт РЕШЕНИЕМ владельца с названной ценой, а
не исполнением; при архивации он уедет в архив вместе с заявкой.

🔴 **Урок дороже самой строки: сутки строка врала «проверить нечем», потому что
у команды не был назван ХОСТ.** Прежняя редакция писала `cd
~/dev/turbo01/src/bot` — без хоста это читается как местный путь, местная
проверка честно не нашла ни `src/`, ни `.env`, ни `asyncpg`, и отсюда родился
вывод «проверка неисполнима». На проде есть всё:

```
ssh mprz 'cd ~/dev/turbo01/src/bot && ../../.venv/bin/python /tmp/chk_receipts.py'
```

**Правило для всего реестра:** у ПРОВЕРКИ обязан быть назван хост. «Проверка,
которая исполняется, ещё не проверка» (строка 7) имеет вот эту вторую сторону —
проверка, объявленная неисполнимой, тоже требует доказательства, а «у меня
локально нет такого пути» им не является.

⚠️ Скрипт на проде по-прежнему в `/tmp`, который метёт
`systemd-tmpfiles-clean.timer`. Копия рядом с заявкой — `chk_receipts.py`
(коммит `0ec66804`), ею и восстанавливать. ⚠️ В `paymentstatus` нет значения
`succeeded`, оно `COMPLETED`.

### 7. Перенос строки инбокса — 🔴 ПРОВЕРКА ПЕРЕПИСАНА, старая давала ложное зелёное

*Прежний предикат «3–5 переносов после выката» сегодня СРАБОТАЛ БЫ ВПУСТУЮ —
разбор ниже. Ждать теперь надо другого события, и оно много реже.*

- **Где:** ветка `check/move-notice-baseline`, ворктри
  `.claude/worktrees/move-notice-baseline`. Ведущей сессии больше нет.
- **Ждёт:** переноса брони, у которой ЕСТЬ строка инбокса. Заявка
  `booking-move-carries-its-notice` в проде с 06.09 10:30.
- 🔴 **Почему старая проверка врала (замер 06.09.2026, 18:10).** Она считала
  переносы, а фикс проверяется только переносом С НАГРУЗКОЙ. После выката
  переносов ровно 4 — порог взят, — но легли они в ВОСЕМЬ СЕКУНД одной пачкой
  (13:41:18–13:41:26, брони 5031–5034), и **ни у одной исходной брони не было
  ни единой строки** `booking_%`. Переносить было нечего: «восьмёрка не
  выросла» верно ВАКУУМНО и о фиксе не говорит ничего.
- 🔴 **Событие много реже, чем считала строка.** Строки `booking_%` несут
  **33 брони из 4970**, сами строки появились лишь 24.08.2026 (144 штуки), и
  из **135 переносов за всю жизнь** исходная бронь несла строки у **ДВУХ** —
  `4993←4987` (04.09) и `4586←4340` (30.08), оба до выката, оба и составляют
  восьмёрку. Попадание ≈1,5 % на перенос при медиане 3 переноса в сутки — это
  один значимый случай примерно раз в три недели, а не «07–08.09».
- **ПРОВЕРКА — теперь ОДИН запрос** (`cd ~/dev/gmb_v2`, stdin в
  `scripts/prod_sql.sh`):

  ```sql
  SELECT count(*) AS строк_на_целях_переноса
  FROM user_notification un JOIN booking b ON b.id = un.subject_id
  WHERE un.kind::text LIKE 'booking_%' AND b.moved_from_booking_id IS NOT NULL;
  ```
- **Замер 06.09 19:12:** цели переноса — **0**, восьмёрка — **8** (из них
  прочитано 5). Оба числа те же, что в базовой линии: события ещё не было.
  При попадании ≈1,5 % на перенос ждать в среднем около трёх недель.
- **Замер 07.09 18:57:** цели переноса — по-прежнему **0**. Ждёт дальше.
- **Замер 07.09 07:06:** цели переноса — **0**, восьмёрка — **8** (прочитано
  5). Третий одинаковый замер подряд; при попадании ≈1,5 % на перенос это
  ожидаемо, ждать в среднем до конца сентября.
- **Сработало, когда:** результат стал больше нуля. На 06.09 18:10 он **0** —
  это базовая линия. Ноль значит «случая ещё не было», а НЕ «фикс не работает».
- ⚠️ **Считать переносы больше не надо** и порога «3–5» больше нет: он ловил
  не то событие. Прежний парный запрос про «восьмёрку» оставлен ниже только
  как контроль — сам по себе он ничего не доказывает.

  ```sql
  SELECT count(*) AS строк_на_мёртвых_бронях,
         count(*) FILTER (WHERE un.read_at IS NOT NULL) AS из_них_прочитано
  FROM user_notification un JOIN booking b ON b.id = un.subject_id
  WHERE un.kind::text LIKE 'booking_%' AND b.status = 'moved';
  ```
- **Тогда:** строки на целях появились И восьмёрка не выросла → фикс работает,
  записать в память `named-open-gaps` и закрыть. Восьмёрка выросла при нулевых
  целях → фикс НЕ работает. Оба нуля → ждать дальше.
  ⚠️ Восьмёрка не уменьшится никогда — это исторические строки до фикса,
  бэкфилл не делаем осознанно. Увидев 8, не считать фикс несработавшим.
  ⚠️ У `user_notification` нет столбца со следом переноса — связь только через
  `subject_id`, поэтому задним числом «была ли нагрузка» не восстановить.
  Отсюда и предикат по ЦЕЛИ, а не по исходной броне.
- **Входить:** `.claude/handoff/proverka-perenosa-na-prode.md`

### 8. ~~Знак Igm~~ — СНЯТА 06.09.2026 в 22:20, выкачена в прод и заархивирована

🟢 Разговор с владельцем закрыт, работа приземлена, выкачена и принята.
Тег `prod-2026-09-06-igm-mark`, дерево на VDS `4c97fde66`, заявка
`igm-mark-two-meanings` заархивирована как `2026-09-06-igm-mark-two-meanings`,
38 задач из 38.

**Открытые пункты этой строки закрыты решениями владельца, а не забыты:**
подпись контекстной кнопки — «Счёт»; на узкой ширине контекстная кнопка и
кебаб — иконки без подписей; «?» рисуется только на панели. Случай «будущее
событие с тремя кнопками» перестал существовать: «Редактировать» уехала в
меню, и в ряду осталась ровно одна контекстная кнопка.

✅ **Проверено на проде замером, а не принято на слово** (06.09 21:55):
`597` прошедших событий несли знак, `20` сохранили, `577` потеряли — это ЦЕЛЬ
правила. Публичный API в обе стороны: 8234/8235/8403 (с партиями) отдают
`index_mark_visible=true`, 8205/8404 (без партий) — `false` при
`goes_to_index=true`. `alembic current: jobrun01` (миграций в наборе нет),
`gmp-crm` и `bot-gmp` active, в логах после рестарта ни одного warning.

🔑 **Числа хендоффа (586/17/569) разошлись с прод-замером (597/20/577) не по
дефекту, а по времени** — между съёмками закрылось ещё несколько событий, три
из них с внесённым счётом. Любой замер этого вида протухает за часы.

🔑 **Свип «код против спеки» окупился** — он нашёл клаузу сценария «а знак
рисуется по `goes_to_index`», противоречившую требованию в той же дельте.
Без свипа она уехала бы в ЖИВУЮ спеку при архивации. Плюс четыре комментария
на фронте, называвших не то поле. Круг критиков снят владельцем третий раз
именно на этом основании.

⚠️ **Строка 2 (планировщик) при этом не задета — проверено, а не предположено.**
Прод-выкат перезапускает `bot-gmp`, а замер строки 2 ровно про то, не тикает ли
планировщик дважды. После рестарта проверка (а) даёт девять минут подряд по
одному исполнителю. `gmp-worker` остался остановленным — его выкат не трогает.

**Ворктри `.claude/worktrees/igm-mark` и ветка `feature/igm-mark-two-meanings`
подлежат жатве** — вся работа в стволе, дерево чистое. История разбора живёт в
`.claude/handoff/знак-igm-и-хвост-редизайна.md`.

### 9. ~~Форма площадки~~ — СНЯТА 07.09.2026 в 21:55, сценарий 403 ПРОЙДЕН

🟢 **Сквозной сценарий, не проверявшийся ни разу, пройден на боевой 07.09 в
21:49 с разрешения владельца.** Взята **Наталья Воблая (user 14982)**, ранг 3
на «СК Искре» (12), у площадки получателя платежей НЕТ. Под «Войти как» →
пространство «Админка» → Настройки → Локации → «СК Искра» → **Сохранить, не
трогая денежное поле**.

**`PATCH /api/locations/12` → `200`.** До фикса `0e80edb58` здесь был бы 403
«Обнулить получателя платежей может только super_admin»: форма слала
`default_legal_entity_id: null`, а гейт читал пустоту как попытку обнуления.

✅ **Проверено, что `200` получен по ПРАВИЛЬНОЙ причине, а не из-за протёкшей
роли.** Это был главный риск: сохранись в имперсонированном токене
`super_admin` — гейт пропустил бы кого угодно, и строку сняли бы зря.
**Различающий признак снят в том же прогоне:** под «Войти как» список локаций
показывал **ОДНУ** площадку, под владельцем — **ТРИНАДЦАТЬ**. Актор не-СА.

✅ **Проверено, что ничего не испорчено:** `location 12` — `updated_at`
`2026-09-07 21:49:52` (значит запись ЛЕГЛА, и `200` не был холостым),
`default_legal_entity_id` по-прежнему **пуст**. Площадки 5 и 11 не тронуты.

✅ **Кандидаты перепроверены ПЕРЕД прогоном, а не взяты из реестра:** замер в
строке был от 06.09, а на Кате сценарий уже однажды протух. На 07.09 у площадок
5 и 12 получателя по-прежнему нет, у обоих пользователей ранг 3 — сценарий
различал.
🔑 Это тот же класс, что правило №4: **строка реестра стареет молча, и её
собственный замер надо освежать перед действием, а не после.**

⚠️ Таблица прав называется **`venue_grant`**, а не `user_location` — последней
не существует. Стоило одного запроса.

---

*Ниже — прежнее тело строки, оставлено надгробием.*

*Сессия `gmb-v2-ef` ответила, но текстом в свой терминал, а не сообщением —
ответ снят с её транскрипта. Два факта в нём протухли, поправлены проверкой.*

- **Где:** `gmb_v2`, ветки и ворктри НЕТ. Фикс `0e80edb58` («гейт получателя
  платежей бьёт по изменению, а не по наличию ключа») приземлён, ветка
  `fix/venue-save-payee-untouched` удалена, дерево сжато. Основной чекаут в
  detached HEAD.
- 🟢 **Вопрос про ОБЪЁМ закрыт 07.09 в 07:12: рекомпозиция УЖЕ СДЕЛАНА.**
  Владелец: «экран выглядит по-другому, но мне всё ещё не нравится, я его
  вылизывать буду позднее». То есть вылизывание — его собственная будущая
  работа, а не ожидание реестра.
  ✅ **Проверено замером, а не принято на слово** (07.09 07:16): в дереве
  прода `f29483a77` лежат `ad9903a75` (рекомпозиция ритма самоброни, 06.09
  18:26), `259d662d0` (ширина поля), `8751bc456` (`aria-invalid` без вида) и
  `7e6a6768e` (решётка длительностей по видам спорта).
- 🔴 **Строка простояла ~13 часов, описывая мир, которого уже не было.** Она
  писалась 06.09 в 14:40 со словами «разбор готов, код НЕ ТРОНУТ», а код
  тронули в тот же день в 18:26–18:45. Поймал это не реестр, а фраза
  владельца.
  🔑 **Родовой признак:** у строки «ждёт СЛОВА человека» ПРОВЕРКА объявлена
  «машинной нет» — и потому строка слепа к тому, что работу успели сделать
  мимо неё. Такой строке нужна вторая, дешёвая проверка: **сделано ли уже**
  (`git log` по затронутым файлам против дерева прода). Иначе вопрос уедет
  владельцу вторично, а он уже ответил делом.
- **ПРОВЕРКА (сделано ли уже):**

  ```
  cd ~/dev/gmb_v2
  git log --oneline -6 origin/main -- crm/web/src/views/locations/LocationEditor.vue
  ```
- **ПРОВЕРКА (остаток строки):** машинной нет — это сквозной сценарий 403
  ниже, и он ТРОГАЕТ БОЕВЫЕ настройки чужой площадки, поэтому требует слова
  владельца перед прогоном.
- 🟢 **Внешнего факта ждать больше не надо** — сверено 06.09.2026 в 14:40:
  у Екатерины Осмачкиной (user 5521) заведено ЮЛ 14 «ИП Осмачкина Екатерина
  Владимировна», создано 06.09 в 10:06, и у площадки 11 «ТТ Ракеттка»
  `default_legal_entity_id = 14`. Прогонялось так (`scripts/prod_sql.sh`):

  ```sql
  SELECT id, name, owner_user_id FROM legal_entity WHERE owner_user_id = 5521;
  SELECT id, name, default_legal_entity_id FROM location WHERE id = 11;
  ```
- 🟢 **Промоушена ждать не надо:** `0e80edb58` уже в `main` (седьмым от головы
  `60cc797cc`) и накрыт тегами `prod-2026-09-06-*`. Сессия сверила и живой
  прод по ssh: дерево на `60cc797cc`, ветка `main`, коммит внутри — фикс не
  просто смержен, а крутится.
- **Тогда — созрело, делать:** пройти сквозной сценарий, который не
  проверялся ни разу: человек с рангом ≥ 2 на площадке БЕЗ получателя правит
  её настройки и НЕ получает 403. Ради этого фикс и писался.
  ⚠️ **На Кате сценарий больше ничего не различает:** у площадки 11 получатель
  теперь ЮЛ 14, которым она владеет, — её пропустит и старый код, и новый.
  Живые кандидаты (сверено 06.09.2026 в 15:00): **user 1657 на «Вешки Спорт»
  (5)** и **user 14982 на «СК Искра» (12)**, у обоих ранг 3. Площадок без
  получателя восемь: 3, 5, 6, 7, 9, 12, 13, 14.
  ✅ **Проверять МОЖНО под «Войти как»**, и здесь это верный способ: гейт
  `patch_location` читает `_viewing_actor` — то есть `user_id` ЦЕЛИ, — а роль
  `super_admin` в имперсонированном токене не сохраняется, так что актор для
  гейта не-СА. `real_user_id` в этой ручке берут только авторство журнала
  настроек (строка 787) и смена подписи (800).
  ⚠️ Это **не** мина строки 3: там ось другая. Одинаково выглядящие ручки
  расходятся именно здесь — сверять по коду, а не по аналогии.
- **Входить:** хендоффа НЕТ. Состояние — коммит `0e80edb58` и память
  `rights-on-the-wrong-axis.md` (строки 297 и 324).
  ⚠️ Сессия указывает на память `first-legal-entity-deadlock.md` — **такого
  файла нет**, его поглотило слияние памяти 06.09 (100 записей → 35).
  ⚠️ В `rights-on-the-wrong-axis.md` строка 324 «у ТТ Ракеттки нет
  `default_legal_entity_id` вовсе» тоже устарела — с 06.09 он равен 14.

---

### 10. sms-gate — алерт с наблюдениями выкачен, ждёт первого падения модема

- **Где:** `~/dev/sms-gate`, ветка `master`, ворктри нет. Выкачено 06.09.2026
  в 19:39 МСК, ревизия `5f6688f`; на 19:57 `NRestarts=0`, боевой трафик идёт.
- **Ждёт:** **первой настоящей эскалации сторожа модема.** Алерт теперь несёт
  `SIM=… reg=… signal=… operator=…`, и первое же падение ответит на вопрос,
  что говорит `AT+CPIN?` при живой неисправности. Это единственный
  недостающий факт для второй заявки — четвёртой причины в лестнице, чтобы
  сторож перестал тратить soft recovery на карту, которую не перечитывает.
  Добыть его иначе можно только выдернув симку на боевом модеме; владелец
  отказался 06.09, и отказ записан в `proposal.md`, а не потерян.
- **Заявка НЕ архивируется до этого события — решение владельца 06.09.**
  Реализована и выкачена полностью, `openspec list` показывает `✓ Complete`,
  но живая спека `outbound-send` дельту ещё не получила.
- **ПРОВЕРКА (сработал ли сигнал):**

  ```
  ssh -p 30022 home.deralsem.ru "journalctl -u sms-gate -S '2026-09-06 19:39' -g 'SIM=' --no-pager"
  ```

  Непусто — сигнал сработал: возьми из строки снимок, открывай заявку на
  четвёртую причину против раздела «Findings carried forward» в
  `openspec/changes/carry-the-observations-into-the-alert/design.md`, там
  лежат три уже оплаченные 06.09 находки.
- 🔴 **Прежняя ПРОВЕРКА давала ЛОЖНОЕ ЗЕЛЁНОЕ — переписана 07.09 в 07:10.**
  Было `journalctl -u sms-gate -S -7d -g 'hard reset'`, и на 07.09 она
  возвращает непустое: эскалация 06.09 **18:28:54** («Modem unrecoverable;
  hard reset + service restart») с чередой `hard reset on cooldown` с 18:14.
  Но выкат наблюдений — **19:39 того же дня**, то есть эти строки снимка НЕ
  несут и нужного факта не дают. Ждём мы не «эскалации», а **эскалации,
  которая напечатает снимок**, поэтому предикат теперь ищет `SIM=` и режет
  окно моментом выката.
  ✅ **Отрицательный образец есть:** с 19:39 по 07.09 07:08 ни `hard reset`, ни
  `SIM=` не встречаются ни разу, а журнал жив (последняя строка 07:04:39).
  Греп находить умеет — до окна он ловит пять строк.
  🔑 **Родовой признак, переносимый на любую строку:** проверка «сработало ли
  НОВОЕ» с окном ШИРЕ выката меряет старое поведение. У каждой строки, которая
  ждёт первого случая после выката, окно обязано начинаться моментом выката, а
  предикат — спрашивать признак, которого до выката быть не могло.
- **ПРОВЕРКА (не заархивировали ли уже):**

  ```
  cd ~/dev/sms-gate
  openspec list
  ```

- ⚠️ **Пуш в `deploy-remote` рестартит боевой сервис.** Коммиты `2f1f674` и
  `5f845ae` (бухгалтерия заявки, кода не меняют) сознательно НЕ отправлены —
  доедут со следующим выкатом. В github не пушили: владелец счёл ненужным.

### 11. 🟢 ПРЕДБРОНЬ СОСТОЯЛАСЬ 07.09 в 09:19 — наблюдение НАПОЛОВИНУ снято

*Заведена 06.09.2026 в 20:40. Нашла сессия `G·галка-фейс-контроль`
(диагностически, кода не правила); каждое звено сверено мной по коду и боевой.*

🔑 **Хозяин разбора назван 06.09 в 21:00:** линия фейс-контроля закрылась,
отдав диагностику владельцу, и объявила область своей — если после первой
предброни понадобится разбор КОДА, входить через
`.claude/worktrees/face-control-toggle/.claude/handoff/feature-face-control-toggle.md`
(ворктри чистый, кода линия не трогала). Имя сессии не способ входа: входить
хендоффом.

- **Ждёт:** ПЕРВОЙ предброни на 8233 — 500 ₽ с пары. Окно узкое: предбронь
  **включена 06.09 в 20:16:53** (`updated_at` события), а правило
  `prebooking_window_is_open` (`start − now >= window_days`, окно 5 дней)
  закрывает её **07.09 в 11:00 MSK** — событие 12.09 11:00, площадка 14
  `Europe/Moscow`. Срок доплаты — 3 дня от закрытия, **10.09 11:00**.
- 🔴 **Что случится:** дверь предброни `POST /me/events/{id}/prebook`
  (`gmp/crm/api/me/router.py:1627`) зовёт `create_payment_qr` **с телефоном
  плательщика**, а в `event_payment_service.py:781` блок `Receipt` собирается
  по условию **`if user_phone:`** — не спрашивая ни вида проводки
  (`kind=PREBOOKING`), ни `fiscal_routes`, ни решения «не фискализировать».
  Отказ от чека (`mark_not_fiscalized`, `PREBOOKING_SKIP_REASON`) гасит только
  НАШ облачный путь; Т-Банк печатает inline по блоку `Receipt` в `Init` и про
  наш отказ не знает.
- ✅ **Режим отказа определён: «чек напечатается», а НЕ «оплата упадёт».**
  ЮЛ 8 — ИП Дячук Ксения Степановна, `tax_system = patent`,
  `payment_provider = tbank`, и документ непротиворечив ⇒ касса его ПРИМЕТ.
  Различать обязательно: противоречивая пара «спецрежим + НДС 22 %» дала бы
  отказ `Init` и сломала бы оплату вовсе (боевое 25.08.2026).
  🔑 **Держится ЗАПОЛНЕННЫМ реквизитом, а не умолчанием** (поправка
  `G·галка-фейс-контроль`, сверена на боевой): у ЮЛ 8 `vat_rate = none` стоит
  ЯВНО, поэтому `fiscal_pair_for` выходит ПЕРВОЙ ветвью
  (`requisites.py:207`, `if req.vat_rate:`) и до умолчания по `СПЕЦРЕЖИМЫ`
  управление не доходит. Очисти кто-нибудь `vat_rate` — защита переедет на
  умолчание, исход тот же, причина другая.
- ⚠️ **Подпись в чеке вдобавок неверна по смыслу:** строка собирается как
  `f"Оплата участия в событии #{event.id}"`, тогда как это предбронь — аванс.
- ⚠️ **Признак расчёта будет `full_payment` (тег 1214), и это РЕШЕНИЕ, а не
  недосмотр.** У ЮЛ 8 `payment_method_default` пуст ⇒ подставится
  `DEFAULT_PAYMENT_METHOD = "full_payment"` (`requisites.py:40`).
  🔴 **Поправка к поправке:** соседняя сессия назвала это «неверно фискально,
  потому что `payment_method_for` на этом пути не спрашивается». Первая
  половина верна — путь предброни берёт `req.payment_method_default` напрямую.
  **Вторая половина ничего не меняет:** `ADVANCE_OVERRIDE_ENABLED = False`
  (`receipt/customer.py:94`), и при выключенном перекрытии `payment_method_for`
  возвращает `configured_default or PAYMENT_METHOD_FULL` — **тот же
  `full_payment`**. Позови его этот путь — результат был бы идентичен.
  🔑 **Почему выключено, названо в коде** (`customer.py:82-94`): признак
  «аванс» создаёт обязанность закрывающего документа с зачётом, а механизма
  зачёта нет нигде, кроме абонементов. Владелец выбрал «full-payment не
  страшно» осознанно. Значит это **принятая цена, а не дефект пути предброни**.
  ⚠️ Следствие всё равно наблюдаемо и стоит взгляда: чек объявит ПОЛНЫЙ расчёт
  на 500 ₽ по услуге, стоящей паре 5500 ₽ (2750 × 2), а доплата 5000 ₽ уйдёт
  через `pair_pay`, который inline-блок не шлёт, — второго чека от банка по ней
  не будет вовсе.
- ⚠️ **Асимметрия двух дверей одного события:** полная парная оплата
  (`pair_pay`) зовёт выпуск QR **без** телефона и блок `Receipt` не шлёт.
  Пробьёт чек именно предбронь, а не полная оплата.
- ⚠️ **Со своей стороны проверить нечем:** забираем ли мы чек, напечатанный
  эквайером, — предмет ЖИВОЙ заявки `acquirer-receipt-is-recorded`.
- 🔴 **ПРОВЕРКА (СЛОМАНА, НЕ ЗАПУСКАТЬ — оставлена как образец ошибки).**
  Отвечает `(0 rows)` при живой проводке: `kind` в базе строчными. Рабочий
  вариант — ниже по строке, через `lower(...)`.

  ```sql
  -- ❌ врёт: kind::text = 'PREBOOKING' не совпадает с 'prebooking'
  SELECT id, kind::text, amount, status::text, created_at
  FROM user_transaction
  WHERE event_id = 8233 AND kind::text = 'PREBOOKING'
  ORDER BY id DESC LIMIT 5;
  ```
- **Замер 07.09 07:06:** проводок `PREBOOKING` по 8233 — **0 строк**,
  предброни не было. 🔑 **Окно закрывается СЕГОДНЯ в 11:00 MSK** (правило
  `prebooking_window_is_open`, событие 12.09 11:00, окно 5 дней) — то есть
  строке осталось около четырёх часов. Не сработает до 11:00 — строку снимать
  как непроверенную: наблюдение не состоялось, а условие `if user_phone:`
  остаётся заряженным для следующего события с предбронью.
- 🟢 **СРАБОТАЛО 07.09.2026 в 09:19:50.** Проводка **`11583`**: `kind =
  prebooking`, **500.00 ₽**, `status = completed`, `event_id = 8233`,
  `receipt_status = not_fiscalized`. Замер мой, 07.09 в 09:50.
- 🔴 **ПРОВЕРКА ЭТОЙ СТРОКИ ВРАЛА И ЧУТЬ НЕ УБИЛА НАБЛЮДЕНИЕ.** Она спрашивала
  `kind::text = 'PREBOOKING'` ЗАГЛАВНЫМИ, а в базе `kind` лежит **строчными**
  (`prebooking`). Прогон в 09:50 вернул `(0 rows)` — «предброни не было» — при
  живой проводке часом ранее. По плану строки в 11:00 её сняли бы как
  непроверенную, и первый боевой случай был бы упущен молча. Поймал только
  потому, что соседняя линия назвала номер проводки и я полез сверять доклад.
  ✅ **ПРОВЕРКА ИСПРАВЛЕНА** (`cd ~/dev/gmb_v2`, stdin в `prod_sql.sh`):

  ```sql
  SELECT id, kind::text, amount, status::text, receipt_status::text, created_at
  FROM user_transaction
  WHERE event_id = 8233 AND lower(kind::text) = 'prebooking'
  ORDER BY id DESC LIMIT 5;
  ```
  🔑 **Родовой признак, переносимый на ВСЕ строки реестра:** предикат на
  РЕГИСТР значения enum — это предикат на деталь хранения, а не на факт.
  Сравнивать через `lower(...)`, а лучше — не по строке вовсе. Проверка,
  врущая в сторону «ещё ждём», неотличима от честного ожидания и потому
  смертельна для реестра: она не поднимает тревогу никогда.
- **Сработало, когда:** строка появилась. Пока строк нет — предброни не было.
- **Тогда:** смотреть ЖИВЬЁМ, напечатался ли чек и с какой подписью.
- 🟢 **НАША половина наблюдения ЗАКРЫТА, чужая — НЕТ.** Проводка несёт
  `not_fiscalized` (+ `patent_exempt` по докладу линии): нашим облачным путём
  чек НЕ пробит, требование «фискализация предброни не вызывается» исполнено
  на боевых данных ВПЕРВЫЕ. Этим же закрылась задача 12.6 заявки
  `tournament-prebooking`.
  🔴 **Но строка ждала не этого.** Она про **inline-чек Т-Банка**, который
  печатает эквайер по блоку `Receipt` в `Init` и про наш отказ не знает.
  `receipt_status` о нём не скажет НИЧЕГО — это наша колонка, а не банковская.
  Три факта из четырёх (напечатался ли, с какой подписью, с каким тегом 1214)
  добываются только глазами на чеке или через живую заявку
  `acquirer-receipt-is-recorded`. **Строку не снимать.**
- 🟢 **Сумма, которую владелец увидит перед доплатой, ИСПРАВЛЕНА 07.09 ~14:30.**
  Карточка «Моя запись» называла полную цену `5500 ₽` там, где экран оплаты
  называл остаток `5000 ₽`: `resolve_payment_entry` звал котировку мимо зачёта
  предброни. Выкачено в прод (`fa1d42d19`, тег
  `prod-2026-09-07-prebooking-credit-on-card`), зачёт теперь одной дверью
  `quote_after_prebooking` на всех трёх поверхностях, включая чек ручного
  перевода. **Ожидаемая доплата по 8233 — ровно 5000 ₽**, и уйдёт она через
  `pair_pay`, то есть inline-блок `Receipt` по ней не поедет (см. асимметрию
  дверей выше). Разбор — `.claude/handoff/fix-entry-amount-credits-prebooking.md`
  в ветке `fix/entry-amount-credits-prebooking` (закрыт).
- 🟢 **ДОПЛАТА СОСТОЯЛАСЬ 08.09 в 10:15:43 — предсказание строки сбылось
  дословно.** Проводка `11669`, **5000.00 ₽**, `completed`, `not_fiscalized`;
  `cash_source` и `created_by` пусты, то есть пришла через `pair_pay`
  эквайрингом. Зачёт предброни отработал на живых деньгах: заплачено 5000, а не
  5500. Обе строки пары (`27338`, `27339`) — `confirmed`, строка 3 снята.
  🔴 **Вторая половина наблюдения от этого НЕ закрылась.** Строка ждёт
  inline-чека Т-Банка по ПРЕДБРОНИ, а не по доплате; по доплате его не будет по
  построению (`pair_pay` блок `Receipt` не шлёт), и это подтверждает асимметрию
  дверей, а не снимает вопрос. Три факта из четырёх (напечатался ли, с какой
  подписью, с каким тегом 1214) по-прежнему добываются только глазами на чеке
  или заявкой `acquirer-receipt-is-recorded`.
- ⚠️ **Первая предбронь вскрыла ДЕФЕКТ, у него есть хозяин.** Выпуск штампует
  платёжную группу всему СОБЫТИЮ вместо пары (`_prebooking_context`,
  `me/router.py:1508`; `prebook_my_event`, `:1636`): восемь чужих строк ушли в
  `PENDING_PAYMENT` и через 15 минут вернулись в резерв с ложными
  уведомлениями игрокам и организатору. Срабатывало четырежды.
  🟢 **РАЗГАДКА 07.09 в 10:35: починка НАПИСАНА, но живёт ТОЛЬКО в ветке.**
  Ветка `fix/prebooking-payment-group-is-the-pair`, голова `f477f6edb`.
  Резолвер `prebooking_payment_group` есть в трёх файлах ветки, портальная
  дверь на `me/router.py:1629` уже зовёт его. **Не приземлена ни в `develop`,
  ни в `main`** — значит ждёт снаружи не выкатки, а ПРИЗЕМЛЕНИЯ через
  `land.sh`, и это работа сессии-автора, не реестра.
  🔴 **Коммит, несущий код, ПОДПИСАН ЛОЖНО — ловушка для того, кто пойдёт по
  `git log`.** `0a2dbfae9` выглядит документным («дефект уже починен в
  develop»), а на деле несёт 48 строк резолвера, правку роутера и два теста:
  чужую незакоммиченную работу загрёб `git add -A` соседней сессии, писавшей
  в том же дереве. Автор поправки признал это сам, артефакты исправлены
  коммитом `f477f6edb`.
  🔑 **Урок, оплаченный дважды за одно утро:** `git add -A` в дереве, где
  может работать кто-то ещё, — не гигиена коммита, а ПРИСВОЕНИЕ. Коммитить
  только явно названные пути. Правило известно по `~/.claude`, а укусило в
  рабочем репозитории — в ворктри, нарезанном «ради изоляции».
  🔴 **А ВЕРСИЯ «починено в `develop`, ждёт промоушена» — ЛОЖНА; в реестр НЕ
  вносить.** Проверена и опровергнута 07.09 в 10:20.
  Её принесла сама нашедшая линия как «срочную поправку» к своему докладу.
  Опровержение прямое:
  - `git diff origin/main origin/develop -- gmp/` **ПУСТ** — кода эти две
    ветки не различают вовсе, значит промоушен не принесёт ничего;
  - `git log --all -S'prebooking_payment_group' -- gmp/` находит **ровно один**
    коммит на весь репозиторий — `0a2dbfae9`, и это ДОКУМЕНТНЫЙ коммит самой
    линии, утверждающий, что починка есть. Кода с таким именем нет ни в одном
    рефе;
  - в `origin/develop` на `me/router.py:1637` стоит та же дефектная строка
    `unpaid_participants=[p for p in группа ...]`;
  - `origin/develop` = `f3edd9832` — коммит про «сбор», к предброни не
    относящийся.
  🔑 **Чем это было опасно:** поправка предлагала переписать строку с «ждёт
  починки» на «ждёт промоушена». Строка ждала бы выкатки, которая не несёт
  ничего, а владельцу было бы сказано, что живой денежный дефект уже закрыт.
  Ошибка ЗАКРЫВАЮЩАЯ — она не поднимает тревогу, она её снимает.
  ⚠️ Вероятный источник обмана: `second_seat_of` в `school_approval.py`
  СУЩЕСТВУЕТ и в `develop`, и в `main` — настоящая функция рядом с выдуманным
  резолвером делает версию правдоподобной.
  🟢 **ЗАКРЫТО 07.09 к вечеру: починка ПРИЗЕМЛЕНА И ВЫКАЧЕНА В ПРОД.** Замер
  18:56: `prebooking_payment_group` присутствует и в `origin/main`, и в
  `origin/develop`, и в ДЕРЕВЕ ПРОДА (`c891944e4`, все три юнита `active`).
  Дефект в проде больше не живёт; ожидание реестра по нему снято.
  ⚠️ Ловушка про ложно подписанный коммит `0a2dbfae9` остаётся полезной —
  она про чтение истории, а не про состояние кода.
  Ветка `fix/prebooking-payment-group-is-the-pair`, хендофф
  `.claude/handoff/fix-prebooking-payment-group-is-the-pair.md`, коммиты
  `f0917aeba` + `766fb24c7`; работа легла разделом 13 в живую заявку
  `tournament-prebooking` (отдельную заводить не стали: спека права, неверен
  код, а вторая заявка на ту же capability дала бы конфликт дельт).
  ✅ **Сверено ЛИНИЕЙ по проду** (её замеры, 07.09): в 09:27:33 все десять
  строк несли ОДИН `payment_group_id` `b165f86a-f810-454e-85e6-d6340cbcd424`,
  статус `pending_payment` и общий дедлайн `09:32:32`; в 09:36:13 — восемь в
  `reserve`, группа снята, пара плательщика осталась `pending_payment`.
  ✅ **Сверено МНОЙ независимо** (09:50): конечное состояние сходится — восемь
  `reserve`, две `pending_payment`.
  ✅ **Уведомления ОРГАНИЗАТОРУ — ФАКТ, а не вывод** (мой замер 07.09 09:55):
  4 строки `org_booking_expired` созданы в 09:36:17, и журнал `gmp-worker`
  показывает два РЕАЛЬНО отправленных сообщения — `Organizer notice sent:
  user=6176 event=8233 reasons=['booking_expired','booking_expired']` и то же
  для `user=14807`. То есть ложные уведомления организаторам действительно
  ушли, и их двое.
  ⚠️ **Уведомления ИГРОКАМ остаются ВЫВОДОМ из кода** (`_notify_expired`,
  `expiry.py:100`), а не наблюдением: в журнале воркера за это окно их нет.
  Проверяется вопросом самим игрокам либо поимённым следом чатов. Линия логи
  не смотрела и честно это назвала.
  🔑 **Ловушка, на которой я сам чуть не оступился:** первым грепом я пошёл в
  `journalctl -u bot-gmp` и получил ЧИСТЫЙ НОЛЬ. Ноль был бы прочитан как
  «уведомлений не было» — а на деле расписание с 07.09 07:57 крутится в
  `gmp-worker`, и бот тут больше ни при чём. **После переезда планировщика все
  проверки по журналу фоновых проходов идут в `gmp-worker`, не в `bot-gmp`.**
- 🟢 **РЕШЕНИЕ ВЛАДЕЛЬЦА 06.09.2026 в 20:45: чек ПРИНЯТЬ и посмотреть живьём.**
  Предбронь не выключать, код в этой линии не чинить. Владелец добавил, что
  починку багов ведёт **другая сессия**, и она идёт первой.
- 🔑 **Значит эта строка теперь про НАБЛЮДЕНИЕ, а не про предотвращение.**
  Первая предбронь на 8233 — это ещё и первый боевой случай для живой заявки
  `acquirer-receipt-is-recorded`: единственный способ узнать, забираем ли мы
  чек, напечатанный эквайером. Смотреть **ЧЕТЫРЕ** факта:
  1. напечатался ли чек вообще;
  2. на какую СУММУ — ожидается 500 ₽;
  3. с какой ПОДПИСЬЮ — ожидается неверная по смыслу «Оплата участия в
     событии #8233» вместо аванса;
  4. с каким ТЕГОМ 1214 — ожидается `full_payment`, и это ожидание
     **подтверждающее решение**, а не находка: увидев `full_payment`, ничего
     не чинить.
- ⚠️ **Условие `if user_phone:` остаётся заряженным и после 8233.** Окно
  закроется само 07.09 в 11:00, но следующее событие с предбронью получит ту
  же сборку. Приняв чек здесь, владелец принял ОДИН случай, а не правило.
- **Входить:** своего хендоффа у строки нет. Состояние — эта строка плюс
  память `who-fiscalizes-what-in-prod` и `tournament-intake-already-built`;
  разбор кода — хендофф фейс-контроля, назван выше.
- ✅ **Четвёртый факт наблюдения проверен ОБЕИМИ линиями независимо и признан
  подтверждающим.** Нашедшая сессия сверила `ADVANCE_OVERRIDE_ENABLED` сама и
  сняла свой вывод: признак расчёта `full_payment` — решение 24.08.2026 на ВСЕ
  чеки проекта, а не дефект пути предброни. Увидев «полный расчёт», не чинить.

- ✅ **ДЕФЕКТ ПЛАТЁЖНОЙ ГРУППЫ, вскрытый этой же предбронью, ПОЧИНЕН И ВЫКАЧЕН
  07.09.2026 ~11:10** (сессия `G·предбронь-группа-это-пара`, ветка
  `fix/prebooking-payment-group-is-the-pair`). Выпуск штамповал
  `payment_group_id`, `PENDING_PAYMENT` и общий срок ВСЕМ неотменённым строкам
  события: на 8233 это десять строк и четыре чужие пары, крон в 09:32 вернул
  восемь чужих строк в резерв. Заведён общий резолвер
  `prebooking_payment_group`; дефект был в ОБЕИХ дверях, портальной и ботовой.
  `main` = `6653ff063`, тег `prod-2026-09-07`.
  ⚠️ Это ОТДЕЛЬНЫЙ дефект от чека: `if user_phone:` остаётся заряженным, и
  пункты про чек выше в силе целиком.

- 🟡 **ЖДЁТ: следующего турнира с предбронью — увидеть починку живьём**
  (задача 13.6 заявки `tournament-prebooking`). Окно 8233 закрылось 07.09 в
  11:00, на нём проверить уже нельзя. Пока не проверено глазами, починка
  зелена только по тестам.

  **ПРОВЕРКА** (локально в `~/dev/gmb_v2`, ходит на ПРОД сама):

  ```
  cd ~/dev/gmb_v2
  scripts/prod_sql.sh <<'SQL'
  SELECT ep.payment_group_id, count(*) AS strok
  FROM event_participant ep
  WHERE ep.payment_group_id = (
    SELECT ep2.payment_group_id
    FROM user_transaction ut
    JOIN event_participant ep2 ON ep2.id = ut.event_participant_id
    WHERE ut.kind = 'prebooking' AND ut.status = 'completed'
    ORDER BY ut.id DESC LIMIT 1)
  GROUP BY 1;
  SQL
  ```

  **Сработало, когда:** запрос вернул строку и `strok` = **2** (пара) — или 3,
  если у якоря есть гость. **4 и больше — починка не держит**, дефект вернулся,
  входить через `.claude/handoff/fix-prebooking-payment-group-is-the-pair.md`.
  ⚠️ **Пустой ответ ничего не доказывает.** Прогнан 07.09 в 11:25 — **0 строк**,
  и это НЕ «всё хорошо»: проводка `11583` на месте, но группу 8233 крон снял
  ещё в 09:32, `payment_group_id` у строк пуст, а `= NULL` не сходится ни с
  чем. То есть проверка исполнима и синтаксически верна, но заговорит только
  на СВЕЖЕЙ предброни, чья группа ещё жива (до истечения срока доплаты).
  Ноль читать как «случая ещё не было».

### 12. Турбо — снятая маскировка push ждёт данных, пороги ждут её

*Заведена 07.09.2026. Репозиторий `~/dev/turbo01`, ветка `main`. Хендоффа нет:
состояние живёт в заявках OpenSpec, входить ими.*

- **Чего ждём:** накопления платежей с заполненным `receipt_url_via`. Замер 1.1
  заявки `turbo-fiscal-observability` уже делали — данных не было ФИЗИЧЕСКИ:
  поле завели миграцией за часы до замера, через него прошло два платежа при
  652 всего и ~2,4 в сутки.
- **Что стоит:** `turbo-unmask-push` 12/16 — открыты только 4.1–4.3, все три
  наблюдательные, кода в них нет. За ними `turbo-fiscal-observability` 5/23:
  её 1.2 «пороги назвать НЕЛЬЗЯ» стоит ровно на этих же данных.
- **ПРОВЕРКА — хост `mprz`, БД `turboroute`:**

```
cd ~/dev/turbo01
.venv/bin/python ~/checks/receipt_via.py
```

- **Сработало, когда:** в разбивке `receipt_url_via` наберутся десятки записей
  (счёт идёт от 22 на 07.09.2026, +2…3 в сутки) — тогда 4.1 закрывается и 1.2
  получает почву. Появление `retry` — отдельное событие: до датчика это
  ЕДИНСТВЕННЫЙ признак, что push-канал лёг.
- ✅ **4.2 фактически уже выполнена:** на 07.09.2026 все 22 записи — `push`,
  ни одной `retry` с 28.08. Канал жив, перевес `retry` не наступал.
- ✅ **ДОКЛАД СЕСТРИНСКОЙ СЕССИИ СВЕРЕН НЕЗАВИСИМО 07.09 в 19:22.** Строку
  заводила не эта линия, поэтому сверка обязательна — правило шапки. Прогон на
  `mprz`: `receipt_url_via` — **24 `push`, 656 `None`, ноль `retry`**; посуточно
  ровно +2…+3, самая ранняя точка 28.08. Числа сходятся с докладом (22 утром →
  24 вечером), темп заявленный. 4.2 подтверждена замером, а не пересказом.
  ✅ Проверка НЕ вакуумна: печатает разбивку и посуточный ряд, то есть покажет и
  `retry`, когда тот появится.
- 🔴 **ПРЕДИКАТ РАЗМЫТ и потому не сработает сам никогда.** «Наберутся десятки»
  не различает 24 от 40: момент срабатывания зависит от читающего, а не от
  данных. Число обязана назвать линия `turbo-fiscal-observability` — почву её
  задаче 1.2 даёт конкретный счёт, а не слово «десятки». До тех пор строка
  меряется темпом: **24 на 07.09 вечер, +2,5 в сутки** — сороковая запись около
  14.09.2026.
  🔑 **Родовой признак, переносимый на весь реестр:** качественный предикат
  («наберётся», «станет заметно», «накопится») — это намерение, а не проверка.
  Он не умеет дать отрицательный ответ, а значит не умеет дать и положительный.
- ⚠️ **`turbo-token-refresh` (11/12) сюда НЕ входит** и в отложке ему не место:
  его 3.3 ждёт нашей выкатки, а не события снаружи. Это недоделанная работа —
  по правилу шапки она живёт в заявке.

### 13. Обмен билетами с «Пропеваем» — 🟢 лента ВЫКАЧЕНА, ждёт формы возврата

*Заведена 07.09.2026. Репозиторий `~/dev/mprz`, ветка `worktree-portal-ticket-sales`
(запушена), хендофф `.claude/handoff/portal-ticket-sales.md` — входить им.*

- **Что сделано:** заявка `propevay-ticket-exchange` реализована, **82 задачи из
  83**. Обе точки написаны и проверены на живом контуре: лента расписания
  отдаёт события, приём записывает многопозиционный заказ, повтор не удваивает.
  Контракт с живыми примерами — `docs/hrm/propevay-exchange.md`, отправлен
  соседям третьим письмом.
- **Чего ждём:** решения СОСЕДНЕЙ стороны о **форме сообщения о возврате**.
  Возврата у них нет вовсе: статус заказа выводится из `paid_at`/`reserved_until`,
  `REFUNDED` объявлен и не производится ничем, эквайринг не выбран. Их
  склонение — отдельное сообщение с признаком возврата, но это склонение, а не
  решение. Разблокировать по ответу, не по догадке.
- **Что стоит:** ровно задача 6.6 — точка приёма возврата. Наше ХРАНЕНИЕ к обеим
  формам готово (`refunded_quantity`, `cancelled_at`), возвращённое выходит из
  счёта «свободно N». Не хватает только приёма сообщения.
- 🔴 **Это НЕ недоделанная работа, поэтому строка тут и стоит.** Ждём не рук, а
  чужого решения, связанного с выбором платёжного контура.
  ⚠️ **Оговорка, появившаяся 07.09 вечером: наполовину уже не так.** Пока строка
  ждала, у ленты завёлся ПОТРЕБИТЕЛЬ, и его блокирует уже наша сторона (ниже).
- 🔴 **НАША ЛЕНТА НЕ ВЫКАЧЕНА, И СОСЕДИ ЭТО ЗАМЕТИЛИ РАНЬШЕ НАС.** Их §9:
  «письмо говорит „эндпоинты подняты“ … на ВДС эндпоинт ещё не выкачен», и
  verify на живом контуре объявлен у них пробелом, а не «готово».
  ✅ **Сверено мной 07.09 в 19:28, а не принято на слово** — двумя независимыми
  способами: `ssh mprz` → `GET 127.0.0.1:3020/api/external/propevay/schedule`
  отдаёт **404**; и решающее — `git grep -l 'external/propevay' origin/main`
  **пуст**, а в `origin/worktree-portal-ticket-sales` находит
  `src/hrm/routers/propevay.py` и четыре файла тестов. Маршрут живёт ТОЛЬКО в
  ветке.
  ⚠️ Один 404 доказательством не был бы: корень той же службы тоже отдаёт 404.
  Различающий признак — отсутствие путей в `origin/main`, а не код ответа.
  🔑 **Слова строки «обе точки проверены на живом контуре» относятся к
  ЛОКАЛЬНОМУ контуру, а не к ВДС.** Формулировка это скрывала: «живой» читается
  как «боевой». Проверено — да; выкачено — нет.
- 🟢 **ОТСЮДА СОЗРЕВШАЯ РАБОТА, и она наша.** Ждать возврата теперь незачем:
  соседи сами отложили его в свою ТРЕТЬЮ заявку за выбор эквайринга, а лента —
  их ПЕРВАЯ, и она уже готова к реализации. Держать ленту в ветке ради задачи,
  которая по чужому плану наступит через две заявки, — это блокировать
  потребителя ради формальности.
  🔑 **Развилки ДВЕ, и их легко слить в одну — я и слил.** Блокирует
  потребителя **невыкат**, а не неархивация. Приземлить и выкатить ленту можно,
  НЕ трогая 6.6 и не архивируя заявку: 82 готовые задачи от этого не зависят.
  Тогда решение «отказ или сделать» остаётся честно открытым и принимается
  тогда, когда соседи назовут форму, — а не выторговывается ради выката.
  **Предлагаемый ход поэтому:** приземлить и выкатить ленту сейчас; 6.6 и
  архивацию оставить ждать соседей на своём месте.
- 🟢 **ВЫКАЧЕНО 07.09.2026 около 19:50 — владелец выбрал этот ход.** Прод `mprz`
  на `main` = `d6b68b4` (ветка `worktree-portal-ticket-sales` приземлена мержем;
  единственный конфликт был в файле хендоффа — в `main` лежала его 1-я редакция,
  взята 3-я). Гейт на результате мержа: **965 passed, 5 failed**, и эти пять
  прогнаны отдельно на чистом `main` — предсуществующие, мерж не добавил ни
  одного. Голова alembic одна, миграция `d4b1e7c92f30` накачена (аддитивная:
  две таблицы, две колонки; `downgrade` симметричен). Рестарт `mprz-hrm` сделал
  владелец руками — `sudo` на ВДС требует пароля.
  ✅ **Проверено с самой ВДС:** `GET 127.0.0.1:3020/api/external/propevay/schedule`
  без секрета → **403**, с заголовком `X-Propevay-Secret` → **200**. До выката
  тот же адрес отдавал 404.
  ✅ **Секрет заведён и передан соседям** (`.env` их репозитория, закрыт
  `.gitignore`; в git не попал — проверено грепом). В контракт дописан §10 с
  доказательствами и двумя ловушками часового; запушено.
- 🔑 **Попутные факты, найденные выкатом и стоившие бы дороже потом:**
  - у соседей заявка-потребитель **78 задач из 79**, а не «готова к реализации»
    — их хендофф отставал от их же кода; мой прежний текст это отставание
    перенёс. Сверять по `tasks.md`, а не по строке хендоффа;
  - умолчание адреса ленты у соседей (`DEFAULT_MPRZ_FEED_URL`) уже совпадает с
    выкаченным адресом побайтно, и в `deploy/portal.env.example` стоит
    `PORTAL_MPRZ_SHARED_SECRET=ЗАМЕНИТЬ` — слот под секрет ждал заранее;
  - боевого `/etc/propevay/portal.env` на ВДС ещё нет: портал не установлен,
    поэтому положить секрет в бой было некуда, и он ждёт установки;
  - все 7 площадок московские ⇒ умолчание миграции по поясу верно; арендатор
    один ⇒ `propevay_tenant_id` можно не задавать.
- 🔴 **ЧТО ОСТАЛОСЬ ЖДАТЬ — только форма возврата, и она НЕ у нас.** Строка
  остаётся живой ровно за этим. 6.6 открыта, заявка не заархивирована.
- 🔴 **ПРЕЖНЯЯ ПРОВЕРКА ДАЛА ЛОЖНОЕ ЗЕЛЁНОЕ В ТОТ ЖЕ ДЕНЬ — переписана 07.09
  в 19:35.** Она спрашивала «появилось ли у соседей новое», и к вечеру 07.09
  сработали ОБА её плеча сразу: завелась заявка `mprz-schedule-sync`
  (потребитель нашей ленты, уже прошла круг критики, 46 → 95 сценариев) и
  появился раздел «§9 Контракт по §7 ЗАФИКСИРОВАН — четвёртое письмо». По
  букве предиката строку следовало снимать. **Ответа о форме возврата нет ни в
  том, ни в другом:** §9 кончается подзаголовком «Возврат — по-прежнему за
  нами», а заявка прямо выносит его за границу — «приём продажи и возврат —
  третья, она ждёт выбора платёжного контура».
  🔑 **Родовой признак, переносимый на любую строку «ждём соседей»:** предикат
  «появилось что-нибудь новое» у стороны, которая ведёт СВОЮ работу, срабатывает
  на любом её шаге. Такой предикат меряет чужую активность, а не наш ответ. У
  строки, ждущей конкретного решения, предикат обязан спрашивать ЭТО решение.
- 🟢 **Ожидание получило срок и хозяина, и это главное изменение 07.09.** Тупика
  нет: обе стороны согласны, что форму называет «Пропевай». Но теперь названо и
  КОГДА — их собственным планом из трёх заявок, где возврат стоит ТРЕТЬИМ и
  привязан к выбору эквайринга. Первая заявка только готова к реализации.
  Значит ждать не дни, а недели; торопить нечем, и заглядывать в проверку чаще
  раза в неделю смысла нет.
- **ПРОВЕРКА** (хост — ЭТА машина, репозиторий соседей локальный):

  ```
  grep -c '' ~/dev/propevay/docs/mprz-schedule-contract.md
  grep -n '^## ' ~/dev/propevay/docs/mprz-schedule-contract.md | tail -2
  ls ~/dev/propevay/openspec/changes/ | grep -v archive
  ```
  **Отсечка 07.09.2026 19:35: 356 строк, последний раздел «## 9», три заявки**
  (`mprz-schedule-sync`, `portal-lead-notification`,
  `portal-payment-stub-and-launch`).
  **Сработало, когда** появился раздел с номером БОЛЬШЕ 9 **и в его теле назван
  выбранный эквайрер либо форма сообщения о возврате** — либо среди заявок
  завелась их третья, про приём продажи и возврат.
  🔴 **Новый раздел САМ ПО СЕБЕ — не срабатывание.** Их переписка идёт своим
  ходом, разделы будут добавляться и без ответа нам; тело читать обязательно.
  Ровно на этом прежняя проверка и врала.
- **ВТОРАЯ ПРОВЕРКА — «не сделали ли уже мимо строки»** (правило шапки для строк,
  ждущих слова человека):

  ```
  cd ~/dev/mprz
  git log --oneline -5 origin/worktree-portal-ticket-sales
  ```
  Коммит про приём возврата означает, что 6.6 закрыли мимо реестра. На 07.09
  19:35 голова — `188cc80 docs(handoff): третья редакция … ждём соседей по
  возврату`, то есть не закрыли.
- ⚠️ **Проверка НЕ построена на исчезновении признака.** Строку
  `orders.py:173` («`REFUNDED` не производит ничто») брать за проверку нельзя:
  её пропажа — отрицательный сигнал, а он врёт в сторону тишины.
- 🔴 **На архивации не ставить `--yes`.** CLI остановится на незакрытой 6.6 и
  сделает правильно. Законных исхода два, оба требуют человека: сделать (если
  соседи ответили) или ОФОРМИТЬ ОТКАЗ до архивации — снять 6.6 из `tasks.md`,
  назвать причину в `proposal.md`, вынести приём возврата отдельной заявкой.
- ⚠️ **К сведению владельца:** спека уже сведена в `openspec/specs`, и требование
  «Возврат и отмена выражаются записью» стоит там ЦЕЛИКОМ, хотя приём сообщения
  не реализован. Хранение и читатели — да, точка приёма — нет.

### 15. sms-gate — атрибуция отчётов ждёт НЕДЕЛИ данных, срок 10.09.2026

- **Где:** `~/dev/sms-gate`, ветка `master`, ворктри нет. Заявка
  `attribute-late-delivery-reports`, 86/89: закрыты фазы 1–8, кроме архивации.
  Выкачено 03.09.2026 в 13:42 МСК, 0.17.0 → 0.18.0.
- **Ждёт:** не человека и не события, а **накопления**. Задача 9.1 просит
  прочитать ledger спустя неделю после выката, то есть **с 10.09.2026**, и
  решить судьбу флага `delivery_report_strict_attribution`. Ретенция ledger —
  30 суток, так что окно наблюдения не истечёт раньше начала октября.
- **Строка заведена 07.09.2026 при удалении хендоффа заявки.** Хендофф убран
  намеренно: он переезжал живой `verify-by-inbound-code` в подборе `hand.sh`
  (см. фикс `8b28e36` в derflow). Состояние при этом не потеряно — дата выката
  и все замеры стоят отметками в `tasks.md`, а не в удалённом файле.
- **ПРОВЕРКА (что накопил ledger):**

  ```
  ssh -p 30022 home.deralsem.ru "sqlite3 -readonly /opt/sms-gate/data/sms.db \"SELECT 'всего', COUNT(*) FROM delivery_reports WHERE received_at > '2026-09-03 10:42' UNION ALL SELECT 'неприткнутых', COUNT(*) FROM delivery_reports WHERE received_at > '2026-09-03 10:42' AND outcome='unplaced' UNION ALL SELECT 'по свежести', COUNT(*) FROM delivery_reports WHERE received_at > '2026-09-03 10:42' AND decided_by='recency' UNION ALL SELECT 'ПОБЕДИТЕЛЬ противоречив', COUNT(*) FROM delivery_reports r, json_each(r.candidates) c WHERE r.received_at > '2026-09-03 10:42' AND json_extract(c.value,'\$.outcome')='chosen' AND json_extract(c.value,'\$.contradicted')=1;\""
  ```

  Окно в UTC: боевая база пишет `received_at` в UTC, выкат 13:42 МСК = 10:42.
- 🔴 **Наивный `LIKE '%contradicted%'` даёт ЛОЖНОЕ КРАСНОЕ — проверено 07.09.**
  `contradicted` в `candidates` это **ключ**, он стоит у каждого кандидата, и
  подстрочный поиск совпадает всегда: первая редакция проверки показала «200
  противоречий из 201» там, где их ноль. Спрашивать надо значение и только у
  ПОБЕДИТЕЛЯ (`outcome='chosen'`), иначе меряется шум коллизии ссылок, ради
  которого ledger и заводился.
- **Замер 07.09 (ПРЕДВАРИТЕЛЬНЫЙ, неделя ещё не вышла — 4 суток из 7):**
  всего 201 отчёт, неприткнутых **0**, по свежести **0**, победитель
  противоречив **0**; проигравших кандидатов вытеснено 222 — это и есть
  переиспользование модемом номера ссылки, отработавшее штатно. Плюс одна
  `unparsable` 04.09 в 10:46 — обрезанная строка `+CDS: 25`, к атрибуции
  отношения не имеет.
- ✅ **ПОЛОЖИТЕЛЬНЫЙ ОБРАЗЕЦ решающего счётчика — добыт 08.09.2026, до этого его
  не было вовсе.** «Победитель противоречив» показывал ноль и 07.09, и 08.09, а
  ноль без образца неотличим от сломанного предиката — ровно та ложь в сторону
  тишины, о которой шапка. Обе половины конъюнкции прогнаны порознь тем же окном:
  `contradicted=1` у ЛЮБОГО кандидата — **228**, `outcome='chosen'` — **206**,
  кандидатов всего 434. Оба ключа читаются, json-путь верен, значит ноль в
  пересечении — настоящий результат, а не молчание сломанной проверки.
- 🔑 **Но этот ноль означает НЕ «противоречий нет», а «путь противоречия ни разу
  не включался».** Разбивка 08.09: `superseded` — 228 кандидатов, и противоречивы
  ВСЕ 228; `chosen` — 206, противоречив ни один. Исходов `contradicted` и
  `outside_window` за пять суток нет вообще. В `app/modem/attribution.py` ступень
  `superseded` (135) стоит РАНЬШЕ ступени `contradicted` (141), а `surviving`
  (165–167) отбрасывает `superseded` независимо от флага. Значит все 228
  противоречий сидят на кандидатах, отсеянных до того, как флаг что-либо решает:
  **включение флага не изменило бы ни одного из 207 решений.** Это сильнее
  прежнего «включать нечего»: механизм не «ничего не нашёл» — он не запускался.
  Задаче 9.1 это и надо предъявить владельцу.
- **Замер 08.09 в 07:05 (5 суток из 7):** всего **207**, неприткнутых 0, по
  свежести 0, победитель противоречив 0. Темп — **+6 в сутки** (201 на 07.09;
  «вытеснено 222» выросло до 228 — тот же счётчик, это `superseded`). По строкам
  ledger: `attributed` 206, `unparsable` 1. Флаг в проде сверен напрямую и
  выключен — `delivery_report_strict_attribution|false`, — то есть замер мерил
  тот мир, для которого писался.
- **Тогда:** при нуле противоречивых победителей включать флаг **нечего** —
  строгий режим отказывает отчёту, чей адрес противоречит единственному
  кандидату, а таких за четверо суток нет ни одного. Решение всё равно за
  владельцем; после флипа задача 9.2 требует проверить ОБА направления на
  боевом шлюзе и записать условие обратного выключения. Затем 8.6 — архивация.
- **Входить:** `~/dev/sms-gate/openspec/changes/attribute-late-delivery-reports/`
  — `tasks.md`, группа 9. Хендоффа у заявки больше нет, и он не нужен.
- ⚠️ **Пуш в `deploy-remote` рестартит боевой сервис** — тот же запрет, что в
  строке 10. Флаг живёт в настройках, а не в коде; выката ради него не нужно.

### 16. Тренер видит 404 и чужие деньги — ОТЛОЖЕНО владельцем до пятницы 11.09.2026

Приёмка 3.3 заявки `coach-manage-only-when-organizer` **падает**: владелец
прогнал её руками 07.09 вечером и поймал два дефекта. Работа не начата
сознательно — отложена до пятницы. Ветки и ворктри нет.

- **Дефект 1:** нет доступа отдаёт `404 "Event not found"` — тот же ответ, что
  у удалённого. Ветка 403 во фронте написана и по этой двери недостижима.
- **Дефект 2:** `_current_user_is_operator` считает тренерство свойством
  ЧЕЛОВЕКА, а не отношением к событию — тренер видит собрано/долг по чужой
  тренировке. Замер: под «Войти как» Юрием (coach 7 / user 15140) в подсказке
  расписания — «5250 / 10500 ₽» по событию Елены.
- Полоса **C**: деньги и авторизация. Форма ответа — дизайн, не сборка.

**ПРОВЕРКА** (первая местно в основном чекауте, две на проде,
хост `deralsem@mprz.ru`):

```
cd ~/dev/gmb_v2 && openspec list | grep coach-manage
ssh deralsem@mprz.ru 'cd /home/deralsem/dev/gmb_v2 && sed -n "/^async def get_event(/,/^    dao = EventDAO/p" gmp/crm/api/events/router.py | grep -c "status_code=404"'
ssh deralsem@mprz.ru 'cd /home/deralsem/dev/gmb_v2 && grep -A9 "def _current_user_is_operator" gmp/crm/api/events/router.py | grep -c coach_id'
```

**Положительный образец — все три прогнаны дословно 07.09.2026 в 23:2x:**
первая вернула `11/12 tasks 11d ago`, вторая `1`, третья `1`.

⚠️ Вторая намеренно сужена `sed`-окном по телу `get_event` (11 строк). Первая
её редакция считала `404 "Event not found"` по ВСЕМУ файлу и давала `29` —
такой счётчик остаётся ненулевым и после починки, то есть не различает ничего.
Различающая сила проверки держалась бы на редкости, а `404` в этом файле частый.

**Сработало, когда:** наступила пятница 11.09.2026 **и** первая команда всё ещё
показывает `11/12` — то есть сестринская сессия за нас это не починила.
⚠️ Если вторая или третья вернут `0` — строку **снять**, а не «ждать дальше»:
это значит, что якорь уехал или дефект уже закрыт, и заходить надо с разбора,
а не с плана из хендоффа.

✅ **Сверено линией ведения 08.09.2026 в 07:0x — все три вернули образец
дословно:** `11/12 tasks 11d ago`, `1`, `1`. Дополнительно закрыта самая дорогая
форма чужой ошибки — закрывающая («дефект уже починен в `develop`»): дифф
`git diff origin/main origin/develop -- gmp/crm/api/events/router.py` ПУСТ, и в
`origin/develop` тот же `coach_id` в `_current_user_is_operator`. Дефект жив в
обеих ветках, сестринская сессия его не трогала. Строка честна, ждёт пятницы.

**Входить:** `~/dev/gmb_v2/.claude/handoff/coach-manage-acceptance-3-3.md`
(основной чекаут, жатве не подлежит). Там боевые номера, два запрета «чего
делать нельзя» и дословное решение владельца от 07.09 про то, что тренер —
описание тренировки.

### 17. ~~Первая ШТАБНАЯ предбронь на проде~~ — СНЯТА 08.09.2026 в 11:50, СРАБОТАЛА ЧЕТЫРЕЖДЫ

Заведена 07.09.2026. Штабная дверь предброни выкачена в прод
(`ba9202084`, тег отката `prod-2026-09-07-hq-prebooking-door`). Механизм
проверен на демо от нажатия до проводки; на проде им **ещё никто не
воспользовался**, и нажать за организатора нельзя — это чужие деньги на живом
турнире.

- **Событие:** турнир **8233 «TWO TOP MIX», сб 12.09, ЮЛ 8 (патент)**,
  организатор Ксения. Замер 07.09 после выката: предикат показа выполняется
  целиком (турнир, `per_person`, 2750 ₽, все три параметра предброни — 500/5/3,
  не отменён), в резерве **6 пар** — кнопка «⏳ в предбронь» есть у каждой.
  Одна пара уже в предзаявке через портальную дверь.
- **Чего ждём:** организатор принимает предбронь наличными и отмечает её в
  панели. Тогда появится ПЕРВАЯ проводка `kind='prebooking'` с непустым
  `cash_source`.
- **Сработало, когда:** запрос ниже вернул хоть одну строку.

**ПРОВЕРКА** (хост — локальная машина, скрипт сам ходит на ВДС):

```
cd ~/dev/gmb_v2
echo "SELECT id, amount, cash_source, created_by, receipt_status, receipt_skip_reason FROM user_transaction WHERE kind='prebooking' AND cash_source IS NOT NULL ORDER BY id DESC LIMIT 5;" | scripts/prod_sql.sh
```

Ждём: `cash_source` = `cash_desk` либо `terminal`, `created_by` — настоящий
человек, `receipt_status` = `not_fiscalized`, `receipt_skip_reason` =
`patent_exempt`. ✅ Проверка НЕ вакуумна: тот же запрос без условия по
`cash_source` находит боевую проводку 11583 (портальная дверь, 500 ₽), то есть
умеет попадать.

⚠️ **Пустой ответ — это «ещё ждём», а не «сломалось».** Отличить одно от
другого можно только запросом выше: строки нет и у исправной двери, пока по ней
не нажали.

**Входить:** `~/dev/gmb_v2/openspec/changes/hq-promotes-to-prebooking/HANDOFF.md`
(ветка `feature/hq-promotes-to-prebooking`). Заявка НЕ заархивирована и до этой
строки архивироваться не должна — вместе с ней открыта зависимость от
`tournament-prebooking`.

🟢 **СРАБОТАЛА 08.09.2026 — четыре штабные предброни, все поля совпали с
ожидаемым дословно.** Проверка вернула 4 строки: `11655` и `11656` (07:34:57 и
07:35:06), `11670` и `11671` (10:40:44 и 10:50:01), каждая по **500.00 ₽**,
`cash_source = terminal`, `created_by = 6176` (Ксения, живой организатор
события), `receipt_status = not_fiscalized`, `receipt_skip_reason =
patent_exempt`. Строка ждала «хоть одну» — пришло четыре, и совпал не только
предикат, а весь перечень ожидаемых значений.

🔑 **Нашла её НЕ своя линия и не по расписанию, а сверка чужого доклада о
доплате пары (строка 3).** Проводки по 8233 показали четыре `prebooking` уже
ПОСЛЕ того, как реестр записал «окно закрылось 07.09 в 11:00, предбронь была
ОДНА». Расхождение и вывело на срабатывание. Правило №4 окупилось восьмой раз,
и впервые — на строке, которую никто не проверял, потому что ждали нажатия.

⚠️ **«Окно предброни закрылось» верно только для ПОРТАЛЬНОЙ двери.**
`gmp/core/services/tournament/prebooking.py:553` — `if not штаб and not
prebooking_window_is_open(...)`: штабная дверь окно не спрашивает вовсе, и это
сделано намеренно. Формулировка «окно закрылось 07.09 в 11:00» в строках 3 и 11
писалась до выката штабной двери и после него читается как «предброни больше не
будет» — а их пришло четыре. Кто увидит `prebooking` после срока — это не
дефект и не сбой часов.

**Дальше — работа СОСЕДНЕЙ линии, не реестра:** заявка
`hq-promotes-to-prebooking` держалась незаархивированной до этой строки, теперь
условие снято. Архивировать её вместе с зависимостью от `tournament-prebooking`,
входить `~/dev/gmb_v2/openspec/changes/hq-promotes-to-prebooking/HANDOFF.md`
(ветка `feature/hq-promotes-to-prebooking`). ⚠️ После архивации — сразу сверить
соседей по общим требованиям (правило проектного `CLAUDE.md`).

### 18. Один язык подстановок — блоки 1–5 В ПРОДЕ, ждут чека и вендорского референса

Заведена 08.09.2026. Заявка `purpose-and-item-one-language`, ветка
`feature/purpose-and-item-one-language`, дерево
`~/dev/gmb_v2/.claude/worktrees/purpose-one-language`. **30/37 задач.**

- ✅ **Уже в проде** (`3f2d824b3`, выкат 09:37 сестринской сессией — деплой
  релизит ствол). Назначение сверено на боевых данных: проводка 11663 у ЮЛ 2
  несёт `…#2609082100-4-0` — побайтово то же, что до выката.
- ⏳ **Ждёт ПЕРВОГО БОЕВОГО ЧЕКА** у ЮЛ 2 (`ofd_business`) или ЮЛ 4 (`evotor`).
  С момента выката не пробит ни один. Это закрывает задачи 7.4 и 7.5.
  ⚠️ Ноль строк сторожа — отсутствие трафика, а не доказательство исправности.

  **ПРОВЕРКА:**
  ```
  ssh mprz 'journalctl -u gmp-crm -u gmp-worker --since "2026-09-08 09:37" | grep "\[RECEIPT\]"'
  ```
  Ждёшь строк подачи. Если среди них есть «не принимает item_context» — правка
  не сработала; если «не принимает payment_method» — это блок 6, так и задумано.

- ⏳ **Ждёт ВЕНДОРСКОГО РЕФЕРЕНСА** «Бизнес.ру» и «Эвотора» — блок 6 (задачи
  6.1–6.5) не отгружен, признак способа расчёта до касс по-прежнему не доезжает.
  Носитель — владелец: нужен доступ в ЛК «Бизнес.ру» либо захваченный сэмпл
  чека. Разбор и перечень уже перепробованного — память `named-open-gaps`,
  глава «Признак способа расчёта не доезжает до двух касс из четырёх».
  🔴 У «ОФД Бизнес» неизвестно даже, ЧТО ЭТО ЗА ПОЛЕ: наш же itemized-путь
  кормит `payment_mode` признаком ПРЕДМЕТА расчёта. Правка вслепую напечатала бы
  в документе ФНС не тот тег.
- ⏳ **Ждёт РЕШЕНИЯ ВЛАДЕЛЬЦА** по семи незакрытым задачам: снять блок 6 из
  заявки или держать её открытой до референса. Снимает задачи человек, не
  ассистент, — до этого заявка не жнётся.
- 💡 Улучшение, не условие: у ЮЛ 2 и ЮЛ 4 поле «Наименование позиции чека»
  пусто, поэтому позиция называется описанием операции. Заполнят — заработает.

**Входить:** `~/dev/gmb_v2/.claude/handoff/purpose-and-item-one-language.md`.

### 19. sms-gate — заявка на uCaller написана, ждёт КЛЮЧА и оплаченного баланса

*Заведена 08.09.2026. Ветка `master`, ворктри нет, ничего не закоммичено.*

- **Где:** `~/dev/sms-gate`, заявка `openspec/changes/route-sends-by-operator`
  (переписана 08.09 с P1SMS на uCaller решением владельца). Валидна
  (`openspec validate --strict`), 37 сценариев, кода не написано ни строки.
- **Ждёт:** владелец заводит аккаунт на https://developer.ucaller.ru, кладёт
  `key` и `service_id` в `/opt/sms-gate/.env` на боевом хосте и пополняет
  баланс. Без этого не проверяется **единственное несущее звено**: доходит ли
  flash call до абонента МегаФона. Не дойдёт — заявка ничтожна целиком, и
  запасной путь это замороженная `verify-by-inbound-code`.
- **ПРОВЕРКА (появился ли ключ):**

  ```
  ssh -p 30022 home.deralsem.ru "grep -ci ucaller /opt/sms-gate/.env"
  ```

  Ответ больше нуля — созрело: входить хендоффом и делать задачу 1.2 —
  один `initCall` на `+79851600019` (мегафоновский номер, владельцем разрешён
  для проб). Ноль — ещё ждёт.
  ⚠️ Отрицательный ответ этой проверки положительным образцом пока не
  подкреплён: файла с ключом никогда не существовало, поэтому «ноль» и
  «grep не туда смотрит» отсюда неразличимы. Первый же положительный прогон
  снимает оговорку.
- **Второе, что ждёт снаружи:** разработчик парковки должен взять новый
  контракт (`POST /verifications`, `/check`, `GET`). Пока `sp_app` его не
  возьмёт, ни один абонент МегаФона улучшения не увидит, сколько бы шлюз ни
  умел. Контракт ещё не написан — задача 3.1.
- ⚠️ **Критик-слой не прогнан** — третья заявка подряд в этом репозитории с
  этим долгом: три сессии не имели права поднимать сабагентов.
- **Входить:** `~/dev/sms-gate/.claude/handoff/ucaller.md`.

## Ждёт без сессии — сигнал есть, хозяина нет

Это уже записано в памяти gmb_v2, здесь только чтобы не терялось при вопросе
«что созрело». Тела и точные предикаты — в названных записях.

- **Эвотор-push** ждёт первого боевого платежа ЮЛ 4; замеры групп 0 и 6
  заявки `evotor-receipt-push` до тех пор не закрыть, заявку не архивировать.
  Память: `inbound-webhook-map`.
- **Облачная касса КОМТЕТ** не проверена ничем и проснётся в день, когда
  кому-то настроят `receipt_provider`; первую же дистанционную продажу после
  этого смотреть руками. Память: `who-fiscalizes-what-in-prod`.
- **Возврат по чеку** живьём не исполнялся; сигнал — первая продажа с пробитым
  чеком на площадке с живой ККТ (сегодня «Сокол»). На демо непроверяем
  структурно. Память: `charge-has-no-refund-state`.
- **Предбронь турнира** в проде с 05.09, но нигде не включена — ждёт живого
  турнира; проверять, что первая проводка несёт `not_fiscalized` +
  `patent_exempt` и чек по ней НЕ пробит. Память: `tournament-intake-already-built`.

- 🔶 **НЕРАЗОБРАННАЯ композиция строк пары на 8233 — замечена 08.09.2026 в
  11:5x, диагноза НЕТ.** Дефектом не объявляю: не знаю, легальна ли такая
  композиция в этой модели. Факты, и только они: user **15180** сидит в ТРЁХ
  строках одного события — `27526` (`is_primary=t`, партнёр 1948,
  `pending_payment`, 09:51) при отменённом партнёре `27527`; `27541`
  (`user_id` ПУСТ, `is_primary=f`, `partner_user_id=15180`, **`confirmed`**,
  10:47:56); `27542` (`user_id=15180`, `is_primary=f`, партнёра нет,
  `reserve`, 10:50:11). У двух последних `is_primary=f` обе, якоря между ними
  нет, и плейсхолдер `confirmed` там, где названный человек `reserve`.
  Времена 10:47–10:50 вплотную к штабным предброням `11670`/`11671`.
  ⚠️ Памяти `seat-state-needs-two-editions` и `pair-halves-get-different-footers`
  говорят, что плейсхолдер — законное состояние, поэтому «странно выглядит» тут
  не довод. Нужен тот, кто знает инвариант композиции пары.
  **ЗАПРОС (хост — локальная машина, скрипт ходит на ВДС):**

  ```
  cd ~/dev/gmb_v2
  echo "SELECT id,user_id,status,is_primary,partner_user_id,created_at FROM event_participant WHERE event_id=8233 AND (user_id IN (15180,1948) OR partner_user_id IN (15180,1948)) ORDER BY id;" | scripts/prod_sql.sh
  ```
  ✅ Не вакуумен: прогнан 08.09 в 11:5x и вернул все четыре строки.

---

## Что всплыло самим опросом

- 🔴 **Две сессии жили в ОДНОМ ворктри одновременно** —
  `F·feature/face-control-toggle` и `G·галка-фейс-контроль`, обе на
  `feature/face-control-toggle` в `.claude/worktrees/face-control-toggle`. Не
  родитель с преемником, а две параллельные линии. Доказано не догадкой: коммит
  `280c9b5b8` одной лёг поверх двух другой, и `land.sh` подобрал его и
  приземлил как свой. Не затёрли ничего только потому, что правили разные
  файлы. По фейс-контролю будить **одну** линию, а не обе.
- ⚠️ **Поле `started` в `ListAgents` — метка старта, а не простоя.** «Пульт.ехе
  · waiting · started 4d ago» читалось как «висит четвёртые сутки», а сессия
  работала, и владелец был в диалоге минутами раньше. Судить об активности по
  этому полю нельзя — надо спрашивать.
- 🔴 **«Не ответила» значит два разных состояния, и на глаз они одинаковы.**
  `gmb-v2-ef` отработала опрос за 18 секунд и написала полный ответ — себе в
  терминал, обычным текстом. Спрашивающему не видно ничего: чужой текст до
  него не доходит, доходит только сообщение. Различить «не дошла очередь» и
  «ответила мимо» можно лишь грепом транскрипта по `msg_id` отправки —
  `grep -rl <msg_id> ~/.claude/projects/*/*.jsonl`, дальше читать хвост файла.
  Не найдя ответа, сперва грепнуть, а уж потом писать «не ответила».
- 🔴 **Скриншоты через CDP не снимаются, и виноват НЕ load.**
  `Page.captureScreenshot` таймаутит за 30 с при живом JS на той же вкладке —
  четыре раза за две сессии, при нагрузке 3.67. Прошлая сессия винила
  load 25–35 и записала это в хендофф; замер опроверг. Рабочий обход: свой
  http-сервер (обязательно `charset=utf-8`, иначе кириллица мохибейком и
  ширины врут) + `javascript_tool` + `getBoundingClientRect`.
- ⚠️ **Опрос сам себе не гарантия: строку 8 я написал по УСТАРЕВШЕМУ хендоффу,
  решив, что сессия молчит.** Она отвечала, мы разминулись, и вышло «ждёт
  человека» вместо «ведёт живой разговор, закрывать нельзя». Молчание в ответ
  на опрос — не факт, а отсутствие факта; строка, снятая с диска, обязана
  нести пометку об этом и перепроверяться у сессии, если та жива.
- ⚠️ **Имя сессии протухает.** «M·остался шаг 5» — шаг 5 исполнен в тот же
  день, имя осталось. По имени о состоянии не судить.

## Опрошены и НЕ в отложке (06.09.2026)

Чтобы не опрашивать заново.

- **dev-0d** — интерактивное администрирование домашнего сервера, не
  разработка, ветки нет. Незакрытая рекомендация, от которой владелец
  отказался: на `derserver` глобально разрешён парольный вход по ssh при
  пробросе снаружи.
- **«Стекло» в muzloto** — заведена строкой 11 06.09 вечером и в тот же
  вечер отработала: владелец прогнал площадочную Windows, подтвердил
  картинку глазами, заявка заархивирована (`2026-09-06-glass`) и влита в
  `master` перемоткой. Строка снята. ⚠️ Держалась в реестре часы, а не дни:
  событие, которого она ждала, оказалось у владельца под рукой. Прежде чем
  заводить строку — спросить, не делается ли это сейчас.
- **Пульт.ехе** — `~/dev/muzloto`, ветка `master`, работает прямо сейчас,
  владелец в диалоге. ⚠️ В `ListAgents` она выглядела «waiting, 4d» — это
  **метка старта, а не простоя**. По этому полю судить об активности нельзя.

### 14. ~~Синк расписания «Пропевая»~~ — СНЯТА 07.09.2026 в 22:00, СРАБОТАЛА

🟢 **Событие наступило в тот же вечер: соседи выкатили маршрут.** Прогон
ПРОВЕРКИ этой строки дословно — **`403`** вместо отсечки `404`. Это ровно
объявленное «сработало»: маршрут есть, часовой на месте.

🔑 **Ждали не соседей, а самих себя — и узнали об этом только сведя две
строки.** Выкат сделала линия отложки в тот же вечер по строке 13: обе стороны
обмена — работа одного владельца, и строка 14 ждала выката, который стоял
заблокированным решением по ЧУЖОЙ задаче 6.6. Развилки разведены, лента
выкачена, потребитель разблокирован.
⚠️ **Урок ведения реестра:** две строки описывали ДВА КОНЦА ОДНОГО ожидания и
не знали друг о друге. Заводя строку «ждём соседей», проверять, нет ли в
реестре встречной строки «соседи ждут нас» — иначе обе стоят, и каждая честно
считает себя заблокированной снаружи.

**Что выкачено и сверено** (подробности в строке 13): прод `mprz` на `d6b68b4`,
миграция `d4b1e7c92f30` накачена, `mprz-hrm` перезапущен. Без секрета `403`,
с секретом `200`.

✅ **Секрет уже передан, добывать не надо.** Лежит в `~/dev/propevay/.env`
(закрыт `.gitignore`, в git не попал), ключ `PORTAL_MPRZ_SHARED_SECRET` — то
самое имя, которого ждёт `deploy/portal.env.example` вместо `ЗАМЕНИТЬ`.
Заголовок `X-Propevay-Secret`, адрес совпадает с вашим `DEFAULT_MPRZ_FEED_URL`
побайтно. В контракт дописан §10 с доказательствами и двумя ловушками часового.

**Дальше по задаче 12.2:** раздел «Синк расписания» в
`~/dev/propevay/deploy/README.md` — секрет в окружение, прогон руками, сверка
каталога с первой лентой, потом таймер.
🔴 **Ходить строго на `127.0.0.1`:** список доверенных адресов у стороны МПРЗ
только петлевой, через `hrm.mprz.ru` часовой ответит 403 — nginx подставит
туда публичный адрес самой машины.

---

*Ниже — прежнее тело строки, оставлено надгробием.*

*Заведена 07.09.2026. Репозиторий `~/dev/propevay`, ветка `master` (коммит
`2977fc5`), хендофф `.claude/handoff/mprz-schedule-integration.md` — входить им.*

- **Что сделано:** заявка `mprz-schedule-sync` реализована, **78 задач из 79**,
  552 теста зелёных. Команда прогнана против живого HTTP-двойника ленты: обе
  даты заводятся, квоты назначаются, все пять исходов обращения различаются по
  проводу. Шесть сторожей покусаны мутацией.
- **Чего ждём:** соседи выкатят маршрут `GET /api/external/propevay/schedule`
  на ВДС. У них он написан и проверен ЛОКАЛЬНО — их же справочник это и
  говорит, — но на машине его нет: 07.09 uvicorn на `127.0.0.1:3020` жив, а
  путь отдаёт 404.
- **Что стоит:** ровно задача 12.2 — живой опрос настоящей ленты. Без неё
  заявка не считается проверенной и НЕ архивируется: архивация утверждает
  истинность дельты в коде.
- 🔴 **Это НЕ недоделанная работа.** Ждём чужой выкатки, а не своих рук. Наш
  код к 404 готов отдельным исходом: он помечает обмен неисправным с причиной
  и не приравнивается к 503 — иначе невыкаченный маршрут был бы неотличим от
  штатной недоступности до истечения предела молчания.
- ⚠️ **Предикат спрашивает НАШ ответ, а не чужую активность** — родовой урок
  строки 13, где «появилось что-нибудь новое» дало ложное зелёное в тот же
  день. Здесь проверка спрашивает ровно одно: отвечает ли маршрут.
- **ПРОВЕРКА** (хост `mprz`, секрет не нужен — 404 и 403 различаются до него):

  ```
  ssh mprz
  U=http://127.0.0.1:3020/api/external/propevay/schedule
  curl -s -o /dev/null -w '%{http_code}\n' $U
  ```
  **Отсечка 07.09.2026: `404`.**
  **Сработало, когда** код стал `403` (маршрут есть, часовой на месте) или
  `200`. Дальше — раздел «Синк расписания» в `~/dev/propevay/deploy/README.md`:
  секрет в окружение, прогон руками, сверка каталога с первой лентой, и только
  потом таймер.

## Не ответили на опрос (06.09.2026)

Спрошены, ответа нет. Простаивающая сессия разбирает очередь только на своём
следующем ходу, а без человека за клавиатурой его может и не быть.

- **G·знак Igm** и **G·перенос строки на проде** — состояние взято с их
  хендоффов, строки 7 и 8 выше. Там всё записано, добирать нечего.
- ~~gmb-v2-ef~~ — ответила 06.09 в 14:11, это строка 9 (форма площадки на
  «ТТ Ракеттке»). ⚠️ Ответ ушёл **текстом в её собственный терминал, а не
  сообщением**, и до спрашивающего не дошёл вовсе.
- ~~gmb-v2-99~~ — ответила позже опроса. Её строка «вход по номеру» **снята
  06.09 вечером**: владелец сам решил проверять на проде, а заявку
  `sms-login-names-the-number` заархивировали — оба SQL отвечали на вопрос
  «пора ли закрывать заявку», и вопроса больше нет. Справка о выкаченном
  состоянии осталась в `.claude/handoff/feature-login-shows-the-number.md`.

⚠️ Опрос покрыл только **интерактивные** сессии. Двадцать пять строк
`Remote Control · offline` недостижимы — их состояние живёт только на диске.
