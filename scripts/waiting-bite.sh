#!/usr/bin/env bash
# Стенд укуса waiting.py: каждое утверждение о строке и хранилище ломается нарочно.
#
#   bash ~/.claude/scripts/waiting-bite.sh
#   WAITING=/путь/к/сломанной/копии bash ~/.claude/scripts/waiting-bite.sh
#
# Зачем в репозитории, а не в скрэтчпаде: сторож, чей стенд умирает вместе с
# сессией, второй раз дефекта не ловит.
#
# 🔴 ПРАВИЛО УКУСА. Отрицательное утверждение НИКОГДА не стоит в укусе одно.
# «Каталога X не появилось», «в выводе нет Y», «поле не дописано» — всё это
# истинно и тогда, когда инструмент не сделал НИЧЕГО и НИГДЕ, а значит зелено на
# заглушке. Каждое отрицательное спаяно в ТОМ ЖЕ укусе с положительным, которое
# даёт только работающий инструмент. То же про файл-фикстуру: «файл не изменился»
# зелено на заглушке, потому что фикстуру писал стенд, а не инструмент.
#
# 🔴 И ТРЕТЬЕ: искомая подстрока не смеет встречаться НИ В ФИКСТУРЕ, НИ В ПУТЯХ
# стенда. Иначе положительную половину обеспечивает стенд, а не инструмент —
# укус при этом положителен, группу сверяет, код спаян с текстом и окно открыто,
# то есть все прежние правила соблюдены, и он всё равно пуст. Шестой вид, найден
# финальным ревью 11.09.2026 и замерен: греп голого `home/` ловил ИМЯ КАТАЛОГА
# стенда ($ROOT/home), а греп `снята` — заголовок собственной фикстуры. Обе
# поломки (id дома не `home`; пометка «снята» не вставляется вовсе) давали
# ✅ 90 ❌ 0. Лечится якорем на печатаемое: `заведена: home/`, `[снята: причина`.
#
# Правило куплено дважды. Приём 1b-1 нашёл пять укусов, проходивших впустую, —
# по слабой формулировке «сверяй rc либо содержимое либо файл». Она недостаточна:
# первый же прогон этого стенда дал ✅ 5 на отсутствующем waiting.py, и все пять
# ей формально удовлетворяли. Сверка одна и та же: заглушка обязана дать ✅ 0.
#
# 🔴 ЗАМЕРЫ ПРИЁМКИ ПОСТАВКИ 2-1 (11.09.2026, стенд был 104 укуса).
# Стенд, который не умеет краснеть, охраняет ничего — поэтому его краснота
# замерена на нарочно сломанных КОПИЯХ:
#   cp scripts/waiting.py /tmp/w.py && правка копии && WAITING=/tmp/w.py bash ...
#
#   живой waiting.py .......................... ✅ 104 ❌ 0
#   заглушка (print + exit 0) ................. ✅ 0   ❌ 104
#   claim_name дописывает pid к имени ......... ✅ 102 ❌ 2
#   repo_box берёт --show-toplevel мимо
#     worktree_main ........................... ✅ 102 ❌ 2
#   boxes() не добавляет HOME/BOX строкой ..... ✅ 58  ❌ 46
#   classify при отсутствии кэша — «молчит» ... ✅ 92  ❌ 12
#   id беспроектной строки не `home` (Р4) ..... ✅ 101 ❌ 3
#   пометка «снята» не вставляется в вывод .... ✅ 102 ❌ 2
#
# Два средних числа ШИРЕ, чем поломка размечена, и это не слабость укусов:
#   · домашний ящик несущий — фикстуры глав «кэш», «done», «stamp» и всего
#     круга починки лежат в $WAITING_HOME/waiting ($HB), и без него скан не
#     видит их вовсе;
#   · «нет кэша → молчит» уводит В ОДНУ ГРУППУ все строки без кэша.
# Две последние поломки — те самые, на которых стенд был зелен ЦЕЛИКОМ до
# 11.09: см. третий абзац правила укуса.
# Расхождение с этими числами после правки укусов разбирать, а не переписывать
# молча: либо укус ослаб, либо поломка задевает не то, что размечено.
#
# 🔴 ЗАМЕРЫ КРУГА ПОЧИНКИ (11.09.2026, 209 укусов). ПЕРЕЗАМЕРЕНЫ ЦЕЛИКОМ.
# Стенд вырос со 196 до 209, и ни одно число не пересчитано: пересчитанное было
# бы ложью с видом замера. Каждая строка ниже — отдельный прогон на нарочно
# сломанной КОПИИ: cp scripts/waiting.py /tmp/w.py && правка && WAITING=/tmp/w.py
#
#   живой waiting.py .......................... ✅ 209 ❌ 0
#   заглушка (print + exit 0) ................. ✅ 0   ❌ 209
#
# Поломки первой приёмки 2-2:
#   probe_one: ненулевой код → silent ......... ✅ 206 ❌ 3
#   write_cache кладёт probe_rc в last_rc ..... ✅ 207 ❌ 2
#   unacked возвращает True всегда ............ ✅ 205 ❌ 4
#   wake печатает заголовок при пустой дельте . ✅ 206 ❌ 3
#   install_hook через json.load → json.dump .. ✅ 207 ❌ 2
#   probeable не отсеивает taken .............. ✅ 208 ❌ 1
#
# Поломки КРУГА КРИТИКИ (каждая — откат одной его починки):
#   cmd_wake ловит только Exception ........... ✅ 208 ❌ 1
#   stderr не уводится в никуда ............... ✅ 208 ❌ 1
#   строгий разбор UTF-8 в выводе пробы ....... ✅ 208 ❌ 1
#   reasons: состояние раньше формы ........... ✅ 207 ❌ 2
#   probeable без сторожа формы ............... ✅ 208 ❌ 1
#   hook_state грепает текст вместо разбора ... ✅ 208 ❌ 1
#   write_json с общим именем .tmp ............ ✅ 208 ❌ 1
#   registry_broken без watching .............. ✅ 208 ❌ 1
#
# Поломки КРУГА ПОЧИНКИ (откат каждой правки 11.09):
#   ack ключуется нынешним составом ........... ✅ 204 ❌ 5
#   квитанция дельты после ack не расходуется . ✅ 208 ❌ 1
#   квитанция не снимается неподтверждённой ... ✅ 206 ❌ 3
#   finally снимает замок, не сверяя владельца  ✅ 208 ❌ 1
#   снятие протухшего не сносит содержимое ..... ✅ 184 ❌ 25
#   set_fields с общим именем .tmp ............ ✅ 208 ❌ 1
#
# 🔴 ГЛАВНОЕ, ЧТО СКАЗАЛ ЭТОТ ПЕРЕЗАМЕР. Все ЧЕТЫРНАДЦАТЬ прежних красных чисел
# (196-й стенд) сошлись с новыми ДО ЕДИНИЦЫ — ни один укус не ослаб и ничего не
# расширилось. Но сошлись они не сразу: первый перезамер дал ДЕВЯТЬ чисел выше
# прежних, и разбор расхождения (а не переписывание его молча — см. правило ниже)
# вывел на дефект ПРОДУКТА, а не стенда. Во всех девяти лишний красный был один и
# тот же укус, и краснел он ЧЕРЕЗ РАЗ: квитанция дельты, не подтверждённая
# следующей дельтой, оставалась лежать и доставалась следующему `ack`. Мерцающий
# укус выглядел шумом стенда, а был незакрытой половиной правила.
# 📏 Правило «расхождение разбирать, а не переписывать» окупилось этим вторично.
#
# Два числа ШИРЕ, чем поломка размечена, и оба честные:
#   · «снятие протухшего не сносит содержимое» — ✅184 ❌25. С маркером владельца
#     каталог замка больше не пуст, и голый rmdir не снимает даже СВОЙ замок:
#     после первой пробы стенд остаётся с вечным замком, и все следующие пробы
#     выходят кодом 3. Снос содержимого — несущая часть починки, а не уборка;
#   · «ack ключуется нынешним составом» — ✅204 ❌5: откат снимает ОБЕ половины
#     квитанции дельты разом (запись в wake и чтение в ack), поэтому он шире
#     каждой из двух отдельных поломок ниже.
#
# Замеры приёмки 2-2 на 196-м стенде и три купленных ими замечания перенесены в
# реестр исполнения (`.superpowers/sdd/2026-09-11-waiting-registry-2-2/`), чтобы
# в шапке стояли числа ОДНОГО стенда. Три замечания, которые стоит помнить и
# здесь: «unacked всегда True» шире плановой разметки (гашение по форме ломается
# вместе с гашением по исходу); укус на БАЙТОВУЮ неизменность settings.json нужен
# отдельно от укуса про якорь, и его первая редакция была слепа (фикстура была
# каноническим JSON, который json.dumps воспроизводит байт в байт — фикстуре дан
# массив В СТРОКУ); пер-строчный try в cmd_probe СОБСТВЕННОГО укуса не имеет, и
# это сказано вслух, а не спрятано — он стоит ради НЕИЗВЕСТНОГО класса, а укус на
# неизвестное написать нельзя (замерено: снятие только его стенд не красит).
#
# ⚠️ Стенд оставляет за собой ОТЦЕПЛЕННЫЕ фоновые процессы: `wake` при устаревшем
# кэше честно отцепляет `probe`, а каталог $ROOT убирает trap — фон, доживший до
# этого момента, пишет в уже удалённый путь. Это не гонка и не течь: писателя кэша
# два не бывает, а живой ~/.claude подменён WAITING_HOME. В главе дельты фон
# заглушён удержанным замком, иначе он переписывает кэш под ногами следующего
# укуса; замок снимается ровно перед укусом 17, который отцепление и проверяет.
#
# Оснастка: `wt` гоняет waiting.py и кладёт вывод в $out, код в $rc; `has`/`nohas`
# смотрят $out; `is` печатает вердикт. Идиома вызова:
#   wt "$D" new "заголовок"
#   { [ "$rc" = 0 ] && has "заведена:"; }; is "ярлык укуса" $?
set -u
W="${WAITING:-$HOME/.claude/scripts/waiting.py}"
ROOT="$(mktemp -d)"; trap 'rm -rf "$ROOT"' EXIT
export WAITING_HOME="$ROOT/home"     # ящик, кэш и корни — НЕ в живой ~/.claude
mkdir -p "$WAITING_HOME" "$ROOT/repos"
# ~/.claude в жизни — репозиторий git, и стенд обязан воспроизводить это, иначе
# особый случай «ящик дома» проверяется не на том, на чём он живёт.
git -C "$WAITING_HOME" init -q -b main .
git -C "$WAITING_HOME" config user.email t@t
git -C "$WAITING_HOME" config user.name t
echo x > "$WAITING_HOME/a.txt"
git -C "$WAITING_HOME" add -A; git -C "$WAITING_HOME" commit -qm init
printf '%s\n' "$ROOT/repos" > "$WAITING_HOME/waiting-roots.txt"
pass=0; fail=0; out=""; rc=0
TODAY="$(date +%Y-%m-%d)"
PLUS30="$(date -v+30d +%Y-%m-%d)"    # BSD date: стенд macOS-только, как и 1b-1
PLUS7="$(date -v+7d +%Y-%m-%d)"

mk() {  # mk <имя> → печатает путь к свежему репозиторию под корнем скана
  d="$ROOT/repos/$1"; mkdir -p "$d"
  git -C "$d" init -q -b main .
  git -C "$d" config user.email t@t; git -C "$d" config user.name t
  echo x > "$d/a.txt"
  git -C "$d" add -A; git -C "$d" commit -qm init
  printf '%s' "$d"
}
# Р18: транспорт подменяется — стенд обязан работать без сети и без чужих хостов.
# Хост приезжает аргументом, поэтому распознаём его перебором: `refuse` роняет
# транспорт кодом 255, `slow` висит дольше потолка пробы, остальные исполняют
# скрипт местно — то есть дают настоящие fired и silent.
export WAITING_SSH="$ROOT/fake-ssh"
export WAITING_PROBE_TIMEOUT_S=1
cat > "$WAITING_SSH" <<'FAKESSH'
#!/usr/bin/env bash
for a in "$@"; do
  case "$a" in
    refuse) exit 255 ;;
    slow)   sleep 3 ;;
  esac
done
exec bash -o pipefail -s
FAKESSH
chmod +x "$WAITING_SSH"
wt() {  # wt <каталог> <аргументы waiting.py…> → $out, $rc
  d="$1"; shift
  out="$(cd "$d" && python3 "$W" "$@" 2>&1)"; rc=$?
}
has()   { printf '%s' "$out" | grep -q -- "$1"; }
nohas() { ! printf '%s' "$out" | grep -q -- "$1"; }
inf()   { grep -q -- "$2" "$1" 2>/dev/null; }   # inf <файл> <подстрока>
is() {  # is <ярлык> <0 если утверждение верно>
  if [ "${2:-1}" = 0 ]; then echo "  ✅ $1"; pass=$((pass+1))
  else
    echo "  ❌ $1 — rc=$rc"
    printf '%s\n' "$out" | sed 's/^/       /'
    fail=$((fail+1))
  fi
}
one() { ls "$1" 2>/dev/null | head -1; }        # one <каталог> → первое имя

echo "=== заведение строки ==="

D="$(mk rk_bot)"
wt "$D" new "ОФД в rk_bot — ждёт первого боевого платежа"
{ [ "$rc" = 0 ] && has "заведена:"; }; is "new говорит, что строка заведена" $?
{ has "rk_bot-"; }; is "new печатает ПОЛНЫЙ id — имя репозитория в нём есть" $?
{ [ -d "$D/.claude/waiting" ]; }; is "new завёл ящик <repo>/.claude/waiting/" $?
f="$(one "$D/.claude/waiting")"
{ printf '%s' "$f" | grep -Eq '^[0-9]{8}-[0-9]{2}\.md$'; }
is "имя файла — ровно YYYYMMDD-NN.md, без pid и без суффиксов" $?
{ inf "$D/.claude/waiting/$f" 'title: "ОФД в rk_bot'; }
is "заголовок лёг в title и лёг В КАВЫЧКАХ" $?
{ inf "$D/.claude/waiting/$f" "review_by: $PLUS30"; }
is "new штампует review_by на +30 дней — дату не придумывает человек" $?
{ inf "$D/.claude/waiting/$f" "stamped_at: $TODAY"; }
is "new пишет stamped_at — без него расхождение штампа с probe: none не поймать" $?
# Путь, а не голое имя (решение Р13). Регулярка, а не литерал: стенд живёт в
# mktemp-каталоге, и python отдаёт его через /private/var, а bash — через /var.
{ inf "$D/.claude/waiting/$f" 'entry: "/.*/rk_bot · main"'; }
is "entry называет ПУТЬ к репозиторию и ветку, а не файл хендоффа" $?
{ inf "$D/.claude/waiting/$f" "state: waiting"; }; is "новая строка в состоянии waiting" $?
{ [ -f "$D/.claude/waiting/$f" ] && ! inf "$D/.claude/waiting/$f" "^probe:"; }
is "new НЕ придумывает probe: заполнить всё сразу нельзя по построению" $?

# Укус 1 спеки. Восемь разом, а не два: у наивной реализации `exists() → write`
# окно гонки узкое, и на двух процессах она проходит зелёной по везению.
D="$(mk race)"
i=0; while [ $i -lt 8 ]; do
  ( cd "$D" && python3 "$W" new "гонка $i" >/dev/null 2>&1 ) &
  i=$((i+1))
done
wait
n="$(ls "$D/.claude/waiting" 2>/dev/null | wc -l | tr -d ' ')"
out="в ящике: $(ls "$D/.claude/waiting" 2>/dev/null | tr '\n' ' ')"; rc=0
{ [ "$n" = 8 ]; }; is "восемь new в одну секунду → восемь разных NN" $?
# Сверка с литеральной восьмёркой, а не с $n: ноль равен нулю, и на пустом
# ящике укус проходил бы, ничего не проверив.
{ [ "$(ls "$D/.claude/waiting" 2>/dev/null | grep -Ec '^[0-9]{8}-[0-9]{2}\.md$')" = 8 ]; }
is "все восемь имён формы YYYYMMDD-NN — pid дописывать нельзя (инвариант 9)" $?

echo "=== ворктри и дом ==="

# Укус 2 спеки: строка живёт в ОСНОВНОМ чекауте, никогда в ворктри.
D="$(mk wt_main)"
git -C "$D" worktree add -q "$ROOT/wt_side" -b side
wt "$ROOT/wt_side" new "заведено из ворктри"
{ [ "$rc" = 0 ] && [ -n "$(one "$D/.claude/waiting")" ]; }
is "new из ворктри положил строку в основной чекаут" $?
{ [ -n "$(one "$D/.claude/waiting")" ] && [ ! -d "$ROOT/wt_side/.claude/waiting" ]; }
is "в самом ворктри ящика не появилось" $?

# Укус 3 спеки: ~/.claude — не корень скана, а именованное второе хранилище.
wt "$WAITING_HOME" new "беспроектная, заведена изнутри дома"
{ [ "$rc" = 0 ] && [ -n "$(one "$WAITING_HOME/waiting")" ]; }
is "new изнутри ~/.claude положил строку в ~/.claude/waiting/" $?
{ [ -n "$(one "$WAITING_HOME/waiting")" ] && [ ! -d "$WAITING_HOME/.claude" ]; }
is "и НЕ завёл ~/.claude/.claude/waiting/ — там строку не найдёт никто" $?
{ has "заведена: home/"; }; is "id беспроектной строки начинается с home/ (Р4)" $?

D="$(mk with_repo)"
wt "$D" new "беспроектная из репозитория" --global
{ [ "$rc" = 0 ] && [ "$(ls "$WAITING_HOME/waiting" | wc -l | tr -d ' ')" = 2 ]; }
is "--global из репозитория кладёт строку в дом, а не в репозиторий" $?
{ [ "$(ls "$WAITING_HOME/waiting" 2>/dev/null | wc -l | tr -d ' ')" = 2 ] \
    && [ ! -d "$D/.claude/waiting" ]; }
is "--global не завёл ящик в репозитории" $?
g="$(ls "$WAITING_HOME/waiting" | tail -1)"
{ inf "$WAITING_HOME/waiting/$g" "entry: none"; }
is "у беспроектной строки entry: none — владеющей линии нет" $?

mkdir -p "$ROOT/plain"
wt "$ROOT/plain" new "заведена вне git"
{ [ "$rc" = 0 ] && has "заведена: home/"; }
is "new вне репозитория кладёт строку в дом, а не падает" $?

line() {  # line <каталог-ящика> <имя без .md> — содержимое файла со stdin
  mkdir -p "$1"; cat > "$1/$2.md"
}
grp() {  # grp <имя строки> → печатает ГРУППУ, в которой она напечатана
  # Ключевая оснастка главы. «id нет в выводе» для ПРАВИЛЬНОЙ строки невозможно:
  # list обязан печатать все группы целиком, и корректная строка неизбежно стоит
  # в «ни разу не опрошена». Проверять надо не присутствие, а ГРУППУ.
  printf '%s\n' "$out" | awk -v n="$1" '/^— /{g=$0} $0 ~ n {print g; exit}'
}
full() {  # full <каталог-ящика> <имя> — заведомо ПРАВИЛЬНАЯ строка-образец
  line "$1" "$2" <<EOF
---
title: "образец: все поля добыты боем"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: mprz
cwd: ~/dev/rk_bot
probe: |
  journalctl -u bot-rk --since "2026-09-06 17:48"
ripe_match: "приёмник (?!—)"
ripe_when: "в строке подачи стоит АДРЕС приёмника, а не прочерк"
sample: "06.09 19:40 у платежа 1442 стоит прочерк — греп находит строки"
entry: "rk_bot · main"
---

Тело.
EOF
}

echo "=== скан: где ищем и где не ищем ==="

S="$(mk scan_a)"; SB="$S/.claude/waiting"
full "$SB" 20260101-01
wt "$S" list
# Кода 0 тут не ждём: в ящике дома уже лежат шаблоны из главы «заведение», и они
# честно недооформлены — list вернёт 1. Укус сверяет СОСТАВ вывода, а не код.
{ has "20260101-01"; }; is "оформленная строка проекта находится сканом" $?
{ has "ни разу не опрошена"; }
is "и лежит в «ни разу не опрошена», а НЕ в «молчит» (инвариант 3)" $?

# Укус 4 спеки: ~/.claude/waiting/ добавляется к результату глоба отдельной
# строкой — сам глоб <root>/*/.claude/waiting его не находит вовсе.
full "$WAITING_HOME/waiting" 20260101-02
wt "$S" list
{ has "home/20260101-02"; }
is "строка в ~/.claude/waiting/ находится сканом наравне с проектной" $?

# Ворктри под самым корнем скана: глоб его увидит, скан обязан отсеять.
git -C "$S" worktree add -q "$ROOT/repos/scan_a_wt" -b sidewt
full "$ROOT/repos/scan_a_wt/.claude/waiting" 20260101-09
wt "$S" list
{ has "20260101-01" && nohas "20260101-09"; }
is "ящик, заведённый в ворктри, сканом не подхватывается (инвариант 6)" $?

O="$ROOT/outside/far"; mkdir -p "$O"; full "$O/.claude/waiting" 20260101-08
wt "$S" list
{ has "20260101-01" && nohas "20260101-08"; }
is "репозиторий вне корней сканом не виден — корни это конфиг" $?

mkdir -p "$ROOT/home2"; printf '# только комментарий\n' > "$ROOT/home2/waiting-roots.txt"
out="$(cd "$ROOT" && WAITING_HOME="$ROOT/home2" python3 "$W" list 2>&1)"; rc=$?
{ [ "$rc" = 2 ] && has "не называет ни одного корня"; }
is "файл корней без единого корня — ошибка конфигурации, код 2, а не «корней нет»" $?

mkdir -p "$ROOT/home3"
out="$(cd "$ROOT" && WAITING_HOME="$ROOT/home3" python3 "$W" list 2>&1)"; rc=$?
{ [ "$rc" != 2 ] && { has "реестр пуст" || has "— "; }; }
is "отсутствие файла корней — не ошибка: работает умолчание ~/dev" $?

echo "=== форма отказывает при list ==="

F="$(mk form)"; FB="$F/.claude/waiting"

# Укус 5 спеки — три утверждения в одном месте, и они разные.
full "$FB" 20260202-01
line "$FB" 20260202-02 <<EOF
---
title: "образца нет вовсе"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: mprz
cwd: ~/dev/rk_bot
probe: |
  journalctl -u bot-rk
ripe_match: "приёмник"
ripe_when: "адрес вместо прочерка"
entry: "rk_bot · main"
---
Тело.
EOF
wt "$F" list
{ [ "$rc" = 1 ]; }; is "строка без sample → list возвращает 1" $?
{ grp 20260202-02 | grep -q "недооформленные"; }
is "…и печатает её в группе «недооформленные»" $?
{ has "sample"; }; is "…называя, какого поля не хватает" $?
{ has "20260202-01"; }
is "…и ОСТАЛЬНЫЕ группы напечатаны полностью, а не заглушены отказом" $?

# Укус 7 спеки: естественная реализация проглотила бы это try/except.
line "$FB" 20260202-03 <<'EOF'
---
title: "тут двоеточие: и кавычек нет
state: waiting
---
Тело.
EOF
wt "$F" list
{ grp 20260202-03 | grep -q "недооформленные"; }
is "непарсящийся франтматтер напечатан, а не пропущен молча" $?

line "$FB" 20260202-04 <<EOF
---
title: "ключ повторяется"
state: waiting
state: done
review_by: $PLUS30
---
Тело.
EOF
wt "$F" list
{ grp 20260202-04 | grep -q "недооформленные"; }
is "повтор ключа во франтматтере — тоже отказ, а не последний выигрывает" $?

echo "=== сторож пробы: конъюнкции нет, но данные не код ==="

C="$(mk conj)"; CB="$C/.claude/waiting"
mkprobe() {  # mkprobe <имя> <<'EOF' — тело блочного скаляра probe
  name="$1"; body="$(cat)"
  { printf -- '---\ntitle: "проба %s"\nstate: waiting\nreview_by: %s\nstamped_at: %s\n' \
      "$name" "$PLUS30" "$TODAY"
    printf 'host: mprz\ncwd: ~/dev/x\nprobe: |\n'
    printf '%s\n' "$body" | sed 's/^/  /'
    printf 'ripe_match: "ok"\nripe_when: "наступило"\nsample: "видел оба исхода"\n'
    printf -- 'entry: "conj · main"\n---\nТело.\n'
  } > "$CB/$name.md"
}
mkdir -p "$CB"
mkprobe 20260303-01 <<'EOF'
cd /srv && journalctl -u bot
EOF
mkprobe 20260303-02 <<'EOF'
psql -f a.sql; psql -f b.sql
EOF
mkprobe 20260303-03 <<'EOF'
./scripts/prod_sql.sh <<'SQL'
select id, state
  from orders
 where state = 'new';
SQL
EOF
mkprobe 20260303-04 <<'EOF'
.venv/bin/python -c 'import x; print(x.n)'
EOF
mkprobe 20260303-05 <<'EOF'
journalctl -u bot
grep -c ошибка /var/log/app.log
EOF
mkprobe 20260303-06 <<'EOF'
journalctl -u bot 2>&1 | tail -50
EOF
wt "$C" list
{ grp 20260303-01 | grep -q "недооформленные"; }
is "конъюнкция через && поймана" $?
{ grp 20260303-02 | grep -q "недооформленные"; }
is "конъюнкция через ; поймана" $?
{ grp 20260303-03 | grep -q "ни разу не опрошена"; }
is "многострочный SQL в heredoc с ; внутри — НЕ конъюнкция: тело heredoc это данные" $?
{ grp 20260303-04 | grep -q "ни разу не опрошена"; }
is "; внутри кавычек — не конъюнкция, а часть аргумента" $?
{ grp 20260303-05 | grep -q "недооформленные"; }
is "две команды в столбик — конъюнкция: перевод строки разделяет так же, как ;" $?
{ grp 20260303-06 | grep -q "ни разу не опрошена"; }
is "конвейер и 2>&1 конъюнкцией не считаются" $?

echo "=== кавычки, none и pending ==="

Q="$(mk quotes)"; QB="$Q/.claude/waiting"
line "$QB" 20260404-01 <<EOF
---
title: заголовок без кавычек
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: mprz
cwd: ~/dev/x
probe: |
  journalctl -u bot
ripe_match: "ok"
ripe_when: "наступило"
sample: "видел оба исхода"
entry: "quotes · main"
---
Тело.
EOF
line "$QB" 20260404-02 <<EOF
---
title: "sample: pending — законный третий вид"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: mprz
cwd: ~/dev/x
probe: |
  grep -c ключ /etc/app.conf
ripe_match: "^[1-9]"
ripe_when: "ключ в конфиге появился"
sample: pending
entry: "quotes · main"
---
Файла с ключом никогда не существовало — образца нет и быть не может.
EOF
line "$QB" 20260404-03 <<EOF
---
title: "машинной пробы нет, срок +7"
state: waiting
review_by: $PLUS7
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "поддержка ответила по тикету 4417"
sample: none
entry: none
---
Ответ человека машинно не проверяется.
EOF
line "$QB" 20260404-04 <<EOF
---
title: "probe: none при штампе +30"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "решение владельца по derflow"
sample: none
entry: none
---
Штамп и проба разошлись.
EOF
line "$QB" 20260404-05 <<EOF
---
title: "проба есть, а штамп короткий — это НЕ расхождение"
state: waiting
review_by: $PLUS7
stamped_at: $TODAY
host: mprz
cwd: ~/dev/x
probe: |
  journalctl -u bot
ripe_match: "#\\d+ готов"
ripe_when: "наряд закрыт"
sample: "видел оба исхода"
entry: "quotes · main"
---
Короткий срок безвреден: он даёт лишний взгляд, а не пропуск.
EOF
line "$QB" 20260404-06 <<EOF
---
title: "состояния третьего не бывает"
state: отложено
review_by: $PLUS7
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "что-нибудь"
sample: none
entry: none
---
Тело.
EOF
line "$QB" 20260404-07 <<EOF
---
title: "комментарий у голого скаляра — форма из самой спеки"
state: waiting            # waiting | taken | done
review_by: $PLUS30        # машиной: +30, потому что проба есть
stamped_at: $TODAY
host: mprz                # local | mprz | …
cwd: ~/dev/x
probe: |
  journalctl -u bot
ripe_match: "ok"
ripe_when: "наступило"
sample: "# 06.09 у платежа 1442 прочерк — греп показал оба исхода"
entry: "quotes · main"
---
Тело.
EOF
wt "$Q" list
{ grp 20260404-01 | grep -q "недооформленные"; }
is "title без кавычек — недооформленная строка" $?
{ grp 20260404-02 | grep -q "ни разу не опрошена"; }
is "sample: pending — законный третий вид, не порок" $?
{ has "образца нет, отрицательный ответ ничего не доказывает"; }
is "…и list печатает про неё именно это, а не молчит" $?
{ grp 20260404-03 | grep -q "ни разу не опрошена"; }
is "probe: none при штампе +7 — законная строка" $?
{ has "машинной пробы нет, созреет только сроком"; }
is "…и list называет это явно, иначе она тихо не сработает никогда" $?
{ grp 20260404-04 | grep -q "недооформленные"; }
is "probe: none при штампе +30 — расхождение, которое обязано быть видным" $?
{ has "stamp"; }; is "…и list называет команду, которой это чинится" $?
{ grp 20260404-05 | grep -q "ни разу не опрошена"; }
is "проба при штампе +7 расхождением НЕ считается — короткий срок безвреден" $?
{ grp 20260404-06 | grep -q "недооформленные"; }
is "state: четвёртого значения не бывает — недооформленная строка" $?
{ grp 20260404-07 | grep -q "ни разу не опрошена"; }
is "комментарий у ГОЛОГО скаляра снимается — иначе review_by перестаёт быть датой" $?
{ has "20260404-01" && nohas "поле sample пустое"; }
is "…а в кавычках решётка часть значения: sample с неё начинается и уцелел" $?

cache() {  # cache <repo-id> <имя строки> <json одной строкой>
  mkdir -p "$WAITING_HOME/waiting-cache/$1"
  printf '%s\n' "$3" > "$WAITING_HOME/waiting-cache/$1/$2.json"
}
NOW="$(date +%s)"
OLD="$((NOW - 4 * 3600))"          # старше 3×TTL при TTL=3600
HB="$WAITING_HOME/waiting"
HC="$WAITING_HOME/waiting-cache"

echo "=== исход берётся из кэша, созрелость вычисляется ==="

full "$HB" 20260505-01; cache home 20260505-01 "{\"outcome\":\"fired\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
full "$HB" 20260505-02; cache home 20260505-02 "{\"outcome\":\"silent\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
full "$HB" 20260505-03; cache home 20260505-03 "{\"outcome\":\"unreachable\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
full "$HB" 20260505-04; cache home 20260505-04 "{\"outcome\":\"silent\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$OLD,\"last_rc\":0}"
full "$HB" 20260505-05; cache home 20260505-05 "{\"outcome\":\"silent\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":4}"
full "$HB" 20260505-06; cache home 20260505-06 "не json вовсе"
full "$HB" 20260505-07
wt "$WAITING_HOME" list
{ grp 20260505-01 | grep -q "созрело"; }
is "кэш fired → «созрело», хотя срок пересмотра не прошёл" $?
{ grp 20260505-02 | grep -q "молчит"; }
is "кэш silent → «молчит»: проба спросила и ответ был пуст" $?
{ grp 20260505-03 | grep -q "недостижима"; }
is "кэш unreachable → «недостижима», а не «молчит» (инвариант 3)" $?
{ grp 20260505-04 | grep -q "данные протухли"; }
is "кэш старше 3×TTL → «данные протухли», а не вчерашняя правда под видом сегодняшней" $?
{ grp 20260505-05 | grep -q "данные протухли"; }
is "последний прогон упал (last_rc≠0) → «данные протухли»" $?
{ grp 20260505-06 | grep -q "ни разу не опрошена"; }
is "битый кэш → строка не пропадает и list не падает" $?
{ grp 20260505-07 | grep -q "ни разу не опрошена"; }
is "кэша нет вовсе → «ни разу не опрошена», а не «молчит»" $?

# Прошедший срок пересмотра созревает строку сам, без всякой пробы: он и есть
# страховка на случай, когда проба врёт в сторону тишины.
line "$HB" 20260505-08 <<EOF
---
title: "срок пересмотра прошёл вчера"
state: waiting
review_by: $(date -v-1d +%Y-%m-%d)
stamped_at: $(date -v-31d +%Y-%m-%d)
host: mprz
cwd: ~/dev/x
probe: |
  journalctl -u bot
ripe_match: "ok"
ripe_when: "наступило"
sample: "видел оба исхода"
entry: "none"
---
Тело.
EOF
cache home 20260505-08 "{\"outcome\":\"silent\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" list
{ grp 20260505-08 | grep -q "созрело"; }
is "прошедший review_by созревает строку, даже когда проба молчит" $?

echo "=== чужой кэш, снятые строки, пропажа из скана ==="

full "$HB" 20260505-09
cache notmine-deadbeef 20260505-09 "{\"outcome\":\"fired\",\"since\":\"x\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" list
{ grp 20260505-09 | grep -q "ни разу не опрошена"; }
is "кэш под ЧУЖИМ repo-id к строке не приклеивается — id глобален" $?
{ has "notmine-deadbeef/20260505-09 пропала из скана"; }
is "…а сам он объявлен пропавшим: скан породил этот исход, скан о нём и говорит" $?
{ [ ! -f "$WAITING_HOME/waiting-cache/notmine-deadbeef/20260505-09.json" ]; }
is "…и забыт: печатается ОДИН раз, ведённого списка нет" $?
wt "$WAITING_HOME" list
{ has "20260505-01" && nohas "пропала из скана"; }
is "второй прогон о той же пропаже молчит" $?

# 🔴 Квитанция дельты — НЕ второй id. Глоб `*.json` + `p.stem` даёт у
# `<id>.seen.json` стем `<id>.seen`, которого в live нет никогда: живая строка
# объявляется пропавшей, а квитанция гасится вместе с фантомом. Замер 11.09:
# двенадцать таких строк на прогоне, и настоящая пропажа тонет среди них.
full "$HB" 20260505-31
cache home 20260505-31 "{\"outcome\":\"silent\",\"since\":\"x\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
cache home "20260505-31.seen" "{\"key\":\"k\",\"kinds\":[\"fired\"],\"at\":\"2026-09-11T00:00:00\"}"
wt "$WAITING_HOME" list
{ has "20260505-31  образец" && nohas "20260505-31.seen пропала из скана"; }
is "квитанция ЖИВОЙ строки пропажей не объявляется — <id>.seen.json не второй id" $?
{ has "20260505-31  образец" && [ -f "$HC/home/20260505-31.seen.json" ]; }
is "…и переживает скан: погасив её, реестр теряет то, что дельта НАПЕЧАТАЛА" $?

# …но у настоящей пропажи квитанция обязана уйти вместе со строкой, и сказано
# о пропаже должно быть ОДИН раз, а не дважды — по файлу на каждый её след.
cache notmine-deadbeef 20260505-32 "{\"outcome\":\"fired\",\"since\":\"x\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
cache notmine-deadbeef "20260505-32.seen" "{\"key\":\"k\",\"kinds\":[\"fired\"],\"at\":\"2026-09-11T00:00:00\"}"
wt "$WAITING_HOME" list
{ [ "$(printf '%s' "$out" | grep -c "notmine-deadbeef/20260505-32 пропала из скана")" = 1 ] \
  && nohas "20260505-32.seen пропала"; }
is "пропажа объявлена ОДИН раз, а не дважды из-за собственной квитанции" $?
{ has "notmine-deadbeef/20260505-32 пропала из скана" \
  && [ ! -f "$HC/notmine-deadbeef/20260505-32.json" ] \
  && [ ! -f "$HC/notmine-deadbeef/20260505-32.seen.json" ]; }
is "…и забыта целиком: удалены оба следа, и кэш пробы, и квитанция" $?

line "$HB" 20260505-10 <<EOF
---
title: "снята, сработала"
state: done
closed_at: $TODAY
closed_because: "платёж прошёл, адрес приёмника встал на место"
review_by: $PLUS30
stamped_at: $TODAY
host: mprz
cwd: ~/dev/x
probe: |
  journalctl -u bot
ripe_match: "ok"
ripe_when: "наступило"
sample: "видел оба исхода"
entry: "none"
---
Тело.
EOF
cache home 20260505-10 "{\"outcome\":\"fired\",\"since\":\"x\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" list
{ has "20260505-01" && nohas "20260505-10"; }
is "снятая строка в обычный list не печатается" $?
{ has "20260505-01" && nohas "20260505-10 пропала"; }
is "…и пропавшей НЕ считается: скан её видел, просто не печатал" $?
wt "$WAITING_HOME" list --all
{ has "20260505-10"; }; is "list --all показывает и снятые" $?
{ has "\[снята: платёж прошёл"; }; is "…с пометкой, что строка снята, а не молча в общем списке" $?

echo "=== снятие, переклейка срока и разрешение id ==="

# Укус 6 спеки. Одноимённые строки в двух хранилищах — норма, а не редкость:
# NN атомарен только ВНУТРИ каталога, а каталогов столько, сколько репозиториев.
R="$(mk twin)"; RB="$R/.claude/waiting"
full "$RB" 20260606-01
full "$HB" 20260606-01
wt "$R" done 20260606-01 "потому что"
{ [ "$rc" = 2 ]; }; is "голый id, разрешающийся в две строки → отказ кодом 2" $?
{ has "home/20260606-01"; }; is "…со списком кандидатов, а не с догадкой" $?
{ has "20260606-01" && printf '%s' "$out" | grep -q "twin-"; }
is "…и в списке названы ОБА полных id" $?
# ОБЕ половины, и здесь это не педантизм: следующая же команда законно снимает
# ту, что в $HB, и окно для обнаружения закрывается необратимо.
{ [ "$rc" = 2 ] && inf "$RB/20260606-01.md" "state: waiting" \
    && inf "$HB/20260606-01.md" "state: waiting"; }
is "…и ни одна из двух не снята: угадать хуже, чем не двигаться" $?

wt "$R" done home/20260606-01 "линия закрыта, ждать больше нечего"
{ [ "$rc" = 0 ] && has "снята:"; }; is "полный id снимает строку" $?
{ inf "$HB/20260606-01.md" "state: done"; }; is "…ровно ту, что названа" $?
{ inf "$HB/20260606-01.md" "state: done" && inf "$RB/20260606-01.md" "state: waiting"; }
is "…и не трогает одноимённую соседку" $?
{ inf "$HB/20260606-01.md" "closed_at: $TODAY"; }; is "done ставит машинный closed_at" $?
{ inf "$HB/20260606-01.md" 'closed_because: "линия закрыта'; }
is "done требует причину смерти и записывает её В КАВЫЧКАХ" $?

wt "$R" done home/20260606-01 ""
{ [ "$rc" = 1 ]; }
is "пустая причина — отказ: надгробие без причины не отвечает на вопрос, ради которого его хранят" $?

wt "$R" done home/20261111-99 "нет такой"
{ [ "$rc" = 2 ]; }; is "снятие несуществующей строки — код 2, а не тихий успех" $?

# Правка машиной не смеет затирать человеческое: у файла один писатель, и это он.
line "$HB" 20260707-01 <<EOF
---
title: "у этой строки есть комментарий и тело"
# этот комментарий человек написал руками
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "владелец решит судьбу derflow"
sample: none
entry: none
---

Замер 08.09: сработали все 14 полос. НЕ ТЕРЯТЬ ЭТУ СТРОКУ ТЕЛА.
EOF
wt "$WAITING_HOME" done home/20260707-01 "решено"
{ inf "$HB/20260707-01.md" "state: done" \
    && inf "$HB/20260707-01.md" "этот комментарий человек написал руками"; }
is "done не съел комментарий человека во франтматтере" $?
{ inf "$HB/20260707-01.md" "closed_at: $TODAY" \
    && inf "$HB/20260707-01.md" "НЕ ТЕРЯТЬ ЭТУ СТРОКУ ТЕЛА"; }
is "…и не съел тело: правка идёт по ключу, а не перезаписью файла" $?
{ inf "$HB/20260707-01.md" "closed_because" \
    && inf "$HB/20260707-01.md" 'ripe_when: "владелец решит судьбу derflow"'; }
is "…и не тронул чужие поля" $?

# Расхождение «probe: none при штампе +30» чинится stamp, а не ack.
line "$HB" 20260707-02 <<EOF
---
title: "проба выяснилась после заведения — её нет"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "поддержка ответит по тикету"
sample: none
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" list
{ grp 20260707-02 | grep -q "недооформленные"; }
is "до stamp строка висит в недооформленных" $?
wt "$WAITING_HOME" stamp home/20260707-02
{ [ "$rc" = 0 ] && inf "$HB/20260707-02.md" "review_by: $PLUS7"; }
is "stamp при probe: none переклеивает срок на +7 — он единственный датчик" $?
{ inf "$HB/20260707-02.md" "review_by: $PLUS7" \
    && ! inf "$HB/20260707-02.md" "acked_outcome"; }
is "stamp НЕ пишет acked_outcome: это не «посмотрел», а «переклеил срок»" $?
wt "$WAITING_HOME" list
{ grp 20260707-02 | grep -q "ни разу не опрошена"; }
is "после stamp строка ушла из недооформленных в честную группу" $?

# Срок и штамп нарочно ЧУЖИЕ: если фикстура уже несёт +30 и сегодняшний штамп,
# stamp'у нечего двигать, и оба укуса зелены даже на заглушке. Короткий срок при
# живой пробе законен (расхождением он не считается) — потому и взят.
line "$HB" 20260707-03 <<EOF
---
title: "срок короткий, а проба живая — stamp обязан выправить"
state: waiting
review_by: $PLUS7
stamped_at: $(date -v-31d +%Y-%m-%d)
host: mprz
cwd: ~/dev/x
probe: |
  journalctl -u bot
ripe_match: "ok"
ripe_when: "наступило"
sample: "видел оба исхода"
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" stamp home/20260707-03
{ [ "$rc" = 0 ] && inf "$HB/20260707-03.md" "review_by: $PLUS30"; }
is "stamp при живой пробе даёт +30 — подстраховка, а не датчик" $?
{ inf "$HB/20260707-03.md" "stamped_at: $TODAY"; }
is "stamp двигает stamped_at, иначе расхождение не пересчитать никогда" $?

line "$HB" 20260707-04 <<'EOF'
---
title: "кавычка не закрыта
state: waiting
---
Тело.
EOF
wt "$WAITING_HOME" stamp home/20260707-04
{ [ "$rc" = 1 ]; }
is "stamp по непарсящейся строке отказывает: срок машина в такой файл не пишет" $?
{ [ "$rc" = 1 ] && inf "$HB/20260707-04.md" 'title: "кавычка не закрыта'; }
is "…и файла не тронул: непонятый франтматтер правке не подлежит" $?

wt "$WAITING_HOME" done home/20260707-04 "снимаю вслепую"
{ [ "$rc" = 1 ] && ! inf "$HB/20260707-04.md" "state: done"; }
is "done по непарсящейся строке отказывает ТОЖЕ — сторож у обеих команд один" $?

echo "=== машина не смеет портить файл человека (круг починки финального ревью) ==="

# Critical: проза переносится строками, и незаэкранированный перевод строки клал
# в ИСПРАВНЫЙ файл незакрытую кавычку. Спаяно тремя половинами: файл разбирается,
# причина цела ЦЕЛИКОМ, и заголовок не подменён на «(без title)».
full "$HB" 20260808-01
wt "$WAITING_HOME" done home/20260808-01 "сработала 11.09
подробности: платёж 1442"
{ [ "$rc" = 0 ] && has "снята: home/20260808-01"; }
is "done с многострочной причиной не падает" $?
wt "$WAITING_HOME" list --all
{ nohas "кавычка у 'closed_because' не закрыта"; } \
  && { has "образец: все поля добыты боем"; }
is "…и файл ПОСЛЕ него разбирается, заголовок на месте — а не «(без title)»" $?
{ has "\[снята: сработала 11.09 подробности: платёж 1442"; }
is "…и причина уцелела ЦЕЛИКОМ, схлопнутая в строку, а не обрублена" $?
wt "$WAITING_HOME" stamp home/20260808-01
{ [ "$rc" = 0 ] && has "пересмотр $PLUS30"; }
is "…и строка чинится своим же инструментом: stamp по ней не отказывает" $?

# Тот же вход со стороны new: код 0 и непарсящийся файл — исход хуже отказа.
wt "$WAITING_HOME" new "$(printf 'многострочный\nзаголовок: беда')"
g="$(printf '%s' "$out" | sed -n 's/^заведена: home\///p')"
wt "$WAITING_HOME" list --all
# Якорь на СВОЮ строку: фикстура 20260707-04 нарочно несёт незакрытую кавычку
# у title, и голый nohas по тексту ошибки краснел бы от соседки.
{ [ -n "$g" ] && has "многострочный заголовок: беда"; }
is "new с переводом строки в заголовке пишет РАЗБИРАЕМЫЙ франтматтер" $?

# Important: хвостовой комментарий человека. Спека приводит его в своём образце
# строки; до починки stamp и done стирали его молча.
line "$HB" 20260808-02 <<EOF
---
title: "строка с комментариями человека"
state: waiting            # waiting | taken | done
review_by: 2026-01-01     # машиной: +30, потому что проба есть (без неё +7)
stamped_at: $TODAY
host: mprz
cwd: ~/dev/rk_bot
probe: |
  journalctl -u bot-rk
ripe_match: "приёмник"
ripe_when: "адрес встал"
sample: "06.09 видел оба исхода"
entry: "rk_bot · main"
---

Тело.
EOF
wt "$WAITING_HOME" stamp home/20260808-02
# Спаяно: дату $PLUS30 в этот файл может положить ТОЛЬКО работающий stamp
# (фикстура несёт 2026-01-01), а комментарий — только сохранивший его stamp.
{ [ "$rc" = 0 ] && inf "$HB/20260808-02.md" "review_by: $PLUS30" \
    && inf "$HB/20260808-02.md" "# машиной: +30"; }
is "stamp не съел хвостовой комментарий человека у review_by" $?
{ inf "$HB/20260808-02.md" "review_by: $PLUS30     # машиной"; }
is "…и не съел ВЫРАВНИВАНИЕ: файл человека не меняется пробелами" $?
wt "$WAITING_HOME" done home/20260808-02 "причина"
{ [ "$rc" = 0 ] && inf "$HB/20260808-02.md" "state: done            # waiting | taken | done"; }
is "done не съел хвостовой комментарий у state" $?

# Important: сторож конъюнкции молчал на неразобранном. Апостроф в комментарии
# открывал кавычку, и ВТОРАЯ команда исчезала вместе с настоящей конъюнкцией.
line "$FB" 20260808-03 <<'EOF'
---
title: "апостроф в комментарии съедал вторую команду"
state: waiting
review_by: 2026-12-01
stamped_at: 2026-09-11
host: mprz
cwd: ~/dev/rk_bot
probe: |
  journalctl -u bot   # don't grep
  grep -c ошибка /var/log/app.log
ripe_match: "ok"
ripe_when: "наступило"
sample: "видел оба исхода"
entry: "rk_bot · main"
---

Тело.
EOF
wt "$F" list
{ [ "$rc" = 1 ] && has "пробу не разобрали"; }
is "неразобранная проба — ОТКАЗ, а не молчание: промах в безопасную сторону" $?
{ grp 20260808-03 | grep -q "недооформленные"; }
is "…и строка с ней висит в недооформленных, а не выдаётся за оформленную" $?

# Important: неизвестный исход в кэше. «Не спрашивали» про строку, которую
# спрашивали и получили ответ, — ровно конфляция инварианта 3.
full "$HB" 20260808-04
cache home 20260808-04 "{\"outcome\":\"timeout\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" list
{ grp 20260808-04 | grep -q "данные протухли"; }
is "неизвестный исход кэша → «данные протухли», а НЕ «ни разу не опрошена»" $?

# Ветка ~-сокращения display_path: в бою по ней идут 100% строк (корень ~/dev),
# а стенд жил в mktemp вне дома и кусал только ValueError-ветку.
mkdir -p "$ROOT/fakehome/repos"
Z="$ROOT/fakehome/repos/hm_bot"; mkdir -p "$Z"
git -C "$Z" init -q -b main .; git -C "$Z" config user.email t@t; git -C "$Z" config user.name t
echo x > "$Z/a.txt"; git -C "$Z" add -A; git -C "$Z" commit -qm init
out="$(cd "$Z" && HOME="$ROOT/fakehome" python3 "$W" new "строка под домашним каталогом" 2>&1)"; rc=$?
zf="$(one "$Z/.claude/waiting")"
{ [ "$rc" = 0 ] && inf "$Z/.claude/waiting/$zf" 'entry: "~/repos/hm_bot · main"'; }
is "entry сокращает домашний каталог до ~ — в бою по этой ветке идут все строки" $?

# Окончания строк переживают правку. Спаяно тройкой: дата появилась (её кладёт
# только работающий stamp), CRLF на месте у ВСЕХ строк, и голого LF в файле нет
# вовсе — до починки переписанная строка теряла \r, а соседние хранили.
printf 'x' > /dev/null
{ printf -- '---\r\ntitle: "файл с CRLF"\r\nstate: waiting\r\n'
  printf -- 'review_by: 2026-01-01     # комментарий человека\r\n'
  printf -- 'stamped_at: %s\r\nhost: mprz\r\ncwd: ~/dev/rk_bot\r\n' "$TODAY"
  printf -- 'probe: |\r\n  journalctl -u bot-rk\r\nripe_match: "ok"\r\n'
  printf -- 'ripe_when: "наступило"\r\nsample: "видел оба исхода"\r\n'
  printf -- 'entry: "rk_bot · main"\r\n---\r\n\r\nТело.\r\n'
} > "$HB/20260808-05.md"
crlf_n="$(tr -cd '\r' < "$HB/20260808-05.md" | wc -c | tr -d ' ')"
wt "$WAITING_HOME" stamp home/20260808-05
after="$(tr -cd '\r' < "$HB/20260808-05.md" | wc -c | tr -d ' ')"
{ [ "$rc" = 0 ] && inf "$HB/20260808-05.md" "review_by: $PLUS30" \
    && [ "$after" = "$crlf_n" ]; }
is "stamp сохранил CRLF: правка по ключу не переписывает файл в LF" $?
{ inf "$HB/20260808-05.md" "review_by: $PLUS30     # комментарий человека"; }
is "…и хвостовой комментарий с выравниванием уцелел и в CRLF-файле" $?

echo "=== три вопроса: код транспорта, код команды, совпадение по stdout ==="

prb() {  # prb <ящик> <имя> <host> <cwd> <команда> <ripe_match>
  line "$1" "$2" <<EOF
---
title: "проба $2"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: $3
cwd: $4
probe: |
  $5
ripe_match: "$6"
ripe_when: "то самое решение"
sample: "видел оба исхода 11.09"
entry: none
---
Тело.
EOF
}

# Укус 8 спеки — центральное утверждение поставки, три половины в одном месте.
prb "$HB" 20260707-11 local /tmp 'echo приёмник 10.0.0.1' 'приёмник'
prb "$HB" 20260707-12 local /tmp 'echo ничего интересного' 'приёмник'
prb "$HB" 20260707-13 local /tmp 'exit 7'                 'приёмник'
prb "$HB" 20260707-14 refuse /tmp 'echo приёмник'         'приёмник'
prb "$HB" 20260707-15 local /tmp 'grep приёмник /dev/null' 'приёмник'
wt "$WAITING_HOME" probe
{ [ "$rc" = 4 ] && has "20260707-11: fired"; }
is "probe вернул 4 — хоть одна строка не смогла спросить, и это не 0" $?
{ jq -er '.outcome=="fired" and .probe_rc==0' "$HC/home/20260707-11.json" >/dev/null; }
is "совпадение по stdout → fired, и код пробы записан нулём" $?
{ jq -er '.outcome=="silent" and .probe_rc==0' "$HC/home/20260707-12.json" >/dev/null; }
is "пусто кодом 0 → silent: проба спросила, ответ был пуст" $?
{ jq -er '.outcome=="unreachable" and .probe_rc==7' "$HC/home/20260707-13.json" >/dev/null \
    && has "20260707-13: unreachable — проба вышла кодом 7"; }
is "ненулевой код команды → unreachable, и код пробы назван" $?
{ jq -er '.outcome=="unreachable" and .probe_rc==255' "$HC/home/20260707-14.json" >/dev/null \
    && has "транспорт отказал (ssh: 255)"; }
is "ssh с кодом 255 → unreachable по транспорту, а не по команде" $?
# 🔴 Плановый укус требовал здесь silent — и противоречил и своему же probe_one,
# и таблице «Три вопроса» спеки («ненулевой код команды → unreachable»). Спека
# решает проблему грепа АРХИТЕКТУРНО: фильтрация уезжает из пробы в ripe_match, и
# проба обязана выходить нулём. Особый случай для кода 1 был бы ложью в сторону
# тишины — настоящий отказ, вышедший единицей, приезжал бы как «честно молчит».
# Укус перевёрнут и стал сторожем ровно этого соблазна: код 7 (ниже) однозначен,
# а единица — та, которую рука норовит простить.
{ jq -er '.outcome=="unreachable" and .probe_rc==1' "$HC/home/20260707-15.json" >/dev/null; }
is "🔴 код 1 у грепа — unreachable, а не silent: фильтрация живёт в ripe_match" $?
# 🔴 Р14 живьём: last_rc — код ПРОГОНА, а не пробы. Пара проверяет обе стороны:
# у недостижимой строки last_rc нуль, и list ставит её в «недостижима», а не в
# «данные протухли». Реализация, сунувшая код пробы в last_rc, красит оба укуса.
{ jq -er '.last_rc==0' "$HC/home/20260707-13.json" >/dev/null; }
is "last_rc нуль у НЕДОСТИЖИМОЙ строки: прогон состоялся, проба — нет" $?
wt "$WAITING_HOME" list
{ grp 20260707-13 | grep -q "недостижима"; }
is "…и list ставит её в «недостижима», а не в «данные протухли»" $?

# Потолок пробы. WAITING_PROBE_TIMEOUT_S=1, подставной ssh спит 3.
prb "$HB" 20260707-16 slow /tmp 'echo приёмник' 'приёмник'
wt "$WAITING_HOME" probe
{ [ "$rc" = 4 ] && has "20260707-16: unreachable — потолок пробы 1 с исчерпан"; }
is "проба, вышедшая за потолок, — unreachable с названной причиной" $?
{ jq -er '.probe_rc==124' "$HC/home/20260707-16.json" >/dev/null; }
is "…и код 124 тот же, каким отвечает timeout — контракт не выдуман" $?

# ripe_match, который не компилируется, — ГРОМКАЯ сторона.
prb "$HB" 20260707-17 local /tmp 'echo приёмник' 'приёмник((('
wt "$WAITING_HOME" probe
{ has "20260707-17: unreachable — ripe_match не компилируется"; }
is "непонятая регулярка → unreachable, а не «событие не наступило»" $?

# cwd, которого нет: код 91 и НАЗВАННАЯ причина вместо тихого «молчит».
prb "$HB" 20260707-18 local /nonexistent-dir-xyz 'echo приёмник' 'приёмник'
wt "$WAITING_HOME" probe
{ jq -er '.probe_rc==91' "$HC/home/20260707-18.json" >/dev/null \
    && has "cwd не нашёлся: /nonexistent-dir-xyz"; }
is "cwd не разрешился → unreachable кодом 91, причина названа (Р21)" $?

echo "=== кого фон НЕ спрашивает и замок ==="

# Р20: четыре класса. Положительная половина — у пятой строки кэш ПОЯВЛЯЕТСЯ.
prb "$HB" 20260707-21 local /tmp 'echo приёмник' 'приёмник'
line "$HB" 20260707-22 <<EOF
---
title: "взята в работу — фон её не спрашивает"
state: taken
review_by: $PLUS30
stamped_at: $TODAY
host: local
cwd: /tmp
probe: |
  echo приёмник
ripe_match: "приёмник"
ripe_when: "то самое"
sample: "видел оба исхода"
entry: none
taken_at: $TODAY
taken_because: "начали 11.09"
---
Тело.
EOF
# 🔴 Плановая фикстура тут была `full` с ПОМЕТКОЙ «probe: none» — а у full проба
# живая (host mprz, journalctl). То есть два класса Р20 из четырёх не проверял
# никто: ни `probe: none`, ни `done`. Найдено сверкой со спекой (её укус 14 требует
# «и в фоновом прогоне не участвует»). Обе фикстуры заведены настоящими.
line "$HB" 20260707-23 <<EOF
---
title: "спрашивать нечем — машинной пробы нет"
state: waiting
review_by: $PLUS7
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "решение человека"
sample: none
entry: none
---
Тело.
EOF
line "$HB" 20260707-25 <<EOF
---
title: "снята — проба живая, но спрашивать уже незачем"
state: done
review_by: $PLUS30
stamped_at: $TODAY
host: local
cwd: /tmp
probe: |
  echo приёмник
ripe_match: "приёмник"
ripe_when: "то самое"
sample: "видел оба исхода"
entry: none
closed_at: $TODAY
closed_because: "линия закрыта"
---
Тело.
EOF
line "$HB" 20260707-24 <<EOF
---
title: "недооформленная — полей пробы нет"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
entry: none
---
Тело.
EOF
rm -rf "$HC"
wt "$WAITING_HOME" probe
{ [ -f "$HC/home/20260707-21.json" ] && [ ! -f "$HC/home/20260707-22.json" ]; }
is "state: taken из фонового опроса выпала, а waiting — опрошена" $?
{ [ ! -f "$HC/home/20260707-24.json" ] && has "20260707-21: "; }
is "недооформленная не опрашивается — полей пробы у неё нет" $?
{ [ ! -f "$HC/home/20260707-23.json" ] && [ -f "$HC/home/20260707-21.json" ]; }
is "probe: none в фоновом опросе не участвует — спрашивать нечем (укус 14)" $?
{ [ ! -f "$HC/home/20260707-25.json" ] && has "20260707-21: "; }
is "снятая не опрашивается: проба у неё живая, но done уже не будит" $?
{ jq -er '.rows>=1' "$HC/_run.json" >/dev/null; }
is "квитанция прогона записана: сколько строк спросили — видно" $?

# Укус 15 спеки. Замок берётся ДО прогона, поэтому второй процесс не идёт в сеть.
mkdir -p "$WAITING_HOME/waiting-lock"
wt "$WAITING_HOME" probe
{ [ "$rc" = 3 ] && has "замок занят"; }
is "второй probe выходит 3 и в сеть не идёт — замок, а не гонка" $?
rmdir "$WAITING_HOME/waiting-lock"

# Протухший замок снимается — и ФАКТ снятия печатается (Р19).
mkdir -p "$WAITING_HOME/waiting-lock"
touch -A -020000 "$WAITING_HOME/waiting-lock" 2>/dev/null \
  || touch -t "$(date -v-2H +%Y%m%d%H%M)" "$WAITING_HOME/waiting-lock"
wt "$WAITING_HOME" probe
{ [ "$rc" != 3 ] && has "снят протухший замок пробы"; }
is "протухший замок снят, и снятие НАЗВАНО — тихое скрыло бы упавшую пробу" $?
{ jq -er '.stale_lock_broken==true' "$HC/_run.json" >/dev/null; }
is "…и записано в квитанцию прогона, а не только в stdout фона" $?

echo "=== третье состояние: taken не будит, но из list не исчезает ==="

# Укус 19 спеки. Пара: снятие ВИДНО в файле и группа СМЕНИЛАСЬ — оба даёт только
# работающий инструмент. Заглушка не пишет файл и печатает «з».
full "$HB" 20260808-11
wt "$WAITING_HOME" list
{ grp 20260808-11 | grep -q "ни разу не опрошена"; }
is "до taken строка стоит в обычной группе" $?
wt "$WAITING_HOME" taken home/20260808-11 "владелец сказал начинать"
{ [ "$rc" = 0 ] && has "взята в работу: home/20260808-11"; }
is "taken говорит, что строка взята, и называет ПОЛНЫЙ id" $?
{ inf "$HB/20260808-11.md" "state: taken" \
    && inf "$HB/20260808-11.md" 'taken_because: "владелец сказал начинать"'; }
is "taken пишет состояние И причину — правкой по ключу" $?
{ inf "$HB/20260808-11.md" "taken_at: $TODAY"; }
is "дату взятия ставит машина, а не человек" $?
wt "$WAITING_HOME" list
{ grp 20260808-11 | grep -q "взята в работу"; }
is "…и list печатает её в СЕДЬМОЙ группе, а не в вычисленной (Р8)" $?
{ has "взята в работу: владелец сказал начинать"; }
is "…и называет причину в самой строке вывода" $?

# taken без причины — отказ, а не запись с пустым полем.
full "$HB" 20260808-12
wt "$WAITING_HOME" taken home/20260808-12 "   "
{ [ "$rc" = 1 ] && nohas "взята в работу: home/20260808-12" \
    && ! grep -q "state: taken" "$HB/20260808-12.md"; }
is "taken без причины отказывает кодом 1 и файл не трогает" $?

# Р23, боевой случай приёмки: done → taken сохраняет надгробие.
line "$HB" 20260808-13 <<EOF
---
title: "закрыта, а работа только началась"
state: done
review_by: $PLUS7
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "слово владельца"
sample: none
entry: none
closed_at: 2026-09-11
closed_because: "ожидание кончилось, работа началась"
---
Тело.
EOF
wt "$WAITING_HOME" taken home/20260808-13 "перевод из ошибочного done"
{ [ "$rc" = 0 ] && inf "$HB/20260808-13.md" "state: taken" \
    && inf "$HB/20260808-13.md" 'closed_because: "ожидание кончилось'; }
is "Р23: done переводится в taken, и надгробие человека уцелело" $?
wt "$WAITING_HOME" list
{ grp 20260808-13 | grep -q "взята в работу"; }
is "…строка перестала быть снятой и печатается снова" $?
{ has "закрытие отменено"; }
is "…и list называет, что строка закрывалась — а не прячет это" $?

# state: taken без taken_because — брак формы, а не законное состояние.
line "$HB" 20260808-14 <<EOF
---
title: "taken без причины — откуда-то взялось руками"
state: taken
review_by: $PLUS7
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "что-нибудь"
sample: none
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" list
{ grp 20260808-14 | grep -q "недооформленные"; }
is "taken без taken_because — недооформленная: форма громче состояния" $?

echo "=== квитанция: ключ — исход, а не дата ==="

# Класс «срок»: гасится пере-штампом, потому что серии у даты нет.
line "$HB" 20260808-21 <<EOF
---
title: "срок прошёл вчера, пробы нет"
state: waiting
review_by: $(date -v-1d +%Y-%m-%d)
stamped_at: $(date -v-8d +%Y-%m-%d)
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "ответ поддержки"
sample: none
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" list
{ grp 20260808-21 | grep -q "созрело"; }
is "прошедший срок созревает строку — до квитанции" $?
wt "$WAITING_HOME" ack home/20260808-21
{ [ "$rc" = 0 ] && has "закрыто «срок»" && inf "$HB/20260808-21.md" "review_by: $PLUS7"; }
is "ack по сроку переклеивает review_by на +7 — у даты серии нет (Р9)" $?
{ inf "$HB/20260808-21.md" "acked_at: $TODAY"; }
is "…и записывает дату квитанции машиной" $?
wt "$WAITING_HOME" list
{ grp 20260808-21 | grep -q "ни разу не опрошена"; }
is "…и строка ушла из «созрело»: гашение видно в группе" $?

# Класс «исход пробы»: ключ — (исход, начало серии).
full "$HB" 20260808-22
cache home 20260808-22 "{\"outcome\":\"fired\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" ack home/20260808-22
{ [ "$rc" = 0 ] && has "закрыто «fired»" && inf "$HB/20260808-22.md" "acked_outcome: fired"; }
is "ack по исходу пишет класс исхода, а не «посмотрел»" $?
{ inf "$HB/20260808-22.md" "acked_key: "; }
is "…и ключ квитанции (Р10) — без него повторный исход неотличим от новой серии" $?
{ has "закрыто «fired»" && nohas "переклеен"; }
is "…и срок НЕ переклеивает: причина была не в сроке" $?

# Гасить нечего — и это сказано, а не сделано вид, что погасили.
full "$HB" 20260808-23
wt "$WAITING_HOME" ack home/20260808-23
{ [ "$rc" = 0 ] && has "гасить нечего" \
    && ! grep -q "acked_key" "$HB/20260808-23.md"; }
is "ack по строке вне дельты ничего не пишет и говорит об этом" $?

# Непарсящаяся строка: машина квитанцию не ставит (инвариант 7).
line "$HB" 20260808-24 <<EOF
---
title: "кавычка не закрыта
state: waiting
---
Тело.
EOF
wt "$WAITING_HOME" ack home/20260808-24
{ [ "$rc" = 1 ] && ! grep -q "acked_key" "$HB/20260808-24.md" \
    && has "квитанцию машина в такой файл не пишет"; }
is "ack в непарсящуюся строку отказывает кодом 1 — и говорит почему" $?

echo "=== дельта: ноль байт, водяной знак, молчание реестра ==="

wake_ms() {  # wake_ms <каталог> → $out, $rc, $ms (миллисекунды)
  d="$1"; : > "$ROOT/ms"
  out="$(cd "$d" && python3 -c 'import pathlib, subprocess, sys, time
t = time.monotonic()
p = subprocess.run(["python3", sys.argv[1], "wake"], capture_output=True, text=True)
pathlib.Path(sys.argv[2]).write_text(str(int((time.monotonic() - t) * 1000)))
sys.stdout.write(p.stdout)
sys.exit(p.returncode)' "$W" "$ROOT/ms")"; rc=$?
  ms="$(cat "$ROOT/ms")"
}

# Чистый ящик: иначе фикстуры прошлых глав дают событий на любой вкус, и «ноль
# байт» проверить нечем. Кэш прогона тоже убираем — иначе поломка реестра.
# 🔴 И КОРНИ СКАНА тоже, а не только домашний ящик: строки прошлых глав лежат в
# репозиториях под $ROOT/repos, и половина из них бракована НАМЕРЕННО (конъюнкция,
# чужое state). При них «ноль байт» недостижим по построению — и укус краснел бы
# на ВЕРНОЙ реализации. Корни уводятся на заведомо пустой каталог: дальше по файлу
# репозитории не нужны ни одной главе.
mkdir -p "$ROOT/empty"
printf '%s\n' "$ROOT/empty" > "$WAITING_HOME/waiting-roots.txt"
rm -rf "$HB" "$HC"; mkdir -p "$HB"
# 🔴 Замок держится ВСЮ главу. План рассчитывал удержать кэш свежим у каждой
# фикстуры, но 20260909-02 обязана быть БЕЗ кэша — она и есть укус «не
# спрашивали». Значит каждый wake честно отцепляет фон, тот переписывает кэш
# соседних строк (fired → unreachable, у фикстур host: mprz), и глава становится
# невоспроизводимой. Занятый замок гасит это механизмом самого инструмента:
# отцепленный probe выходит кодом 3 и не пишет ничего. Снимается перед укусом 17,
# который отцепление и проверяет, — то есть сам факт отцепления укусом не потерян.
mkdir -p "$WAITING_HOME/waiting-lock"

# Укус 11 спеки. 🔴 Голое «вывод пуст» зелено на заглушке, поэтому пара: тот же
# wake при неотработанном событии печатает его, и печатает id.
full "$HB" 20260909-01
cache home 20260909-01 "{\"outcome\":\"silent\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ [ "$rc" = 0 ] && [ -z "$out" ]; }
is "неотработанных событий нет → wake выдал РОВНО ноль байт" $?
cache home 20260909-01 "{\"outcome\":\"fired\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ [ "$rc" = 0 ] && has "home/20260909-01" && has "проба СРАБОТАЛА"; }
is "…а сработавшая проба в той же команде печатается — ноль байт не от немоты" $?
{ has "НЕ на экране человека"; }
is "дельта велит назвать себя вслух: иначе прочитана и не названа" $?
{ has "по прямой просьбе"; }
is "…и называет правило, что ack/done зовутся только по просьбе владельца" $?

# 🔴 ВЕЧНЫЙ СПУТНИК. Дальше половина укусов — про ОТСУТСТВИЕ строки в дельте, а
# голое отрицание зелено на заглушке. Непарсящаяся строка в дельте навсегда:
# машина квитанцию в неё не пишет (инвариант 7), — значит её присутствие и есть
# положительная половина, которую даёт только работающий инструмент. Заводится
# ПОСЛЕ укусов про ноль байт: при ней ноль байт невозможен по построению.
line "$HB" 20260909-09 <<EOF
---
title: "кавычка не закрыта — в дельте навсегда
state: waiting
---
Спутник отрицательных укусов этой главы.
EOF

# Укус 9 спеки: ни разу не опрошенная в дельту НЕ идёт — «не спрашивали» не
# событие. Пара: та же строка с кэшем fired идёт.
full "$HB" 20260909-02
wt "$WAITING_HOME" wake
{ nohas "20260909-02" && has "20260909-01" && has "20260909-09"; }
is "строка без кэша в дельту не идёт: «не спрашивали» — не событие (инв. 3)" $?

# Укус 10 спеки: недостижимость дольше суток — дельта, свежая — нет.
full "$HB" 20260909-03
cache home 20260909-03 "{\"outcome\":\"unreachable\",\"since\":\"$(date -v-2H '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
full "$HB" 20260909-04
cache home 20260909-04 "{\"outcome\":\"unreachable\",\"since\":\"$(date -v-3d '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ nohas "20260909-03" && has "20260909-04" && has "дольше суток"; }
is "недостижима 2 ч молчит, 3 дня — дельта: ssh, который мигнул, не событие" $?

# Укус 12 спеки: напечатанная и не отработанная печатается СНОВА; после ack нет.
wt "$WAITING_HOME" wake
{ has "20260909-01"; }
is "дельта, не отработанная, печатается снова — она не расходуется чтением" $?
wt "$WAITING_HOME" ack home/20260909-01
wt "$WAITING_HOME" wake
{ nohas "20260909-01" && has "20260909-04"; }
is "…после ack не печатается, а СОСЕДНЯЯ неотквитованная печатается" $?

# Укус 13 спеки — вот это водяной знак и доказывает. Пара обязательна: без неё
# реализация «гасим по id навсегда» проходит укус 12 тривиально.
wt "$WAITING_HOME" ack home/20260909-04
wt "$WAITING_HOME" wake
{ nohas "20260909-04" && has "20260909-09"; }
is "unreachable отквитован — молчит, а спутник печатается" $?
cache home 20260909-04 "{\"outcome\":\"fired\",\"since\":\"$(date '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ has "20260909-04" && has "проба СРАБОТАЛА"; }
is "🔴 …затем проба дала fired → печатается: ключ квитанции — ИСХОД, не id" $?
# И обратно: повторный тот же unreachable после квитанции — не печатается.
# 🔴 Между сменой кэша и ack добавлен wake, и это не украшение. Квитанция теперь
# ключуется тем, что дельта НАПЕЧАТАЛА, а не нынешним составом причин; без wake
# последней напечатанной причиной осталась бы `fired` строкой выше, и ack закрыл
# бы её — то есть укус проверял бы гашение НЕ ТОГО исхода, который в ярлыке.
# Владелец квитует то, что увидел: дельта обязана показать недостижимость снова.
cache home 20260909-04 "{\"outcome\":\"unreachable\",\"since\":\"$(date -v-3d '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
wt "$WAITING_HOME" ack home/20260909-04
wt "$WAITING_HOME" wake
{ nohas "20260909-04" && has "20260909-09"; }
is "…а тот же исход с той же серией после квитанции молчит" $?

# Укус 14 спеки: probe: none и прошедший review_by — с явной пометкой.
line "$HB" 20260909-05 <<EOF
---
title: "ответ поддержки — машинной пробы нет"
state: waiting
review_by: $(date -v-1d +%Y-%m-%d)
stamped_at: $(date -v-8d +%Y-%m-%d)
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "поддержка ответила"
sample: none
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" wake
{ has "20260909-05" && has "срок единственный датчик"; }
is "probe: none с прошедшим сроком — дельта с ЯВНОЙ пометкой (укус 14)" $?

# Укус 22 спеки: недооформленная гасится ack, но сброс — по смене СОСТАВА причин.
line "$HB" 20260909-06 <<EOF
---
title: "недооформлена намеренно — её дооформляет миграция"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" wake
{ has "20260909-06" && has "форма не сходится"; }
is "недооформленная идёт в дельту с перечислением причин" $?
wt "$WAITING_HOME" ack home/20260909-06
wt "$WAITING_HOME" wake
{ nohas "20260909-06" && has "20260909-09"; }
is "…ack гасит её: иначе намеренно недооформленная орёт на каждом старте (Р12)" $?
python3 - "$HB/20260909-06.md" <<'PY'
import pathlib, sys
p = pathlib.Path(sys.argv[1])
# Дописана ПРОБА, и она несёт конъюнкцию. Плановая вставка `host: local` состава
# причин не меняла: host требуется только при живой пробе, а пробы у строки не
# было — набор оставался прежним («нет поля probe»), и квитанция законно держалась.
p.write_text(p.read_text(encoding="utf-8").replace(
    'entry: none', 'probe: |\n  cd /tmp && ls\nentry: none'), encoding="utf-8")
PY
wt "$WAITING_HOME" wake
{ has "20260909-06"; }
is "🔴 …а смена СОСТАВА причин сбрасывает квитанцию: новая дыра не заглушена" $?

# Укус 16 спеки: поломка самого реестра — единственная строка, которой можно
# нарушить «пустая дельта пуста».
rm -rf "$HB"; mkdir -p "$HB"
# Одна ПРОБИВАЕМАЯ строка со свежим кэшем: событий она не даёт, но фон обязан её
# спрашивать. Без неё спрашивать некого — и тогда старая квитанция прогона не
# поломка, а норма (фон не звали, потому что не за чем). Пустой ящик тут был бы
# не «строже», а другим случаем; собственный укус на него — ниже, в круге критики.
full "$HB" 20260909-08
cache home 20260909-08 "{\"outcome\":\"silent\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
printf '{"finished_at":"2026-09-01T10:00:00","finished_at_ts":1,"rows":3,"unreachable":0,"stale_lock_broken":false}\n' > "$HC/_run.json"
wt "$WAITING_HOME" wake
{ [ -n "$out" ] && has "реестр молчит" && has "вчерашнюю правду" && nohas "20260909-08"; }
is "кэш прогона старше 3×TTL → поломка РЕЕСТРА, при ящике без событий" $?
printf '{"finished_at":"x","finished_at_ts":%s,"rows":3,"unreachable":0,"stale_lock_broken":true}\n' "$NOW" > "$HC/_run.json"
wt "$WAITING_HOME" wake
{ has "не дожил до конца"; }
is "…и снятый протухшим замок печатается как поломка, а не тонет в /dev/null" $?
printf '{"finished_at":"x","finished_at_ts":%s,"rows":3,"unreachable":0,"stale_lock_broken":false}\n' "$NOW" > "$HC/_run.json"
wt "$WAITING_HOME" wake
{ [ -z "$out" ]; }
is "…свежая честная квитанция прогона — снова ноль байт" $?

# Укус 17 спеки. Фон отцепляется: подставной ssh на хосте `slow` спит 3 с.
# Спутник возвращается: без него «вернулся быстро» зелено на заглушке, которая
# тоже возвращается быстро и не делает ничего.
line "$HB" 20260909-09 <<EOF
---
title: "кавычка не закрыта — в дельте навсегда
state: waiting
---
Спутник отрицательных укусов этой главы.
EOF
rmdir "$WAITING_HOME/waiting-lock"     # фон снова волен отцепиться — это и проверяем
prb "$HB" 20260909-07 slow /tmp 'echo приёмник' 'приёмник'
rm -f "$HC/home/20260909-07.json"
wake_ms "$WAITING_HOME"
{ [ "$rc" = 0 ] && [ "$ms" -lt 1000 ] && has "20260909-09"; }
is "wake при недостижимом хосте вернулся быстрее секунды: фон ОТЦЕПЛЕН" $?
sleep 3
{ [ -f "$HC/home/20260909-07.json" ]; }
is "…и отцепленный фон дожил до записи кэша — start_new_session, а не сирота" $?

echo "=== doctor: своего отсутствия дельта не видит ==="

# Фикстура настроек с ЧУЖИМИ ключами — они обязаны уцелеть байт в байт.
# Короткий массив записан В СТРОКУ намеренно: канонический json.dumps(indent=2)
# воспроизводит красиво отформатированный файл байт в байт, и укус на байтовую
# неизменность на нём слеп (замерено 11.09). Файл, который правят люди и шесть
# сессий, каноническим не бывает — фикстура обязана это воспроизводить.
cat > "$WAITING_HOME/settings.json" <<'SETT'
{
  "permissions": {
    "allow": ["Bash(/Users/x/.claude/scripts/hand.sh:*)"]
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.claude/hooks/context-meter.py",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "theme": "dark"
}
SETT
before="$(cksum < "$WAITING_HOME/settings.json")"
cp "$WAITING_HOME/settings.json" "$ROOT/settings-before.json"

# Укус 21 спеки, первая половина: doctor краснеет, когда записи нет.
wt "$WAITING_HOME" doctor
{ [ "$rc" = 2 ] && has "записи хука SessionStart НЕТ"; }
is "doctor возвращает 2 и НАЗЫВАЕТ отсутствие записи хука" $?
{ has "неотличим от пустого"; }
is "…и говорит, почему это молчание, а не пустота" $?

# Вторая половина: установка ставит и НЕ трогает чужого.
wt "$WAITING_HOME" doctor --install
{ [ "$rc" = 0 ] && has "вставлена" \
    && inf "$WAITING_HOME/settings.json" "waiting.py wake"; }
is "doctor --install вставил запись — и сказал куда" $?
{ inf "$WAITING_HOME/settings.json" '"matcher": "startup|resume|clear"'; }
is "матчер именно startup|resume|clear — compact и fork не включены (Р3)" $?
# 🔴 «Чужое цело» писал СТЕНД, и на отсутствующем инструменте оно цело даром.
# Спаяно со своей записью: уцелеть чужому есть смысл только там, где вставка
# состоялась. Обе половины в одном укусе, положительную даёт только инструмент.
{ inf "$WAITING_HOME/settings.json" "waiting.py wake" \
    && inf "$WAITING_HOME/settings.json" "context-meter.py" \
    && inf "$WAITING_HOME/settings.json" '"theme": "dark"' \
    && inf "$WAITING_HOME/settings.json" "hand.sh"; }
is "🔴 запись вставлена, и чужие ключи целы: permissions, сосед и theme" $?
{ python3 -c 'import json,sys
d=json.load(open(sys.argv[1]))
sys.exit(0 if "SessionStart" in d["hooks"] else 1)' "$WAITING_HOME/settings.json"; }
is "…и файл остался валидным JSON, а запись легла ВНУТРЬ hooks, не рядом" $?
{ python3 -c 'import json,sys
d=json.load(open(sys.argv[1]))
s=d["hooks"]["SessionStart"][0]
sys.exit(0 if s["hooks"][0]["timeout"]==5 and "wake" in s["hooks"][0]["command"] else 1)' \
    "$WAITING_HOME/settings.json"; }
is "…и разбирается именно как SessionStart с потолком 5 с" $?
# 🔴 Замер приёмки назвал этот укус недостающим. Поломка «install_hook пишет через
# json.load → json.dump» покрасила только укус про неоднозначный якорь: чужие
# КЛЮЧИ сериализация сохраняет, а отступы, порядок и пробелы переписывает целиком —
# то есть правка по якорю перестаёт отличаться от перезаписи, и инвариант 7
# соблюдается лишь на словах. Проверяется побайтово: в дифе не смеет быть ни одной
# убранной либо изменённой строки, только добавленные. Спаяно с положительной
# половиной — среди добавленных обязана быть своя запись.
{ ! diff "$ROOT/settings-before.json" "$WAITING_HOME/settings.json" | grep -q '^<' \
    && diff "$ROOT/settings-before.json" "$WAITING_HOME/settings.json" \
       | grep -q '^> .*waiting.py wake'; }
is "🔴 ни одной ЧУЖОЙ строки не изменено — только добавленные (Р5 побайтово)" $?

# Повторная установка — no-op, и она так и говорит.
after="$(cksum < "$WAITING_HOME/settings.json")"
wt "$WAITING_HOME" doctor --install
{ [ "$rc" = 0 ] && has "уже на месте" \
    && [ "$(cksum < "$WAITING_HOME/settings.json")" = "$after" ]; }
is "вторая установка ничего не меняет и называет это, а не молчит" $?
{ [ "$before" != "$after" ]; }
is "…а первая файл ИЗМЕНИЛА — иначе предыдущий укус зелен на бездействии" $?

# Якорь не единственный — отказ, а не вставка наугад.
cp "$WAITING_HOME/settings.json" "$ROOT/ok-settings.json"
# Мутируется файл БЕЗ нашей записи: иначе раньше отказывает сторож дубля
# SessionStart (он по делу, и у него свой укус в круге критики), и этот укус
# проверял бы не то, что написано на его ярлыке.
cp "$ROOT/settings-before.json" "$WAITING_HOME/settings.json"
python3 - "$WAITING_HOME/settings.json" <<'PY'
import pathlib, sys
p = pathlib.Path(sys.argv[1])
t = p.read_text(encoding="utf-8").replace('"hooks": {', '"hooks": {\n  "hooks": {', 1)
p.write_text(t, encoding="utf-8")
PY
wt "$WAITING_HOME" doctor --install
{ [ "$rc" = 2 ] && has "найден 2 раз" && has "Вставить руками"; }
is "неоднозначный якорь — отказ кодом 2 с готовым блоком, а не догадка" $?
cp "$ROOT/ok-settings.json" "$WAITING_HOME/settings.json"

# Укус 20 спеки: два счётчика замера растут ПО-РАЗНОМУ.
rm -rf "$HB" "$HC"; mkdir -p "$HB"
prb "$HB" 20260909-21 local /tmp 'echo ничего' 'приёмник'
wt "$WAITING_HOME" probe
{ jq -er '.same_truth_runs==0 and .changed_runs==1 and .outcome=="silent"' \
    "$HC/home/20260909-21.json" >/dev/null; }
is "первый прогон — смена исхода: счётчик changed, а не same" $?
s1="$(jq -r '.since' "$HC/home/20260909-21.json")"
wt "$WAITING_HOME" probe
{ jq -er '.same_truth_runs==1 and .changed_runs==1' \
    "$HC/home/20260909-21.json" >/dev/null; }
is "второй прогон с тем же исходом двигает same и НЕ двигает changed" $?
# `[ -n "$s1" ]` — не украшение: без него на отсутствующем кэше jq молчит с обеих
# сторон, и укус сравнивает пустоту с пустотой. Начало серии есть только там, где
# кэш написан инструментом.
{ [ -n "$s1" ] && [ "$(jq -r '.since' "$HC/home/20260909-21.json")" = "$s1" ]; }
is "…и начало серии не сдвинулось: серия та же (ключ водяного знака)" $?
# `since` пишется с точностью до СЕКУНДЫ, а три прогона укладываются в одну: без
# паузы новая серия получает байт-в-байт ту же строку, и укус краснеет на верной
# реализации. В бою пауза между прогонами не меньше TTL (3600 с), так что
# секундная точность контракта достаточна — чинится стенд, а не формат кэша.
sleep 1
prb "$HB" 20260909-21 local /tmp 'echo приёмник' 'приёмник'
wt "$WAITING_HOME" probe
{ jq -er '.same_truth_runs==1 and .changed_runs==2 and .outcome=="fired"' \
    "$HC/home/20260909-21.json" >/dev/null; }
is "смена исхода двигает changed — и замер различает тесный TTL от широкого" $?
{ [ "$(jq -r '.since' "$HC/home/20260909-21.json")" != "$s1" ]; }
is "…и переставляет начало серии: квитанция прежнего исхода сброшена" $?
wt "$WAITING_HOME" doctor
{ has "замер TTL (CACHE_TTL_S=3600)" && has "смена исхода 2"; }
is "doctor печатает замер числами, а не «надо бы посмотреть»" $?

echo "=== круг критики 11.09: то, чего стенд не ловил ==="

rm -rf "$HB" "$HC"; mkdir -p "$HB"

# 🔴 Битый байт в выводе пробы уносил ВЕСЬ прогон: UnicodeDecodeError не ловил ни
# один except, фон отцеплен, stderr в /dev/null. Строки после битой не
# опрашивались, квитанция прогона не писалась.
printf 'x\377y\n' > "$ROOT/binout"
prb "$HB" 20260910-01 local /tmp "cat $ROOT/binout" 'приёмник'
prb "$HB" 20260910-02 local /tmp 'echo приёмник'    'приёмник'
wt "$WAITING_HOME" probe
{ [ -f "$HC/home/20260910-02.json" ] && has "20260910-02: fired"; }
is "🔴 битый байт не уносит прогон: СЛЕДУЮЩАЯ строка всё равно опрошена" $?
{ jq -er '.rows>=2' "$HC/_run.json" >/dev/null; }
is "…и квитанция прогона записана — иначе отказ всплыл бы через три часа" $?
# Разводка двух разных починок: предыдущая пара держится пер-строчным сторожем и
# зелена даже при СТРОГОМ разборе (замерено). Здесь — сам разбор: с
# errors="replace" проба честно ответила и не совпала, то есть silent. Со строгим
# она приезжает как unreachable, и живой греп по журналу с одним битым байтом
# поднимал бы ложную тревогу на каждом старте — ровно то, против чего глава
# «Три вопроса».
{ jq -er '.outcome=="silent"' "$HC/home/20260910-01.json" >/dev/null; }
is "🔴 …а сама битая строка — silent, а не unreachable: байт разобран" $?

# 🔴 Сторож формы в probeable не был обкусан ни разу: единственный укус,
# ссылавшийся на него, обеспечивался вторым условием (поля probe нет вовсе).
# Здесь проба ЖИВАЯ, а ripe_match ещё не дописан — штатное состояние шаблона.
# Без сторожа probe_one падает KeyError на ней.
line "$HB" 20260910-03 <<EOF
---
title: "дозаполнена наполовину: проба есть, ripe_match ещё нет"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: local
cwd: /tmp
probe: |
  echo приёмник
ripe_when: "то самое"
sample: "видел оба исхода"
entry: none
---
Тело.
EOF
rm -rf "$HC"
wt "$WAITING_HOME" probe
{ [ ! -f "$HC/home/20260910-03.json" ] && [ -f "$HC/home/20260910-02.json" ]; }
is "🔴 недооформленная с ЖИВОЙ пробой не опрашивается — Р20 держится формой" $?

# 🔴 Имя временного файла было общим на все процессы, и замок становился условием
# КОРРЕКТНОСТИ вопреки спеке. Захватчик на общем имени ловит это детерминированно.
rm -rf "$HC"; mkdir -p "$HC/home"
mkdir -p "$HC/home/20260910-02.json.tmp"
wt "$WAITING_HOME" probe
{ [ -f "$HC/home/20260910-02.json" ] && has "20260910-02: fired"; }
is "🔴 имя временного файла НЕ общее: занятый <id>.json.tmp не срывает запись" $?

# Дальше — про дельту. Замок держится, чтобы отцепленный фон не переписывал кэш.
mkdir -p "$WAITING_HOME/waiting-lock"
rm -rf "$HB" "$HC"; mkdir -p "$HB"

# 🔴 Хук выходил кодом 2 с текстом на stderr при сломанной конфигурации: die()
# бросает SystemExit, а он не наследник Exception. Радиус — старт ЛЮБОЙ сессии.
# Пара: тот же вход через list ОБЯЗАН отказать, иначе укус зелен на пустоте.
cp "$WAITING_HOME/waiting-roots.txt" "$ROOT/roots.bak"
printf '# все корни закомментированы — штатное состояние конфига\n' > "$WAITING_HOME/waiting-roots.txt"
wt "$WAITING_HOME" wake
{ [ "$rc" = 0 ] && [ -z "$out" ]; }
is "🔴 сломанный конфиг корней: wake — ноль байт и код 0, старт не падает" $?
wt "$WAITING_HOME" list
{ [ "$rc" = 2 ] && has "не называет ни одного корня"; }
is "…а list на ТОМ ЖЕ входе отказывает кодом 2 — глохнет только хук" $?
cp "$ROOT/roots.bak" "$WAITING_HOME/waiting-roots.txt"

# 🔴 Строка, сломанная руками уже ПОСЛЕ taken, исчезала из дельты навсегда:
# reasons смотрел состояние раньше формы. Её state прочитан из полей, набранных
# ДО ошибки, — то есть «взята» из такого файла недостоверна.
line "$HB" 20260910-11 <<EOF
---
title: "взята в работу, потом сломана руками"
state: taken
taken_because: "начали"
это не ключ-значение
---
Тело.
EOF
wt "$WAITING_HOME" wake
{ has "20260910-11" && has "форма не сходится"; }
is "🔴 сломанная ПОСЛЕ taken идёт в дельту: форма раньше состояния" $?

# Укус 19 спеки, первая половина — её не было ни одной: «в дельту не идёт».
# Спаяна со сломанной соседкой, которая идти ОБЯЗАНА.
full "$HB" 20260910-12
wt "$WAITING_HOME" taken home/20260910-12 "работа идёт"
line "$HB" 20260910-13 <<EOF
---
title: "снята, а срок её давно прошёл"
state: done
review_by: $(date -v-1d +%Y-%m-%d)
stamped_at: $(date -v-8d +%Y-%m-%d)
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "линия закрыта"
sample: none
entry: none
closed_at: $TODAY
closed_because: "линия закрыта"
---
Тело.
EOF
wt "$WAITING_HOME" wake
{ nohas "20260910-12" && nohas "20260910-13" && has "20260910-11"; }
is "🔴 здоровые taken и done в дельту не идут, а сломанная идёт (укус 19)" $?

# 🔴 «Опрашивать некого» объявлялось поломкой реестра — и погасить это было
# нечем: фон не звали, потому что не за чем, а квитанция старела сама.
rm -rf "$HB" "$HC"; mkdir -p "$HB" "$HC"
line "$HB" 20260910-21 <<EOF
---
title: "ответа ждём от человека — спрашивать машиной нечего"
state: waiting
review_by: $PLUS7
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "владелец решит"
sample: none
entry: none
---
Тело.
EOF
printf '{"finished_at":"давно","finished_at_ts":1,"rows":0,"unreachable":0,"stale_lock_broken":false}\n' > "$HC/_run.json"
wt "$WAITING_HOME" wake
{ [ "$rc" = 0 ] && [ -z "$out" ]; }
is "🔴 спрашивать некого → старая квитанция НЕ поломка: фон и не звали" $?
prb "$HB" 20260910-22 local /tmp 'echo приёмник' 'приёмник'
wt "$WAITING_HOME" wake
{ has "реестр молчит" && has "вчерашнюю правду"; }
is "…а появилась пробиваемая строка — та же квитанция снова поломка" $?
rmdir "$WAITING_HOME/waiting-lock"

echo "=== doctor судит разбором, а не грепом по тексту ==="

# 🔴 Все три состояния ниже doctor объявлял здоровыми: он грепал подстроку.
# Зелёный doctor при мёртвом хуке — «молчащий неотличим от пустого», внесённое
# в самого сторожа, а других сторожей у этого класса нет.

# 1. Команда упомянута, но в permissions — естественная запись для этого файла.
cat > "$WAITING_HOME/settings.json" <<'SETT'
{
  "permissions": {
    "allow": ["Bash(python3 $HOME/.claude/scripts/waiting.py wake:*)"]
  },
  "hooks": {
    "UserPromptSubmit": []
  }
}
SETT
wt "$WAITING_HOME" doctor
{ [ "$rc" = 2 ] && has "НЕ в живом блоке SessionStart"; }
is "🔴 упоминание wake в permissions — не хук: doctor краснеет и говорит почему" $?

# 2. Файл не разбирается — харнесс игнорирует его ЦЕЛИКОМ, хуков нет ни одного.
cat > "$WAITING_HOME/settings.json" <<'SETT'
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [{"type": "command", "command": "python3 $HOME/.claude/scripts/waiting.py wake"}],
      }
    ]
  },
}
SETT
wt "$WAITING_HOME" doctor
{ [ "$rc" = 2 ] && has "не разбираются как JSON"; }
is "🔴 висячая запятая: хуков нет ни одного, а греп сказал бы «на месте»" $?

# 3. Чужой SessionStart уже есть — вставка дала бы ВТОРОЙ одноимённый ключ, и
#    разбор оставил бы последний: наш хук молча не работал бы.
cat > "$WAITING_HOME/settings.json" <<'SETT'
{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [{"type": "command", "command": "$HOME/.claude/hooks/чужой.py", "timeout": 5}]
      }
    ]
  },
  "theme": "dark"
}
SETT
chk="$(cksum < "$WAITING_HOME/settings.json")"
wt "$WAITING_HOME" doctor
{ [ "$rc" = 2 ] && has "записи хука SessionStart НЕТ"; }
is "чужой SessionStart без нашей команды — это «записи нет», а не «есть»" $?
wt "$WAITING_HOME" doctor --install
{ [ "$rc" = 2 ] && has "уже есть ключ SessionStart" \
    && [ "$(cksum < "$WAITING_HOME/settings.json")" = "$chk" ]; }
is "🔴 …и install ОТКАЗЫВАЕТ, не тронув файл: дубль ключа выключил бы хук молча" $?

echo "=== круг починки 11.09: квитанция дельты, владение замком, имя .tmp ==="

# 🔴 БОЕВАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ, ради которой заведена квитанция дельты.
# `wake` печатает и ТУТ ЖЕ отцепляет фон — именно потому, что кэш устарел. Пока
# владелец читает «недостижима» и говорит `ack`, фон успевает записать `fired`.
# До починки `cmd_ack` ключевался НЫНЕШНИМ составом причин и гасил `fired`,
# которого не печатал никто: сработавшая проба — то самое событие, ради которого
# реестр существует, — терялась молча и навсегда.
rm -rf "$HB" "$HC"; mkdir -p "$HB"
# Замок держится всю главу: отцепленный фон переписывал бы кэш под ногами укуса.
mkdir -p "$WAITING_HOME/waiting-lock"
full "$HB" 20260911-31
cache home 20260911-31 "{\"outcome\":\"unreachable\",\"since\":\"$(date -v-3d '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ has "20260911-31" && has "дольше суток"; }
is "дельта напечатала недостижимость — это и есть то, что видел человек" $?
{ jq -er '.kinds|index("unreachable")' "$HC/home/20260911-31.seen.json" >/dev/null \
    && has "дольше суток"; }
is "🔴 wake кладёт рядом с кэшем квитанцию ДЕЛЬТЫ с напечатанным составом" $?
# Фон сменил исход, пока владелец читал. Сам кэш при этом законно перезаписан —
# писатель у него один, и это не он виноват.
cache home 20260911-31 "{\"outcome\":\"fired\",\"since\":\"$(date '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" ack home/20260911-31
{ [ "$rc" = 0 ] && has "закрыто «unreachable»"; }
is "🔴 ack гасит НАПЕЧАТАННОЕ, а не нынешнее — иначе гасит невиденное" $?
{ [ ! -f "$HC/home/20260911-31.seen.json" ] \
    && inf "$HB/20260911-31.md" "acked_outcome: unreachable"; }
is "…квитанция дельты израсходована, а в строку записан НАПЕЧАТАННЫЙ исход" $?
wt "$WAITING_HOME" wake
{ has "20260911-31" && has "проба СРАБОТАЛА"; }
is "🔴 …и сработавшая проба, которой не видел никто, всё-таки печатается" $?

# 🔴 Квитанция дельты, которую СЛЕДУЮЩАЯ дельта не подтвердила, обязана исчезнуть.
# Иначе она описывает печать позавчерашнего дня и глушит будущее событие — ровно
# то, ради чего её расходует `ack`. Найдено не чтением кода: без этого стенд
# КРАСНЕЛ ЧЕРЕЗ РАЗ, и девять чисел таблицы разошлись на одном и том же укусе.
# Спутник обязателен: голое «файла нет» зелено на заглушке, которая его и не
# создавала. Спутник — непарсящаяся строка, она в дельте навсегда (инвариант 7).
line "$HB" 20260911-39 <<EOF
---
title: "кавычка не закрыта — в дельте навсегда
state: waiting
---
Спутник отрицательных укусов этой главы.
EOF
full "$HB" 20260911-38
cache home 20260911-38 "{\"outcome\":\"unreachable\",\"since\":\"$(date -v-3d '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ [ -f "$HC/home/20260911-38.seen.json" ] && has "20260911-38"; }
is "дельта напечатала строку — квитанция легла (положительная половина)" $?
# Владелец НЕ погасил, а исход сменился на молчание: строка ушла из дельты.
cache home 20260911-38 "{\"outcome\":\"silent\",\"since\":\"$(date '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ [ ! -f "$HC/home/20260911-38.seen.json" ] && has "20260911-39"; }
is "🔴 …дельта её больше не печатает → квитанция СНЯТА, а спутник печатается" $?
# И следствие: ack работает по нынешнему составу, а не по мёртвой квитанции.
cache home 20260911-38 "{\"outcome\":\"fired\",\"since\":\"$(date '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" ack home/20260911-38
{ [ "$rc" = 0 ] && has "закрыто «fired»"; }
is "🔴 …и ack не гасит по мёртвой квитанции: она сказала бы «unreachable»" $?

# Пара к главному укусу главы: без квитанции дельты `ack` работает по-прежнему.
# Без этого укуса реализация «гасить нечем, если нет .seen.json» прошла бы главу
# зелёной.
rm -f "$HC/home/20260911-31.seen.json"
full "$HB" 20260911-32
cache home 20260911-32 "{\"outcome\":\"fired\",\"since\":\"$(date '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" ack home/20260911-32
{ [ "$rc" = 0 ] && has "закрыто «fired»" \
    && inf "$HB/20260911-32.md" "acked_outcome: fired"; }
is "квитанции дельты нет → ack гасит по нынешнему составу, как и прежде" $?

rmdir "$WAITING_HOME/waiting-lock"

# 🔴 ВЛАДЕНИЕ ЗАМКОМ. `finally` снимал замок безусловно — в том числе ЧУЖОЙ,
# только что взятый тем, кто снял протухший: A берёт → замок протухает → B
# снимает протухший и берёт свой → A доходит до finally и удаляет замок B → C
# входит третьим. Детерминированного укуса у гонки нет; обкусывается то, что
# обкусывается — маркер пишется, и чужой замок со свежим маркером не снимается.
rm -rf "$HB" "$HC"; mkdir -p "$HB"
rm -rf "$WAITING_HOME/waiting-lock"
prb "$HB" 20260911-33 local /tmp 'echo приёмник' 'приёмник'
wt "$WAITING_HOME" probe
{ [ "$rc" = 0 ] && has "20260911-33: fired" \
    && [ ! -d "$WAITING_HOME/waiting-lock" ]; }
is "свой замок прогон снимает за собой — иначе второй probe не пройдёт никогда" $?

# Протухший замок теперь НЕ ПУСТ: внутри маркер владельца, не дожившего до
# снятия. `rmdir` по непустому каталогу отказывает — и снятие протухшего перестало
# бы работать вовсе, то есть не опрашивалась бы НИ ОДНА строка, а снаружи это
# выглядит как «все молчат». Содержимое обязано сноситься вместе с каталогом.
mkdir -p "$WAITING_HOME/waiting-lock"
printf '{"token":"не-дожил-до-снятия","pid":1,"at":1}\n' \
  > "$WAITING_HOME/waiting-lock/owner.json"
touch -A -020000 "$WAITING_HOME/waiting-lock" 2>/dev/null \
  || touch -t "$(date -v-2H +%Y%m%d%H%M)" "$WAITING_HOME/waiting-lock"
wt "$WAITING_HOME" probe
{ [ "$rc" != 3 ] && has "снят протухший замок пробы" && has "20260911-33: fired"; }
is "🔴 протухший замок снимается ВМЕСТЕ с маркером — rmdir по непустому отказал бы" $?

# 🔴 Сама гонка, сведённая к детерминизму. Пока идёт прогон A, держатель B сносит
# протухший замок и берёт свой — каталог при этом ПУСТ, ровно как его оставлял бы
# прогон до починки. `finally` прогона A обязан увидеть, что замок уже не его, и
# не трогать: иначе он удаляет замок B, и C входит третьим.
cat > "$ROOT/steal-lock" <<'ST'
#!/usr/bin/env bash
# B: снёс протухший (как его снял бы take_lock) и взял свой.
rm -rf "$WAITING_HOME/waiting-lock"
mkdir -p "$WAITING_HOME/waiting-lock"
echo приёмник
ST
chmod +x "$ROOT/steal-lock"
prb "$HB" 20260911-35 local /tmp "bash $ROOT/steal-lock" 'приёмник'
rm -rf "$WAITING_HOME/waiting-lock"
wt "$WAITING_HOME" probe
{ has "20260911-35: fired" && [ -d "$WAITING_HOME/waiting-lock" ]; }
is "🔴 замок, перехваченный ЧУЖИМ держателем, прогон за собой НЕ снимает" $?
rm -rf "$WAITING_HOME/waiting-lock"

# 🔴 `set_fields` писал во временный файл с ОБЩИМ именем — тот же класс, что уже
# починен в write_json и install_hook. Писатели здесь — команды, которые зовёт
# человек, поэтому ущерб меньше, но класс тот же. Захватчик на общем имени ловит
# это детерминированно: каталогом файл не перезаписать.
full "$HB" 20260911-34
mkdir -p "$HB/20260911-34.md.tmp"
wt "$WAITING_HOME" done home/20260911-34 "линия закрыта"
{ [ "$rc" = 0 ] && inf "$HB/20260911-34.md" "state: done" \
    && inf "$HB/20260911-34.md" "closed_because: \"линия закрыта\""; }
is "🔴 имя .tmp у set_fields НЕ общее: занятый <файл>.md.tmp не срывает правку" $?

echo
echo "=== надгробие: дописку в выведенный файл не предотвратить, но видно ==="

# 🔴 Сторож надгробия (поставка 2-3). Пара обязательна: «doctor не ругается»
# зелено и на инструменте, который про ОТЛОЖКА.md не знает вовсе. Положительную
# половину даёт ПЕЧАТАЕМАЯ строка — её печатает только живой сторож.
#
# ⚠️ Кода возврата в первом укусе нет НАРОЧНО: doctor возвращает 2 на любой
# найденной поломке, а соседние укусы выше ломают запись хука. Привязка к rc
# покрасила бы этот укус от ЧУЖОЙ поломки, и он перестал бы говорить о своей.
printf '%s\n' '# Переехало в реестр' 'Зови `waiting.py list`.' > "$WAITING_HOME/ОТЛОЖКА.md"
wt "$WAITING_HOME" doctor
{ has "надгробие цело" && ! has "надгробие ВЫРОСЛО"; }
is "🔴 надгробие ЦЕЛО: короткий редирект doctor поломкой не считает" $?

seq 1 60 >> "$WAITING_HOME/ОТЛОЖКА.md"
wt "$WAITING_HOME" doctor
{ has "надгробие ВЫРОСЛО" && [ "$rc" = 2 ]; }
is "🔴 надгробие ВЫРОСЛО: дописка в конец видна и красит doctor кодом 2" $?

# Третье состояние, и оно НЕ «всё хорошо»: редирект удалили целиком. Следующая
# сессия, не найдя файла, заведёт отложку заново с чистого листа — то есть
# поставка 2-3 молча откатится. Молчащий сторож тут неотличим от пустого.
rm -f "$WAITING_HOME/ОТЛОЖКА.md"
wt "$WAITING_HOME" doctor
{ has "надгробие ПРОПАЛО" && [ "$rc" = 2 ]; }
is "🔴 надгробие ПРОПАЛО: удалённый редирект — поломка, а не тишина" $?

# И четвёртое: файл на месте, длина в норме, а редиректа в нём нет — ровно то
# состояние, в котором отложка живёт ДО поставки. Зелёный doctor здесь означал
# бы «переехали», не переехав.
printf '%s\n' '## Ждёт: 20 строк' 'тело старого файла' > "$WAITING_HOME/ОТЛОЖКА.md"
wt "$WAITING_HOME" doctor
{ has "надгробие НЕ ПОСТАВЛЕНО" && [ "$rc" = 2 ]; }
is "🔴 надгробие НЕ ПОСТАВЛЕНО: короткий файл без редиректа зелёным не считается" $?
rm -f "$WAITING_HOME/ОТЛОЖКА.md"

echo
echo "итог: ✅ $pass   ❌ $fail"
[ "$fail" = 0 ]
