---
title: "Мёртвый файл тестов «ОФД Бизнес» — 249 строк выглядят покрытием и не являются им"
state: waiting
review_by: 2026-10-11
stamped_at: 2026-09-11
entry: "~/dev/gmb_v2 (ящик проекта недоступен: основной чекаут отцеплен нарочно, main держит ворктри deploy-main) · ~/dev/gmb_v2 · main — заявки нет, полоса B, skip_specs: true"
host: local
cwd: ~/dev/gmb_v2
probe: |
  cat tests/test_ofd_business.py
ripe_match: "providers[./]ofd_business"
ripe_when: "файл тестов стал грузить НАСТОЯЩИЙ модуль (gmp/core/services/receipt/providers/ofd_business.py) вместо несуществующего services/receipt/ofd_business.py — то есть 249 строк перестали пропускаться целиком и стали покрытием"
sample: "pending. Отрицательная половина снята 11.09: файл на месте, проба выходит нулём, и строки providers/ofd_business в нём НЕТ — он жёстко зашит на путь '..','services','receipt','ofd_business.py' (строка 21) и зовёт pytest.skip с allow_module_level (строка 24). Положительного исхода не было."
---

**Заголовок в отложке:** 18д. Мёртвый файл тестов «ОФД Бизнес» — 249 строк, заявки НЕТ

Заведена 11.09.2026 там же.

- 🔴 **Что:** `tests/test_ofd_business.py` грузит несуществующий путь
  `services/receipt/ofd_business.py` и пропускается ЦЕЛИКОМ. Двести сорок девять
  строк выглядят покрытием адаптера и не являются им.
- ✅ **Решение владельца** (10.09.2026): «надо сделать же, следом» — следом за 18г.
- ⚖️ **Полоса B, не C:** поведение продукта не меняется, заявка с `skip_specs: true`.
- ⏳ Ждёт очереди, не события.

**ПРОВЕРКА — ЛОКАЛЬНО**, в основном чекауте (не в ворктри: `.venv` лежит там):

```
cd ~/dev/gmb_v2
.venv/bin/python -m pytest tests/test_ofd_business.py -m "not integration" -q
```

Сейчас отвечает `1 skipped in 0.02s`, код возврата **5** — прогнано 11.09.2026.
Сработало, когда тесты ИСПОЛНЯЮТСЯ (`N passed`, код возврата 0).
⚠️ Код 5 значит «ни одного теста не собрано», и в общем прогоне он тонет:
набор зелён, файл мёртв, и одно другому не противоречит.

Разбор — память `ofd-business-adapter-gaps`.

🔴 **Проба в реестре ДРУГАЯ, чем в отложке, и подмена названа вслух.** Отложка
предлагала гонять `pytest`: он отвечает `1 skipped in 0.02s` кодом **5**, а
ненулевой код в реестре означает «источник не ответил», и строка вечно висела бы
недостижимой вместо честного «молчит». Сверх того тестовый прогон на каждом
фоновом круге — цена без нужды.

Замена спрашивает ровно то же самое дешевле: файл ГРУЗИТ не тот путь, и это
видно чтением. Команда отложки оставлена человеку:

```
cd ~/dev/gmb_v2
.venv/bin/python -m pytest tests/test_ofd_business.py -m "not integration" -q
```

⚠️ **Первая версия этой пробы была неверна и отвергнута замером.** Она искала
появление файла `ofd_business.py` — а он уже существует, по пути
`gmp/core/services/receipt/providers/`. Проба сработала бы немедленно и соврала
бы в сторону «готово». Дефект не в отсутствии модуля, а в адресе внутри теста.

ОТЛОЖКА.md, пункт 18д, заведён 11.09.2026
