# Реестр не-сейчас-работы, поставка 2-2 «Пробуждение» — план реализации

> ✅ **ПОСТАВЛЕНО 11.09.2026.** Числами, а не словами: стенд 104 → **181 укус**,
> живой прогон ✅ 181 ❌ 0, заглушка ✅ 0 ❌ 181. Шесть нарочных поломок покрасили
> 3 · 2 · 4 · 2 · 2 · 1 — разбор каждой в шапке `scripts/waiting-bite.sh`.
> Прогноз промежуточных чисел (122 · 142 · 162 · 178) сошёлся во всех четырёх
> точках; расхождения были не в числах, а в семи местах самого плана — они
> записаны решениями в реестре исполнения
> `.superpowers/sdd/2026-09-11-waiting-registry-2-2/progress.md`.
> Не закрыт один шаг: **приёмка на свежей сессии (Задача 5, шаг 6)** — матчер,
> доставка вывода в контекст и `timeout` проверяются только живым стартом,
> изнутри этой сессии недоступным.

> **Для агентов-исполнителей:** ОБЯЗАТЕЛЬНЫЙ ПОДНАВЫК — `superpowers:subagent-driven-development`
> (рекомендуется) либо `superpowers:executing-plans`. Шаги размечены чекбоксами `- [ ]`.
> Реестр исполнения — `.superpowers/sdd/2026-09-11-waiting-registry-2-2/progress.md`,
> образец формы (семнадцать `Ruling:` с ценой ошибки у каждого) — реестр поставки 2-1.

**Цель:** реестр перестаёт врать в сторону тишины. Созревшая строка приезжает САМА, на
старте сессии, без того чтобы кто-то спросил «что в отложке». Пробы ходят фоном и не
задерживают старт ни на секунду. Молчание самого реестра становится событием.

**Архитектура.** Тот же один скрипт `~/.claude/scripts/waiting.py` без зависимостей —
второго файла поставка не заводит. Добавляются пять команд: `probe` (фоновый опрос под
замком), `wake` (дельта в контекст, зовётся хуком), `ack` (квитанция), `taken` (третье
состояние), `doctor` (жив ли механизм + установка записи хука). Кэш исходов, который 2-1
только читала, начинает писаться — теми же файлами, по одному на строку, одним писателем.
Запись `SessionStart` появляется в `~/.claude/settings.json` последним действием поставки.

**Стек:** Python 3.14 (stdlib), bash-стенд укусов `scripts/waiting-bite.sh` (сейчас 104
укуса, идиома `memo-bite.sh`), git 2.50, macOS (`date -v`). Сети стенд не трогает: `ssh`
подменяется через `WAITING_SSH` (решение Р18).

**Спека:** `~/.claude/specs/2026-09-08-waiting-registry.md` — читать вместе с планом, главы
«Три вопроса, а не один», «2-2 · Пробуждение», «Коды возврата», «Укусы» (пункты 8–17),
«Открытые вопросы». **Спека в трёх местах противоречит решениям владельца от 11.09** — это
не повод догадываться, это Задача 0.

**Предшественница:** план 2-1 `plans/2026-09-10-waiting-registry-2-1.md` (поставлен
11.09.2026). Его решения Р1–Р13 остаются в силе и здесь не переголосовываются; ссылки вида
«Р11 плана 2-1» указывают туда, голые «Р7» — на этот план.

---

## Глобальные ограничения

- **Инварианты 1–9 спеки действуют целиком.** Поставка добавляет к ним рабочую нагрузку, а
  не исключения. Три, которые эта поставка нарушит первой, если ослабить внимание:
  - **Инвариант 3** — «не сработало», «не смогла спросить» и «не спрашивали» три разных
    исхода, и ни один не выводится из пустоты вывода. Вся глава «Три вопроса» — про него.
  - **Инвариант 7** — у файла строки один писатель, и это человек. Всё машинно-производное
    в кэше. `ack` и `taken` пишут во франтматтер строки — значит только `set_fields`, по
    ключу, никогда перезаписью.
  - **Инвариант 8** — фоновый прогон не актор. `probe` ничего не решает и не чинит; за
    побочные эффекты самой пробы отвечает автор строки.
- **Коды возврата:** `0` сделано либо печатать нечего · `1` форма строки · `2` конфигурация
  или положение · **`3` замок занят (не ошибка)** · **`4` проба не смогла спросить**.
  **Код 5 не занимать** — у 1b он значит «нужно решение владельца», и расхождение в пятом
  читалось бы опечаткой. Коды 3 и 4 впускаются ЭТОЙ поставкой и возвращаются только из
  `probe` (Р17).
- 🔴 **`wake` не имеет права упасть и не имеет права молчать не по делу.** Падение или
  таймаут = **ноль вывода, а не отказ старта**. Радиус глобальный: хук в
  `~/.claude/settings.json` стреляет на старте **любой** сессии в любом проекте.
- 🔴 **Пустая дельта — буквально ноль байт.** Не «событий нет», не пустая строка, не
  заголовок без содержимого. Требование стоит токенов на каждом из шести ежедневных стартов.
  Единственное исключение — строка о поломке самого реестра (спека, «Молчание реестра»).
- 🔴 **`~/.claude/ОТЛОЖКА.md` не трогать вовсе** — ни переименовывать, ни править, ни
  переносить строки. Решение владельца 10.09.2026: файл выводится из обращения поставкой 2-3.
- 🔴 **`~/.claude/settings.json` правят 4–6 сессий разом.** Только текстовая вставка по
  якорю с проверкой единственности (Р5). Никогда `json.load` → `json.dump`: он переписывает
  файл целиком и затрёт чужую главу молча, без конфликта git. Спека: **если запись хука
  пропала, реестр молчит НАВСЕГДА, а молчащий неотличим от пустого.**
- **Правка любого файла `~/.claude`** — read-modify-write по якорю, `git status --short` до
  и после. Репозиторий работают 4–6 сессий; git конфликта не даст.
- **Ворктри в `~/.claude` не изолирует, а ОТКЛЮЧАЕТ:** скиллы, хуки и настройки читаются из
  живого каталога, копия в ворктри не действует до мержа. **Работать в основном чекауте
  `main`.** Защита вместо изоляции — read-modify-write, мелкие коммиты, push сразу.
- **Коммитить мелко, пушить сразу.** После коммита сверять **`git show --stat`, а не
  `git status`**: 09.09 `.gitignore` тихо съел новый файл, `git add -A` не пожаловался,
  `git status` был чист, а на стволе код падал.
- **Обратные кавычки в сообщении коммита исполняются шеллом** — в `-m` их не ставить.
- **`git add -A` не звать следующим вызовом после `cd`:** рабочий каталог между вызовами
  инструмента сбрасывается. Путь абсолютным либо `cd` в том же вызове, и `git status --short`
  рядом.
- **`git stash` не звать вовсе** — стеш общий на весь репозиторий, `pop` достаёт работу
  сестринской сессии.
- **Никаких русских имён у файлов.** Содержимое по-русски, имена латиницей.
- `waiting-cache/`, `waiting-lock/` и `waiting-roots.txt` в белый список `.gitignore` **не
  впущены и впускать не надо**: кэш и замок машинно-производные, корни машинно-локальные.
  `/hooks/`, `/scripts/`, `/specs/`, `/plans/`, `/waiting/`, `/settings.json` — впущены.

---

## Главный улов предшественницы: шесть видов ПУСТОГО укуса

Стенд, который не умеет краснеть, охраняет ничего. За поставку 2-1 найдено шесть способов,
которыми укус притворяется рабочим. Правило живёт в шапке `scripts/waiting-bite.sh` (три
абзаца) — **прочитать её до первого нового укуса.**

| вид | чем зелен на пустоте |
|---|---|
| голое отрицание | «каталога не появилось» истинно, когда не создано НИЧЕГО |
| присутствие вместо группы | id в выводе есть, но группа не та |
| фикстура стенда | «файл не изменился» — его писал стенд, а не инструмент |
| голый код возврата | заглушка выходит нулём всегда |
| закрытое окно | вторую половину пары затирает следующая же команда |
| **эхо фикстуры** | искомая подстрока приезжает из фикстуры либо из ПУТЕЙ стенда |

🔴 **Класс воспроизводится, пока пишутся новые укусы.** За 2-1 он ловился семь раз, и три
раза — уже ПОСЛЕ починки, включая укусы, которые писал агент, знавший про все шесть видов.
Это постоянный сторож, а не разовая правка.

🔴 **Приёмка укуса одна: заглушка обязана дать РОВНО НОЛЬ зелёных.** Промежуточный прогон
стенда (укусы есть, кода нет) — **слабый**: за 2-1 его число трижды разошлось с прогнозом
плана и трижды это было неважно. Настоящая приёмка — живой прогон плюс заглушка.

**Опасность именно этой поставки.** У 2-2 половина утверждений — про ОТСУТСТВИЕ вывода
(«ноль байт», «после ack не печатается», «в дельту не идёт»), то есть про то самое голое
отрицание. Каждое такое обязано стоять В ТОМ ЖЕ укусе с положительным, которое даёт только
работающий инструмент: та же команда при неотработанном событии печатает его.

---

## Решения, принимаемые этим планом

Спека оставила их открытыми, не заметила либо противоречит себе. Каждое названо здесь,
чтобы исполнитель не принимал его заново по ходу и не принимал молча.

**Р1. Объём 2-2 — `probe`, `wake`, `ack`, `taken`, `doctor`; `list` получает седьмую
группу.** `new`, `done`, `stamp`, `list` уже есть (2-1). `entries` (скан хендоффов) вынесен
из объёма спекой целиком и здесь не заводится. Миграция строк — 2-3. Глоб `check.py` —
отдельная малая работа полосы B (решение владельца 11.09), в объём не входит и её не
задерживает.

**Р2. Хук зовёт `waiting.py wake` напрямую; второго файла в `hooks/` не заводится.**
Соседи по `settings.json` — отдельные скрипты (`context-meter.py`, `canon-drift.py`), и
соблазн повторить форму есть. Отвергнуто: обёртка, которая только пере-вызывает
`waiting.py`, — это файл, способный разойтись с тем, что вызывает, и второе место, где
живёт имя команды. Разбор франтматтера, скан, кэш и классификатор уже в `waiting.py`;
дельта — их потребитель, а не новая подсистема.
Цена: в `hooks/` не видно, что реестр вешает хук. Гасится тем, что `doctor` называет запись
хука первой строкой своего вывода.

**Р3. Матчер `startup|resume|clear`. `compact` НЕ включается.** Спека требует выбрать матчер
явно: на `compact` длинная сессия платила бы за дельту многократно. `clear` включён — `/clear`
вытирает контекст, и дельта, доставленная на старте, исчезает вместе с ним. `resume` включён:
между смертью сессии и возобновлением проходят часы, а дубль дельты стоит дешевле пропущенного
события. Образец контракта — захваченный, а не угаданный: плагин `superpowers` объявляет
`"matcher": "startup|clear|compact"` в `plugins/cache/claude-plugins-official/superpowers/6.3.0/hooks/hooks.json`,
то есть матчер `SessionStart` сверяется именно с этими значениями источника.
⚠️ Что матчер действительно фильтрует ПО ИСТОЧНИКУ, проверяемо только живой сессией — отсюда
Р6 и шаг приёмки на свежей сессии.

**Р4. Вывод хука — голый stdout, не JSON `hookSpecificOutput`.** Плагин `superpowers`
печатает JSON, но делает это ради кросс-харнесной развилки (Cursor ждёт `additional_context`,
Copilot — `additionalContext`), которой у локального хука нет. Решающий довод другой:
**обёртка JSON нулём байт быть не может.** Требование «пустая дельта — буквально ноль байт»
с `{"hookSpecificOutput": …}` невыполнимо в принципе, а оно жёстче удобства. Образец голого
stdout рядом и работает: `hooks/context-meter.py` и git-строка в `UserPromptSubmit` печатают
текстом, и он доезжает до контекста.

**Р5. Запись хука в `settings.json` ставит САМ инструмент — `doctor --install`, текстовой
вставкой по якорю.** Отвергнуты оба соседних варианта:
- *правит ассистент руками* — это и есть та правка, чей известный способ отказа записан в
  спеке («если запись пропала, реестр молчит навсегда»); повторная установка после того, как
  её затёрла сестринская сессия, обязана быть ОДНОЙ командой, а не упражнением в аккуратности;
- *`json.load` → `json.dump`* — переписывает файл целиком, то есть ровно дефект №3 из
  «Задачи» спеки, воспроизведённый в собственном инструменте.
Механика: якорь — **единственная** строка, содержащая `"hooks": {`; блок вставляется сразу
после неё; всё остальное байт в байт не трогается; запись через временное имя и `os.replace`.
Якорь не единственный или отсутствует — **отказ кодом 2** с готовым блоком для вставки
руками. Вторая установка — no-op кодом 0, который так и говорит.

**Р6. Хук вешается ПОСЛЕДНИМ действием поставки, в приёмке.** Его радиус — старт любой
сессии в любом проекте, и обкатывать его не на чем: `SessionStart`-хуков в `~/.claude`
сегодня ноль (проверено 11.09.2026: в `settings.json` только `UserPromptSubmit` и
`PreToolUse`). Поставить запись до того, как стенд зелен, значит раздать шести сессиям
сломанный старт. Пока записи нет, `doctor` честно кричит «хук не установлен» — и это
ожидаемое состояние всей поставки до шага приёмки.

**Р7. `taken` требует причину — поле `taken_because` обязательно.** Решение владельца ввело
третье состояние, но не сказало про причину. Довод спеки про `closed_because` переносится
дословно: «надгробие без причины смерти не отвечает на вопрос, ради которого его хранят».
Для `taken` он даже сильнее — строка в работе живёт в этом состоянии неделями, и «кто и по
какому событию её взял» выветривается быстрее, чем повод снятия. Поля: `state: taken`,
`taken_at` (машиной), `taken_because` (в кавычках).

**Р8. У `list` появляется СЕДЬМАЯ группа — «взята в работу», последней в порядке печати.**
Это прямой спор с Р10 плана 2-1, который отказался от седьмой группы, и спор законный:
тогда группы отказывала спека, теперь их состав меняет решение владельца («из `list` не
исчезает: работа видна, пока не закрыта»). Альтернатива — печатать `taken`-строку в её
вычисленной группе с пометкой — отвергнута: строка с прошедшим `review_by` встала бы в
«созрело», то есть реестр просил бы действия по работе, которая уже идёт. Последней в
порядке, потому что порядок групп — это порядок «что требует действия»: взятая в работу не
требует ничего.

**Р9. Квитанция двухмеханизменная, и это не дублирование.** Классов созревания три, и серия
исхода есть только у двух:

| класс | чем гасится | почему так |
|---|---|---|
| исход пробы (`fired` · `unreachable` дольше суток) | `acked_outcome` + `acked_key` (отпечаток исхода и `since` из кэша) | пере-штамп `review_by` кэша не трогает, и строка со сработавшей пробой печаталась бы вечно — дефект первой редакции спеки. `silent` в дельту не идёт вовсе: честно молчащая проба и есть нормальное ожидание |
| форма (недооформлена, франтматтер не парсится) | `acked_outcome: форма` + `acked_key` по СОСТАВУ причин | печатается независимо от `review_by`, значит сроком не гасится вовсе; сменился состав причин — квитанция сброшена. Непарсящаяся строка не гасится и машиной: в то, чего она не разобрала, машина не пишет (инвариант 7) |
| срок (`review_by` прошёл) | пере-штамп `review_by` (+30/+7) | у даты нет «серии исхода»: следующее созревание — это следующая дата, и она сама себе ключ |

**Р10. Заводится поле `acked_key` — отпечаток ВСЕГО состава причин, кроме срока.** Спека
называет `acked_at` и `acked_outcome`, но ключом объявляет тройку `(id, класс исхода, начало
текущей серии этого исхода)` — третьего элемента в полях нет. Без него «повторный
`unreachable` после квитанции» неотличим от «новая серия `unreachable` после промежуточного
`fired`», то есть укус 13 не выполняется.
Почему отпечаток набора, а не одна серия: `ack` гасит все причины разом (Р11), и хранить по
паре полей на класс значило бы держать три пары ради случая, который почти всегда один.
Отпечаток даёт то же свойство даром — любое изменение состава причин даёт другой ключ, и
строка снова в дельте. Класс поля тот же, что у `review_by`, `stamped_at`, `closed_at`:
машина пишет, человек читает.

**Р11. `ack` гасит ВСЕ нынешние причины строки разом.** Строка бывает созревшей и по форме,
и по исходу, и по сроку одновременно. Гасить по одной значило бы требовать трёх `ack` на
одну строку, и после первого дельта печатала бы ту же строку с другой причиной — на вид
неотличимо от «квитанция не работает». `ack` пишет все применимые поля и печатает одной
строкой, что именно закрыл.

**Р12. `ack` на НЕДООФОРМЛЕННОЙ строке законен и гасит её до `review_by`.** Иначе Р12 плана
2-1 обращается против реестра: строка `home/20260908-01` недооформлена НАМЕРЕННО, её
дооформляет миграция 2-3, — и дельта орала бы о ней на каждом из шести ежедневных стартов до
октября. Сменится состав причин — квитанция сбросится (Р9), так что заглушить настоящую новую
дыру этим нельзя.

**Р13. Замер TTL — два счётчика в файле кэша строки, читает `doctor`.** Решение владельца
11.09: TTL остаётся 3600 с, и число через неделю выбирает замер, а не оценка. Поля
`same_truth_runs` (прогон вернул ту же правду) и `changed_runs` (прогон сменил исход).
Отдельного файла замера не заводится: он был бы общим изменяемым файлом на шесть сессий —
ровно то, чего нет в реестре по инварианту 7. Пер-строчный `probe_every` **не вводить**:
спека запрещает, пока одна цифра не показала себя тесной, и теперь это проверяемо.
`CACHE_TTL_S` уже заведена одним местом (Р11 плана 2-1) — менять цифру только там.

**Р14. 🔴 `last_rc` — код ПРОГОНА, `probe_rc` — код ПРОБЫ. Это разные поля, и путать их
нельзя.** Классификатор 2-1 читает `last_rc != 0` как «данные протухли», то есть как
поломку самого реестра. Записать туда код упавшей пробы значит отправить каждую недостижимую
строку в «данные протухли» вместо «недостижима» — и укус 10 не выполнится, а инвариант 3
сломается в третий раз. `last_rc` остаётся нулём всегда, когда прогон состоялся, чем бы ни
кончилась проба. Естественная реализация делает здесь ошибку: поле называется «rc», проба
вернула «rc», рука сама.

**Р15. Числа поставки названы здесь и заведены константами в одном месте.**

| константа | значение | довод |
|---|---|---|
| `CACHE_TTL_S` | 3600 | решение владельца 11.09, выбирается замером через неделю (Р13) |
| `PROBE_TIMEOUT_S` | 30 | потолок на ОДНУ пробу |
| `LOCK_TTL_S` | 900 | «заметно меньше TTL замка» из спеки — 30-кратный запас над потолком пробы; двенадцати строк по 30 с хватает с четырёхкратным запасом |
| `SSH_CONNECT_TIMEOUT_S` | 10 | вместе с `BatchMode=yes` |
| `STALE_UNREACHABLE_S` | 86400 | «недостижима дольше суток» — дельта (спека, укус 10) |

**Р16. `wake` собирает вывод В ПАМЯТИ и печатает одним куском в самом конце.** Не ради
красоты: при печати по ходу исключение на середине оставило бы в контексте шести сессий
половину дельты, и отличить её от полной было бы нечем. Собранный список даёт даром и второе
требование — «ноль байт при пустой дельте» становится проверкой `if not lines: return 0`.
Наружу `wake` выходит нулём ВСЕГДА, любое исключение съедается.

**Р17. Коды 3 и 4 возвращает только `probe`. `wake` не возвращает их никогда.** Код `wake`
читает харнесс, а не человек, и ненулевой код хука — это сообщение харнессу о сбое, не о
состоянии реестра. Занятый замок для `wake` вообще не событие: он просто не отцепляет
второго фона.

**Р18. `WAITING_SSH` — подмена `ssh` ради стенда**, как `WAITING_HOME` (Р6 плана 2-1) и
`MEMO_HOME` у 1b-1. Стенд обязан работать без сети и без чужих хостов: иначе укус про код
255 либо не воспроизводится, либо стучится в живой `mprz`. Подменяется только транспорт —
местные пробы (`host: local`) идут своим путём и `ssh` не зовут вовсе.

**Р19. Квитанция ПРОГОНА — `waiting-cache/_run.json`, писатель один: держатель замка.**
Нужна затем, чтобы снятие протухшего замка ПЕЧАТАЛОСЬ. Фон отцеплен, его stdout уходит в
`/dev/null`, и тихое снятие скрыло бы упавшую пробу — а сосед держит тот же принцип (1b:
«протухшая аренда снимается автоматически, но факт снятия печатается»). Файл лежит рядом с
каталогами `<repo-id>/`, и `vanished` его не видит: она перебирает только каталоги.
Одного писателя гарантирует замок — писать `_run.json` разрешено только под ним.

**Р20. `probe` не опрашивает четыре класса строк:** `state: done`, `state: taken`,
недооформленные (включая непарсящиеся) и `probe: none`. Первые два — потому что они не
будят; третий — потому что спрашивать нечем и форма уже названа дельтой; четвёртый — по
спеке («в фоновом прогоне не участвует»). Экономия не главный довод: проба по строке с
полупустой формой ходила бы в сеть по угаданным полям.

**Р21. `cd` в пробу подставляется так: значение, начинающееся с `~/`, печатается голым,
остальное — через `shlex.quote`.** Тильду обязан раскрыть шелл-приёмник (на удалённом хосте
домашний каталог не наш), а `shlex.quote` её экранирует и `cd` промахивается. Предел назван
вслух: путь, в котором есть И тильда, И пробел, не поддержан. Отказ смещён в безопасную
сторону — `cd` не найдёт каталога, скрипт выйдет кодом **91**, и исход будет `unreachable`
с названной причиной «cwd не нашёлся на хосте», а не тихое «молчит».

**Р22. Задача 0 сводит спеку с решениями владельца ДО первой строки кода.** Сейчас спека
противоречит себе в трёх местах: тело говорит «состояний два, третьего нет», а «Открытые
вопросы» вводят `taken`; глава про кэш не знает про счётчики замера; таблица укусов 2-2 не
содержит ни одного укуса на `taken`. Исполнитель, читающий спеку первой (а он обязан), получил
бы противоречие и решил бы его молча. Код против спеки = права спека — значит спеку надо
привести в то состояние, в котором она права.

**Р23. `taken` принимает и `done`-строку; надгробие при переводе СОХРАНЯЕТСЯ.** Это не
воскрешение, а исправление ошибочного закрытия — и ровно этот случай назначен приёмочным:
`home/20260911-01` снята 11.09 как `done`, а по существу была `taken`, потому что решение
владельца про третье состояние принято ПОСЛЕ снятия. Сторож «снятую не трогать» здесь
отвергнут: он превратил бы единственный боевой случай новой команды в ручную правку файла.
`closed_at` и `closed_because` не стираются — их писала машина по слову человека, и стереть
значит спрятать, что строку однажды закрыли; `list` печатает такую строку с пометкой
«закрывалась <дата>, закрытие отменено».
Цена названа вслух: во франтматтере одновременно живут `state: taken` и `closed_because`,
и это выглядит противоречием. Оно и есть противоречие — записанное, а не заметённое.

---

## Структура файлов

| файл | что с ним делает поставка |
|---|---|
| `~/.claude/specs/2026-09-08-waiting-registry.md` | Задача 0: три решения владельца сводятся в тело спеки, read-modify-write по якорю |
| `~/.claude/scripts/waiting.py` | всё: замок, транспорт, три вопроса, запись кэша, счётчики замера, `taken`, `ack`, водяной знак, дельта, `doctor`, установка хука. Один файл, ~750 → ~1150 строк |
| `~/.claude/scripts/waiting-bite.sh` | стенд укусов, 104 → ~160. **Обязан уметь краснеть:** задача 5 гонит его на заглушке и требует ровно ноль зелёных |
| `~/.claude/settings.json` | Задача 5 (не раньше, Р6): запись `SessionStart`, текстовой вставкой по якорю |
| `~/.claude/waiting/20260911-01.md` | Задача 5: ретроспективный перевод в `taken` — первый боевой случай новой команды |
| `~/.claude/plans/2026-09-11-waiting-registry-2-2.md` | этот план: отметка о поставке в шапке |

Что НЕ трогается ни одной задачей: `ОТЛОЖКА.md`, `CLAUDE.md`, `scripts/memo.py`,
`scripts/hand.sh`, `scripts/check.py`, `hooks/*`, чужие строки в `waiting/`.

---

### Задача 0: свести спеку с решениями владельца

Спека — первый источник, и исполнитель читает её раньше плана. Сейчас она противоречит себе
в трёх местах, и противоречие решилось бы молча (Р22). Кода задача не пишет.

**Файлы:**
- Изменить: `~/.claude/specs/2026-09-08-waiting-registry.md`

⚠️ Каждая правка — **read-modify-write по якорю**, который ты только что прочитал. Файл
правят сестринские сессии; перезаписи целиком не делать. `git status --short` до и после.

- [ ] **Шаг 1: третье состояние в теле спеки**

Якорь — строка внутри блока «Формат строки»:

```
state: waiting            # waiting | done — третьего нет
```
Заменить на:
```
state: waiting            # waiting | taken | done
```

Следом, в списке пояснений к формату (после пункта про `entry`), вставить пункт:

```markdown
- **`state: taken` — работа началась и НЕ кончилась** (решение владельца 11.09.2026). Строка
  в `taken` не будит и в дельту не идёт, но из `list` не исчезает: работа видна, пока не
  закрыта. Первое созревание реестра показало дыру, ради которой состояние и заведено:
  `ack` («видел, ждём дальше») события не описывает, `done` («ждать кончили») лжёт о
  завершённости. Поля `taken_at` (машиной) и `taken_because` (обязательно, в кавычках) —
  довод тот же, что у `closed_because`: запись без причины не отвечает на вопрос, ради
  которого её хранят.
```

- [ ] **Шаг 2: команда `taken` в списке команд**

Якорь — строка списка в главе «2-1 · Строка и хранилище»:

```markdown
- **`ack <id>`** — квитанция: пере-штамп `review_by` и запись `acked_outcome`.
```
Заменить на:
```markdown
- **`ack <id>`** — квитанция (2-2): гасит все нынешние причины созревания разом. По исходу
  пробы — `acked_outcome` + `acked_key`; по форме — то же, ключом состав причин; по сроку
  — пере-штамп `review_by`. Серия исхода есть не у всех классов, отсюда разные механизмы.
- **`taken <id> "<почему>"`** — работа началась (2-2): `state: taken`, машинный `taken_at`,
  обязательная строка `taken_because`. Не будит, из `list` не исчезает.
- **`probe`** (2-2) — фоновый опрос под замком. Не актор (инвариант 8).
- **`wake`** (2-2) — дельта в контекст ассистента. Зовётся хуком `SessionStart`.
```

- [ ] **Шаг 3: контракт кэша — счётчики замера и два разных кода**

Якорь — строка таблицы «Хранилища»:

```markdown
| кэш исходов | `~/.claude/waiting-cache/<repo-id>/<id>.json` | **файл на строку, писатель один** — форма 1b («уникальные имена и один писатель каждый»), а не общий JSON |
```
Сразу ПОСЛЕ таблицы (перед абзацем «**Водяного знака отдельным файлом нет**») вставить:

```markdown
**Форма файла кэша — контракт, и в нём два разных кода возврата.** `last_rc` отвечает
«состоялся ли ПРОГОН», `probe_rc` — «чем кончилась ПРОБА». Путать их нельзя: классификатор
читает `last_rc != 0` как поломку реестра, и код упавшей пробы, положенный туда, отправил бы
каждую недостижимую строку в «данные протухли» вместо «недостижима» — инвариант 3, сломанный
в том самом месте, где он охраняется.

```json
{"outcome": "fired|silent|unreachable", "since": "ISO — начало ТЕКУЩЕЙ серии этого исхода",
 "last_run_at_ts": 0.0, "last_rc": 0, "probe_rc": 0, "note": "причина недостижимости",
 "same_truth_runs": 0, "changed_runs": 0}
```

**Два счётчика — ЗАМЕР TTL** (решение владельца 11.09.2026 вместо догадки о числе).
`same_truth_runs` — сколько прогонов вернули ту же правду, `changed_runs` — сколько сменили
исход. Частое «то же самое» значит TTL тесен, редкая смена — что широк. Через неделю число
выбирает замер; читает его `doctor`. Отдельного файла замера нет намеренно: он был бы общим
изменяемым файлом на шесть сессий — ровно то, чего в реестре нет по инварианту 7.
Пер-строчный `probe_every` по-прежнему не вводить.

**Квитанция прогона — `waiting-cache/_run.json`, писатель один: держатель замка.** Нужна
затем, чтобы снятие протухшего замка ПЕЧАТАЛОСЬ: фон отцеплен, его stdout уходит в
`/dev/null`, и тихое снятие скрыло бы упавшую пробу. Сосед держит тот же принцип (1b:
«протухшая аренда снимается автоматически, но факт снятия печатается»).
```

- [ ] **Шаг 4: `taken` в главе «2-2 · Пробуждение»**

Якорь — абзац «🔴 **Квитанцию ставит владелец, не ассистент.**». Вставить ПЕРЕД ним:

```markdown
**Строка в `taken` из фонового опроса выпадает.** Четыре класса не спрашиваются никогда:
`done`, `taken`, недооформленные и `probe: none`. Не ради экономии ssh: проба по строке с
полупустой формой ходила бы в сеть по угаданным полям, а работа, которая уже идёт, не
нуждается в том, чтобы её будили.
```

- [ ] **Шаг 5: укусы на то, чего в таблице нет**

Якорь — конец списка поставки 2-2 (строка про укус 17), перед заголовком «Поставка 2-3».
Дописать:

```markdown
19. **`taken` переводит строку из `waiting`, требует причину и НЕ гасит её из `list`:**
    строка печатается в группе «взята в работу», в дельту не идёт, в фоновом опросе не
    участвует. `taken` без причины — отказ кодом 1.
20. **Два счётчика замера растут по-разному:** прогон с тем же исходом двигает
    `same_truth_runs` и не двигает `changed_runs`; прогон со сменой исхода — наоборот, и
    заодно переставляет `since`.
21. **`doctor` краснеет, когда записи хука в `settings.json` нет**, и `doctor --install`
    её ставит, не тронув ни одного чужого ключа файла. Повторная установка — no-op.
22. **`ack` на недооформленной строке гасит её до `review_by`, но сброс происходит при
    смене СОСТАВА причин** — иначе намеренно недооформленная `home/20260908-01` (Р12 плана
    2-1) орала бы на каждом из шести ежедневных стартов до октября.

(Номера 19–22 идут после 17 и до 18: 18 принадлежит поставке 2-3 и перенумерация сломала бы
ссылки на него. Нумерация глобальная, порядок внутри главы — по поставке.)
```

- [ ] **Шаг 6: коммит**

```bash
cd ~/.claude
git add specs/2026-09-08-waiting-registry.md
git commit -m "спека реестра: taken, контракт кэша с двумя кодами и замером, укусы 19-22"
git show --stat HEAD
git push
```

---

### Задача 1: фоновая проба — замок, транспорт, три вопроса, запись кэша

Центральная задача поставки. Всё утверждение «реестр отличает „не сработало“ от „не смогла
спросить“ от „не спрашивали“» живёт здесь, и грепом оно ломается первым.

**Файлы:**
- Изменить: `~/.claude/scripts/waiting.py` (константы, шесть функций, команда `probe`)
- Изменить: `~/.claude/scripts/waiting-bite.sh` (подставной ssh + глава укусов)

- [ ] **Шаг 1: константы и импорт**

К шапке `waiting.py` дописать `import shlex` (рядом с `re`, по алфавиту) и после блока
существующих констант:

```python
LOCK = HOME / "waiting-lock"     # замок пробы: экономия на ssh, НЕ условие корректности
RUN = CACHE / "_run.json"        # квитанция прогона (Р19): писатель — держатель замка
PROBE_TIMEOUT_S = int(os.environ.get("WAITING_PROBE_TIMEOUT_S") or 30)
LOCK_TTL_S = 900                 # 30-кратный запас над потолком пробы (Р15)
SSH_CONNECT_TIMEOUT_S = 10
STALE_UNREACHABLE_S = 86400      # «недостижима дольше суток» — дельта (укус 10)
# Р18: транспорт и его потолок подменяются ради стенда — как WAITING_HOME у 2-1.
# Стенд обязан работать без сети: иначе укус про код 255 либо не воспроизводится,
# либо стучится в живой mprz.
SSH = os.environ.get("WAITING_SSH") or "ssh"
```

- [ ] **Шаг 2: замок**

```python
def take_lock():
    """Замок пробы. Возвращает (взят ли, снят ли протухший).

    Замок — экономия на ssh, а НЕ условие корректности: корректность держится
    разрезом кэша по файлам с одним писателем каждый. Нужен затем, чтобы шесть
    сессий, стартовавших в одну минуту, не позвали ssh шесть раз.
    """
    CACHE.mkdir(parents=True, exist_ok=True)
    try:
        LOCK.mkdir(parents=True)
        return True, False
    except FileExistsError:
        pass
    try:
        age = time.time() - LOCK.stat().st_mtime
    except OSError:
        return False, False
    if age <= LOCK_TTL_S:
        return False, False
    # Держатель не дожил до снятия. Проба, попросившая пароль, висит без TTY и
    # держала бы замок до его TTL — тогда не опросилась бы НИ ОДНА строка, а
    # снаружи это выглядит как «все молчат».
    try:
        LOCK.rmdir()
        LOCK.mkdir()
    except OSError:
        return False, False      # успел другой: это его очередь, а не ошибка
    return True, True


def write_json(path, obj):
    """Атомарно. Обрыв посередине оставил бы кэш нечитаемым, а нечитаемый кэш
    честно читается как «ни разу не опрошена» — то есть событие потерялось бы.
    """
    tmp = path.with_name(path.name + ".tmp")
    tmp.write_text(json.dumps(obj, ensure_ascii=False, indent=1) + "\n",
                   encoding="utf-8")
    os.replace(tmp, path)
```

- [ ] **Шаг 3: кого спрашиваем и как**

```python
def probeable(row):
    """Кого фон спрашивает (Р20). Четыре класса не спрашиваются никогда.

    done и taken — потому что не будят; недооформленные — потому что спрашивать
    нечем и форма уже названа дельтой; probe: none — по спеке. Довод не экономия:
    проба по строке с полупустой формой ходила бы в сеть по угаданным полям.
    """
    if row.error or row.faults:
        return False
    if state_of(row) in ("done", "taken"):
        return False
    probe = row.fields.get("probe")
    return probe is not None and probe.value != "none"


def probe_call(row):
    """Как позвать пробу. Возвращает (argv, скрипт для stdin).

    Скрипт уезжает в STDIN, а не в аргумент: иначе кавычки живой пробы пришлось
    бы экранировать дважды — для своего шелла и для удалённого, — и четыре живые
    строки с многострочным SQL сломались бы на этом молча.

    `bash -o pipefail`, а не голый bash: живые пробы это грепы по журналу через
    конвейер, и без pipefail код возврата брал бы ПОСЛЕДНЮЮ команду конвейера,
    то есть «источник ответил» подменялось бы «хвост дочитался».
    """
    cwd = row.fields["cwd"].value
    # Р21: тильду раскрывает шелл-ПРИЁМНИК — на удалённом хосте дом не наш, а
    # shlex.quote её экранирует и cd промахивается. Предел назван вслух: путь с
    # тильдой И пробелом сразу не поддержан, и отказ смещён в безопасную сторону
    # — cd не найдёт каталога, скрипт выйдет 91, исход будет unreachable с
    # названной причиной, а не тихое «молчит».
    where = cwd if cwd.startswith("~/") else shlex.quote(cwd)
    script = f"cd {where} || exit 91\n{row.fields['probe'].value}\n"
    host = row.fields["host"].value
    if host == "local":
        return ["bash", "-o", "pipefail", "-s"], script
    # BatchMode=yes обязателен: фон отцеплен, TTY у него нет, и проба,
    # попросившая пароль, повисла бы и удержала замок до его TTL.
    return [SSH, "-o", "BatchMode=yes",
            "-o", f"ConnectTimeout={SSH_CONNECT_TIMEOUT_S}",
            host, "bash", "-o", "pipefail", "-s"], script


def probe_one(row):
    """Три вопроса, а не один. Возвращает (исход, код ПРОБЫ, причина).

    Ни один из трёх не выводится из ответа на другой: `grep` без совпадений
    выходит кодом 1, а живые пробы — это грепы по журналу (14 в нынешнем
    реестре). Вывести исход из одного кода значило бы присылать ложную тревогу
    по честно молчащей строке на каждом из шести стартов, и дельту перестали бы
    читать за неделю. Тот же результат, что у лжи в сторону тишины, только с
    другой стороны.
    """
    argv, script = probe_call(row)
    try:
        p = subprocess.run(argv, input=script, capture_output=True, text=True,
                           timeout=PROBE_TIMEOUT_S)
    except (FileNotFoundError, PermissionError) as e:
        return "unreachable", 127, f"нечем спросить — {e}"
    except subprocess.TimeoutExpired:
        return "unreachable", 124, f"потолок пробы {PROBE_TIMEOUT_S} с исчерпан"
    if p.returncode == 255:
        return "unreachable", 255, "транспорт отказал (ssh: 255)"
    if p.returncode == 91:
        return "unreachable", 91, f"cwd не нашёлся: {row.fields['cwd'].value}"
    if p.returncode != 0:
        return "unreachable", p.returncode, f"проба вышла кодом {p.returncode}"
    try:
        rx = re.compile(row.fields["ripe_match"].value)
    except re.error as e:
        # Громкая сторона: непонятая регулярка — это «не смогли спросить», а не
        # «событие не наступило». Проглотить её тихо значит соврать в тишину.
        return "unreachable", 1, f"ripe_match не компилируется — {e}"
    return ("fired" if rx.search(p.stdout) else "silent"), 0, ""
```

- [ ] **Шаг 4: запись кэша со счётчиками замера**

🔴 Здесь живёт Р14. Прочитать его перед тем, как писать.

```python
def write_cache(row, outcome, probe_rc, note):
    """Кэш строки: исход, начало серии, два кода и ДВА СЧЁТЧИКА ЗАМЕРА (Р13).

    🔴 `last_rc` — код ПРОГОНА, `probe_rc` — код ПРОБЫ. Классификатор читает
    `last_rc != 0` как поломку реестра; положить туда код упавшей пробы значит
    отправить каждую недостижимую строку в «данные протухли» вместо
    «недостижима» — то есть сломать инвариант 3 там, где он и охраняется.
    Рука делает это сама: поле называется «rc», проба вернула «rc».
    """
    old = cache_of(row) or {}
    same = old.get("outcome") == outcome
    now_iso = datetime.now().isoformat(timespec="seconds")
    c = {
        "outcome": outcome,
        # Начало ТЕКУЩЕЙ серии этого исхода — ключ водяного знака (Р9). Та же
        # серия продолжается — `since` не двигается; сменился исход — новая.
        "since": (old.get("since") or now_iso) if same else now_iso,
        "last_run_at_ts": time.time(),
        "last_rc": 0,            # прогон состоялся, чем бы ни кончилась проба
        "probe_rc": probe_rc,
        "note": note,
        "same_truth_runs": int(old.get("same_truth_runs") or 0) + (1 if same else 0),
        "changed_runs": int(old.get("changed_runs") or 0) + (0 if same else 1),
    }
    p = CACHE / row.box.repo_id / f"{row.name}.json"
    p.parent.mkdir(parents=True, exist_ok=True)
    write_json(p, c)
```

- [ ] **Шаг 5: команда `probe`**

```python
def cmd_probe(a):
    """Фоновый опрос. Не актор (инвариант 8): ничего не решает и не чинит.

    Код 3 — замок занят, и это НЕ ошибка. Код 4 — хоть одна строка не смогла
    спросить; он отделён от 0 намеренно, иначе возвращается ровно тот дефект,
    ради которого написан инвариант 3.
    """
    ok, broken = take_lock()
    if not ok:
        print("замок занят — фоновая проба уже идёт")
        return 3
    started = datetime.now().isoformat(timespec="seconds")
    bad = 0
    try:
        rows = [r for r in scan() if probeable(r)]
        for row in rows:
            outcome, probe_rc, note = probe_one(row)
            write_cache(row, outcome, probe_rc, note)
            if outcome == "unreachable":
                bad += 1
            print(f"{row.id}: {outcome}" + (f" — {note}" if note else ""))
        write_json(RUN, {
            "started_at": started,
            "finished_at": datetime.now().isoformat(timespec="seconds"),
            "finished_at_ts": time.time(),
            "rows": len(rows),
            "unreachable": bad,
            "stale_lock_broken": broken,
        })
    finally:
        try:
            LOCK.rmdir()
        except OSError:
            pass
    if broken:
        print("снят протухший замок пробы — предыдущий прогон не дожил до конца")
    return 4 if bad else 0
```

И парсер в `main`, рядом с остальными:

```python
    p_probe = sub.add_parser("probe", help="опросить строки (зовётся фоном)")
    p_probe.set_defaults(fn=cmd_probe)
```

- [ ] **Шаг 6: подставной ssh в стенде**

Вставить в оснастку стенда, после определения `mk()`:

```bash
# Р18: транспорт подменяется — стенд обязан работать без сети и без чужих хостов.
# Хост приезжает аргументом, поэтому распознаём его перебором: `refuse` роняет
# транспорт кодом 255, `slow` висит дольше потолка пробы, остальные исполняют
# скрипт местно — то есть дают настоящие fired и silent.
export WAITING_SSH="$ROOT/fake-ssh"
export WAITING_PROBE_TIMEOUT_S=1
cat > "$WAITING_SSH" <<'FAKESSH'
#!/usr/bin/env bash
for a in "$@"; do
  case "$a" in
    refuse) exit 255 ;;
    slow)   sleep 3 ;;
  esac
done
exec bash -o pipefail -s
FAKESSH
chmod +x "$WAITING_SSH"
```

- [ ] **Шаг 7: укусы главы «три вопроса»**

Новая глава стенда. Каждая фикстура — правильная строка с ЖИВОЙ пробой, поэтому `full` не
годится (у неё `host: mprz` и `journalctl`); заводится помощник:

```bash
echo "=== три вопроса: код транспорта, код команды, совпадение по stdout ==="

prb() {  # prb <ящик> <имя> <host> <cwd> <команда> <ripe_match>
  line "$1" "$2" <<EOF
---
title: "проба $2"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
host: $3
cwd: $4
probe: |
  $5
ripe_match: "$6"
ripe_when: "то самое решение"
sample: "видел оба исхода 11.09"
entry: none
---
Тело.
EOF
}

# Укус 8 спеки — центральное утверждение поставки, три половины в одном месте.
prb "$HB" 20260707-11 local /tmp 'echo приёмник 10.0.0.1' 'приёмник'
prb "$HB" 20260707-12 local /tmp 'echo ничего интересного' 'приёмник'
prb "$HB" 20260707-13 local /tmp 'exit 7'                 'приёмник'
prb "$HB" 20260707-14 refuse /tmp 'echo приёмник'         'приёмник'
prb "$HB" 20260707-15 local /tmp 'grep приёмник /dev/null' 'приёмник'
wt "$WAITING_HOME" probe
{ [ "$rc" = 4 ] && has "20260707-11: fired"; }
is "probe вернул 4 — хоть одна строка не смогла спросить, и это не 0" $?
{ jq -er '.outcome=="fired" and .probe_rc==0' "$HC/home/20260707-11.json" >/dev/null; }
is "совпадение по stdout → fired, и код пробы записан нулём" $?
{ jq -er '.outcome=="silent" and .probe_rc==0' "$HC/home/20260707-12.json" >/dev/null; }
is "пусто кодом 0 → silent: проба спросила, ответ был пуст" $?
{ jq -er '.outcome=="unreachable" and .probe_rc==7' "$HC/home/20260707-13.json" >/dev/null \
    && has "20260707-13: unreachable — проба вышла кодом 7"; }
is "ненулевой код команды → unreachable, и код пробы назван" $?
{ jq -er '.outcome=="unreachable" and .probe_rc==255' "$HC/home/20260707-14.json" >/dev/null \
    && has "транспорт отказал (ssh: 255)"; }
is "ssh с кодом 255 → unreachable по транспорту, а не по команде" $?
{ jq -er '.outcome=="silent"' "$HC/home/20260707-15.json" >/dev/null; }
is "🔴 grep без совпадений выходит кодом 1 — и это silent, НЕ unreachable" $?
# 🔴 Р14 живьём: last_rc — код ПРОГОНА, а не пробы. Пара проверяет обе стороны:
# у недостижимой строки last_rc нуль, и list ставит её в «недостижима», а не в
# «данные протухли». Реализация, сунувшая код пробы в last_rc, красит оба укуса.
{ jq -er '.last_rc==0' "$HC/home/20260707-13.json" >/dev/null; }
is "last_rc нуль у НЕДОСТИЖИМОЙ строки: прогон состоялся, проба — нет" $?
wt "$WAITING_HOME" list
{ grp 20260707-13 | grep -q "недостижима"; }
is "…и list ставит её в «недостижима», а не в «данные протухли»" $?

# Потолок пробы. WAITING_PROBE_TIMEOUT_S=1, подставной ssh спит 3.
prb "$HB" 20260707-16 slow /tmp 'echo приёмник' 'приёмник'
wt "$WAITING_HOME" probe
{ [ "$rc" = 4 ] && has "20260707-16: unreachable — потолок пробы 1 с исчерпан"; }
is "проба, вышедшая за потолок, — unreachable с названной причиной" $?
{ jq -er '.probe_rc==124' "$HC/home/20260707-16.json" >/dev/null; }
is "…и код 124 тот же, каким отвечает timeout — контракт не выдуман" $?

# ripe_match, который не компилируется, — ГРОМКАЯ сторона.
prb "$HB" 20260707-17 local /tmp 'echo приёмник' 'приёмник((('
wt "$WAITING_HOME" probe
{ has "20260707-17: unreachable — ripe_match не компилируется"; }
is "непонятая регулярка → unreachable, а не «событие не наступило»" $?

# cwd, которого нет: код 91 и НАЗВАННАЯ причина вместо тихого «молчит».
prb "$HB" 20260707-18 local /nonexistent-dir-xyz 'echo приёмник' 'приёмник'
wt "$WAITING_HOME" probe
{ jq -er '.probe_rc==91' "$HC/home/20260707-18.json" >/dev/null \
    && has "cwd не нашёлся: /nonexistent-dir-xyz"; }
is "cwd не разрешился → unreachable кодом 91, причина названа (Р21)" $?

echo "=== кого фон НЕ спрашивает и замок ==="

# Р20: четыре класса. Положительная половина — у пятой строки кэш ПОЯВЛЯЕТСЯ.
prb "$HB" 20260707-21 local /tmp 'echo приёмник' 'приёмник'
line "$HB" 20260707-22 <<EOF
---
title: "взята в работу — фон её не спрашивает"
state: taken
review_by: $PLUS30
stamped_at: $TODAY
host: local
cwd: /tmp
probe: |
  echo приёмник
ripe_match: "приёмник"
ripe_when: "то самое"
sample: "видел оба исхода"
entry: none
taken_at: $TODAY
taken_because: "начали 11.09"
---
Тело.
EOF
full "$HB" 20260707-23            # probe: none — спрашивать нечем
line "$HB" 20260707-24 <<EOF
---
title: "недооформленная — полей пробы нет"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
entry: none
---
Тело.
EOF
rm -rf "$HC"
wt "$WAITING_HOME" probe
{ [ -f "$HC/home/20260707-21.json" ] && [ ! -f "$HC/home/20260707-22.json" ]; }
is "state: taken из фонового опроса выпала, а waiting — опрошена" $?
{ [ ! -f "$HC/home/20260707-24.json" ] && has "20260707-21: "; }
is "недооформленная не опрашивается — полей пробы у неё нет" $?
{ jq -er '.rows>=1' "$HC/_run.json" >/dev/null; }
is "квитанция прогона записана: сколько строк спросили — видно" $?

# Укус 15 спеки. Замок берётся ДО прогона, поэтому второй процесс не идёт в сеть.
mkdir -p "$WAITING_HOME/waiting-lock"
wt "$WAITING_HOME" probe
{ [ "$rc" = 3 ] && has "замок занят"; }
is "второй probe выходит 3 и в сеть не идёт — замок, а не гонка" $?
rmdir "$WAITING_HOME/waiting-lock"

# Протухший замок снимается — и ФАКТ снятия печатается (Р19).
mkdir -p "$WAITING_HOME/waiting-lock"
touch -A -020000 "$WAITING_HOME/waiting-lock" 2>/dev/null \
  || touch -t "$(date -v-2H +%Y%m%d%H%M)" "$WAITING_HOME/waiting-lock"
wt "$WAITING_HOME" probe
{ [ "$rc" != 3 ] && has "снят протухший замок пробы"; }
is "протухший замок снят, и снятие НАЗВАНО — тихое скрыло бы упавшую пробу" $?
{ jq -er '.stale_lock_broken==true' "$HC/_run.json" >/dev/null; }
is "…и записано в квитанцию прогона, а не только в stdout фона" $?
```

⚠️ **Оснастка главы требует двух вещей, которых у стенда 2-1 не было.** Первое — `jq`
(проверено: в системе есть, `openspec-critique-gate.sh` его уже зовёт). Второе — короткое
имя каталога кэша: дописать к оснастке `HC="$WAITING_HOME/waiting-cache"` рядом с `HB`.

🔴 **Эхо фикстуры здесь особенно близко.** Искомые подстроки `fired`, `silent`,
`unreachable` не смеют встречаться ни в заголовках фикстур, ни в путях стенда — поэтому
`title` у `prb` не содержит ни одного исхода, а проверки исходов идут через `jq` по ПОЛЮ, а
не грепом по выводу. Там, где всё же грепается вывод (`has "20260707-13: unreachable — …"`),
подстрока содержит id и тире — форму, которую печатает только `cmd_probe`.

- [ ] **Шаг 8: прогон стенда и заглушка**

```bash
cd ~/.claude
bash scripts/waiting-bite.sh | tail -3
printf 'import sys\nprint("з")\nsys.exit(0)\n' > /tmp/waiting-stub.py
WAITING=/tmp/waiting-stub.py bash scripts/waiting-bite.sh | tail -3
```
Ожидается: живой `✅ 122  ❌ 0`, заглушка `✅ 0  ❌ 122`.

🔴 **Живое число — прогноз, и расхождение с ним разбирать поимённо, а не переписывать.**
Число заглушки — приёмка, и любой зелёный на ней есть дефект укуса.

- [ ] **Шаг 9: коммит**

```bash
cd ~/.claude
git add scripts/waiting.py scripts/waiting-bite.sh
git commit -m "реестр 2-2: фоновая проба — замок, три вопроса, кэш со счётчиками замера"
git show --stat HEAD
git push
```

---

### Задача 2: третье состояние, квитанция и водяной знак

Задача пишет во франтматтер строки — то есть в файл, чей единственный писатель человек
(инвариант 7). Значит только `set_fields`, по ключу, и только в то, что разобралось.

**Файлы:**
- Изменить: `~/.claude/scripts/waiting.py` (`faults`, `GROUPS`, `classify`, `line_of`,
  четыре новые функции, команды `taken` и `ack`)
- Изменить: `~/.claude/scripts/waiting-bite.sh` (глава укусов + два косметических правила)

- [ ] **Шаг 1: форма принимает третье состояние**

Якорь в `faults`:

```python
    st = fields.get("state")
    if st is not None and st.value not in ("waiting", "done"):
        out.append(f"state: {st.value!r} — состояний два, waiting и done; третьего нет")
```
Заменить на:

```python
    st = fields.get("state")
    if st is not None and st.value not in ("waiting", "taken", "done"):
        out.append(f"state: {st.value!r} — состояния три: waiting, taken, done")
    if st is not None and st.value == "taken" and "taken_because" not in fields:
        out.append("state: taken без taken_because — запись без причины не "
                   "отвечает на вопрос, ради которого её хранят")
```

Заодно два косметических следа старого текста в стенде (оба — фикстуры, не логика):
- ярлык укуса `"state: третьего значения — недооформленная строка"` → `"state: четвёртого
  значения не бывает — недооформленная строка"`;
- комментарий `state: waiting            # waiting | done — третьего нет` →
  `state: waiting            # waiting | taken | done`. **Он встречается ДВАЖДЫ** — в
  фикстуре `20260404-07` (глава про комментарий у голого скаляра) и в `20260808-02` (глава
  про сохранность хвостового комментария человека), проверено грепом 11.09. Заменить в обоих:
  фикстура образца обязана совпадать со спекой, а «поправил одно из двух» здесь неотличимо
  от «поправил».

- [ ] **Шаг 2: седьмая группа**

```python
GROUPS = ("созрело", "молчит", "недостижима", "ни разу не опрошена",
          "недооформленные", "данные протухли", "взята в работу")
```

В `classify`, сразу ПОСЛЕ проверки формы и ДО проверки срока:

```python
    if state_of(row) == "taken":
        # Р8: своя группа, последняя в порядке печати. Печатать её в вычисленной
        # группе значило бы поставить строку с прошедшим сроком в «созрело» — то
        # есть просить действия по работе, которая уже идёт.
        return "взята в работу"
```

Форма проверяется РАНЬШЕ: недооформленная взятая строка громче, чем взятая, и в фоновом
опросе она всё равно не участвует.

⚠️ **Фильтр `cmd_list` не трогать.** Он скрывает только `state == "done"`, поэтому `taken`
из вывода не исчезает даром — это и требует решение владельца. Проверить чтением, а не
предположением.

- [ ] **Шаг 3: пометки в строке вывода**

В `line_of`, рядом с существующей веткой про `done`:

```python
    if state_of(row) == "taken":
        why = row.fields.get("taken_because")
        marks.insert(0, "взята в работу: " +
                     (why.value if why else "причина не названа"))
        # Р23: строка, закрытая по ошибке и переведённая в taken, СОХРАНЯЕТ
        # надгробие. Стереть его значило бы спрятать, что строку однажды
        # закрыли, — а писал его человек своим словом.
        was = row.fields.get("closed_at")
        if was is not None:
            marks.append(f"закрывалась {was.value}, закрытие отменено")
```

- [ ] **Шаг 4: причины созревания и водяной знак**

`hashlib` уже импортирован — его зовёт `repo_id_of`. Проверить и не дублировать.

```python
def field_or(row, key, default):
    f = row.fields.get(key)
    return f.value if f is not None else default


def iso_ts(s):
    try:
        return datetime.fromisoformat(s).timestamp()
    except (TypeError, ValueError):
        return None


def faults_digest(row):
    """Отпечаток СОСТАВА причин брака — ключ квитанции класса «форма» (Р9).

    У формы нет начала серии во времени: она не событие, а состояние. Ключом
    служит сам набор причин. Сменился набор — квитанция сброшена, строка снова в
    дельте; иначе `ack` гасил бы и НОВУЮ дыру, появившуюся после квитанции.
    """
    why = [row.error] if row.error else sorted(row.faults)
    return hashlib.sha256("\n".join(why).encode("utf-8")).hexdigest()[:8]


def reasons(row, c, today, now):
    """Почему строка в дельте. Список пар (класс, ключ); пусто — не в дельте.

    Классов может быть несколько разом, и `ack` гасит их ВСЕ (Р11). `silent` в
    дельту не идёт никогда: честно молчащая проба и есть нормальное ожидание, а
    тревога по ней — та же ложь, что и молчание, только с другой стороны.
    """
    out = []
    if state_of(row) in ("done", "taken"):
        return out          # снятая не будит, взятая в работу — тоже
    if row.error or row.faults:
        out.append(("форма", faults_digest(row)))
    if c is not None:
        outcome, since = c.get("outcome"), (c.get("since") or "")
        if outcome == "fired":
            out.append(("fired", since))
        elif outcome == "unreachable":
            began = iso_ts(since)
            # Дольше суток (укус 10). Свежая недостижимость — это ssh, который
            # мигнул; дельта на каждый мигающий ssh обесценила бы дельту.
            if began is not None and now - began > STALE_UNREACHABLE_S:
                out.append(("unreachable", since))
    rb = day(row.fields.get("review_by"))
    if rb is not None and rb <= today:
        out.append(("срок", rb.isoformat()))
    return out


def ack_key(kinds):
    """Ключ квитанции — отпечаток ВСЕГО нынешнего состава причин, кроме срока.

    Квитанция гасит состояние, а не отдельную причину (Р11): любое изменение
    состава — новый исход вместо прежнего, добавившаяся дыра формы — даёт другой
    ключ, и строка снова в дельте. «Переход unreachable → fired печатается»
    выполняется отсюда даром.

    Срок в ключ НЕ входит: его гасит пере-штамп `review_by`, и следующее
    созревание по сроку — это следующая дата, сама себе ключ. Включи его — и
    пере-штамп сразу после квитанции изменил бы состав, то есть строка печаталась
    бы снова тем же вечером.
    """
    body = "\n".join(f"{k}={s}" for k, s in sorted(kinds) if k != "срок")
    return hashlib.sha256(body.encode("utf-8")).hexdigest()[:8]


def unacked(row, kinds):
    """Есть ли среди причин хоть одна НЕОТРАБОТАННАЯ."""
    if not kinds:
        return False
    if any(k == "срок" for k, _ in kinds):
        # Срок снова прошёл — значит это новое созревание, а не то же самое:
        # квитанция по сроку выражается пере-штампом, и он бы его отодвинул.
        return True
    a = row.fields.get("acked_key")
    return not (a is not None and a.value == ack_key(kinds))
```

- [ ] **Шаг 5: команды `taken` и `ack`**

```python
def cmd_taken(a):
    """Работа началась и НЕ кончилась (решение владельца 11.09.2026).

    `ack` («видел, ждём дальше») этого не описывает, `done` («ждать кончили»)
    лжёт о завершённости. Строка в `taken` не будит и в дельту не идёт, но из
    `list` не исчезает: работа видна, пока не закрыта.

    Р23: `done`-строка принимается. Это исправление ошибочного закрытия, а не
    воскрешение, и надгробие при переводе СОХРАНЯЕТСЯ — его писал человек своим
    словом, а стереть значит спрятать, что строку однажды закрыли.
    """
    row = resolve(a.id, scan())
    if row.error:
        die(1, f"{row.id}: франтматтер не разбирается ({row.error}) — "
               f"машина в такой файл не пишет; почини форму и повтори")
    if not a.because.strip():
        die(1, "запись без причины не отвечает на вопрос, ради которого её "
               "хранят: чем именно началась работа и по какому событию")
    set_fields(row.path, [
        ("state", "taken"),
        ("taken_at", datetime.now().date().isoformat()),
        ("taken_because", yaml_quote(a.because)),
    ])
    print(f"взята в работу: {row.id} — {a.because}")
    return 0


def cmd_ack(a):
    """Квитанция: гасит ВСЕ нынешние причины созревания разом (Р11).

    🔴 Квитанцию ставит ВЛАДЕЛЕЦ, не ассистент. Существует полностью
    «корректный» прогон, где ассистент прочитал дельту, не назвал её человеку и
    погасил — строка исчезает навсегда при ненаступившей отработке. Механически
    это не удержать (сторож дороже сторожимого), поэтому правило сказано вслух
    и повторяется в выводе `wake`.
    """
    row = resolve(a.id, scan())
    if row.error:
        # Непарсящаяся строка гасится только руками: машина не пишет в то, чего
        # не разобрала (инвариант 7). Она будет в дельте каждый старт — это
        # самый громкий класс, и починить его может только человек.
        die(1, f"{row.id}: франтматтер не разбирается ({row.error}) — "
               f"квитанцию машина в такой файл не пишет; почини форму")
    today = datetime.now().date()
    kinds = reasons(row, cache_of(row), today, time.time())
    if not kinds:
        print(f"{row.id}: гасить нечего — строка в дельту не идёт")
        return 0
    pairs, closed = [], [k for k, _ in kinds]
    if "срок" in closed:
        probe = row.fields.get("probe")
        days = (DAYS_NO_PROBE if probe is not None and probe.value == "none"
                else DAYS_WITH_PROBE)
        when = today + timedelta(days=days)
        pairs += [("review_by", when.isoformat()), ("stamped_at", today.isoformat())]
    pairs += [("acked_at", today.isoformat()),
              ("acked_outcome", "+".join(sorted(closed))),
              ("acked_key", ack_key(kinds))]
    set_fields(row.path, pairs)
    print(f"{row.id}: квитанция — закрыто «{'+'.join(sorted(closed))}»")
    if "срок" in closed:
        print(f"  срок пересмотра переклеен на {pairs[0][1]}")
    return 0
```

Парсеры:

```python
    p_taken = sub.add_parser("taken", help="работа началась и не кончилась")
    p_taken.add_argument("id", help="<repo-id>/YYYYMMDD-NN либо голый YYYYMMDD-NN")
    p_taken.add_argument("because", help="чем началась работа — обязательно")
    p_taken.set_defaults(fn=cmd_taken)

    p_ack = sub.add_parser("ack", help="квитанция: видел, ждём дальше")
    p_ack.add_argument("id", help="<repo-id>/YYYYMMDD-NN либо голый YYYYMMDD-NN")
    p_ack.set_defaults(fn=cmd_ack)
```

- [ ] **Шаг 6: укусы главы «третье состояние и квитанция»**

⚠️ **Укусы 12, 13 и 22 (дельта печатается снова / после `ack` нет) живут в Задаче 3.**
Здесь `wake` ещё не написан, и проверить подавление дельты нечем: укус, написанный тут,
сверял бы поля вместо поведения — то есть проверял бы реализацию по её же описанию.

```bash
echo "=== третье состояние: taken не будит, но из list не исчезает ==="

# Укус 19 спеки. Пара: снятие ВИДНО в файле и группа СМЕНИЛАСЬ — оба даёт только
# работающий инструмент. Заглушка не пишет файл и печатает «з».
full "$HB" 20260808-11
wt "$WAITING_HOME" list
{ grp 20260808-11 | grep -q "ни разу не опрошена"; }
is "до taken строка стоит в обычной группе" $?
wt "$WAITING_HOME" taken home/20260808-11 "владелец сказал начинать"
{ [ "$rc" = 0 ] && has "взята в работу: home/20260808-11"; }
is "taken говорит, что строка взята, и называет ПОЛНЫЙ id" $?
{ inf "$HB/20260808-11.md" "state: taken" \
    && inf "$HB/20260808-11.md" 'taken_because: "владелец сказал начинать"'; }
is "taken пишет состояние И причину — правкой по ключу" $?
{ inf "$HB/20260808-11.md" "taken_at: $TODAY"; }
is "дату взятия ставит машина, а не человек" $?
wt "$WAITING_HOME" list
{ grp 20260808-11 | grep -q "взята в работу"; }
is "…и list печатает её в СЕДЬМОЙ группе, а не в вычисленной (Р8)" $?
{ has "взята в работу: владелец сказал начинать"; }
is "…и называет причину в самой строке вывода" $?

# taken без причины — отказ, а не запись с пустым полем.
full "$HB" 20260808-12
wt "$WAITING_HOME" taken home/20260808-12 "   "
{ [ "$rc" = 1 ] && nohas "взята в работу: home/20260808-12" \
    && ! grep -q "state: taken" "$HB/20260808-12.md"; }
is "taken без причины отказывает кодом 1 и файл не трогает" $?

# Р23, боевой случай приёмки: done → taken сохраняет надгробие.
line "$HB" 20260808-13 <<EOF
---
title: "закрыта, а работа только началась"
state: done
review_by: $PLUS30
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "слово владельца"
sample: none
entry: none
closed_at: 2026-09-11
closed_because: "ожидание кончилось, работа началась"
---
Тело.
EOF
wt "$WAITING_HOME" taken home/20260808-13 "перевод из ошибочного done"
{ [ "$rc" = 0 ] && inf "$HB/20260808-13.md" "state: taken" \
    && inf "$HB/20260808-13.md" 'closed_because: "ожидание кончилось'; }
is "Р23: done переводится в taken, и надгробие человека уцелело" $?
wt "$WAITING_HOME" list
{ grp 20260808-13 | grep -q "взята в работу"; }
is "…строка перестала быть снятой и печатается снова" $?
{ has "закрытие отменено"; }
is "…и list называет, что строка закрывалась — а не прячет это" $?

# state: taken без taken_because — брак формы, а не законное состояние.
line "$HB" 20260808-14 <<EOF
---
title: "taken без причины — откуда-то взялось руками"
state: taken
review_by: $PLUS30
stamped_at: $TODAY
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "что-нибудь"
sample: none
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" list
{ grp 20260808-14 | grep -q "недооформленные"; }
is "taken без taken_because — недооформленная: форма громче состояния" $?

echo "=== квитанция: ключ — исход, а не дата ==="

# Класс «срок»: гасится пере-штампом, потому что серии у даты нет.
line "$HB" 20260808-21 <<EOF
---
title: "срок прошёл вчера, пробы нет"
state: waiting
review_by: $(date -v-1d +%Y-%m-%d)
stamped_at: $(date -v-8d +%Y-%m-%d)
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "ответ поддержки"
sample: none
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" list
{ grp 20260808-21 | grep -q "созрело"; }
is "прошедший срок созревает строку — до квитанции" $?
wt "$WAITING_HOME" ack home/20260808-21
{ [ "$rc" = 0 ] && has "закрыто «срок»" && inf "$HB/20260808-21.md" "review_by: $PLUS7"; }
is "ack по сроку переклеивает review_by на +7 — у даты серии нет (Р9)" $?
{ inf "$HB/20260808-21.md" "acked_at: $TODAY"; }
is "…и записывает дату квитанции машиной" $?
wt "$WAITING_HOME" list
{ grp 20260808-21 | grep -q "ни разу не опрошена"; }
is "…и строка ушла из «созрело»: гашение видно в группе" $?

# Класс «исход пробы»: ключ — (исход, начало серии).
full "$HB" 20260808-22
cache home 20260808-22 "{\"outcome\":\"fired\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" ack home/20260808-22
{ [ "$rc" = 0 ] && has "закрыто «fired»" && inf "$HB/20260808-22.md" "acked_outcome: fired"; }
is "ack по исходу пишет класс исхода, а не «посмотрел»" $?
{ inf "$HB/20260808-22.md" "acked_key: "; }
is "…и ключ квитанции (Р10) — без него повторный исход неотличим от новой серии" $?
{ has "закрыто «fired»" && nohas "переклеен"; }
is "…и срок НЕ переклеивает: причина была не в сроке" $?

# Гасить нечего — и это сказано, а не сделано вид, что погасили.
full "$HB" 20260808-23
wt "$WAITING_HOME" ack home/20260808-23
{ [ "$rc" = 0 ] && has "гасить нечего" \
    && ! grep -q "acked_key" "$HB/20260808-23.md"; }
is "ack по строке вне дельты ничего не пишет и говорит об этом" $?

# Непарсящаяся строка: машина квитанцию не ставит (инвариант 7).
line "$HB" 20260808-24 <<EOF
---
title: "кавычка не закрыта
state: waiting
---
Тело.
EOF
wt "$WAITING_HOME" ack home/20260808-24
{ [ "$rc" = 1 ] && ! grep -q "acked_key" "$HB/20260808-24.md" \
    && has "квитанцию машина в такой файл не пишет"; }
is "ack в непарсящуюся строку отказывает кодом 1 — и говорит почему" $?
```

- [ ] **Шаг 7: прогон стенда и заглушка**

```bash
cd ~/.claude
bash scripts/waiting-bite.sh | tail -3
WAITING=/tmp/waiting-stub.py bash scripts/waiting-bite.sh | tail -3
```
Ожидается: живой `✅ 142  ❌ 0`, заглушка `✅ 0  ❌ 142`.

- [ ] **Шаг 8: коммит**

```bash
cd ~/.claude
git add scripts/waiting.py scripts/waiting-bite.sh
git commit -m "реестр 2-2: taken, седьмая группа и квитанция с ключом по исходу"
git show --stat HEAD
git push
```

---

### Задача 3: `wake` — дельта, ноль байт и отцепленный фон

Задача, ради которой поставка называется «Пробуждение». Здесь же живут два жёстких
требования, легко нарушаемых незаметно: **ноль байт** и **старт не ждёт ssh**.

**Файлы:**
- Изменить: `~/.claude/scripts/waiting.py` (четыре функции, команда `wake`)
- Изменить: `~/.claude/scripts/waiting-bite.sh` (глава укусов + помощник `wake_ms`)

- [ ] **Шаг 1: отцепление фона**

```python
def detach_probe():
    """Старт сессии не ждёт ssh НИКОГДА (укус 17).

    start_new_session=True обязателен: иначе дочерний процесс умрёт вместе с
    группой сессии, и проба, начатая на старте, не доживёт до записи кэша.
    Потоки в /dev/null — всё, что фон говорит, живёт в кэше и в квитанции
    прогона, а не в чьём-то stdout.
    """
    try:
        subprocess.Popen(
            [sys.executable, str(pathlib.Path(__file__).resolve()), "probe"],
            stdin=subprocess.DEVNULL, stdout=subprocess.DEVNULL,
            stderr=subprocess.DEVNULL, start_new_session=True)
    except OSError:
        pass        # не вышло отцепить — это не повод ронять старт сессии
```

- [ ] **Шаг 2: молчание реестра как событие**

```python
def registry_broken(now):
    """Молчание реестра — само событие. Возвращает текст или None.

    Если проба падает при старте, кэш просто перестаёт обновляться: исходы
    остаются прежними, дельта пуста, `list` печатает вчерашнюю правду. Молчащий
    реестр неотличим от пустого, а пустой — нормальное состояние, поэтому
    тревогу не поднимет никто.

    🔴 Своего ОТСУТСТВИЯ `wake` обнаружить не может: если запись хука пропала из
    settings.json (а файл правят 4–6 сессий), его просто никто не зовёт. Этот
    класс закрывает только `doctor`, и потому он отдельная команда для человека.

    Это ЕДИНСТВЕННАЯ строка, которой позволено нарушить «пустая дельта пуста».
    """
    try:
        run = json.loads(RUN.read_text(encoding="utf-8"))
    except (OSError, ValueError):
        return None     # прогонов не было вовсе — это старт, а не поломка
    if run.get("stale_lock_broken"):
        return ("предыдущий фоновый прогон не дожил до конца — замок снимали "
                "протухшим; исходы могли остаться вчерашними")
    ts = run.get("finished_at_ts")
    if not isinstance(ts, (int, float)):
        return "квитанция фонового прогона без времени — фон писал её сломанным"
    if now - ts > 3 * CACHE_TTL_S:
        return (f"фоновая проба не отчитывалась {int((now - ts) / 3600)} ч — "
                f"реестр печатает вчерашнюю правду")
    return None
```

- [ ] **Шаг 3: дельта**

```python
def why_line(kind, key, row, c):
    """Причина одной строкой. Класс назван словами, а не кодом."""
    if kind == "форма":
        why = [row.error] if row.error else row.faults
        return "форма не сходится: " + "; ".join(why)
    if kind == "fired":
        return (f"проба СРАБОТАЛА (серия с {key}) — "
                f"{field_or(row, 'ripe_when', 'решение прозой не названо')}")
    if kind == "unreachable":
        note = (c or {}).get("note") or ""
        return f"недостижима с {key}, дольше суток" + (f" — {note}" if note else "")
    tail = ("" if field_or(row, "probe", "") != "none"
            else " — машинной пробы нет, срок единственный датчик")
    return f"срок пересмотра {key} прошёл{tail}"


def wake():
    """Дельта на старте сессии. Собирается В ПАМЯТИ и печатается одним куском.

    🔑 Вывод хука уходит в КОНТЕКСТ АССИСТЕНТА, а не на экран человека
    (проверено 08.09.2026). Значит «печатает» — неверное слово: хук КЛАДЁТ
    дельту в контекст, а вынести её на экран обязан ассистент, и это сказано
    строкой в самом выводе. Иначе созревшая строка будет прочитана и не названа.

    Р16: печать одним куском в конце. По ходу — и исключение на середине
    оставило бы в контексте половину дельты, неотличимую от полной.
    """
    today = datetime.now().date()
    now = time.time()
    items, stale = [], False
    for row in scan():
        c = cache_of(row)
        kinds = reasons(row, c, today, now)
        if kinds and unacked(row, kinds):
            items.append((row, kinds, c))
        if probeable(row):
            ts = (c or {}).get("last_run_at_ts")
            if not isinstance(ts, (int, float)) or now - ts > CACHE_TTL_S:
                stale = True
    broken = registry_broken(now)
    if stale:
        detach_probe()
    if not items and not broken:
        # 🔴 Ноль байт. Не «событий нет», не пустая строка, не заголовок без
        # содержимого: требование стоит токенов на каждом из шести стартов.
        return 0
    lines = ["РЕЕСТР НЕ-СЕЙЧАС-РАБОТЫ — дельта на старте сессии."]
    if broken:
        lines.append(f"🔴 реестр молчит: {broken}")
    for row, kinds, c in sorted(items, key=lambda it: sort_key(it[0])):
        lines.append(f"  {row.id}  {field_or(row, 'title', '(без title)')}")
        for kind, key in kinds:
            lines.append(f"      • {why_line(kind, key, row, c)}")
    lines.append("Это лежит в контексте, а НЕ на экране человека: назови дельту "
                 "вслух своими словами. Неназванная созревшая строка потеряна.")
    lines.append("`ack`, `taken` и `done` зовутся ТОЛЬКО по прямой просьбе "
                 "владельца: ассистент, погасивший дельту сам, уничтожает "
                 "строку при ненаступившей отработке.")
    print("\n".join(lines))
    return 0


def cmd_wake(a):
    """Хук не имеет права уронить старт сессии: падение = НОЛЬ ВЫВОДА.

    Радиус глобальный — старт ЛЮБОЙ сессии в любом проекте. Поэтому наружу
    всегда нуль и поэтому ловится всё: сломанный кэш, уехавший корень, чужой
    файл в ящике. Ни одно из этого не стоит отказа старта.
    """
    try:
        return wake()
    except Exception:
        return 0
```

Парсер:

```python
    p_wake = sub.add_parser("wake", help="дельта в контекст (зовётся хуком)")
    p_wake.set_defaults(fn=cmd_wake)
```

- [ ] **Шаг 4: укусы дельты**

⚠️ **Фикстуры этой главы держат кэш СВЕЖИМ везде, где отцепление фона не проверяется.**
Иначе `wake` отцепит настоящий фоновый процесс, тот перепишет кэш под ногами следующего
укуса, и глава станет невоспроизводимой. Отцепление проверяется двумя укусами, и они идут
последними.

```bash
echo "=== дельта: ноль байт, водяной знак, молчание реестра ==="

wake_ms() {  # wake_ms <каталог> → $out, $rc, $ms (миллисекунды)
  d="$1"; : > "$ROOT/ms"
  out="$(cd "$d" && python3 -c 'import pathlib, subprocess, sys, time
t = time.monotonic()
p = subprocess.run(["python3", sys.argv[1], "wake"], capture_output=True, text=True)
pathlib.Path(sys.argv[2]).write_text(str(int((time.monotonic() - t) * 1000)))
sys.stdout.write(p.stdout)
sys.exit(p.returncode)' "$W" "$ROOT/ms")"; rc=$?
  ms="$(cat "$ROOT/ms")"
}

# Чистый ящик: иначе фикстуры прошлых глав дают событий на любой вкус, и «ноль
# байт» проверить нечем. Кэш прогона тоже убираем — иначе поломка реестра.
rm -rf "$HB" "$HC"; mkdir -p "$HB"

# Укус 11 спеки. 🔴 Голое «вывод пуст» зелено на заглушке, поэтому пара: тот же
# wake при неотработанном событии печатает его, и печатает id.
full "$HB" 20260909-01
cache home 20260909-01 "{\"outcome\":\"silent\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ [ "$rc" = 0 ] && [ -z "$out" ]; }
is "неотработанных событий нет → wake выдал РОВНО ноль байт" $?
cache home 20260909-01 "{\"outcome\":\"fired\",\"since\":\"2026-09-10T10:00:00\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ [ "$rc" = 0 ] && has "home/20260909-01" && has "проба СРАБОТАЛА"; }
is "…а сработавшая проба в той же команде печатается — ноль байт не от немоты" $?
{ has "НЕ на экране человека"; }
is "дельта велит назвать себя вслух: иначе прочитана и не названа" $?
{ has "по прямой просьбе"; }
is "…и называет правило, что ack/done зовутся только по просьбе владельца" $?

# 🔴 ВЕЧНЫЙ СПУТНИК. Дальше половина укусов — про ОТСУТСТВИЕ строки в дельте, а
# голое отрицание зелено на заглушке. Непарсящаяся строка в дельте навсегда:
# машина квитанцию в неё не пишет (инвариант 7), — значит её присутствие и есть
# положительная половина, которую даёт только работающий инструмент. Заводится
# ПОСЛЕ укусов про ноль байт: при ней ноль байт невозможен по построению.
line "$HB" 20260909-09 <<EOF
---
title: "кавычка не закрыта — в дельте навсегда
state: waiting
---
Спутник отрицательных укусов этой главы.
EOF

# Укус 9 спеки: ни разу не опрошенная в дельту НЕ идёт — «не спрашивали» не
# событие. Пара: та же строка с кэшем fired идёт.
full "$HB" 20260909-02
wt "$WAITING_HOME" wake
{ nohas "20260909-02" && has "20260909-01" && has "20260909-09"; }
is "строка без кэша в дельту не идёт: «не спрашивали» — не событие (инв. 3)" $?

# Укус 10 спеки: недостижимость дольше суток — дельта, свежая — нет.
full "$HB" 20260909-03
cache home 20260909-03 "{\"outcome\":\"unreachable\",\"since\":\"$(date -v-2H '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
full "$HB" 20260909-04
cache home 20260909-04 "{\"outcome\":\"unreachable\",\"since\":\"$(date -v-3d '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ nohas "20260909-03" && has "20260909-04" && has "дольше суток"; }
is "недостижима 2 ч молчит, 3 дня — дельта: ssh, который мигнул, не событие" $?

# Укус 12 спеки: напечатанная и не отработанная печатается СНОВА; после ack нет.
wt "$WAITING_HOME" wake
{ has "20260909-01"; }
is "дельта, не отработанная, печатается снова — она не расходуется чтением" $?
wt "$WAITING_HOME" ack home/20260909-01
wt "$WAITING_HOME" wake
{ nohas "20260909-01" && has "20260909-04"; }
is "…после ack не печатается, а СОСЕДНЯЯ неотквитованная печатается" $?

# Укус 13 спеки — вот это водяной знак и доказывает. Пара обязательна: без неё
# реализация «гасим по id навсегда» проходит укус 12 тривиально.
wt "$WAITING_HOME" ack home/20260909-04
wt "$WAITING_HOME" wake
{ nohas "20260909-04" && has "20260909-09"; }
is "unreachable отквитован — молчит, а спутник печатается" $?
cache home 20260909-04 "{\"outcome\":\"fired\",\"since\":\"$(date '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" wake
{ has "20260909-04" && has "проба СРАБОТАЛА"; }
is "🔴 …затем проба дала fired → печатается: ключ квитанции — ИСХОД, не id" $?
# И обратно: повторный тот же unreachable после квитанции — не печатается.
cache home 20260909-04 "{\"outcome\":\"unreachable\",\"since\":\"$(date -v-3d '+%Y-%m-%dT%H:%M:%S')\",\"last_run_at_ts\":$NOW,\"last_rc\":0}"
wt "$WAITING_HOME" ack home/20260909-04
wt "$WAITING_HOME" wake
{ nohas "20260909-04" && has "20260909-09"; }
is "…а тот же исход с той же серией после квитанции молчит" $?

# Укус 14 спеки: probe: none и прошедший review_by — с явной пометкой.
line "$HB" 20260909-05 <<EOF
---
title: "ответ поддержки — машинной пробы нет"
state: waiting
review_by: $(date -v-1d +%Y-%m-%d)
stamped_at: $(date -v-8d +%Y-%m-%d)
host: none
cwd: none
probe: none
ripe_match: none
ripe_when: "поддержка ответила"
sample: none
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" wake
{ has "20260909-05" && has "срок единственный датчик"; }
is "probe: none с прошедшим сроком — дельта с ЯВНОЙ пометкой (укус 14)" $?

# Укус 22 спеки: недооформленная гасится ack, но сброс — по смене СОСТАВА причин.
line "$HB" 20260909-06 <<EOF
---
title: "недооформлена намеренно — её дооформляет миграция"
state: waiting
review_by: $PLUS30
stamped_at: $TODAY
entry: none
---
Тело.
EOF
wt "$WAITING_HOME" wake
{ has "20260909-06" && has "форма не сходится"; }
is "недооформленная идёт в дельту с перечислением причин" $?
wt "$WAITING_HOME" ack home/20260909-06
wt "$WAITING_HOME" wake
{ nohas "20260909-06" && has "20260909-09"; }
is "…ack гасит её: иначе намеренно недооформленная орёт на каждом старте (Р12)" $?
python3 - "$HB/20260909-06.md" <<'PY'
import pathlib, sys
p = pathlib.Path(sys.argv[1])
p.write_text(p.read_text(encoding="utf-8").replace(
    'title: "недооформлена', 'host: local\ntitle: "недооформлена'), encoding="utf-8")
PY
wt "$WAITING_HOME" wake
{ has "20260909-06"; }
is "🔴 …а смена СОСТАВА причин сбрасывает квитанцию: новая дыра не заглушена" $?

# Укус 16 спеки: поломка самого реестра — единственная строка, которой можно
# нарушить «пустая дельта пуста».
rm -rf "$HB"; mkdir -p "$HB"
printf '{"finished_at":"2026-09-01T10:00:00","finished_at_ts":1,"rows":3,"unreachable":0,"stale_lock_broken":false}\n' > "$HC/_run.json"
wt "$WAITING_HOME" wake
{ [ -n "$out" ] && has "реестр молчит" && has "вчерашнюю правду"; }
is "кэш прогона старше 3×TTL → строка о поломке РЕЕСТРА, при пустом ящике" $?
printf '{"finished_at":"x","finished_at_ts":%s,"rows":3,"unreachable":0,"stale_lock_broken":true}\n' "$NOW" > "$HC/_run.json"
wt "$WAITING_HOME" wake
{ has "не дожил до конца"; }
is "…и снятый протухшим замок печатается как поломка, а не тонет в /dev/null" $?
printf '{"finished_at":"x","finished_at_ts":%s,"rows":3,"unreachable":0,"stale_lock_broken":false}\n' "$NOW" > "$HC/_run.json"
wt "$WAITING_HOME" wake
{ [ -z "$out" ]; }
is "…свежая честная квитанция прогона — снова ноль байт" $?

# Укус 17 спеки. Фон отцепляется: подставной ssh на хосте `slow` спит 3 с.
# Спутник возвращается: без него «вернулся быстро» зелено на заглушке, которая
# тоже возвращается быстро и не делает ничего.
line "$HB" 20260909-09 <<EOF
---
title: "кавычка не закрыта — в дельте навсегда
state: waiting
---
Спутник отрицательных укусов этой главы.
EOF
prb "$HB" 20260909-07 slow /tmp 'echo приёмник' 'приёмник'
rm -f "$HC/home/20260909-07.json"
wake_ms "$WAITING_HOME"
{ [ "$rc" = 0 ] && [ "$ms" -lt 1000 ] && has "20260909-09"; }
is "wake при недостижимом хосте вернулся быстрее секунды: фон ОТЦЕПЛЕН" $?
sleep 3
{ [ -f "$HC/home/20260909-07.json" ]; }
is "…и отцепленный фон дожил до записи кэша — start_new_session, а не сирота" $?
```

⚠️ **Последний укус стоит 3 секунды стенда, и это осознанная цена.** Без него «вернулся
быстрее секунды» проходит на реализации, которая не отцепляет фон вообще, а просто ничего
не делает, — то есть на голом отрицании.

⚠️ **Стенд оставляет за собой отцепленные процессы.** Каталог `$ROOT` убирает `trap`, и
фоновая проба, дожившая до этого момента, пишет в уже удалённый путь. Вреда нет: писателя
кэша два не бывает, а живой `~/.claude` подменён `WAITING_HOME`. Записать это в шапку
стенда, чтобы следующий не искал гонку.

- [ ] **Шаг 5: прогон стенда и заглушка**

```bash
cd ~/.claude
bash scripts/waiting-bite.sh | tail -3
WAITING=/tmp/waiting-stub.py bash scripts/waiting-bite.sh | tail -3
```
Ожидается: живой `✅ 162  ❌ 0`, заглушка `✅ 0  ❌ 162`.

- [ ] **Шаг 6: коммит**

```bash
cd ~/.claude
git add scripts/waiting.py scripts/waiting-bite.sh
git commit -m "реестр 2-2: wake — дельта, ноль байт при пустоте, отцепленный фон"
git show --stat HEAD
git push
```

---

### Задача 4: `doctor` и запись хука по якорю

`doctor` закрывает класс, который `wake` закрыть не может по построению: **своего
отсутствия он не обнаружит.** Пропала запись из `settings.json` — его просто никто не
зовёт, дельта не приходит, и это выглядит как «событий нет».

**Файлы:**
- Изменить: `~/.claude/scripts/waiting.py` (константы хука, три функции, команда `doctor`)
- Изменить: `~/.claude/scripts/waiting-bite.sh` (глава укусов)

🔴 **Эта задача НЕ ставит запись в живой `~/.claude/settings.json`.** Ставит Задача 5 (Р6).
Здесь пишется механизм и кусается на фикстуре в `$WAITING_HOME`.

- [ ] **Шаг 1: константы**

```python
SETTINGS = HOME / "settings.json"
HOOK_ANCHOR = '"hooks": {'       # единственная строка верхнего уровня
HOOK_MARK = "waiting.py wake"    # по чему узнаём свою запись
HOOK_BLOCK = """    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "python3 $HOME/.claude/scripts/waiting.py wake",
            "timeout": 5
          }
        ]
      }
    ],
"""
```

Матчер — Р3: `compact` не включён (длинная сессия платила бы за дельту многократно),
`clear` включён (`/clear` вытирает контекст вместе с доставленной дельтой), `resume`
включён (между смертью и возобновлением проходят часы, и дубль дешевле пропуска).
`timeout: 5` — как у соседей по файлу. `$HOME` разворачивает харнесс: соседняя запись
`$HOME/.claude/hooks/context-meter.py` работает именно так.

- [ ] **Шаг 2: чтение и установка**

```python
def hook_state():
    """Жива ли запись хука. Возвращает (текст, здорово ли)."""
    try:
        raw = SETTINGS.read_text(encoding="utf-8")
    except OSError as e:
        return f"настройки не читаются — {e}", False
    if HOOK_MARK in raw:
        return "запись хука SessionStart на месте", True
    return ("записи хука SessionStart НЕТ — реестр не будит никого, и молчащий "
            "реестр неотличим от пустого. Поставить: waiting.py doctor --install"), False


def install_hook():
    """Текстовая вставка ПО ЯКОРЮ (Р5). Никогда json.load → json.dump.

    Сериализация переписала бы файл целиком и затёрла главу сестринской сессии
    молча, без конфликта git, — то есть воспроизвела бы в собственном инструменте
    дефект №3 из «Задачи» спеки, ради которого реестр и пишется. Здесь
    вставляется одна строка после якоря; всё остальное остаётся байт в байт.

    Якорь не единственный — ОТКАЗ, а не вставка наугад: так же отказывает
    hand.sh на нескольких хендоффах ветки, потому что угадать хуже, чем не
    двигаться.
    """
    try:
        raw = SETTINGS.read_text(encoding="utf-8", newline="")
    except OSError as e:
        die(2, f"настройки не читаются — {e}")
    if HOOK_MARK in raw:
        print("запись хука уже на месте — ничего не меняю")
        return 0
    eol = "\r\n" if "\r\n" in raw else "\n"
    lines = [ln[:-1] if ln.endswith("\r") else ln for ln in raw.split("\n")]
    hits = [i for i, ln in enumerate(lines) if ln.strip() == HOOK_ANCHOR]
    if len(hits) != 1:
        die(2, f"якорь {HOOK_ANCHOR!r} в {SETTINGS} найден {len(hits)} раз — "
               f"вставлять наугад нельзя. Вставить руками сразу после него:\n"
               f"{HOOK_BLOCK}")
    at = hits[0] + 1
    lines[at:at] = HOOK_BLOCK.rstrip("\n").split("\n")
    tmp = SETTINGS.with_name(SETTINGS.name + ".tmp")
    tmp.write_text(eol.join(lines), encoding="utf-8", newline="")
    os.replace(tmp, SETTINGS)
    print(f"запись хука вставлена в {SETTINGS} — по якорю, чужие ключи не тронуты")
    print("Проверить: новая сессия, и дельта обязана прийти в её контекст.")
    return 0
```

- [ ] **Шаг 3: `doctor`**

```python
def cmd_doctor(a):
    """Жив ли механизм. Молчащий реестр неотличим от пустого.

    Отдельная команда для ЧЕЛОВЕКА, а не хук: единственный класс, который не
    виден изнутри дельты, — это отсутствие самой дельты. Поэтому первой строкой
    называется запись хука, а не состояние кэша.

    Возвращает 2 (конфигурация) при любой найденной поломке. До Задачи 5 это
    штатное состояние поставки: записи хука ещё нет, и doctor честно кричит.
    """
    if a.install:
        return install_hook()
    now = time.time()
    bad = 0
    text, ok = hook_state()
    print(("✅ " if ok else "🔴 ") + text)
    bad += 0 if ok else 1
    broken = registry_broken(now)
    if broken:
        print(f"🔴 {broken}")
        bad += 1
    else:
        try:
            run = json.loads(RUN.read_text(encoding="utf-8"))
            print(f"✅ фон отчитался {run.get('finished_at')}: строк "
                  f"{run.get('rows')}, недостижимых {run.get('unreachable')}")
        except (OSError, ValueError):
            print("· фоновых прогонов ещё не было — это не поломка, это старт")
    if LOCK.is_dir():
        try:
            age = int(now - LOCK.stat().st_mtime)
        except OSError:
            age = -1
        print(f"· замок пробы занят {age} с (TTL {LOCK_TTL_S})")
    same = changed = 0
    for p in (sorted(CACHE.glob("*/*.json")) if CACHE.is_dir() else []):
        try:
            c = json.loads(p.read_text(encoding="utf-8"))
        except (OSError, ValueError):
            continue
        same += int(c.get("same_truth_runs") or 0)
        changed += int(c.get("changed_runs") or 0)
    total = same + changed
    if total:
        # Замер TTL (Р13): число выбирается наблюдением, а не оценкой.
        print(f"замер TTL (CACHE_TTL_S={CACHE_TTL_S}): прогонов {total}, "
              f"та же правда {same}, смена исхода {changed} — "
              f"{100 * changed // total}% смен")
        print("Частое «то же самое» значит TTL тесен, редкая смена — что широк.")
    else:
        print("замер TTL: прогонов ещё нет — числу не на чем выбраться")
    return 2 if bad else 0
```

Парсер:

```python
    p_doctor = sub.add_parser("doctor", help="жив ли механизм реестра")
    p_doctor.add_argument("--install", action="store_true",
                          help="поставить запись хука SessionStart по якорю")
    p_doctor.set_defaults(fn=cmd_doctor)
```

- [ ] **Шаг 4: укусы главы «doctor и запись хука»**

```bash
echo "=== doctor: своего отсутствия дельта не видит ==="

# Фикстура настроек с ЧУЖИМИ ключами — они обязаны уцелеть байт в байт.
cat > "$WAITING_HOME/settings.json" <<'SETT'
{
  "permissions": {
    "allow": [
      "Bash(/Users/x/.claude/scripts/hand.sh:*)"
    ]
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.claude/hooks/context-meter.py",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "theme": "dark"
}
SETT
before="$(cksum < "$WAITING_HOME/settings.json")"

# Укус 21 спеки, первая половина: doctor краснеет, когда записи нет.
wt "$WAITING_HOME" doctor
{ [ "$rc" = 2 ] && has "записи хука SessionStart НЕТ"; }
is "doctor возвращает 2 и НАЗЫВАЕТ отсутствие записи хука" $?
{ has "неотличим от пустого"; }
is "…и говорит, почему это молчание, а не пустота" $?

# Вторая половина: установка ставит и НЕ трогает чужого.
wt "$WAITING_HOME" doctor --install
{ [ "$rc" = 0 ] && has "вставлена" \
    && inf "$WAITING_HOME/settings.json" "waiting.py wake"; }
is "doctor --install вставил запись — и сказал куда" $?
{ inf "$WAITING_HOME/settings.json" '"matcher": "startup|resume|clear"'; }
is "матчер именно startup|resume|clear — compact не включён (Р3)" $?
{ inf "$WAITING_HOME/settings.json" "context-meter.py" \
    && inf "$WAITING_HOME/settings.json" '"theme": "dark"' \
    && inf "$WAITING_HOME/settings.json" "hand.sh"; }
is "🔴 чужие ключи целы: permissions, соседний хук и theme на месте" $?
{ python3 -c 'import json,sys;json.load(open(sys.argv[1]))' "$WAITING_HOME/settings.json"; }
is "…и файл остался валидным JSON — вставка попала внутрь hooks" $?
{ python3 -c 'import json,sys
d=json.load(open(sys.argv[1]))
s=d["hooks"]["SessionStart"][0]
sys.exit(0 if s["hooks"][0]["timeout"]==5 and "wake" in s["hooks"][0]["command"] else 1)' \
    "$WAITING_HOME/settings.json"; }
is "…и разбирается именно как SessionStart с потолком 5 с" $?

# Повторная установка — no-op, и она так и говорит.
after="$(cksum < "$WAITING_HOME/settings.json")"
wt "$WAITING_HOME" doctor --install
{ [ "$rc" = 0 ] && has "уже на месте" \
    && [ "$(cksum < "$WAITING_HOME/settings.json")" = "$after" ]; }
is "вторая установка ничего не меняет и называет это, а не молчит" $?
{ [ "$before" != "$after" ]; }
is "…а первая файл ИЗМЕНИЛА — иначе предыдущий укус зелен на бездействии" $?

# Якорь не единственный — отказ, а не вставка наугад.
cp "$WAITING_HOME/settings.json" "$ROOT/ok-settings.json"
python3 - "$WAITING_HOME/settings.json" <<'PY'
import pathlib, sys
p = pathlib.Path(sys.argv[1])
t = p.read_text(encoding="utf-8").replace('"hooks": {', '"hooks": {\n  "hooks": {', 1)
p.write_text(t.replace("waiting.py wake", "ничего"), encoding="utf-8")
PY
wt "$WAITING_HOME" doctor --install
{ [ "$rc" = 2 ] && has "найден 2 раз" && has "Вставить руками"; }
is "неоднозначный якорь — отказ кодом 2 с готовым блоком, а не догадка" $?
cp "$ROOT/ok-settings.json" "$WAITING_HOME/settings.json"

# Укус 20 спеки: два счётчика замера растут ПО-РАЗНОМУ.
rm -rf "$HB" "$HC"; mkdir -p "$HB"
prb "$HB" 20260909-21 local /tmp 'echo ничего' 'приёмник'
wt "$WAITING_HOME" probe
{ jq -er '.same_truth_runs==0 and .changed_runs==1 and .outcome=="silent"' \
    "$HC/home/20260909-21.json" >/dev/null; }
is "первый прогон — смена исхода: счётчик changed, а не same" $?
s1="$(jq -r '.since' "$HC/home/20260909-21.json")"
wt "$WAITING_HOME" probe
{ jq -er '.same_truth_runs==1 and .changed_runs==1' \
    "$HC/home/20260909-21.json" >/dev/null; }
is "второй прогон с тем же исходом двигает same и НЕ двигает changed" $?
{ [ "$(jq -r '.since' "$HC/home/20260909-21.json")" = "$s1" ]; }
is "…и начало серии не сдвинулось: серия та же (ключ водяного знака)" $?
prb "$HB" 20260909-21 local /tmp 'echo приёмник' 'приёмник'
wt "$WAITING_HOME" probe
{ jq -er '.same_truth_runs==1 and .changed_runs==2 and .outcome=="fired"' \
    "$HC/home/20260909-21.json" >/dev/null; }
is "смена исхода двигает changed — и замер различает тесный TTL от широкого" $?
{ [ "$(jq -r '.since' "$HC/home/20260909-21.json")" != "$s1" ]; }
is "…и переставляет начало серии: квитанция прежнего исхода сброшена" $?
wt "$WAITING_HOME" doctor
{ has "замер TTL (CACHE_TTL_S=3600)" && has "смена исхода 2"; }
is "doctor печатает замер числами, а не «надо бы посмотреть»" $?
```

- [ ] **Шаг 5: прогон стенда и заглушка**

```bash
cd ~/.claude
bash scripts/waiting-bite.sh | tail -3
WAITING=/tmp/waiting-stub.py bash scripts/waiting-bite.sh | tail -3
```
Ожидается: живой `✅ 178  ❌ 0`, заглушка `✅ 0  ❌ 178`.

- [ ] **Шаг 6: коммит**

```bash
cd ~/.claude
git add scripts/waiting.py scripts/waiting-bite.sh
git commit -m "реестр 2-2: doctor и установка записи хука текстовой вставкой по якорю"
git show --stat HEAD
git push
```

---

### Задача 5: приёмка поставки

Сторож проверяется укусом, а не зелёным цветом — включая сторож этого сторожа. Задача не
добавляет возможностей и целиком состоит из проверок, ловящих дефект ПРИЁМКИ, а не кода.
Плюс единственное действие поставки с глобальным радиусом: запись хука в живые настройки.

**Файлы:**
- Изменить: `~/.claude/scripts/waiting-bite.sh` (шапка — числа боевых замеров)
- Изменить: `~/.claude/settings.json` (запись хука, машиной)
- Изменить: `~/.claude/waiting/20260911-01.md` (боевой случай — `taken`)
- Изменить: `~/.claude/plans/2026-09-11-waiting-registry-2-2.md` (отметка о поставке)

- [ ] **Шаг 1: заглушка обязана дать РОВНО ноль зелёных**

```bash
cd ~/.claude
printf 'import sys\nprint("з")\nsys.exit(0)\n' > /tmp/waiting-stub.py
WAITING=/tmp/waiting-stub.py bash scripts/waiting-bite.sh | tail -3
```
Ожидается: `итог: ✅ 0   ❌ 178`.

🔴 **Любой зелёный — дефект укуса, и его надо чинить, а не объяснять.** Шесть известных
видов — в шапке стенда. У этой поставки особенно вероятны два: **голое отрицание** (половина
утверждений про отсутствие вывода) и **голый код возврата** (`wake` выходит нулём ВСЕГДА по
построению, и заглушка тоже — значит `[ "$rc" = 0 ]` в укусе про `wake` не проверяет ничего
без второй половины).

- [ ] **Шаг 2: сломать сторожа нарочно и замерить, что стенд это видит**

Шесть поломок, по одной за раз, на КОПИИ:
`cp scripts/waiting.py /tmp/w.py` → правка копии → `WAITING=/tmp/w.py bash scripts/waiting-bite.sh`.
Числа записать в шапку стенда рядом с шестью замерами поставки 2-1.

| поломка | что должно покраснеть |
|---|---|
| `probe_one` возвращает `silent` при ненулевом коде команды | ≥3 в главе «три вопроса» — центральное утверждение поставки |
| `write_cache` пишет `probe_rc` в `last_rc` (Р14) | ≥2: «last_rc нуль у недостижимой» и «list ставит её в недостижима» |
| `unacked` возвращает `True` всегда | ≥3 в главе дельты: гашение перестаёт гасить |
| `wake` печатает заголовок, когда `items` пуст | ≥3: «ровно ноль байт» и оба «снова ноль байт» |
| `install_hook` пишет через `json.load` → `json.dump` | ≥1: чужие ключи уцелеют, но отступы и порядок поедут — проверить, ЧТО именно краснеет, и если ничего, укус на сохранность чужого слаб |
| `probeable` не отсеивает `taken` | ≥1 в главе «кого фон НЕ спрашивает» |

⚠️ **Расхождение с ожиданием разбирать, а не переписывать число молча:** либо укус ослаб,
либо поломка задевает шире, чем размечено. У 2-1 две поломки из шести оказались ШИРЕ разметки,
и оба раза причина была настоящая и записана в шапку.

⚠️ **Пятая поломка — единственная в таблице, чей прогноз может не подтвердиться.** Если
`json.dump` не красит ни одного укуса, значит сохранность чужих ключей проверяется слабо, и
надо добавить укус на БАЙТОВУЮ неизменность всего, кроме вставленного блока (`diff` фикстуры
с эталоном, у которого блок вырезан). Это находка, а не формальность.

- [ ] **Шаг 3: живой прогон на живом реестре — до установки хука**

```bash
cd ~/.claude
python3 scripts/waiting.py list
echo "код: $?"
python3 scripts/waiting.py doctor
echo "код: $?"
```
Ожидается: `list` — код 1 и строка `home/20260908-01` в «недооформленных» (Р12 плана 2-1,
её дооформляет миграция 2-3; **не чинить**). `doctor` — код 2 и `🔴 записи хука
SessionStart НЕТ`: это штатное состояние до шага 5.

```bash
cd ~/.claude
python3 scripts/waiting.py probe
echo "код: $?"
```
Ожидается: опрошена строка `home/20260911-02` (`find plans -name '*waiting-registry-2-3*'`,
`host: local`, `cwd: ~/.claude`). Исход — `silent`: плана 2-3 ещё нет. **Код 0.**

🔴 Если код 4 — разобрать поимённо, какая строка не смогла спросить. `unreachable` на
местной пробе означает либо промах `cwd` (Р21), либо что `pipefail` съел код там, где его
съедать не надо.

```bash
cd ~/.claude
python3 scripts/waiting.py wake
echo "код: $?"
```
Ожидается: непустой вывод (`home/20260908-01` недооформлена) и **код 0**.

- [ ] **Шаг 4: боевой случай — ретроперевод `home/20260911-01` в `taken`**

Первый живой потребитель новой команды — сама история реестра. Строка снята 11.09 как
`done` с причиной «владелец сказал начинать 2-2 — ожидание кончилось, работа началась»; по
существу она в `taken`, а не в `done`, и решение владельца об этом принято ПОСЛЕ снятия.

```bash
cd ~/.claude
python3 scripts/waiting.py taken home/20260911-01 "поставка 2-2 идёт: план написан, исполнение началось"
python3 scripts/waiting.py list | grep -A2 "взята в работу"
```
Ожидается: строка в группе «взята в работу», с пометками «взята в работу: поставка 2-2
идёт…» и «закрывалась 2026-09-11, закрытие отменено» — надгробие уцелело (Р23).

🔴 **Синтетический случай вместо этого не годится.** Ретроперевод — единственное
наблюдение, в котором `taken` понадобился не по проекту, а по факту; заменить его
придуманным значит проверить команду на том, под что её писали.

- [ ] **Шаг 5: поставить хук — глобальный радиус, последнее действие**

```bash
cd ~/.claude
git status --short
python3 scripts/waiting.py doctor --install
git diff settings.json
```
Ожидается: `git diff` показывает **только добавленные строки блока `SessionStart`**, ни
одной изменённой чужой. Любая правка соседней строки — дефект вставки, и её надо
откатить (`git checkout -- settings.json`), а не «поправить руками».

⚠️ **`git status --short` до установки обязателен.** Если `settings.json` уже изменён
сестринской сессией, вставка ляжет поверх её незакоммиченной правки, и разобрать, чьё
что, будет нечем.

```bash
cd ~/.claude
python3 scripts/waiting.py doctor
git add settings.json
git commit -m "реестр 2-2: запись хука SessionStart — дельта приходит на старте сессии"
git show --stat HEAD
git push
```
Ожидается: `doctor` — `✅ запись хука SessionStart на месте` и код 0 (если фон уже
отчитался) либо 0 при отсутствии поломок.

- [ ] **Шаг 6: приёмка на СВЕЖЕЙ сессии — единственная проверка, которую нельзя сделать иначе**

Матчер, доставка вывода в контекст и `timeout` проверяются только живым старта. Открыть
новую сессию в любом проекте и убедиться:

- [ ] дельта пришла в контекст, и ассистент назвал её человеку вслух (требование строки в
      выводе `wake`);
- [ ] старт не задержался — ощутимой паузы перед первым ответом нет;
- [ ] в сессии, где событий нет, в контексте НЕТ ни строки от реестра (ноль байт);
- [ ] после `/clear` дельта приходит снова, а после автоматической компакции — нет (Р3).

🔴 **Если старт сессии сломался — снять запись немедленно:** `git revert` коммита шага 5,
и только потом разбираться. Радиус — любая сессия в любом проекте, и сломанный старт
дороже отложенной поставки.

- [ ] **Шаг 7: завести строку на выбор числа TTL — реестр меряет себя сам**

```bash
cd ~/.claude
python3 scripts/waiting.py new "TTL кэша: выбрать число по замеру, а не по оценке"
```
Дозаполнить (решение владельца 11.09: число выбирается через неделю):

```yaml
host: local
cwd: ~/.claude
probe: |
  python3 scripts/waiting.py doctor
ripe_match: "прогонов ([1-9][0-9]{2,})"
ripe_when: "накопилось не меньше сотни прогонов — на такой выборке доля смен исхода уже различает тесный TTL от широкого"
sample: "11.09 doctor печатает «замер TTL (CACHE_TTL_S=3600): прогонов N, та же правда …, смена исхода …» — на новом реестре N меньше сотни, регулярка не совпадает; при N=142 совпадает. Оба исхода наблюдались"
```
Затем `python3 scripts/waiting.py stamp <id>` — срок станет +30 (проба есть).

⚠️ **Образец обязан быть добыт, а не описан.** Если сотня прогонов к моменту приёмки не
набралась, `sample` пишется `pending` с пометкой — но тогда и регулярка проверена только
одной половиной, и это надо сказать вслух, а не выдать за образец.

- [ ] **Шаг 8: сверить план со спекой по объёму 2-2**

Прочитать в спеке «2-2 · Пробуждение», «Коды возврата», «Укусы» (8–17 и 19–22) и проверить
построчно:

- [ ] все укусы 2-2 закрыты поимённо: **8** и **15** — Задачей 1; **9, 10, 11, 12, 13, 14,
      16, 17** и **22** — Задачей 3; **19** — Задачей 2; **20** и **21** — Задачей 4;
- [ ] код 5 не занят: `grep -n "die(5\|exit(5\|return 5" scripts/waiting.py` — пусто;
- [ ] коды 3 и 4 возвращает ТОЛЬКО `cmd_probe`:
      `grep -n "return 3\|return 4" scripts/waiting.py` — обе строки внутри `cmd_probe`;
- [ ] `ОТЛОЖКА.md` не изменён: `git log --oneline -- ОТЛОЖКА.md` без новых коммитов;
- [ ] ни одна строка из `ОТЛОЖКА.md` не перенесена (это 2-3);
- [ ] `check.py` не правился: `git log --oneline -- scripts/check.py` без новых коммитов;
- [ ] `CACHE_TTL_S` по-прежнему заведена ОДНИМ местом и равна 3600.

- [ ] **Шаг 9: закрыть поставку**

Дописать в шапку `waiting-bite.sh` шесть новых чисел из шага 2 и строку про отцепленные
процессы (Задача 3). Отметить поставку в шапке этого плана — числами, а не словами:
живой прогон, заглушка, шесть поломок.

```bash
cd ~/.claude
git add scripts/waiting-bite.sh plans/2026-09-11-waiting-registry-2-2.md
git commit -m "реестр 2-2: приёмка — стенд краснеет на шести нарочных поломках"
git show --stat HEAD
git push
```

Затем снять строку про саму поставку, если она созрела, — **только по прямой просьбе
владельца** (правило выведено в вывод `wake` и действует и здесь).

---

## Самопроверка плана

Прогнана после написания, против спеки с чистой головы.

**Покрытие спеки.** Главы «Три вопроса, а не один», «2-2 · Пробуждение», «Коды возврата»,
«Укусы 8–17» — покрыты задачами 1–4. Главы «Формат строки», «Хранилища», «2-1» — правятся
Задачей 0 ровно в тех местах, где решения владельца 11.09 сделали их неверными. «2-3 ·
Миграция», «Отношение к 1b», «Что срезано» — вне объёма по разложению самой спеки.

**Найденные дыры и что с ними сделано.**

1. 🔴 **Спека противоречит себе в трёх местах** после закрытия открытых вопросов: тело
   говорит «состояний два», глава про кэш не знает про счётчики, таблица укусов не бьёт
   `taken`. Заведена Задача 0 (Р22) — до первой строки кода.
2. 🔴 **`last_rc` конфлатит два разных вопроса.** Классификатор 2-1 читает его как признак
   поломки реестра; естественная реализация положила бы туда код упавшей пробы и отправила
   бы каждую недостижимую строку в «данные протухли». Заведено отдельное поле `probe_rc`
   (Р14) плюс парный укус, где обе половины проверяются в разных местах — в поле и в группе.
3. **Ключ квитанции спекой назван, а полем не выражен.** Тройка `(id, класс, начало серии)`
   требует третьего поля — заведено `acked_key` (Р10).
4. **Квитанция по СРОКУ ключом не выражается вовсе:** у даты нет серии. Разведено на три
   механизма по классу (Р9), и срок сознательно исключён из ключа — иначе пере-штамп сразу
   после квитанции менял бы состав причин, и строка печаталась бы снова тем же вечером.
5. **Строка может созреть по нескольким причинам разом**, и гашение по одной выглядело бы
   как неработающая квитанция. `ack` гасит все (Р11).
6. **Намеренно недооформленная `home/20260908-01`** (Р12 плана 2-1) орала бы на каждом из
   шести ежедневных стартов до октября. `ack` гасит форму, а сброс — по смене СОСТАВА
   причин (Р12), так что новая дыра этим не заглушается.
7. **`wake` своего отсутствия не обнаружит** — если запись хука пропала, его никто не
   зовёт. Класс закрывает только `doctor`, и потому он отдельная команда для человека.
8. **Ретроперевод `done → taken` упирался в сторож «снятую не воскрешать».** Сторож снят,
   надгробие сохраняется (Р23): это исправление ошибочного закрытия, а не воскрешение.
9. **`probe: none` и `taken` попали бы в фоновый опрос** по умолчанию — заведён `probeable`
   с четырьмя классами (Р20).
10. **`cd` в пробу нельзя ни цитировать, ни не цитировать.** Тильду раскрывает приёмник,
    `shlex.quote` её ломает. Предел назван вслух, отказ смещён в безопасную сторону кодом 91
    (Р21).
11. **Стенд не мог проверить ни код 255, ни потолок пробы** без сети и без чужого хоста.
    Заведены `WAITING_SSH` и `WAITING_PROBE_TIMEOUT_S` (Р18).
12. **Половина новых утверждений — про отсутствие вывода**, то есть готовое голое
    отрицание. Заведён «вечный спутник» — непарсящаяся строка, которая в дельте навсегда,
    потому что машина квитанцию в неё не пишет: её присутствие и есть положительная
    половина каждого отрицательного укуса главы.

**Согласованность имён.** Новые: `take_lock`, `write_json`, `probeable`, `probe_call`,
`probe_one`, `write_cache`, `cmd_probe`, `field_or`, `iso_ts`, `faults_digest`, `reasons`,
`ack_key`, `unacked`, `cmd_taken`, `cmd_ack`, `detach_probe`, `registry_broken`, `why_line`,
`wake`, `cmd_wake`, `hook_state`, `install_hook`, `cmd_doctor`. Константы: `LOCK`, `RUN`,
`PROBE_TIMEOUT_S`, `LOCK_TTL_S`, `SSH_CONNECT_TIMEOUT_S`, `STALE_UNREACHABLE_S`, `SSH`,
`SETTINGS`, `HOOK_ANCHOR`, `HOOK_MARK`, `HOOK_BLOCK`. Каждое определено ровно в одной задаче
и вызывается под тем же именем. Правятся существующие: `faults`, `GROUPS`, `classify`,
`line_of`, `main`.

**Новые поля франтматтера:** `taken_at`, `taken_because`, `acked_at`, `acked_outcome`,
`acked_key`. Все машинные, все пишутся только через `set_fields`.

**Числа укусов по задачам:** 104 + 18 · 20 · 20 · 16 · 0 = **178**. Каждый шаг «прогнать
стенд» называет ожидаемую пару; расхождение — сигнал, а не мелочь. 🔴 **Промежуточные числа
(122 · 142 · 162 · 178) — ПРОГНОЗ.** У 2-1 он разошёлся трижды и трижды это было неважно:
настоящая приёмка — живой прогон плюс заглушка.

## Что эта поставка НЕ делает

- не переносит ни одной строки из `ОТЛОЖКА.md` и не трогает сам файл (2-3);
- не выводит `ОТЛОЖКА.md` из обращения и не правит главу «Отложка» в `CLAUDE.md` (2-3);
- не чинит глоб `check.py` — отдельная малая работа полосы B (решение владельца 11.09);
- не вводит пер-строчный `probe_every` — спека запрещает, пока одна цифра не показала себя
  тесной; теперь это проверяемо замером, а не на глаз;
- не меняет `CACHE_TTL_S` — 3600 остаётся, число выбирает замер через неделю;
- не сканирует хендоффы (`entries` вынесен из объёма спекой целиком);
- не занимает код 5 — у 1b он значит «нужно решение владельца»;
- не заводит поле `unattended` и не пытается отличить читающую пробу от действующей:
  удержать это механически нельзя, а формулировка выглядела бы гарантией, которой нет;
- не решает, что делать с `waiting-cache/` при смене машины: кэш машинно-производный и в
  git не впущен, пересобирается первым же фоновым прогоном.
