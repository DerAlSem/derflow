# Сторож проверяется укусом, а не зелёным цветом (архив derflow, глава _gates до 13.09.2026)

## Сторож проверяется укусом, а не зелёным цветом (v4.6)

Новый сторож обязан быть проверен **мутацией**: сломай то, что он охраняет, и
убедись, что он покраснел. Непроверенный на укус сторож — это отметка о проверке
без проверки, и он дороже отсутствующего: тот хотя бы не врёт.

- **Греп по исходнику — не сторож.** `assert "FAILED" in inspect.getsource(...)`
  и `assert "<имя>" in Path(...).read_text()` зеленеют от строки импорта и от
  докстринга строкой выше самого предиката. Четыре прохода критиков подряд
  считали заявку готовой, пока такие сторожа охраняли удалённую ветку отказа.
  Проверяешь поведение — пиши поведенческий тест; проверяешь именно устройство
  кода — разбирай AST (комментарии и докстринги в дерево не попадают).

- **Сам укус требует чистого байткода (v4.9).** Мутация чаще всего переставляет
  строки местами, и **размер файла при этом не меняется**. Питон судит о годности
  `.pyc` по паре «размер и время правки», причём время с точностью до секунды: в
  скрипте «мутировать → прогнать → откатить» всё укладывается в эту секунду, и
  после ОТКАТА тест продолжает читать мутированный байткод. Читается это не как
  кэш, а как сломанный тест — исправленный код показывает ту же красноту, и
  чинить принимаются его. Отказ выглядит так: сторож краснеет на мутации (верно),
  а потом краснеет и на откате. Тупик, который стоит исключить сразу: **дело не в
  тесте** — переписывать его бесполезно, я переписал дважды. Лечение — `rm -rf
  <пакет>/__pycache__` перед КАЖДЫМ прогоном укуса; заодно `-p no:cacheprovider`,
  чтобы не путал и кэш pytest. (Боевое, muzloto 03.09.2026: пять сторожей нового
  слоя, четвёртый «не покусался» — и он ДЕЙСТВИТЕЛЬНО смотрел не туда, но
  разглядеть это удалось только после чистки кэша.)

- 🔴 **Откат мутации — копией файла, а НЕ `git checkout -- <файл>` (v4.13).**
  Пока правка не в коммите, `checkout` откатывает не мутацию, а **весь
  незакоммиченный файл вместе с самой правкой**. Дальше скрипт укуса честно
  краснеет на каждой следующей мутации — потому что кусает пустоту, — и это
  неотличимо от «сторож поймал». Порядок: `cp <файл> <скрэтчпад>` до цикла,
  `cp` обратно после каждой мутации, и **первым шагом прогон «ИСХОДНОЕ обязано
  быть зелёным»** — он и ловит соскочивший откат. Тупик, который стоит
  исключить сразу: краснота после отката — это не грязный `__pycache__`, если
  первый прогон цикла тоже красный. (Боевое, gmb_v2 04.09.2026: три мутации,
  из них две кусали снесённую правку и выглядели пойманными.)

- 🔴 **Не покусалось — сперва спроси, ДОСТИЖИМА ли мутируемая ветвь в этом
  сценарии (v4.21).** Мутация проезжает зелёной и тогда, когда сторож исправен,
  а до испорченной строки просто не доходит: выше по ходу стоит ранний возврат,
  и тест уходит по нему. Тогда сторож проверяет не условие, а СОВПАДЕНИЕ его с
  другим условием — и держится это совпадение ровно до первой правки соседней
  ветви. Признак: мутация зелёная, но и удаление всей строки тоже зелёное.
  Лечение — сторож, который ставит проверяемое условие НАПРЯМУЮ (подменить
  результат верхнего слоя так, чтобы ветвь стала достижимой), а не подбирает
  входные данные и надеется до неё дойти. Тупик, который стоит исключить сразу:
  это не грязный `__pycache__` и не соскочивший откат — оба ловятся прогоном
  «исходное зелёное», а он проходит. (Боевое, propevay 07.09.2026: «проверка
  пропаж только на дочитанном окне» — мутация «убрать признак полноты окна»
  зелёная, потому что недочитанное окно сегодня всегда приходит с неуспешным
  исходом и возвращается раньше. Совпадение верное, норма — нет.)

- 🔴 **Стенд, отдающий двум источникам ОДИН объект, отменяет проверку молча.**
  Мутация «читать не то, что ушло, а исходную настройку» проехала ЗЕЛЁНОЙ: мок
  конфига возвращал один и тот же словарь на каждый вызов, а продукт дописывал
  в него подставленное значение. Оба источника читали результат работы
  продукта, и сторож различал их только на бумаге. Признак: сторож заявляет,
  что различает ДВА источника одного значения. Лечение: `side_effect=lambda:
  dict(...)` вместо `return_value=dict(...)`, и различающий случай, где
  источники обязаны разойтись (пустая настройка + подстановка по каналу).

- 🔴 **Не покусалось — спроси и о том, не ЧАСТИЧНА ли сама мутация (v4.33).**
  Сторож, проверяющий «хотя бы одно вхождение», зеленеет, когда мутация тронула
  одну из НЕСКОЛЬКИХ однотипных позиций: он честно находит нетронутую. Снаружи
  это неотличимо от «сторож смотрит не туда», и принимаются переписывать его.
  Признак механический: охраняемое место существует в коде **более одного раза**
  (два поля формы, две двери, оба конца пары), а проверка сформулирована
  существованием, не счётом. Лечение — сторож СЧИТАЕТ (`toHaveLength(2)`), а
  скрипт укуса меняет ВСЕ вхождения, и отдельной мутацией — ровно одно: обе
  обязаны краснеть.
  Названный отказ: переписывать предикат бесполезно — он был исправен; правится
  его арность. Тупик, который стоит исключить сразу: это не грязный
  `__pycache__` и не соскочивший откат (оба ловятся прогоном «исходное
  зелёное», а он проходит) и не недостижимая ветвь (удаление всей строки
  краснеет). (Боевое, gmb_v2 12.09.2026: проверка `type="number"` у полей счёта
  искала одно вхождение, мутация правила первое из двух полей — проехала
  зелёной при верном сторожe и верном коде.)

- 🔴 **Не покусалось — спроси и о том, не гасит ли эффект мутации ВТОРОЙ
  фильтр ниже по ходу (v4.36).** Ветвь достижима, мутация исполняется и честно
  меняет промежуточную величину — а наблюдаемое число не двигается, потому что
  ниже стоит второе условие, отбрасывающее результат по ДРУГОЙ причине. Признак:
  у охраняемой строки есть своя величина, а сторож утверждает про соседнюю.
  Лечение — утверждать про ту величину, которую мутируемая строка кормит
  НАПРЯМУЮ.
  Названный отказ: переписывать предикат бесполезно — он исправен; правится то,
  НА ЧТО он смотрит. Тупик, который стоит исключить сразу: это не грязный
  байткод и не соскочивший откат (оба ловятся прогоном «исходное зелёное», а он
  проходит) и не недостижимая ветвь (строка исполняется, промежуточная величина
  меняется). (Боевое, `~/.claude` 12.09.2026: в `toolcarry.py` мутация
  `lt = toks(ln) - ct` → `toks(ln)` проехала зелёной. Токены самой команды лежат
  в записи с `tool_use`, то есть всегда РАНЬШЕ своего вывода, и строгий счёт
  отбрасывал их вторым фильтром — новизной. Вычитание видно только в МЯГКОМ
  счёте, и укус переписан на него.)

- 🔴 **Опыт над живым рабочим местом владельца: уборка — часть опыта, и она
  обязана быть ГРОМКОЙ и по точной ссылке (v4.37).** Продолжение v4.20 с другой
  стороны: там движущаяся мишень портила замер, здесь опыт портит мишень. Два
  независимых промаха сложились. Первый: закрытие элементов ВНУТРИ обхода
  коллекции (`repeat with t in tabs of w` + `close tab t`) сносит не то —
  индексы едут под обходом. Второй: `2>/dev/null` на уборке сделал снос
  невидимым. Признак механический: уборка правит состояние, которого ты не
  создавал, и её stderr заглушён. Лечение: закрывать по точному id с `return`
  сразу после закрытия, ошибки уборки НЕ глушить, состояние до и после сверять
  ПЕРЕЧИСЛЕНИЕМ.
  Названный отказ: правило «teardown обязан молчать» (`waiting-bite.sh`) сюда не
  переносится — там глушится названный осадок в своём `mktemp`, здесь правится
  чужое. Тупик, который стоит исключить сразу: счётчик сразу после закрытия
  врёт — дважды дал 17 и 12 при верных 16 и 15, потому что AppleScript отвечает
  раньше, чем окно перестроилось; на нём же снос и выглядел вдвое большим.
  (Боевое, `~/.claude` 12.09.2026: уборка двух пробных табов снесла ТРИ таба
  владельца, один — с `docker`-оболочкой, чей контейнер после этого не нашёлся
  ни в `ps`, ни в `ps -a`; различить «убил» и «был мёртв до меня» уже нечем.)

- 🔴 **Фикстура, которую производит сама проверяемая сессия, — не фикстура
  (v4.20).** Стенд, стоящий на живом транскрипте текущей сессии, стоит на
  движущейся мишени: сессия сама трогает охраняемые файлы и сама печатает их
  содержимое в свой же транскрипт. Дважды за один вечер: (1) снимком скилла
  опознался 747-символьный кусок СОБСТВЕННОГО вывода `grep` — лечится порогом
  длины и отказом читать результаты инструментов; (2) мутация `utime(None)`
  упала внутрь окна снисхождения, потому что предыдущая команда стенда обновила
  касание файла, — и это выглядело как «сторож не кусается», хотя код был верен.
  Признак: стенд краснеет и зеленеет между прогонами, а код не менялся. Лечение:
  ставить мутацию относительно ИЗМЕРЕННОЙ величины (`касание + окно + запас`), а
  не относительно «сейчас». Тупик, который стоит исключить сразу: переписывать
  предикат — он был исправен оба раза.

- **У отрицательного сторожа обязан быть положительный контроль.** «Покрытие не
  применилось», «уведомление не ушло», «предпросмотр ничего не обещал» — рядом
  пиши парный тест, где то же самое ДОЛЖНО произойти. Не удаётся воспроизвести
  положительный — это и есть ответ: путь в обвязке не исполняется, отрицательный
  сторож пуст и не исполнил ни строчки проверяемого кода.
  Правило симметрично: у требования «должно произойти X» обязан быть парный
  случай «а когда не должно — не происходит», иначе его удовлетворяет реализация,
  которая делает X всегда.

- **Задачу «тест: X уважает Y» нельзя отмечать, пока теста нет.** Отметка за
  счётчик прячет не отсутствие проверки, а её РЕЗУЛЬТАТ: написанные позже тесты
  первым же прогоном вскрыли, что главный путь заявки закрыт целиком и участие
  уезжает в долг — при том, что «поведение реализовано и соседние тесты зелёные».
  Соседние тесты шли другим путём. Отмечать только после того, как тест НАПИСАН
  и ПАДАЕТ без правки.

- **Меняешь носитель — переводи ОБЕ стороны сразу.** Переключённое чтение при
  старой записи означает, что новый носитель всегда пуст, а пустой носитель почти
  всегда толкуется как «без ограничений»: фича выключена, а снаружи выглядит
  работающей. Expand-фаза — это ДВОЙНАЯ ЗАПИСЬ, а не разовый перенос; сторож
  ставится на ЗАПИСЬ, а не только на чтение.
  Родственное: **пустое значение в гейте по умолчанию открывает дверь, а не
  закрывает** — искать такие места в любом forward-only контуре специально.

- **Красный сторож — сперва подозреваемый ПРОДУКТ, но проверять надо обе
  стороны.** Ложная тревога стоит своего: гейт краснеет каждую выкатку, его
  начинают проматывать, и настоящая поломка тонет в шуме.

